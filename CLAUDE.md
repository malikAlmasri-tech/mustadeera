# CLAUDE.md — المستديرة | حالة المشروع

## 📌 ما هو المشروع
منصّة حجز ملاعب كرة القدم — SPA بـ Vanilla JS/HTML/CSS (بلا مكتبات)، باكند Google Apps Script + Google Sheets.
واجهة **ثنائية اللغة (عربي RTL / إنجليزي LTR)**، هوية **Teal `#0F4B53` + Lime `#8CC63E` مسطّحة** (من الشعار logo_2)، ثيمان نهاري/ليلي.

---

## ⏭️ نقطة الاستئناف — اقرأ هذا أولًا (آخر تحديث 2026-07-28)

> ## 🟩 حالة 2026-07-28 (نهاية الجلسة) — الموقع الرسمي حيّ ومنشور
>
> **ستّة كوميتات:** `69d0b47` · `4e0ac4a` · `a898945` · `c626f14` · `64e393f` · `febf43d`.
> **28 صفحة** في `public/` (8 مسارات × ar/en + 6 ملاعب × ar/en) + `/admin`.
> **البناء أمرٌ واحد كما كان:** `powershell -ExecutionPolicy Bypass -File app\build.ps1` (يبني الموقع القديم + التطبيق + الموقع الجديد). **معاينة: `koora-site` على 8020.**
>
> ### ✅ البنود الأربعة أُغلقت — 2026-07-29 (أبلغ المستخدم: «تم تطبيق كل البنود»)
> | # | ما كان | الحال |
> |---|-------|-------|
> | 1 | `migration/07_hide_review_phone.sql` | ✅ **شُغِّل، ومُتحقَّق حيًّا بالمفتاح العام:** `select=phone` و`select=*` ⇒ **401** · `order=phone.desc` و`phone=like.*` (قناتان جانبيتان) ⇒ **401** · `rating,comment,author_name` ⇒ **200**. التسريب مغلق ولم يُكسر شيء |
> | 2 | سرّ `SUPABASE_DB_PASSWORD` + Run workflow | ✅ طُبِّق |
> | 3 | اختبار `/admin` وتغيير كلمة السرّ | ✅ طُبِّق |
> | 4 | التأكّد من بناء Vercel | ✅ **البناء ناجح والنشر على Vercel** |
>
> **🌐 الاستضافة محسومة: Vercel** (`https://mustadeera.vercel.app`) — قرار المستخدم 2026-07-29، يَنسخ اقتراح Cloudflare Pages أدناه. `vercel.json` هو المعتمد، و`_headers`/`_redirects` يبقيان مولَّدَين بلا ضرر. **يبقى القيد قائمًا: لا أنت ولا أنا نفتح `vercel.app` (حجب SNI)** ⇒ التحقّق الحيّ من الموقع يمرّ عبر لوحة `vercel.com` أو جهاز على شبكة أخرى.
>
> ### 🟢 2026-07-29 — المرحلة (و) أُغلقت، و(د) أُلغيت
> - **بثّ حيّ لسجلّ التدقيق في `/admin`** — عميل Phoenix/Realtime **مكتوب باليد** (المشروع فانيلا؛ `supabase-js` ~40KB لمقبس واحد لا يستحقّ). معه **مؤشّر حالة صادق** + **استطلاع احتياطي** + **تجديد جلسة** (التوكن ينتهي بعد ساعة، ولوحة حيّة تبقى مفتوحة أطول ⇒ كانت ستتحوّل إلى جداول فارغة بصمت).
>   - 🔴 **معلَّق عليك:** تشغيل [`migration/08_realtime_audit.sql`](migration/08_realtime_audit.sql). **بدونه اللوحة تعمل** بالاستطلاع كل 20ث وتقول ذلك صراحةً بدل ادّعاء الحياة.
>   - ⚠️ **مزلق يستحقّ التذكّر:** في Realtime **`phx_reply:"ok"` ليس دليل نجاح** — الخادم يقبل الاشتراك بجدول **غير موجود** (مقيس). الإشارة الصادقة هي رسالة **`system`** التي تليه. وقبل `08` كانت `status:"error"` بنصّ «Unable to subscribe… check Realtime is enabled».
>   - ⚠️ **CSP: المخطّط يجب أن يطابق تمامًا** — `https://*.supabase.co` **لا يُجيز** `wss://` لنفس المضيف. أُضيف `wss://*.supabase.co` إلى `_headers` و`vercel.json`. **مقيس بالسياسة الحقيقية: بدونه BLOCKED، ومعه ALLOWED.**
>   - 🔒 البثّ يرسل الصفّ **كاملًا** ومنه `before`/`after` (لقطات قد تحوي هواتف). الصفحة **تنتقي خمسة أعمدة** ولا تخزّن الباقي — مُتحقَّق: لا أثر للبيانات الخاصّة في الـDOM.
> - **~~المرحلة (د)~~ أُلغيت:** سقطت أرضيتها بقرار «الحجز من التطبيق فقط» (لا `public/app` أصلًا، و`hasRating` مُنجَزة في مولّد الموقع). بقي منها **P5** وحده — إيقاف Apps Script، **محجوب بلوحات الـAI الثلاث** في لوحة المالك ([`app.js:2538`](app/native/app.js:2538)) ⇒ قرار المالك: إبقاء Apps Script خادمَ AI، أو نقل الـAI إلى Supabase Edge Function.
>
> **🔑 `/admin` بالرابط فقط عمدًا** (`/admin` · بلا أي رابط وارد + `Disallow` في robots). الإخفاء **ليس** أمانًا — الأمان في RLS و`is_admin()` وفحص `role==='admin'` بعد الدخول. الوصول اليومي: مفضّلة المتصفّح.
>
> ### 📌 قرارات المالك 2026-07-28 (لا تُعاد مناقشتها)
> - **الحجز من التطبيق فقط.** الـSPA **لم تعد تُنشَر** على الموقع (`/app` و`/app/*` ⇒ 301 إلى `/download`). السبب: كانت على Apps Script/Sheets بينما التطبيق على Postgres ⇒ **قاعدتان لا تريان بعضهما ⇒ حجز مزدوج ممكن**. ⚠️ **الأثر: مستخدمو الآيفون لا يستطيعون الحجز إطلاقًا** — صفحة `/download` تقول ذلك صراحةً وتطلب منهم التواصل.
> - **لا PWA.** للمشروع وجهان فقط: الموقع، والتطبيق. حُذف المانيفست والـSW والأيقونات، **والتذييل يُلغي أي SW مسجَّل + يمسح كاشه** (وإلّا بقي زوّار الأمس يرون نسخة قديمة إلى الأبد).
> - **المرحلة (ز) مؤجَّلة** إلى الإطلاق الرسمي.
> - **تمريرة فخامة:** هيرو داكن ثابت (لا يتبع الثيم) + الهاتف بميلة 3D + شبكة ملعب من تدرّجات + لوح ختام داكن + خطّ يمتدّ تحت روابط التنقّل + خطّ زمني يربط الخطوات.
>
> ### ⚠️ مزالق تعلّمتها هذه الجلسة
> - **`$rel` و`$Rel` متغيّر واحد** في PowerShell (لا تفرقة بحالة الأحرف) · **`$pid` محجوز** (معرّف العملية).
> - **البناء يمسح `public/` أوّلًا الآن** — بدونها يبقى ملفّ حُذف من المولّد يُخدَم إلى الأبد (هكذا نجا الـSW من حذفه).
> - **المسارات العربية:** مرمَّزة في الروابط/canonical/sitemap، **خام على القرص** (الخادم يفكّ الترميز قبل البحث). مُختبَرة عبر HTTP.
> - **الحجم الحقيقي للرئيسية 11KB مضغوطة** (38KB خامًا) — الميزانية الخام مضلِّلة.
> - **قراءة 200 من المتصفّح قد تكون من كاشه لا من القرص** — افحص بـ`cache:'no-store'`.
>
> ---
>
> **🔀 الترتيب (قرار المستخدم 2026-07-28): نُنهي التطبيق أوّلًا، ثمّ ننتقل إلى الموقع.**
> - **الخطة ① [`PLAN-1-completion.md`](PLAN-1-completion.md) — إكمال التطبيق: ✅ أُغلقت من طرف المساعد.** لم يبقَ إلّا ثلاثة بنود على المالك: اختبار جهاز حقيقي (3) · أرقام المُلّاك الحقيقية (5) · مفتاح التوقيع (7). **ولا يحجب أيّ منها بدء الخطة ②.**
> - **الخطة ② [`PLAN-2-website.md`](PLAN-2-website.md) — الموقع الرسمي: 🟢 التالية، ابدأ من مرحلتها (أ).** فصل الموقع عن التطبيق (`/` موقع ثابت · `/app` الـSPA · `/download` · `/admin`)، و**صفحة الأدمن على الموقع فقط**. **اقرأ فيها جدول «ما تغيّر منذ كتابة الخطة» أوّلًا** — روجِعت 2026-07-28 وفيها ستّ نقاط محدَّثة.
>   - **🚀 نُشر على Vercel 2026-07-28** — ثلاثة كوميتات (`69d0b47` · `4e0ac4a` · `a898945`). `vercel.json` يوجّه Vercel إلى `public/` ويعكس ترويسات `_headers`. ⚠️ **النشر غير مُتحقَّق منه حيًّا** (شبكتك تحجب `vercel.app` وأنا كذلك) — **افتح لوحة `vercel.com` (سالكة) لرؤية حالة البناء.**
>     - **`/` صار الموقع التسويقي، والـSPA انتقل إلى `/app/`** (نفس الأصل ⇒ جلسات `localStorage` محفوظة). زرّ «احجز من المتصفّح» يوصل إليه.
>     - **ملفّان يملؤهما المالك ويتبدّل حالهما آليًّا:** [`site/release.txt`](site/release.txt) (إصدار/بصمة/رابط الـAPK) و[`site/contact.txt`](site/contact.txt) (واتساب/بريد). ما داما فارغين تعرض الصفحتان حالة صادقة بدل رقم مخترَع. **الفرعان مبنيّان ومُختبَران.**
>   - **🟢 المراحل (أ) و(ب) و(هـ) أُنجزت، و(ج) إلّا الـAPK الموقَّع — 2026-07-28.** **28 صفحة** (8 مسارات × لغتين + 6 ملاعب × لغتين) · PWA كاملة (مانيفست + SW مسجَّل + أيقونتان) · `/places` **مولَّد من Postgres وقت البناء** مع كاش `site/data/places.json` يمنع كسر البناء عند انقطاع الشبكة، و`hasRating` مكرَّرة فيه (2 من 6 لهما نجوم). **المتبقّي: (د) تبديل باكند الموقع · (و) `/admin` · (ز) الإطلاق.**
>     - ⚠️ **مزلقان في PowerShell كلّفاني بناءَين:** `$rel`/`$Rel` **اسمان لمتغيّر واحد** (لا تفرقة بحالة الأحرف) ⇒ مسار الملفّ سحق بيانات الإصدار بعد أوّل صفحة · و`$pid` **محجوز** (معرّف العملية) فلا يُكتب.
>     - **المسارات العربية:** مرمَّزة (`%D9%…`) في الروابط والـcanonical وsitemap، و**خام على القرص** — الخادم يفكّ الترميز قبل البحث عن الملفّ. مُختبَرة عبر HTTP لا في المخرَج فقط. الموقع يُبنى فعلًا: `site/` (قاموسا نصوص · قوالب · CSS · صفحات) ⇐ `site/build-site.ps1` ⇐ `public/` = **14 صفحة** (7 مسارات × ar/en) + `sitemap.xml` + `robots.txt` + `_headers` + `_redirects` + صورة OG. مربوط بذيل `app/build.ps1` فأمر البناء واحد كما كان. **معاينة: `koora-site` على 8020.** التالي: **(ج) `/download`**، ثمّ الصفحات الباقية.
>     - ⚠️ **قيد صارم في `site/*.ps1`: صفر محرف عربي داخل أي `.ps1`.** كل النصوص في `site/strings/{ar,en}.txt` تُقرأ بـ`ReadAllText` (لا تتأثّر بالـBOM). السبب: PS 5.1 يُسيء فكّ ترميز UTF-8 بلا BOM، والـBOM يضيع بسهولة عند التحرير. وربطُ المولّد بـ`build.ps1` تمّ بـ**`AppendAllText`** لا بمحرّر نصّ — كي لا يُمسّ الـBOM في مقدّمة الملفّ.
>     - 🐛 **عطل RTL يستحقّ التذكّر:** `inset-inline-start:50%` + `translateX(-50%)` **لا يتمركز في العربية** — `transform` غير مدرك للاتجاه فيزيح إلى الجهة نفسها في اللغتين (مقيس: مركز توهّج الهيرو 200px بدل 640، ونوتش الهاتف 43 بدل 130). البديل الصحيح: **`inset-inline:0;margin-inline:auto`** بعرض محدَّد.
>   - **⚠️ بطل جزئيًّا 2026-07-29:** الاستضافة **بقيت على Vercel** بقرار المستخدم (البناء ناجح) — اقتراح Cloudflare Pages أدناه **لم يُنفَّذ**، و`vercel.json` موجود ومعتمد. ما يبقى صحيحًا من الفقرة هو **القياس** وحده: `*.vercel.app` محجوب بـSNI على شبكتك وعلى بيئتي ⇒ لا نتحقّق من الموقع حيًّا من هنا.
>   - **~~القرار 1 (الاستضافة) أُسقط 2026-07-28 بالقياس~~:** الشبكة تحجب `*.vercel.app` بفلترة **SNI** — DNS يحلّ وTCP يتّصل ثمّ **تُقطع مصافحة TLS**، والجذر `vercel.app` نفسه محجوب بينما `vercel.com` يعمل (200) ⇒ ليس حجب شركة. و`*.pages.dev` **سالك (200)**. ⇒ **الاستضافة: Cloudflare Pages مجانًا.** الأثر العملي: **لا `vercel.json`** — المكافئ `_headers` + `_redirects` يولّدهما `build.ps1`، ومنهما ترويستا الـAPK.
>
> **⚠️ أهمّ فخّ ينتظر الخطة ②:** الموقع والتطبيق **ثلاثيّان منفصلان**. كل ما أُنجز مؤخّرًا (`hasRating` · تغيير كلمة السرّ · تنعيم الثيم · haptics) موجود في `app/native/*` **فقط** — الموقع لا يعرف عنه شيئًا. عند المرحلة (د) يجب نقل ما يخصّ الموقع يدويًّا.
>
> **🗂️ خريطة الملفّات:** [`README.md`](README.md) — أُعيدت كتابته 2026-07-28 ليكون خريطة حقيقية (أين أُحرّر · ما هو مولَّد · ما هو ميّت).

### ✅ عطل خصوصية — أُغلق 2026-07-29 (مُكتشَف 2026-07-28 أثناء بناء `/admin`)
> **الحال:** `07` شُغِّل ومُتحقَّق حيًّا بالمفتاح العام — الجدول محجوب و`phone` غير مقروء حتى عبر `order`/`filter`. **الأعمدة العامّة المسموحة:** `id, legacy_id, created_at, place_id, field_id, author_name, rating, comment`. ⚠️ **أي قراءة جديدة لـ`reviews` يجب أن تذكر الأعمدة صراحةً** — `select=*` يفشل عمدًا. ما دون هذا السطر هو وصف العطل الأصلي، للسياق.

**جدول `reviews` يكشف `phone` و`author_name` لأي أحد بلا تسجيل دخول.** السياسة `reviews_read ... for select using (true)` تعني كل الأعمدة، والمفتاح العام مضمَّن في الـAPK وفي الموقع والمستودع عامّ. **مقيس بـcurl: أعاد أرقامًا حقيقية بلا مصادقة.**
⇒ **على المستخدم:** تشغيل [`migration/07_hide_review_phone.sql`](migration/07_hide_review_phone.sql) (سحب SELECT عن الجدول ثمّ منح الأعمدة العامّة وحدها — `revoke` على عمود وحده **لا يُبطل** منح الجدول في Postgres).
> التطبيق **يكتب** الرقم ولا يقرأه، فالإصلاح لا يكسر شيئًا. وبعده `select=*` من عميل مجهول يفشل عمدًا — اذكر الأعمدة صراحةً في أي قراءة جديدة.
> **وفحص جانبي طمأن:** `admin_daily` و`audit_log` و`profiles` و`bookings` تُعيد `[]` للمجهول ✓ · و`admin_places` تُعيد أسماء الأماكن (عامّة أصلًا) بإيراد **صفر** لأن RLS يحجب الحجوزات ⇒ لا تسريب.

### 🔴 الحاجز الأول — يُشغَّل قبل أي شيء آخر
**كل الحسابات المستورَدة لا تستطيع الدخول** (مُشخَّص حيًّا 2026-07-28). ثمانية أعمدة توكن في `auth.users` تُركت `NULL` أثناء الاستيراد، وخدمة المصادقة تقرأها في حقول نصّية لا تقبل NULL ⇒ **HTTP 500 «Database error querying schema»** بكلمة السرّ الصحيحة والخاطئة معًا.
⇒ **على المستخدم:** تشغيل [`migration/05_fix_auth_users.sql`](migration/05_fix_auth_users.sql). والمولّد [`build_import.mjs`](migration/build_import.mjs) أُصلح وأُعيد توليد `02`/`03`.
> ⚠️ **العرَض يُضلّل تمامًا:** يبدو كأنه «كلمة السرّ القديمة قصيرة (3-4 أرقام) والحدّ الأدنى 6». **لا علاقة له.** الطول يُفحص عند **إنشاء الحساب فقط**. الاختبار الحاسم: جرّب كلمة سرّ **خاطئة عمدًا** — إن أعطت الخطأ نفسه فالعطل قبل فحص الهوية.

**سياق الترحيل (مرجع):** [`PLAN-db-migration.md`](PLAN-db-migration.md) · [`migration/README.md`](migration/README.md). **P3ب** (باكند الموقع) و**P5** (إيقاف Apps Script) خارج الخطة ① عمدًا — يُغلقان في نهاية الخطة ②.

| ما أُنجز | ما تبقّى |
|---------|---------|
| ✅ المشروع حيّ + المخطّط + RLS + سجلّ تدقيق + عروض | ✅ **1-2 الحاجز زال** — `05` شُغِّل، ومُتحقَّق حيًّا: كل الحسابات المستورَدة صارت `400 invalid_credentials` بدل `500` |
| ✅ **تغيير كلمة السرّ** في «حسابي» (يشترط الحالية) — مبنيّ في APK | ⚠️ **عرض كلمات السرّ مستحيل** (bcrypt أحادي الاتجاه). الإعادة: [`RECIPES.sql`](migration/RECIPES.sql) §8 · واجهتها في `/admin` بالخطة ② |
| ✅ استيراد كل البيانات · وحدة `API` في التطبيق على Postgres ومبنيّة في APK | ⏳ **3** اختبار جهاز حقيقي (دخول · حجز · إيماءة · اهتزاز) |
| ✅ **«Confirm email» مطفأ** و**`04_policy_fixes.sql` شُغِّل** — مُتحقَّقان حيًّا | 🟡 **4** `.github/` مدفوع (`fa594e0`) + السرّان مُضافان — يبقى **Run workflow** للتحقّق |
| ✅ **إنشاء حساب جديد يعمل** من الطرف إلى الطرف (تسجيل حقيقي) | ⏳ **5·7** حسابات المُلّاك الحقيقية · APK بتوقيع release |
| ✅ **6 التقييمات الحقيقية فقط** — `rating_seed` أُسقط من القراءة، ومكان بلا تقييم بلا شارة نجمة (مُتحقَّق حيًّا) | ⏳ تنظيف اختياري: [`06_drop_rating_seed.sql`](migration/06_drop_rating_seed.sql) |
| ✅ **8 الصقل** — haptics أصلية · skeletons للوحة المالك (bottom sheet وتبويب Browse: لاغيان، التفصيل في الخطة) | 🟡 **7** تُنشئ مفتاح التوقيع بكلمة سرّك ⇐ [`android/SIGNING.md`](android/SIGNING.md) |
| ✅ **9 تبديل الثيم صار سلسًا** — سببان جذريّان أُزيلا (أدناه) | ⏳ **3** اختبار جهاز حقيقي بعد فكّ الحاجز |

**لم يُختبر على جهاز حقيقي بعد:** التطبيق بعد الترحيل كلّه (تصفّح/تفاصيل/دخول/حجز).

> ⚠️ **`Code.gs` — لا تصرف وقتًا على نشره.** تعديلاته المعلّقة (`reviews_dist` · إلغاء اللاعب · الملاعب الموقوفة) **ذابت كلّها في الترحيل**: الأولى صارت عرض `place_stats`، والثانية والثالثة صارتا سياستَي RLS. يبقى منشورًا كشبكة أمان حتى P5 فقط.

---

## 🚦 آخر حالة (2026-07-28) — الدفعة الحادية عشرة ✅ **إغلاق التطبيق: تقييمات حقيقية · ثيم سلس · صقل · توقيع**

**1. 🎚️ تبديل الثيم لم يكن سلسًا — سببان جذريّان، لا ضبط أرقام.**
- **🐛 إيقاف انتقالات اللون لم يكن يعمل إطلاقًا.** [`app.css:193`](app/native/app.css:193) يضع `transition:.45s` على **ستّ خصائص لون** (منها `box-shadow`) لكل وسم شائع في الصفحة. والقاعدة التي يُفترض أن توقفه لحظة التبديل كانت مكتوبة **`animation:none`** — خاصية أخرى تمامًا لا تمسّ `transition`. الأثر المركّب: لقطة «بعد» تُلتقط والألوان ما زالت **القديمة** (الانتقال بدأ للتوّ) ⇒ الدائرة تكشف صورة شبه مطابقة، ثم تُكمل الصفحة كلّها تحوّلها اللوني 450ms **تحتها** = طبقتان متزامنتان، **وإعادة رسم كل عقدة في الشجرة على الخيط الرئيسي**. صارت `transition:none!important`. **مقيس حيًّا: `.45s × 6 خصائص` ⟵ `none | 0s`.**
- **`clip-path` ليست خاصية مركَّبة على الـGPU** في Chromium — تكبير نصف قطر الدائرة يُلزم الخيط الرئيسي بإعادة رسم لقطة بحجم الشاشة **في كل فريم**، وحافّتها حادّة تمامًا فتُقرأ كحلقة تجتاح الشاشة لا كتحوّل. استُبدلت بـ`opacity`+`transform` **فقط** (كلتاهما مركَّبتان) مع `transform-origin` عند الإصبع ⇒ اللقطة الجديدة **تتفتّح** من لمستك بصفر رسم لكل فريم. 560ms بمنحنى ease-out خالص.
  > ⚠️ **درس عامّ:** قبل تنعيم حركة، افحص أوّلًا هل الخاصية التي تُحرّكها **مركَّبة** (`transform`/`opacity` نعم · `clip-path`/`box-shadow`/`filter` لا). ثم افحص هل ما تظنّه «مُوقَفًا» مُوقَف فعلًا — `animation` و`transition` اسمان مختلفان لآليتين مختلفتين، والخلط بينهما لا يُنتج خطأ ولا تحذيرًا.

**2. ⭐ التقييمات صارت حقيقية فقط (البند 6).** كان الرجوع إلى `rating_seed` يعرض «4.8 من 120 تقييمًا» بينما التقييمات الحقيقية **ستّة في القاعدة كلّها**. أُسقط. والإسقاط وحده كان سينتج `★ 0 (0)` — يُقرأ كتقييم سيّئ لا كغياب تقييم — فأُضيف `hasRating()`: **مكان بلا تقييم حقيقي لا تُرسم له شارة نجمة إطلاقًا**. **مُتحقَّق حيًّا: 6 بطاقات، 2 فقط بتقييم (`★ 5 (5)` + `★ 5 (1)` = 6).** وأُصلح في الطريق [`app.js:3001`](app/native/app.js:3001) الذي كان يُعيد بناء شارات التفاصيل يدويًّا بدل `renderDetailBadges` ⇒ **شارة الدوام كانت تختفي بعد إرسال أي تقييم**.

**3. ✨ الصقل (البند 8) — واثنان منه كانا موجودَين أصلًا.** `@capacitor/haptics` ثُبّت ومُسجَّل في الـAPK، وجسر `window.__haptic` يشتقّ الشدّة من نفس أرقام الـms ⇒ **صفر تغيير في نقاط النداء**. و**skeletons للوحة المالك**: بلاطات الإحصاء كانت تعرض «0» أثناء الجلب — يُقرأ كخبر لا كانتظار. أمّا **bottom sheet للفلاتر فكان مُنفَّذًا أصلًا** ([`app.css:763`](app/native/app.css:763): مثبّت أسفل · مقبض · سحب للإغلاق · `.fx-actions` لاصقة)، و**تبويب Browse منفصل لاغٍ** لأن `home` **هي** التصفّح.

**4. 🔏 توقيع release (البند 7).** `signingConfigs` في [`build.gradle`](android/app/build.gradle) يقرأ `android/keystore.properties` **إن وُجد** (غيابه لا يكسر البناء — مُتحقَّق: `assembleDebug` نجح)، و[`.gitignore`](.gitignore) يمنع رفع المفتاح، والدليل في [`android/SIGNING.md`](android/SIGNING.md). **المفتاح وكلمة سرّه على المالك — لا تُخزَّن في الريبو ولا أتعامل معها.**

**التحقّق:** 72 فحص overflow (375 و320 × ثيمين × لغتين × 9 صفحات) صفر فشل · كونسول نظيف · `node --check` ✓ · **بناء الموقع 638,160 محرفًا ثابت** · APK 7.07MB مبنيّ ومنسوخ للسطح، ومُتحقَّق **من داخله**: جسر haptics ✓ · إصلاح `transition` ✓ · `--vt-ox` ✓ · `scale(1.045)` ✓ · **صفر أثر لـ`clip-path` الدائرية** · هيكل المالك ✓ · **صفر قراءة لـ`rating_seed`** · `HapticsPlugin` في `capacitor.plugins.json` ✓.

---

## 🚦 حالة سابقة (2026-07-26) — الدفعة العاشرة ✅ **السبب الجذري الحقيقي لـ«الشمسين» وُجد وأُزيل**

**🐛 خمس محاولات فشلت لأنها كانت تُصلح المكان الخطأ. الرسم الثاني لم يكن في الأيقونة إطلاقًا — كان قاعدة CSS منسيّة.**

في [`app.css`](app/native/app.css) بقيت **قاعدة من زمن الإيموجي** (يوم كان `applyTheme` يكتب `🌙`/`☀️` نصًّا فيُخفى بـ`font-size:0` ويُرسم فوقه):
```css
button[data-action="toggleTheme"]::before{content:'';…mask:var(--icon-moon)…}
body.dark button[data-action="toggleTheme"]::before{mask-image:var(--icon-sun)}
```
حين استُبدل الإيموجي بـ`<svg class="thm-ic">` في `app.html` **لم تُحذف هذه القاعدة**. والزرّ `display:inline-grid` ⇒ الـ`::before` والـ`<svg>` صارا **خليّتَي شبكة** ⇒ **رسمان متطابقان مكدّسان رأسيًا**، وكلاهما يتبع الثيم (قمران نهارًا · شمسان ليلًا) = صورة المستخدم حرفيًّا.

> ⚠️⚠️ **لماذا نجت من كل فحص عبر خمس دفعات — الدرس الأهم:**
> 1. **الـpseudo-element ليس عقدة DOM.** كل الفحوص السابقة عدّت `b.querySelectorAll('svg').length` و`'path'` فأرجعت `1` دائمًا **مهما تكرّر الرسم**. العدّ كان صادقًا والاستنتاج خاطئًا.
> 2. **الفحص بحث في المكان الخطأ:** «لا `::before` على `.icon-btn`» — والقاعدة على **محدّد السمة** `button[data-action="toggleTheme"]` الذي يطال الأزرار الأربعة بأصنافها المختلفة.
> 3. **لم يُنظر إلى بكسل واحد قط.** اللقطات كانت معطّلة بيئيًا، فصار «التحقّق» قياسَ DOM حصرًا — وهو أعمى تمامًا عن هذا العطل. (اللقطات تعمل الآن ✓ — استعملها.)
> **القاعدة المستخلصة:** لعدّ الرسوم في عنصر، لا تعدّ الأبناء — اقرأ `getComputedStyle(el,'::before').content` و`::after` معهم، **ثم انظر إلى الصورة**.

**الإصلاح — حذف + حارس بنيوي:** حُذفت القاعدة القديمة، وحلّ محلّها حارس يجعل الرسم الثاني **مستحيلًا** لا مجرّد محذوف:
```css
html body button[data-action="toggleTheme"][data-action][data-action]::before,
… ::after{content:none!important;display:none!important;mask:none!important;background:none!important}
… >:not(.thm-ic){display:none!important}
```
المحدّدية مرفوعة عمدًا (تكرار السمة + `html body`) لتفوز على أي قاعدة قد تُكتب لاحقًا ولو بـ`!important` — بما فيها النمط الأرجح `body.dark .icon-btn::before`. **الأيقونة تبقى `<path>` واحدًا يبدّل `applyTheme` سمة `d` له** (بنية الدفعة التاسعة سليمة — لم تكن هي المشكلة).

**التحقّق (هذه المرّة بالطريقة التي كانت ناقصة):**
- **اختبار إجهاد بحقن القاعدة المعطوبة نفسها حيًّا** + قاعدتين عدائيتين (`!important` بمحدّدية أعلى، وواحدة مقيّدة بـ`body.dark`) ⇒ **الحارس أبطلها الثلاث: 1 رسم/زرّ**. وابن `<svg>` إضافي مزروع في الـDOM ⇒ مُخفى.
- 8 حالات متتابعة (إقلاع · 4 تبديلات · لغة ذهابًا وإيابًا · رشقة 5 ضغطات) ⇒ **`1,1,1,1` في كل حالة والشكل مطابق للثيم دائمًا**.
- **لقطات فعلية:** قمر واحد نهارًا · شمس واحدة ليلًا، على زرّ البداية وزرّ الرئيسية. التمركز `dy=0`.
- كونسول نظيف · صفر overflow · **بناء الموقع 638,160 محرفًا ثابت** · APK 7.07MB ومُتحقَّق **من داخله**: 4 أزرار · 4 `svg` · 4 مسارات · **صفر أثر للقاعدة القديمة** · الحارس حاضر.

---

## 🗄️ ترحيل الباكند إلى قاعدة بيانات (2026-07-26) — **الخطة جاهزة، التنفيذ معلّق على المستخدم**

الخطة الكاملة في [`PLAN-db-migration.md`](PLAN-db-migration.md)، والمخطّط جاهز للتشغيل في [`migration/01_schema.sql`](migration/01_schema.sql) (جداول + RLS + سجلّ تدقيق بـtriggers + عروض تُبقي شكل استجابة الواجهة + عروض لوحة الأدمن).
- **الوجهة:** Supabase (Postgres مُدار) — مجاني فعليًّا بمقاس المشروع؛ مصيدتاه: توقّف بعد أسبوع خمول (نبضة GitHub Action) وغياب النسخ الاحتياطية (`pg_dump` يومي). و**مصادقة الهاتف بـOTP ليست مجانية** ⇒ نستعمل بريدًا مشتقًّا من الرقم داخليًّا بلا تغيير في تجربة المستخدم.
- **مكتشفات أمنية تبرّر الترحيل (من قراءة `Code.gs`):** كلمات السرّ **نصّ صريح** في الشيت ([`Code.gs:453`](Code.gs:453)) — لا Base64 كما كان مذكورًا سابقًا؛ والتوكن `base64(id|phone|place_id)` **بلا توقيع ولا انتهاء** فلا يمكن إبطاله. وكلمات السرّ الصريحة تعني أن الاستيراد يستطيع تجزئتها ⇒ **لا أحد يحتاج إعادة تعيين كلمة سرّه**.
- **✅ أُنجز (2026-07-26):** المشروع حيّ `nxqddfuwtrsabprxcfez` · المخطّط والبيانات مُستورَدة ومُتحقَّقة (18 مكانًا · 25 ملعبًا · 65 حجزًا · 6 تقييمات · أدمن + 3 مُلّاك) · **وحدة `API` في التطبيق تتحدّث مع Postgres ومبنيّة في APK** (صفر طلبات إلى Apps Script عدا AI).
- **العقد محفوظ:** الطبقة الجديدة تُعيد **نفس شكل الاستجابة** القديم، فلم يتغيّر أيّ من الـ19 موضع استدعاء. `row_number` صار uuid الحجز فيدور دورة كاملة بلا تعديل. والتوكن صار JSON لجلسة Supabase يمرّ عبر `Session` كنصّ معتم ⇒ `Session` لم تُلمس.
- **⏳ معلّق على المستخدم (يحجب التسجيل والتقييم):** تشغيل [`migration/04_policy_fixes.sql`](migration/04_policy_fixes.sql) — سياسة `insert` على `profiles` كانت مفقودة (RLS مفعّل بلا سياسة إدراج ⇒ كل تسجيل جديد يفشل) — **وتعطيل «Confirm email»** في Authentication ← Providers ← Email (مُتحقَّق: `mailer_autoconfirm=false` الآن ⇒ التسجيل يُنشئ حسابًا بلا جلسة).
- **⏳ المتبقّي:** تبديل `API` في الموقع (`app/app.js`) · صفحة الأدمن + `audit_log` الحيّ · التحويل النهائي.
- **دليل التعديل اليومي:** [`migration/RECIPES.sql`](migration/RECIPES.sql) — **مرجع لا سكربت**: انسخ الوصفة المطلوبة وحدها، ولا تُشغّل الملفّ كاملًا (فيه معرّفات وهمية وتعديلات فعلية).
- **⚠️ يؤثّر على المعلّق القديم:** إن تمّ الترحيل خلال أسابيع، فلا داعي لصرف وقت على نشر تعديلات `Code.gs` (`reviews_dist` + إلغاء اللاعب + الملاعب الموقوفة) — ستذوب في مرحلتَي الاستيراد وتبديل `API`.

---

## 🚦 حالة سابقة (2026-07-26) — الدفعة التاسعة ✅ **بنية الأيقونة (سليمة، لكنها لم تكن سبب العطل)**

**🐛 «شمسان فوق بعض» — أُغلق نهائيًّا بإزالة إمكانية وجود رسمين أصلًا.** التحليل: العَرَض الذي أبلغ عنه المستخدم ثلاث مرّات كان **رسمين متطابقين** دائمًا (شمسان في الليلي، قمران في النهاري) — والتطابق يعني **تكرارًا** لا تراكب حالتين. تُتبّعت كل مصادر التكرار الممكنة ونُفيت واحدة واحدة: `app.html` فيه 4 أزرار فقط (تُحقّق)، والبناء يحوي 4 (تُحقّق)، و`applyTranslations` لا يلمس هذه الأزرار (`data-i18n-aria` يكتب سمة لا محتوى)، و`native.js` لا يذكرها إطلاقًا (تُحقّق بالبحث)، ولا `::before/::after` على `.icon-btn` (تُحقّق). ومع ذلك تكرّر العطل على الجهاز رغم سلامة المعاينة في كل مرة.
**القرار الهندسي: لا تُطارد سببًا غير قابل لإعادة الإنتاج — أزل الشيء الذي يمكن أن يتكرّر.** الأيقونة صارت **`<path>` واحدًا** يبدّل `applyTheme` سمة `d` له بين القمر والشمس (الشمس مرسومة كاملةً في نفس السمة: قرص بقوسين + ثمانية أشعّة). **لم يعد في الزرّ سوى رسم واحد** — لا مجموعتان، لا عنصران، لا تراكب، لا شفافية، لا تحويلات متقابلة ⇒ يستحيل ظهور رسمين مهما فشل من CSS أو تكرّر من DOM.
**التحقّق الشامل:** تعداد آليّ لكل زرّ (4 أزرار) عبر: الإقلاع · 4 تبديلات ثيم · تبديل لغة ذهابًا وإيابًا (يشغّل `applyTranslations` على الشجرة كلّها) · 3 تبديلات إضافية · رشقة 5 ضغطات كل 120ms ⇒ **في كل لحظة: `1 svg / 1 path / 0 عناصر رسم أخرى`، والشكل مطابق للثيم دائمًا**، وصفر حالة عالقة. وشبكة أمان القفل خُفّضت إلى 900ms فلا تُبتلع ضغطة تالية (تُحقّق: 4/4 ضغطات بطيئة سُجّلت).
> ⚠️ **درس:** حين يتكرّر عطل لا يُعاد إنتاجه محليًّا بعد ثلاث محاولات إصلاح، توقّف عن إصلاح الآلية وابحث عن **أبسط بنية لا تسمح بالعطل**. «رسم واحد» أقوى من «رسمان مضبوطان».

**التحقّق العام:** 108 فحص overflow · كونسول نظيف · `node --check` ✓ · **بناء الموقع 638,160 محرفًا ثابت** · APK 7.07MB ومُتحقَّق من 8 مؤشّرات فيه.

---

## 🚦 حالة سابقة (2026-07-26) — الدفعة الثامنة ✅ **ثلاثة، مبنيّة في APK**

1. **🐛 «شمسان فوق بعض» — التحليل الجذري أخيرًا.** المحاولتان السابقتان كدّستا **`<svg>`ين ككتلتين في تخطيط CSS**: مرّة بـ`position:absolute` ومرّة بخليّة `grid-area`. كلتاهما تفترض صمود سلسلة كاملة من خصائص التخطيط — الزرّ نفسه `display:grid` (`.icon-btn`/`.onb-tool`)، والغلاف `inline-grid`، والابنان يتراكبان. **سقوط أي حلقة** (نسخة WebView · قاعدة تفوز على `display` · فشل تحليل `grid-template-areas`) يُعيد الابنين إلى التدفّق الطبيعي **فيتكدّسان رأسيًا** = بالضبط ما صوّره المستخدم. **الحلّ الجذري: الشكلان `<g>` داخل `<svg>` واحد** ⇒ يتموضعان بإحداثيات الـviewBox لا بتخطيط CSS، فالتراكب **خاصيّة بنيوية لا نتيجة قواعد**. **تُحقّق باختبار إجهاد:** فُرض عمدًا `display:block` على الزرّ والغلاف وأُلغي `position`/`grid-area` من المجموعتين ⇒ **بقيتا متمركزتين تمامًا** (فارق المركز <1.5px). يستحيل أن تتكدّسا مهما فشل من CSS.
   > ⚠️ **درس:** حين يتكرّر عطل بصري بعد إصلاحين، لا تُصلح العَرَض — **أزل الاعتماد** الذي يسمح بالعطل أصلًا. تراكب عنصرين عبر CSS هشّ؛ تراكبهما داخل SVG واحد مضمون.
2. **🌗 التبديل ما عاد يغطّي المحتوى:** أُعيد **View Transitions** كمسار أساسي (كان إزالته في الدفعة السادسة مبنيًّا على التشخيص الخاطئ أعلاه). لقطة «قبل» تبقى ساكنة تحت، ولقطة «بعد» تُقصّ بدائرة تكبر من الإصبع ⇒ **الواجهة الجديدة بمحتواها الحقيقي** هي ما ينتشر، فلا لون مصمت يغطّي الشاشة ولا لحظة عمياء. **تُحقّق: صفر `.theme-reveal` (القرص المصمت) يُنشأ أثناء التبديل.** القرص بقي **احتياطيًا فقط** لأي WebView بلا الـAPI. وإيقاف الانتقالات محصور بقائمة الوسوم ولحظة التقاط اللقطة فقط — لا `*` إطلاقًا (سبب التلعثم سابقًا).
3. **⚽ رموز الرياضات وُحّدت في كل مكان:** كانت صفحة البداية وهيرو التفاصيل تحملان نسخًا **مكتوبة يدويًا** بالرمز القديم. صارت تُشتقّ من نفس خريطة `ICON` في `app.js` (مصدر واحد للحقيقة). **تُحقّق: شرائح البداية الأربع + هيرو التفاصيل + زرّ الرياضة + القائمة (5 متمايزة) كلّها من المجموعة الجديدة.**

**التحقّق:** 108 فحص overflow · كونسول نظيف · رشقة 6 ضغطات ثيم ⇒ صفر تعليق وتزامن سليم · `node --check` ✓ · **بناء الموقع 638,160 محرفًا ثابت** · APK 7.07MB ومُتحقَّق من 10 مؤشّرات فيه.

---

## 🚦 حالة سابقة (2026-07-26) — الدفعة السابعة ✅ **أربعة، مبنيّة في APK**

بلاغات المستخدم بالصور من الجهاز:

1. **🐛 «شمسان وقمران» — التشخيص السابق كان خاطئًا.** عُزي في الدفعة السادسة إلى لقطتَي View Transitions، لكن صورة المستخدم أظهرت الازدواج **ساكنًا** (خارج أي انتقال) و**رمزين متطابقين مكدّسين رأسيًا** — وهذا توقيع انهيار `position:absolute`: عندها يتدفّق الـsvgان في `display:grid` صفًّا تحت صفّ. **الحلّ البنيوي:** التكديس صار بـ**خليّة شبكة واحدة** (`grid-template-areas:'thm'` + `grid-area:thm`) وهو نفس النمط المستعمل أصلًا في `.place-loader` بهذا الملف ⇒ لا اعتماد على التموضع المطلق إطلاقًا. **تُحقّق على الأزرار الأربعة × الثيمين: متمركزان تمامًا (فارق المركز <1px) وواحد فقط ظاهر، و`position:static`.**
   > ⚠️ **درس:** حين يُظهر بلاغ المستخدم رمزين **متطابقين**، فالسبب تكرار تخطيطي لا تراكب حالتين. وصورة الشاشة تحسم ما لا تحسمه المعاينة.
2. **🌗 لحظة العتمة أثناء تبديل الثيم:** كانت الشاشة مغطّاة بالكامل ~200ms. قُصّر النموّ إلى 340ms بمنحنى ينهي التغطية سريعًا، والتلاشي إلى 130ms ⇒ **نافذة التغطية 148ms مقيسة**. ولأن لون القرص = `--bg-primary` للثيم الوجهة، فما «يظهر» أثناء التلاشي هو المحتوى فوق خلفية مطابقة لا شاشة فارغة.
3. **⚽ الكرة النطّاطة رجعت بفيزياء حقيقية** (بطلب المستخدم، بدل لمعة البطاقة الكاملة): الإزاحة ∝ t² ⇒ **الهبوط يتسارع** (`cubic-bezier(.45,0,.9,.45)`) و**الصعود يتباطأ**، مع **انضغاط عند الارتطام** (`scale(1.2,.8)`) واستطالة عند الانطلاق، ودورة **1.25s بطيئة عمدًا**. الظلّ يكبر ويعتم متزامنًا. **محصورة في منطقة الصورة فقط** (تُحقّق: مستطيل اللودر = مستطيل `.sk-img` بالضبط، 190px). وshimmer **هادئ** (2.2s، تباين منخفض، قيم صريحة لكل ثيم) على الأسطر الثلاثة والزرّ أسفلها.
4. **✨ فتح التطبيق أفخم:** **حلقتان متتاليتان** (`introRing`/`introRing2` بفارق 300ms) تُقرآن كنبضة إشارة · **استقرار تباعد حروف الاسم** (.18em→−.02em) مع كشف القناع ⇒ الاسم «يستوي» في مكانه · **الخروج ستارة ترتفع** (`clip-path` من الأسفل للأعلى) بدل تلاشٍ مسطّح، فتنكشف صفحة البداية من جهة اللوح السفلي أولًا بينما الشعار يزوم عبر الشاشة. الإجمالي **1.76s** ومؤقّت الحذف 1800ms.

**التحقّق:** 108 فحص overflow (320/360/390 × ثيمين × لغتين × 9 صفحات) صفر فشل · كونسول نظيف · `node --check` ✓ · **بناء الموقع 638,160 محرفًا ثابت** · APK 7.07MB ومُتحقَّق من 11 مؤشّرًا فيه.

---

## 🚦 حالة سابقة (2026-07-25) — الدفعة السادسة ✅ **ستّة إصلاحات، مبنيّة في APK**

بلاغات المستخدم بعد تجربة الدفعة الخامسة على الجهاز:

1. **🐛 «شمسان وقمران» على زرّ الثيم + تعليق أثناء التبديل — سببهما واحد: View Transitions.** أثناء الانتقال تكون **لقطة «قبل» ولقطة «بعد» معًا على الشاشة**، فيظهر القمر القديم والشمس الجديدة في آنٍ واحد. وإبطال الانتقالات بالمحدّد الشامل `*` أثناءه يُبطل أنماط كل عقدة في الشجرة ⇒ تلعثم. **أُزيل المسار كليًّا** (JS + CSS) واستُبدل بـ**قرص يتمدّد بـ`transform:scale` فقط** — مركَّب على الـGPU بالكامل (تُحقّق: الأنيميشن يحرّك `transform` حصرًا)، والثيم يُبدَّل **تحت القرص وهو يغطّي الشاشة** فتنقلب الأيقونة مخفيّة ⇒ صفر ازدواج. وإيقاف الانتقالات صار محصورًا بقائمة الوسوم نفسها لا بـ`*`. **تُحقّق: 6 ضغطات متتالية كل 90ms ⇒ صفر قرص عالق، الأيقونات متزامنة، والتبديل ما زال يعمل بعدها.**
   > ⚠️ **درس:** View Transitions ممتاز لتنقّل الصفحات، لكنه **غير مناسب لتبديل ثيم فيه أيقونة تتغيّر** — اللقطتان تُظهران حالتَي الأيقونة معًا. المظهر «الأحدث» لا يساوي الأفضل.
2. **🐛 علامة «تذكّرني» مقلوبة:** `.remember-box::after` يبني الصح من `border-inline-start` + `border-bottom`. الحدّ **المنطقي** ينقلب إلى اليمين في العربية فتُرسم الزاوية معكوسة. **علامة الصح شكل لا نصّ** ⇒ ثُبِّتت فيزيائيًا `border-left`. تُحقّق: الحدود متطابقة في rtl وltr.
3. **🐛 «الكل» تختفي خلف زرّ الفلاتر:** الزرّ كان **لاصقًا داخل مسار الانزلاق** بدرع 8px وتلاشٍ 10px = 18px تغطية، بينما الفجوة 10px ⇒ التبويب الأول تحته حتى في السكون. (محاولة `scroll-padding` فشلت: محرّك الالتقاط حاذى التبويب بالحافّة فابتلع الحشوة — قيس انزلاق تلقائي −12px.) **الحلّ البنيوي: أُخرج الزرّ من الصفّ** فصار الصفّ محصورًا بين زرّين ثابتين، وأُلغي الالتقاط. **تُحقّق: تقاطع مستطيل الصفّ مع الزرّين = 0px ⇒ يستحيل بنيويًا أن يظهر تبويب فوق زرّ.** والارتفاع بقي **58.8px** بتوزيع الحشوة (6px للحاوية + 3px للصفّ — الصفّ حاوية تمرير ومنطقة لمس التبويب تمتدّ 3px، فحشوة صفر كانت تجعله **يتمرّر عموديًا**).
4. **🎨 رموز رياضات أوضح:** 4 من 5 كانت دوائر متشابهة عند 20px. صارت: كرة قدم (خماسي مصمت + درزات) · سلة (الدرزات الكلاسيكية) · طائرة (3 درزات منحنية) · **تنس = مضرب مشبّك** · **بادل = مضرب مثقّب** ⇒ ظلال ظليّة مميّزة لا دوائر متكرّرة.
5. **✨ لودر البطاقات:** حُذفت «الكرة النطّاطة» (حركة صاخبة تجذب العين إلى الانتظار) واستُبدلت بـ**لمعة مائلة واحدة تعبر البطاقة كاملةً** بتتالٍ 0.12s بين البطاقات ⇒ تُقرأ كموجة واحدة تنزل القائمة. transform فقط على عنصر واحد لكل بطاقة.

**التحقّق:** **108 فحص overflow** (320/360/390 × ثيمين × لغتين × 9 صفحات) صفر فشل · كونسول نظيف · `node --check` ✓ · **بناء الموقع 638,160 محرفًا ثابت** · APK 7.07MB منسوخ للسطح ومُتحقَّق من عشرة مؤشّرات فيه.

---

## 🚦 حالة سابقة (2026-07-25) — الدفعة الخامسة ✅ **اكتملت ومبنيّة في APK**

خمسة طلبات من المستخدم (مع إحالته إلى `Desktop\المالية\finance-entry.html` كمرجع لحركة المبدّل) + بحث إنترنت طُبّق:

1. **⚡ بطء الإجراءات — السبب الجذري وُجد وأُصلح.** `openDetail` كان ينفّذ `await loadPublicBookings()` **قبل** `showPage`، فالضغط على «احجز الآن» يتجمّد طوال طلب شبكة كامل (**مقيس هنا: 2.65-3.38s**، وأسوأ على شبكة الهاتف) والزرّ بلا أي تغذية بصرية (لا `withLoading` عليه). صار: **الصفحة تُرسم فورًا** من التوفّر المخزّن (الذي يصل أصلًا مع `getInitialData`)، والتحديث يجري **بالخلفية** عبر `refreshDetailAvailability` بكاش 45s (`ensurePublicBookings`) مع شريط «يُحدَّث» رفيع فوق شبكة الأوقات. **مقيس بعد الإصلاح: 344ms للتنقّل، و8 أوقات مرسومة فورًا.** آمن لأن `confirmBooking` يتحقّق محليًا ثم الخادم هو الحكم النهائي (LockService)، وإن صار الوقت المختار محجوزًا أثناء التحديث الخلفي يُسقَط الاختيار مع توست. `resumePendingBooking` يمرّر `{awaitFresh:true}` لأنه يقرأ `bookedSlots` مباشرة بعد الفتح.
2. **🌗 تبديل الثيم أحدث:** تُرك الغطاء المسطّح كمسار احتياطي فقط، والمسار الأساسي صار **View Transitions API** (مدعوم في WebView كروميوم — تُحقّق `startViewTransition` متاح): المتصفّح يلتقط لقطتَي قبل/بعد، فنقصّ **لقطة «بعد»** بدائرة تكبر من الزرّ فوق لقطة «قبل» ⇒ **الواجهة الجديدة بمحتواها** تنتشر من الإصبع (أثر تيليجرام الحقيقي) لا مجرّد لون. + **أيقونة شمس/قمر تتحوّل** بدوران وتلاشٍ متبادل بدل تبديل إيموجي (`applyTheme` صار يبدّل صنفًا فقط — لمس `textContent` كان سيمحو الـSVG).
3. **🧹 صفّ التصفّح أُعيد تصميمه من الصفر:** الازدحام كان من **24 حافّة بصرية** (8 حبوب × حدّ+ظلّ+سطح) + زر رياضة يحجز ~120px دائمًا. صار **تبويبات هادئة بخطّ سفلي** (نمط شريط تصنيفات Airbnb: نصّ عارٍ + خطّ 2.5px للنشط فقط) ⇒ حافّة واحدة في الصفّ كلّه، وعرض كل عنصر أقلّ ~30% (الكل 64→42px · المفضّلة 107→84 · عرجان 73→50). وزرّ الرياضة صار **دائرة أيقونية 44px** تطابق زرّ الفلاتر (الاسم بقي في الـDOM لقارئ الشاشة وداخل القائمة — كان يحجز أثمن عرض ليقول شيئًا لا يتغيّر). **الارتفاع 59px — مقيس مطابقًا للتصميم الأصلي وللدفعة الرابعة معًا (صفر بكسل إضافي).**
4. **🎚️ المبدّل أنعم:** تُبنّي `--sp-soft` من مرجع `finance-entry.html` (نابض ζ=0.70 · **620ms** بدل 420ms خاطف — المسافة نصف عرض اللوح فتحتاج زمنًا لتُقرأ كحركة)، والحبّة صارت **متدرّجة بتوهّج ملوّن** (Teal نهاري · Lime ليلي) والنص النشط أبيض/داكن (**تباين مقيس 6.92-9.76:1**)، + **موجة ضوء من نقطة الإصبع بالضبط** (`--rx/--ry` من `pointerdown` مفوّض — النمط المرشّح في مرجع المالية، استُخدم أخيرًا).
5. **🌃 صورة جديدة — ملعب ليلي عمودي** (Pexels 16826134): **عمودية أصلًا** (760×1140) بنفس اتجاه الإطار ⇒ القصّ ~12% رأسيًا فقط بدل فقدان **49% من العرض** في الأفقية السابقة؛ وهي **ملعب خماسي مسوّر بكشّاف** = المنتج نفسه لا استاد محترفين، والكرة الليمونية تصدح مع أكسنت الهوية. **الحجاب انقلب شكله بالقياس:** السماء سوداء أصلًا (اسم العلامة **15.9:1** بلا حجاب) فخُفّف الدرع الأعلى إلى `.32→0 عند 26%` (الصورة تظهر أخيرًا)، والثقل انتقل للوسط حيث الكشّاف والعشب المُضاء. **كل النطاقات تنجح بأسوأ بكسل:** اسم العلامة 15.88 · الأدوات 19.42 · الشارة 5.25 · العنوان 6.43 · الوصف 11.77.

**التحقّق:** **108 فحص overflow** (320×640 · 360×780 · 390×844 × ثيمين × لغتين × 9 صفحات) صفر فشل · كونسول نظيف · `node --check` ✓ · **بناء الموقع 638,160 محرفًا ثابت** · APK 7.07MB منسوخ للسطح ومُتحقَّق من أصوله الإحدى عشرة.

> **مزلق معاينة إضافي (2026-07-25):** حين يكون جزء المتصفّح **مخفيًا** لا يُصيّر إطارات ⇒ `requestAnimationFrame` و`img.decode()` و`setTimeout` الطويلة **تتعلّق** (فتنتهي مهلة الأداة 30s). البدائل المُثبتة: `createImageBitmap(await (await fetch(url)).blob())` بدل `Image.decode()`، واستطلاع بـ`void el.offsetWidth` (إعادة تدفّق متزامنة) بدل rAF/انتظار. وتذكّر أن الانتقالات مجمّدة: أي قراءة لـ`color/opacity/transform` بعد تبديل صنف **يجب** أن تسبقها `el.style.transition='none'` وإلا قرأت قيمة ما قبل التغيير (وقعتُ فيها مرتين هنا).

---

## 🚦 حالة سابقة (2026-07-25) — الدفعة الرابعة ✅ **اكتملت كلها ومبنيّة في APK**

الـ6 تعديلات في [`app/native/PLAN-next-session.md`](app/native/PLAN-next-session.md) نُفّذت ومُحقَّقة حيًّا (الملف مُذيَّل بـ«✅ مُنفّذ»):
1. **مبدّل لاعب/مالك منزلق بلا قفز:** عنصر `.onb-seg-thumb` (حبّة فيزيائية) ينزلق بـ`--seg-i` × `--nav-dir` بنابض `--sp-snap` 420ms، و`.onb-seg{gap:0}` مقصودة كي يساوي عرض الزرّ نصف المسار بالضبط فينزلق 100% بلا تصحيح فجوة. الأزرار شفّافة دائمًا (زال `background` من `.active`، بقي لون النص). اللوحان في **غلاف `.onb-panels{display:grid}` بخلية واحدة** ويتبادلان بصنف `.off` (opacity/visibility/translateY) **لا بـ`hidden`** (يصفّر الارتفاع ⇒ قفزة). **مقيس: ارتفاع اللوح 352.8px ثابت عبر كل تبديل × لغتين**، وعرض المؤشّر = عرض الزرّ 169.2px بالضبط، والإزاحة −169.2 بالعربي / +169.2 بالإنجليزي.
2. **صورة لاعبين حقيقية:** `assets/onb/players-3148452.jpg` (لقطة أكشن) حُقنت base64 (134KB) عبر `embed-onb-photo.ps1`، و`background-position` صار `center 45%`. **الحجاب أُعيد ضبطه بقياس فعلي** (الصورة نهارية ساطعة بعكس الغروب السابق): الطبقة الوسطى صارت `transparent 16% → .52 عند 42% → .80 عند 64% → .96 عند 88%`، و**الشارة `.onb-eyebrow` نالت قاعدة داكنة** `rgba(6,32,37,.5)` تحت صبغة Lime (بدونها 3.06:1 راسب — تقع فوق أفتح نطاق: قمصان بيضاء وعشب مشمس). **كل النطاقات الخمسة تنجح بمعيار «أسوأ بكسل» لا المتوسط:** اسم العلامة 5.84 · الأدوات 6.02 · الشارة 5.19 · العنوان 5.92 · الوصف 10.26.
3. **شريط التصفّح:** زر الفلاتر صار **أيقونة دائرية 44×44 بلا كلمة «الفلاتر»** (النص انتقل إلى `aria-label` عبر `data-i18n-aria`)، والعدّاد صار **شارة ركنية** Lime بطوق أبيض. حبوب المناطق أوسع (حشوة 18px · خط 13px · فجوات 10px). **الميزانية الرأسية محفوظة بالضبط: 58.8px قبل وبعد** (مقيسة حيًّا بمحاكاة الحالة السابقة — أول محاولة زادت 2px فصُحّحت بخفض `padding-block` 9→7px). ⚠️ **حشوة الصفّ ودرع الزرّ اللاصق مقترنان** (`inset:-7px -10px`): الصفّ حاوية تمرير، فأي تجاوز يجعله يتمرّر عموديًا (تُحقّق: `scrollHeight==clientHeight==58`). وأُعيد تعريف `.has-filters` صراحةً لأن قواعد التصغير كانت تفوز عليه بترتيب المصدر.
4. **INTRO أجمل:** لمعان قطري يجتاز الشعار (`introShine` 820ms/470ms) · **كشف الاسم بقناع** `clip-path` يتبع الاتجاه (`introWipeRTL`/`introWipeLTR`) · **حلقة منتصف الملعب** خلف الشعار (`introRing`) · **خروج «مرورًا عبر الشعار»** (`introThrough` scale→1.5 + تلاشٍ) بينما `onbPhotoIn` مُدّدت إلى **1900ms** فتبقى الصورة تستقرّ أثناء الخروج ⇒ زووم متّصل. الإجمالي 1.24+0.52=**1.76s** ومؤقّت الحذف في `app.js` صار **1800ms**.
5. **فقاعة تبديل الثيم:** `toggleTheme(btn,e)` ينشئ غطاء `.theme-bubble` مطليًّا بلون **الثيم الوجهة** (`THEME_BG`) ويقصّه بدائرة `0 → Math.hypot(أبعد ركن)` بـ`Element.animate` 460ms. **الترتيب حاسم:** التبديل يقع **عند اكتمال الدائرة** لا قبلها (التبديل المبكّر يُظهر الثيم الجديد خارجها ⇒ يزول الأثر أصلًا)، مع `html.theme-swap` يوقف انتقالات اللون لحظة التبديل ثم تلاشي الغطاء 220ms. حوارس: `prefers-reduced-motion` · بلا WAAPI · نقرة أثناء فقاعة سابقة ⇒ تبديل فوري، **وشبكتا أمان بمؤقّت** فلا يبقى غطاء عالقًا أبدًا.
6. **البحث** كان مُنجزًا في الخطة (روابط المصادر داخلها).

**التحقّق (حيّ على `_preview_app.html`:8012 ببيانات حقيقية — 6 ملاعب و8 مناطق):** صفر overflow على **320×640 و360×780 و390×844 × ثيمين × لغتين × 8-9 صفحات (100 فحص)** · كونسول نظيف · `node --check` ✓ · **بناء الموقع 638,160 محرفًا ثابت = صفر أثر على الموقع** · `cap sync` ✓ · **APK جديد 7.07MB** نُسخ إلى `Desktop\المستديرة-التطبيق.apk` وتُحقّقت أصوله المدمجة العشرة.

> **مزلق معاينة جديد مؤكَّد:** المُصيّر **يجمّد جدول WAAPI أيضًا** (`animation.currentTime` يبقى 0 و`playState` يبقى `running` فلا يُطلق `onfinish` أبدًا) — لذلك فقاعة الثيم عملت هنا عبر **شبكة الأمان بالمؤقّت** لا عبر `onfinish`. أي منطق معلّق على `onfinish` **يجب** أن يحمل مؤقّتًا احتياطيًّا. اللقطات معطّلة بيئيًّا («pane not displayed») ⇒ التحقّق بقياس DOM.

> **⏳ المتبقّي:** اختبار على جهاز حقيقي (الفقاعة/الانزلاق/الاهتزاز/الـINTRO بلمس فعلي — احذف النسخة القديمة قبل التثبيت: تعارض توقيع debug) · bottom sheet للفلاتر بدل المودال · skeletons للوحة المالك · `@capacitor/haptics` بدل `navigator.vibrate` · تبويب Browse منفصل.

---

## 🚦 حالة سابقة (2026-07-24) — اقرأ هذا للسياق، وهو يَنسخ ما دونه

### ⭐ تحوّل جذري 2026-07-24: **فصل كامل** بين الموقع والتطبيق + إعادة هيكلة أصلية (جارية)
بقرار المستخدم (بعد أن أراد إعادة هيكلة التطبيق ليبدو تطبيقًا عالميًا لا موقعًا مصغّرًا) صار **فصل كامل**:
- **الموقع (مُجمّد، لا يُلمس):** `app/index-new.html` + `app/style-new.css` + `app/app.js`.
- **التطبيق (تطوير نشط، ثلاثي مستقل):** `app/native/app.html` + `app/native/app.css` + `app/native/app.js` (نُسخت من الموقع كخط أساس، تتفرّع بحرية) + طبقة `native.css/native.js`.
- **⚠️ بروتوكول صيانة:** أي إصلاح منطق/باكند يخصّ الاثنين يُطبَّق **يدويًا في الملفّين** (منطق مكرّر — ثمن الفصل).
- **`build.ps1` أُعيد كتابته = بناءين مستقلّين:** موقع ⇒ `المستديرة محدث.txt`+`_preview_mohdath.html`؛ تطبيق ⇒ `www/index.html`+`app/native/_preview_app.html`. **لا يلمس root index.html ولا Vercel** (النشر مؤجّل للشحن كي لا ينكسر التطبيق الحيّ).
- **معاينة التطبيق:** خادم `koora-app` في `.claude/launch.json` (بايثون `app/native/` منفذ 8012) ⇒ `localhost:8012/_preview_app.html` (فيه علم `body.native`).
- **إعادة الهيكلة الأصلية (تمريرة «NATIVE APP REDESIGN» مُذيّلة في `app/native/app.css`):** لغة مينِمال أبيض/محايد + Teal `#0F4B53` (أزرار نهارية) + Lime `#8CC63E` (أزرار ليلية + أكسنت اختيار). **✅ أُنجز ومُتحقَّق (ثيمين×لغتين):** خلفية مسطّحة (إلغاء زخارف `#app::before/::after`)، توكنات أسطح صلبة، ظلال أصلية، أزرار إجراء Teal/Lime، تبويبات سفلية صلبة نظيفة، و**هيدر هوم أبيض بتحية زمنية** (`#greetEyebrow` صباح/مساء الخير + اسم مفرد `#playerWelcome` + موقع؛ دالة `updatePlayerGreeting()` بدل 6 مواضع setText).
- **✅ أُنجز أيضًا (تتمّة 2026-07-24):**
  - **حيّز الكاميرا أعلى** (`env(safe-area-inset-top)` بحدّ أدنى على كل الرؤوس: home 16px · compact/auth 24px · `.detail-back` · `.lp-nav`)، و**كل الرؤوس صارت بيضاء أصلية** (compact: حجوزاتي/حسابي/مالك · auth: دخول/تسجيل بأيقونة في دائرة `primary-soft`).
  - **🆕 onboarding جديد كليًّا خاصّ بالتطبيق** (بطلب المستخدم: «لا تبنِ الترحيب زي الموقع») — استُبدل محتوى `#page-welcome` (كان landing تسويقي 233 سطرًا) بنمط **هيرو Teal متدرّج + لوح إجراءات سفلي** فيه **segmented control** لاعب/صاحب ملعب (`data-action="onbRole"` — معالج جديد في `Actions`) يبدّل لوحين. كل الوظائف محفوظة (browse/playerLogin/playerRegister/ownerLogin/theme/lang). CSS = الجزء 12.
  - **أنيميشن ملائمة للهاتف:** تغذية اللمس في `native.css` تحوّلت من **رفع لأعلى** (إحساس hover) إلى **انضغاط لأسفل** `scale` بـ120ms + إيقاف الحركة الخلفية اللانهائية (بطارية) + شبكة أمان `prefers-reduced-motion`.
  - **⚡ تمريرة أداء (الجزء 13):** أُزيل `backdrop-filter` من ~12 سطحًا **معتمًا** (بطاقات/مودال/تبويبات/شريط تصفّح/حقول…) — كان بلا أثر بصري لكنه يُنشئ طبقة دمج GPU لكل عنصر (سبب تقطيع التمرير على أندرويد المتوسط). أُبقي فقط على الطبقات الشفّافة (`modal-overlay`/`success-overlay`/`detail-back`).
  - **🐛 إصلاح: خروج المالك كان غير مرئي** — `.ghost-btn` بنص أبيض على رأس compact الأبيض ⇒ اختفى. صار مرئيًا (وزر الخروج أحمر).
  - **م4 جزئي:** بطاقات إحصاء المالك نُظّفت من زخرفة الويب (بقعة ملوّنة ضبابية + شريط 3px ملوّن) ⇒ بطاقات نظيفة بمربّع أيقونة فقط (نمط المرجع) + إصلاح انكسار عنوان «لوحة المالك» لسطرين.

#### ✅ الدفعة الثانية (2026-07-24، جلسة لاحقة) — م5 + م3 + م4 اكتملت
**مرجع مُلهِم:** `Desktop\المالية\finance-entry.html` (أشار له المستخدم) — استُخرج منه نمطان: **منحنيات نابضة حقيقية بدالة `linear()`** (مشتقّة من معادلة النابض المخمّد) و**قراءة `visualViewport` لارتفاع الكيبورد**.
- **✅ م5 انتقالات صفحات + إيماءة رجوع (الجزء 15 في `app.css`):**
  - توكنان مولّدان لكل مدّة على حدة: `--sp-page` (ζ=0.86 · 320ms · بلا تجاوز) و`--sp-snap` (ζ=0.76 · 260ms · تجاوز 2.5%)، كلٌّ مع سطر `cubic-bezier` احتياطي قبله. **لا تُستعمل بمدّة أخرى** — المنحنى ملزوم بمدّته.
  - `showPage` صار يمنح الصفحة صنف اتجاه: **`pg-fwd`** (تعمّق: هوم⇒تفاصيل) · **`pg-back`** (رجوع) · **`pg-fade`** (تنقّل بين تبويبات الشريط السفلي — التطبيقات الأصلية لا تنزلق بين التبويبات). الاتجاه يتبع `dir` عبر `--nav-dir` (rtl=-1 · ltr=1)، والأصناف تُنظَّف في كل تنقّل + `void offsetWidth` لإعادة تشغيل مضمونة.
  - **إيماءة رجوع من الحافة** (`edgeBackGesture` في `app.js`): سحب من الحافة **الابتدائية** (يمين بالعربي · يسار بالإنجليزي)، الصفحة تتبع الإصبع بـ`translate3d`، تُحسم بـ**32% من العرض** أو **سرعة >0.5px/ms مع مسافة >12%** (حدّ المسافة يمنع رفّة إصبع عابرة من الرجوع)، وإلا ترتدّ بنابض `pg-settle`. حوارس: مودال مفتوح ⇒ معطّلة · `home`/`welcome` ⇒ معطّلة · بدء داخل عنصر يتمرّر أفقيًا فعلًا (`.days-row`…) ⇒ ملغاة · تمرير عمودي ⇒ تُترك · نبضة `navigator.vibrate(8)` عند الإنجاز.
  - **⚡ مهم:** مستمع `touchmove` **غير السلبي** يُركَّب فقط عند لمسة مؤهّلة من الحافة ويُفكّ عند النهاية — مستمع دائم `passive:false` كان سيُلزم المتصفّح بانتظار الـJS في كل حركة تمرير (نفس سبب تقطيع التمرير الذي عالجته تمريرة الأداء).
- **✅ كيبورد الهاتف:** `visualViewport` ⇒ `--kb` + `html.kb-open` ⇒ الشريط السفلي وشريط الحجز اللاصق ينزلان تحت الكيبورد (بدل أن يطفوا بمنتصف الشاشة على iOS/pan mode) والتوست يعلوه (`bottom:calc(108px + var(--kb))`).
- **✅ م3 شارة تصنيف:** نوع الأرضية انتقل من سطر النص السفلي إلى **شارة صغيرة أعلى الصورة** على الطرف المقابل للمفضّلة (`.place-cat-badge` — أبيض 95% نهاري · `rgba(14,20,23,.86)` ليلي) ⇒ صفر تكرار للمعلومة (أُزيل `.place-strip-type`).
- **✅ م4 اكتملت:** أفاتار أحرف العميل في بطاقة حجز المالك (`initials()` + `.owner-bk-av` 38px) · **مصغّرة صورة** للملعب في صفوف الإدارة (`.field-thumb` 52px + أيقونة كرة عند غياب الصورة) · شارات الحالة **مسطّحة بلا نقطة/هالة** (radius 8px، كالمرجع) · FAB والمبدّل بضغطة أصلية (أُزيلت رفعة `hover` الويب).
- **🐛 إصلاح شريط الحالة (كان يخالف الرؤوس البيضاء):** كان Teal دائمًا بأيقونات فاتحة فوق رأس أبيض. صار: `#page-welcome` ⇒ Teal + أيقونات فاتحة · بقية الصفحات ⇒ أبيض (`#FFFFFF`) بأيقونات داكنة (`style:'LIGHT'`) · ليلي ⇒ `#0A0E11` بأيقونات فاتحة. يتزامن مع الثيم (MutationObserver) و**مع الصفحة** عبر حدث جديد `app:page` يبثّه `showPage` (خامل على المتصفح).
- **التحقّق (حيّ، `_preview_app.html` على 8012، ببيانات حقيقية — 6 ملاعب):** أصناف الاتجاه صحيحة في كل مسار · الإيماءة مُختبرة بـTouchEvent مُصنَّعة: سحب كامل ⇒ رجوع · 8% سريع ⇒ لا رجوع · عمودي ⇒ متروك · `.days-row` ⇒ ملغاة · مودال ⇒ معطّلة · صفر transform متبقٍّ · صفر أخطاء كونسول · **صفر overflow على 320 و390 × ثيمين × لغتين** (`scrollWidth==innerWidth`) · تباين الأفاتار/الشارات مقيس في الثيمين · `node --check` ✓ · `build.ps1` ✓ (**بناء الموقع بقي 638,160 محرفًا بالضبط = صفر أثر على الموقع**) · `npx cap sync android` ✓.
- **⏳ يحتاج إعادة بناء APK ليصل للجهاز** (التطبيق مدمج): `cd android; .\gradlew.bat assembleDebug`. غير مُختبر على جهاز حقيقي بعد (الإيماءة/الاهتزاز/شريط الحالة تُقاس بلمس فعلي).

#### ✅ الدفعة الثالثة (2026-07-24) — أنيميشن ترحيبي + صفحة بداية فاخرة بصورة + هيئة عالمية
- **🎬 أنيميشن ترحيبي عند فتح التطبيق (الجزء 18):** عنصر `#intro` **مكتوب في `app.html` قبل `#app`** فيظهر من أول فريم (صفر وميض). لوح Teal متدرّج + توهّج Lime، ثم: الشعار يكبر بنابض `--sp-mark` (ζ=0.62·620ms) ⇒ الاسم يصعد (220ms) ⇒ خطّ Lime يُرسَم `scaleX` (430ms) ⇒ الشطر يصعد (560ms) ⇒ عند 1040ms يرتفع المحتوى ويتلاشى اللوح (460ms). **`app.js` يحذف العنصر من DOM عند 1520ms** (لا حاجز ولا طبقة GPU باقية). `prefers-reduced-motion` ⇒ لوح ساكن 500ms.
- **🖼️ صورة حقيقية في صفحة البداية:** صورة ملعب (Pexels — رخصة مجانية) **مضمّنة base64 داخل `app.css`** في المتغيّر `--onb-photo` (1000px · جودة 68 · 72KB ⇒ 96KB base64). **لماذا base64:** التطبيق مدمج في الـAPK والبناء ملف واحد ⇒ الرابط الخارجي يفشل بلا إنترنت وملف منفصل يحتاج خطوة نسخ. **سكربت الحقن:** `app\native\embed-onb-photo.ps1 -Src <jpg>` (يستبدل سطر `--onb-photo` — قابل لإعادة التشغيل بأي صورة).
- **✨ إعادة هيكلة صفحة البداية (الجزء 19):** `.onb-photo` (تكبير خفيف عند الدخول) + حجاب `.onb-hero-bg` بثلاث طبقات (**درع أعلى** يضمن قراءة اسم العلامة + تعميق سفلي + توهّج Lime) + **شارة علوية** `.onb-eyebrow` (Lime زجاجي) + عنوان أكبر بظلّ + شرائح رياضات زجاجية + **لوح سفلي بمقبض** `.onb-grabber` بنصف قطر 28px يرتفع 26px فوق الصورة.
  - **🐛 اكتُشف وأُصلح أثناء التحقّق:** `.onb-hero-bg` كان **بارتفاع صفر** (قاعدة `.onb-hero>*{position:relative}` في الجزء 12 تأتي بعد `position:absolute` بمحدّدية مساوية فتسحقها) ⇒ الحجاب لم يكن يرسم شيئًا (والتوهّج الأصلي كان ميتًا من قبل أيضًا). أُصلح بمحدّدية أعلى `.onb-hero .onb-hero-bg`.
  - **قياس تباين فعلي (لا تخمين):** رُسمت الصورة في canvas بنفس هندسة `cover`+`center 66%`، وحُسبت لمعان كل نطاق نصّي بعد تركيب ألفا الحجاب طبقةً طبقة ⇒ الشارة 9.6:1 · العنوان 11.4:1 · الوصف 14.7:1 · **اسم العلامة 3.18:1 (راسب!)** ⇒ قوّيت الدرع الأعلى (.88/.52/.16) + ظلّ نص ⇒ **5.38:1** والأدوات 5.35:1.
  - شاشات قصيرة (≤760px ارتفاعًا): ضغط تلقائي للهيرو واللوح ⇒ **360×640 يدخل الشاشة كاملًا بلا تمرير** (كان يفيض 20px).
- **🌍 هيئة عالمية للأزرار والأشكال (الجزء 20 — «BOOKING-CLASS SHAPES»):** نصف القطر قُصِّر عبر التوكنات (`--r-sm..--r-3xl` = 8/10/12/14/16/20 بدل 10/12/14/18/22/28)، و**أُلغي `letter-spacing` من كل الأزرار** (كان .05em يفكّك الحروف العربية)، وزر الحجز/البحث 10px بلا ظلّ ملوّن، والثانوي صار **محدّدًا بحدّ 1.5px بلون الهوية** (لا سطح رمادي — نمط Booking)، والبطاقات 14px بحدّ رقيق وظلّ خفيف، والحقول 10px، والشارات 6-8px، والأوقات 10px والأيام 12px، والمودال 20px أعلى. الحبوب (فلاتر/مناطق) بقيت دائرية كالمرجع العالمي.
- **التحقّق:** أنيميشن الترحيب مُتحقَّق بقراءة الأنيميشن المحسوب لكل عنصر (أسماء/مدد/تأخيرات مطابقة) + تغطية الشاشة 390×844 + حذفه من DOM بعد 1.7s · الصورة محمّلة (97KB data-URI) · **صفر overflow أفقي على 9 صفحات × ثيمين × لغتين (320/360/390)** · كونسول نظيف · `node --check` ✓ · **بناء الموقع 638,160 محرفًا (ثابت = صفر أثر)** · `cap sync` + **APK جديد مبنيّ (7.07MB) ونُسخ إلى `Desktop\المستديرة-التطبيق.apk`** وتحقّقت أصوله المدمجة (intro/photo/eyebrow/grabber/shapes ✓).

### 🚀 شُحن (2026-07-24) — التطبيق صار **مدمجًا** لا OTA
**أُلغي `server.url` من `capacitor.config.json`** (صار `{appId,appName,webDir:"www"}`) ⇒ التطبيق يفتح ملفاته المدمجة داخل الـAPK بلا اعتماد على Vercel.
- **السبب:** `net::ERR_CONNECTION_RESET` عند المستخدم — شبكته تحجب `*.vercel.app` (فلترة SNI؛ عرَض مميّز: يعمل على جهاز واحد فقط لكل شبكة). **ليس حدًّا من Vercel.**
- **`build.ps1` صار يكتب جذر `index.html` = بناء الموقع** (Vercel للزوّار). الموقع **يعمل كما هو** (كوده مُجمّد).
- **APK مبنيّ بنجاح:** `android\app\build\outputs\apk\debug\app-debug.apk` (7.07 MB) ⇒ نُسخ إلى `Desktop\المستديرة-التطبيق.apk`. **أوامر إعادة البناء:** `app\build.ps1` ⇒ `npx cap sync android` ⇒ `cd android; .\gradlew.bat assembleDebug`. (عند التثبيت: احذف النسخة القديمة — تعارض توقيع debug.)
- **⏳ معلّق على المستخدم:** `git push` لجذر `index.html` (تنظيف اختياري — الموقع يعمل في الحالتين).

- **⏭️ الاستئناف من:** ✅ م3 و م4 و م5 + الأنيميشن الترحيبي + صفحة البداية الفاخرة + الهيئة العالمية — كلها اكتملت ومبنيّة في APK جديد. المتبقّي: **اختبار الجهاز** (إيماءة/اهتزاز/شريط حالة/انتقالات بلمس حقيقي — احذف النسخة القديمة قبل التثبيت) · **bottom sheet للفلاتر** بدل المودال · **skeletons** للوحة المالك · **@capacitor/haptics** بدل `navigator.vibrate` · تبويب Browse منفصل (تبويبات 4 كالمرجع).
- التفاصيل الكاملة: الخطة `~/.claude/plans/prancy-booping-aurora.md` + ذاكرة `project_native_redesign_2026-07-24` + `project_native_app_split` + `reference_vercel_url`.

> **مزالق معاينة مؤكَّدة:** مُصيّر المعاينة **يجمّد الأنيميشن عند currentTime:0** ⇒ أي عنصر يبدأ `opacity:0` بأنيميشن دخول (`.place-card`) يظهر **فارغًا في اللقطات** رغم سلامته (تحقّق بالـDOM لا باللقطة) · الصور الخارجية محجوبة في بيئة الوكيل · المتصفّح يبدأ داكنًا (`localStorage.setItem('mustadaira:theme','light')` ثم reload للنهاري) · `$` غير معرّف بنطاق الصفحة (داخل IIFE — استخدم `document.querySelector`) · قراءة `getComputedStyle` مباشرة بعد `classList.add('dark')` تُرجع قيم ما قبل التغيير (اقرأ باستدعاء منفصل) · معاينة لوحة المالك بلا توكن: افرض `page-owner.active` + `nav-owner.show`.

> **ملاحظة:** كل ما دون هذا القسم يصف **النموذج القديم (مصدر مشترك)** — صار مرجعًا تاريخيًا للموقع؛ تطوير التطبيق الآن على ثلاثي `app/native/app.*` حصريًّا.

---

## 🚦 حالة سابقة (2026-07-23) — النموذج القديم (مصدر مشترك)

### 📱 نسختان الآن: ويب + تطبيق هاتف (Capacitor) — **بنية الفصل (الأحدث 2026-07-23)**
- **مصدر مشترك واحد** يغذّي النسختين: `app/index-new.html` + `app/style-new.css` + `app/app.js`. أي تعديل تصميم/محتوى ينعكس على الاثنين.
- **طبقة التطبيق فقط** (لا تلمس الويب أبدًا): `app/native/native.css` + `app/native/native.js` — فيها: **أنيميشن ضغط ref-4** (رفعة `translateY(-2px)` للأوقات `.tbtn`/الأيام `.day-btn`/الرياضات `.sport-tab` + رفعة وظلّ للبطاقة `.place-card`، كلها `:active` مقيّدة بـ`body.native`)، **زر الرجوع الفيزيائي** (`@capacitor/app` backButton ⇒ يغلق المودال عبر Escape، أو يرجع عبر NavStack، أو `exitApp` على الجذر)، **شريط الحالة** (`@capacitor/status-bar` لون Teal نهاري/فحمي ليلي + أيقونات فاتحة، يتزامن مع الثيم عبر MutationObserver)، حواف آمنة `env(safe-area-*)`.
- **الجسر:** `app/app.js` فيه سطر خامل محروس بعد `showPage`: `if(Capacitor.isNativePlatform()) window.__native={back:navigateBack,page:activePageName}` — **خامل تمامًا على المتصفح** (تحقّق حيّ: `window.__native`=undefined، `body.native`=false، كونسول نظيف).
- **البناء الموحّد:** `powershell -ExecutionPolicy Bypass -File app\build.ps1` (⚠️ الملف بـUTF-8 **مع BOM** كي يقرأه PS 5.1 صحيحًا) ⇒ ينتج: (ويب) `المستديرة محدث.txt` + `app/_preview_mohdath.html`، و(تطبيق) `www/index.html` = الويب + طبقة native محقونة قبل `</body>`. **يستبدل سكربت الدمج اليدوي القديم أدناه.**
- **تحديث التطبيق بعد أي تعديل:** `app\build.ps1` ثم `npx cap sync android` ثم بناء APK (`cd android && ./gradlew installDebug`). طالما التعديل ويب فقط لا حاجة لإعادة تهيئة Capacitor.
- **الإضافات المثبّتة:** `@capacitor/app@8.1.1` + `@capacitor/status-bar@8.0.3` (+ core/cli/android). `capacitor.config.json`: appId `com.almustadira.app`، appName `AL-Mustadira`، webDir `www`.
- **✅ التوزيع — اختار المستخدم الخيار ② (server.url→Vercel):** `capacitor.config.json` صار فيه `server:{url:"https://mustadeera.vercel.app",cleartext:false}` ⇒ التطبيق **يفتح Vercel مباشرة**، فكل `git push` يوصل **كل** المستخدمين فورًا بلا إعادة تثبيت APK. لذا **Vercel يخدم بناء التطبيق** (root `index.html` = `www/index.html` المحقون بـnative)، وطبقة native **خاملة تمامًا على المتصفح** (تحقّق حيّ: بناء التطبيق مفتوحًا بمتصفح ⇒ `window.__native`=undefined، `body.native`=false، كونسول نظيف). لا يُعاد بناء APK إلا عند تغيير شيء native. **مقايضة مقبولة:** الإقلاع البارد بلا إنترنت ⇒ شاشة خطأ WebView (التطبيق يعتمد الإنترنت أصلًا).
- **ترتيب:** `www/` صار `index.html` فقط (نُظّف)، وصور الهوية نُقلت إلى `brand/` (logo.png، logo_2.png، «full brand kit.png» — مراجع تصميم غير مستعملة بالكود، الشعار مضمّن base64 في CSS).
- **✅ أيقونة التطبيق (2026-07-23):** مُولّدة من `brand/logo_2.png` (الإمبلم Teal+Lime على خلفية بيضاء). قُصّ الإمبلم بدقّة (alpha bbox 811×667) ورُكّب يدويًا عبر PowerShell بمقاسات تكيّفية صحيحة (108dp: 81/108/162/216/324/432) + مربّعة/دائرية قديمة، في `android/app/src/main/res/mipmap-*`. **إصلاح مهم:** أُعيد كتابة `mipmap-anydpi-v26/ic_launcher*.xml` بلا `inset` (الأداة `@capacitor/assets` وضعت inset 16.7% على الخلفية ⇒ حواف شفافة/شكل مكسور)؛ صار: خلفية بيضاء ممتلئة + مقدمة (إمبلم 60% داخل المنطقة الآمنة). المصادر في `assets/` (icon-foreground/background/only). تُحقَّق بمحاكاة قناع دائري ⇒ بلا قصّ. **شاشة البداية (splash) لم تُعمَل بعد** (طُلبت الأيقونة فقط). لا حاجة `cap sync` (تغيير في android/ مباشرة) — فقط إعادة بناء APK.

- **المصادر الفعلية للتطوير:** `app/index-new.html` + `app/style-new.css` + `app/app.js` (ملفات `index.html`/`style.css` القديمة مهجورة).
- **المخرَج النهائي:** **`المستديرة محدث.txt`** (~657KB على القرص) + معاينة `app/_preview_mohdath.html` — خادم `koora` ⇒ `localhost:8000/_preview_mohdath.html`.
- **النشر (Vercel):** الريبو `malikAlmasri-tech/mustadeera` يتتبّع **ملفًا واحدًا = root `index.html`**. **بعد الخيار ②: `build.ps1` يكتب root `index.html` مباشرةً = بناء التطبيق** (لا حاجة لـ`Copy-Item` بعد الآن)؛ الخطوة صارت فقط: `git add index.html` + commit + `git push origin main` ⇒ Vercel ينشر تلقائيًا على **https://mustadeera.vercel.app/** ويخدمه للتطبيق (server.url) وللمتصفح معًا. الاعتماد مخزّن (بلا PAT). ملاحظة: أطوال `build.ps1` = عدد **المحارف** لا البايتات (العربي متعدّد البايتات).
- **سكربت الدمج الحالي (بعد كل تعديل):**
```powershell
$dir="C:\Users\malik\OneDrive\Desktop\koora\app"; $root="C:\Users\malik\OneDrive\Desktop\koora"
$h=[IO.File]::ReadAllText("$dir\index-new.html");$c=[IO.File]::ReadAllText("$dir\style-new.css");$j=[IO.File]::ReadAllText("$dir\app.js")
$m=$h.Replace('<link rel="stylesheet" href="style.css">',"<style>`r`n$c`r`n</style>").Replace('<script src="app.js"></script>',"<script>`r`n$j`r`n</script>")
$e=New-Object System.Text.UTF8Encoding $false
[IO.File]::WriteAllText("$root\المستديرة محدث.txt",$m,$e);[IO.File]::WriteAllText("$dir\_preview_mohdath.html",$m,$e)
```
- **🟢 آخر جلسة (2026-07-23 — الأحدث) أنجزت + نُشرت على Vercel (4 commits) — مراجع «مراجع/1،2،3» (React/HTML/PDF مرجعية بصرية فقط):**
  - **أُرجعت شاشة التأكيد بطاقة صغيرة** (تراجُع عن `success-full`): `showBookingSuccess` بلا كلاس الشاشة الكاملة؛ العنوان `bookingSuccessTitle`؛ `.scard{max-width:340;max-height:90vh;overflow:hidden auto}`.
  - **5 أفكار مختارة (commit `c20b30d`):** (1) **تبويبات صفحة التفاصيل** `[احجز·المرافق·التقييمات·الموقع]` — `.detail-tabs/.dtab-btn/.dtab-panel` + `setDetailTab()` + `Actions.setDetailTab` + `setDetailTab('book')` في `openDetail`؛ الهيرو يبقى فوق. (2) **توزيع تقييمات بأشرطة** `renderRatingDist(place)` في تبويب التقييمات (`#ratingDist`، `.rd-*`) يقرأ `place.reviews_dist` (مصفوفة [1★..5★])؛ 0 تقييم ⇒ `noReviewsYet`. (3) **إبراز قبول/رفض للطلب المعلّق** في `ownerBookingCard` (`.owner-decide` قبول lime + رفض أحمر، للـpending فقط). (4) **FAB «أضف ملعب»** `#ownerAddFab` يظهر بتبويب الملاعب فقط (عبر `showOwnerTab`). (5) **Switch تشغيل/إيقاف الملعب** في `renderOwnerFields` (`.field-switch`) + `toggleFieldActive()` يستدعي `ownerUpdateField` القائم.
  - **صقل جمالي (commit `cac73d7`، ضمن Teal+Lime):** نجمة تقييم كهرمانية موحّدة `.sr-star #F59E0B` (بطاقة الملعب + شارة التفاصيل) · بلاطات إحصاء بأيقونة `.stat-ic` بمربّع مصبوغ (تبويبَي اليوم/التقارير) · هيرو `.lp-title` وزن **Montserrat 900** (أُضيف الوزن للخط) + سطر أضيق.
  - **dropdown الرياضات (commit `53fc6b0`):** `#sportDD` في `browse-sticky` على **الطرف المقابل للفلاتر** (`browse-sticky` صار flex، `browse-rail` flex:1) — زر يعرض الرياضة الحالية + قائمة الـ5 (شارات «قريباً»)؛ `renderSportDropdown/toggleSportDD/closeSportDD` + `Actions.toggleSportDD` + إغلاق بالنقر الخارجي/Escape؛ مزامنة مع مبدّل الفلاتر عبر `setSport`. مفاتيح i18n جديدة: `actApprove/actDecline، fieldActive/Inactive/Enabled/Disabled، dtabBook/Amenities/Reviews/Location، ariaDetailTabs، noReviewsYet، ratingsCount`.
  - **تباعد «التنفّس» (commit `9fc484f`):** إيقاع رأسي موحّد على شبكة الـ4 — `mb-sm/mb/md/lg/xl/card`=12/16/16/20/24/16؛ `stat-grid` gap14 mb16؛ `card+card`20؛ `sec-title` أعلى20؛ `.card`/`.detail-section` حشوة **عمودية** 22؛ `.pad` عمودي 28؛ `body line-height 1.65`؛ هوامش بطاقات القوائم JS 10/11→14. **كل الزيادات رأسية فقط — لا لمس أفقي** ⇒ صفر overflow.
  - **التحقّق:** محليًا حيًّا (`_preview_mohdath.html` على منفذ خاص 8011 — الخادم على 8000 يخصّ محادثة أخرى وغير قابل للوصول من الوكيل) — كونسول نظيف، صفر i18n-parity، صفر overflow **320/375/1280 × ar/en × نهاري/ليلي**، `node --check` ✓. لوحة المالك/التفاصيل بحقن markup مطابق (بلا توكن). الإنتاج غير مُتحقَّق حيًّا.
  - **⚠️ تعديل باكند معلّق النشر (`Code.gs`):** `getReviewsStats` صار يحسب `dist:[0,0,0,0,0]`، و`getInitialData` يضيف `reviews_dist: reviewStat?reviewStat.dist:null` لكل مكان (بيانات حقيقية بلا معلومات شخصية). **قبل نشره:** أشرطة التوزيع تُخفى (يظهر المتوسط فقط)، و**Switch الملعب غير آمن** (قد يخفي ملعبًا موقوفًا من لوحة المالك ما لم يُنشر إصلاح `getOwnerData` قراءة الموقوفة). `node --check` ✓.

- **آخر جلسة (2026-07-23) أنجزت + نُشرت على Vercel — 4 تعديلات من مجلد «مراجع» (مراجع تصميم React مرجعية بصرية فقط):**
  1. **شاشة التأكيد → شاشة كاملة** (ترتيب مرجع App، بألوان Teal+Lime لا لون واحد): `showBookingSuccess` + `.success-full` + عنصر `#successEyebrow` (✓ ← سطر «تم الحجز» ← اسم المكان ← ملعب·موقع ← شبكة إيصال 2×2 + رقم حجز monospace ← حالة «بانتظار التأكيد» ← 3 أزرار). النجاح البسيط (`showSimpleSuccess`) يبقى بالبطاقة المركزية.
  2. **إعادة هيكلة الترحيب** (مرجع ux pilot): شريط قيم متحرّك `.lp-ticker` (**بلا نشاط وهمي** — قاعدة الصدق م5)، «كيف يعمل» → خط زمني `.lp-timeline`، الآراء `.lp-reviews` → تمرير أفقي snap، ونقل «كيف يعمل» قبل «المميزات».
  3. **رسوم لوحة المالك SVG** (فانيلا بلا مكتبات، امتداد `createSparkline`): `createBarChart`/`createDonut`/`renderOwnerCharts` (أعمدة إيراد يومي + دونات إشغال + أعمدة الساعات) بتبويب التقارير، معزولة بـ`safeRender('charts',…)`.
  4. **شريط أكسنت جانبي** (4px lime `::before`) لبطاقة وضع القائمة (مرجع App #6).
  - **اكتشافات صادقة (أُبلغ بها المستخدم):** App2 #1 (GlassCard+LimeBtn) و#2 (زر بحث lime بالشريط) و#3 (شبكة/aurora + خط lime بالهيدر) **كانت موجودة أصلاً** ← لا تغيير. وبطاقة القائمة الأفقية `.places-list[data-view="list"] .place-card` (grid `118px|1fr`) **موجودة أصلاً** ← أُضيف فقط الشريط الجانبي بعد أن تعارضت محاولتي المكرّرة الأولى معها. App #7 (سعر تحت كل خانة) مُتجاوَز (كل أوقات الملعب بنفس السعر ⇒ تكرار). مفاتيح i18n جديدة (ar+en): `tickerNoCall/Clear/Follow/NoAccount`، `chartsTitle/chartRevenue/chartOccupancy/chartHours/last7short`.
  - **التحقّق:** محليًا فقط (`_preview_mohdath.html`) — كونسول نظيف، صفر تحذير i18n-parity، صفر overflow 375/1280 × ar/en × نهاري/ليلي، `node --check` ✓. شاشة التأكيد ورسوم المالك بحقن markup مطابق (بلا توكن لاعب/مالك). **الإنتاج غير مُتحقَّق حيًّا** (بيئة الوكيل لا تصل لمضيفات خارجية — TLS/socket). دُمج ونُشر (commit `560968a`).

- **آخر جلسة (2026-07-21) أنجزت — كله مُتحقَّق حيًّا (ثيمان × لغتان × 320→1440، صفر كونسول/overflow):**
  1. **بطاقات الأماكن**: صورة نظيفة (مفضلة + شارة «غير متاح» فقط عند الامتلاء — لا شارة عند التوفّر بطلب المستخدم) ← `.place-body` (اسم/تقييم/موقع/مرفقان/نوع/عدد — **بلا دوام**) ← `.place-action-row` (سعر + «احجز الآن»). الدوام صار شارة برأس التفاصيل (`renderDetailBadges` — تُستدعى أيضًا من `setLanguage`).
  2. **أوضاع عرض Grid/List**: `.vt-btn` بجانب الترتيب، `State.view` + مفتاح `mustadaira:viewMode` + سمة `data-view` على `#placesList` (يصمد بعد بحث/فلترة/تحديث/reload).
  3. **مكدّس تنقّل داخلي**: `NavStack` داخل `showPage(name,opts)` + `navigateBack(fallback)` + `Actions.navBack` (`data-action="navBack"` + `data-fallback` على زرّي الرجوع)؛ التحويلات الداخلية تمرّر `{redirect:true}`؛ تمرير محفوظ لكل صفحة (`State.pageScroll` + `pageScrollGet/Set` — **`#app` هو حاوية التمرير**).
  4. **مراجعة الحجز**: إيصال `rc-*` (رأس مكان/شبكة/لاعب/إجمالي بارز/«جاهز للإرسال»).
  5. **أنيميشن نجاح**: `successPop`+`successHalo` فوق `checkDraw` + `void icon.offsetWidth` لإعادة تشغيل مضمونة.
  6. **صورة هيرو الترحيب**: Pexels `114296` كـ`::before` على `.lp-hero` بتعتيم teal + fallback صلب + نسخة w=900 للجوال؛ نصوص الهيرو بيضاء دائمًا.
  7. **تمريرة نصوص** (فصحى أوضح + فكاهة كروية بالحالات الفارغة): عنوان الهيرو «احجز ملعبك بلا مكالمات»، تحسينات noResults/noBookings/noFavs/bookingConflict/comingSoon/callout + إصلاح «ع الأقل» و🚫/✕. **لم تُلمس**: `API_MESSAGE_MAP` وقوالب واتساب والتسميات الكنسية للأوقات.
  - مفاتيح i18n الجديدة (ar+en): `unavailableBadge، priceFrom، viewModeAria، viewGrid، viewList، rvSize، rvTotal، rvReadyNote`.
  8. **هرمية التصفّح (بروح الـbrand kit)**: قسم «BROWSE HIERARCHY POLISH» — الرياضات تبقى الـlime المصمت الوحيد؛ المناطق/المفضلة/الفلاتر chips أنحف (40px) شفافة، والنشطة صبغة lime خافتة (داكن) / teal soft (نهاري). ملاحظة: `full brand kit.png` بالجذر هو مرجع الهوية (تايبوغرافي Montserrat، chips مسطّحة، شعار «Book Your Pitch Instantly» **لا نستخدمه نصيًّا** — يخالف قاعدة الصدق م5).
  9. **دليل النصوص `docs/دليل-نصوص-الكودين-فقط.xlsx` حُدّث شاملًا**: 234 نصًّا صُحّح + 69 مفتاحًا جديدًا ⇒ 501 مفتاح I18N مطابقة للكود، بأوسمة «حُدّث/جديد 2026-07-21».
  10. **SCROLL OWNER FIX** (آخر style-new.css): التمرير عاد للمستند بدل `#app` — `html,body{height:auto;min-height:100%}` + `#app{overflow-x:clip}` (fallback hidden). يصلّح تعليق أطراف التمرير بالجوال وخاصية «اضغط أعلى الشاشة» في iOS. **مزلق «#app هو حاوية التمرير» المذكور أدناه بَطَل** — المتمرّر الآن window/المستند؛ `pageScrollGet/Set` يقرآن الاثنين فبقيا سليمين، وقفل `body.modal-open` صار يعمل فعليًا، والـsticky (lp-nav/owner-tabs) سليم.
- **⏭️ المعلّق الوحيد (على المستخدم) — نشر `Code.gs`:** نسخ `Code.gs` الجذر إلى Apps Script → تشغيل `testAiSetup` مرة بالمحرر (منح صلاحية UrlFetchApp) → التأكد من `GEMINI_API_KEY` في Script Properties → Deploy جديد. **يشحن الآن:** إصلاح إلغاء اللاعب + إظهار الملاعب الموقوفة (`getOwnerData`) + نقاط AI + **`reviews_dist` لأشرطة توزيع التقييمات (2026-07-23)**. **وبعد النشر يصير آمنًا:** Switch تشغيل/إيقاف الملعب في لوحة المالك (قبله قد يخفي ملعبًا موقوفًا من اللوحة).
- **غير مُختبر بجلسة حقيقية:** مودال المراجعة وشاشة النجاح الفعليان (يحتاجان حساب لاعب — تحقّقنا بحقن markup مطابق)، ولوحة المالك بتوكن حقيقي.
- **مزالق بيئية مهمة:** transitions/animations مجمّدة بمُصيّر المعاينة (عطّل الـtransition قبل قراءة الألوان)؛ ~~`#app` هو حاوية التمرير~~ **بَطَل بعد SCROLL OWNER FIX — المتمرّر هو window/المستند**؛ توكنات الثيم على `body.dark`؛ OneDrive قد يرجّع الملفات — تحقّق بـprobe محتوى بعد الكتابة.
- تفاصيل أدق في ذاكرة المشروع: `plan_uiux_batch_2026-07-21.md` + `project_premium_redesign.md`.

---

## 🚧 أرشيف: حالة إعادة التطوير الكبرى (تاريخي — المصادر المذكورة أدناه قديمة)

التطوير كان يتم على **`app/index.html` + `app/style.css` + `app/app.js`**، ثم يُدمَج بسكربت PowerShell في **`المستديرة.txt`** و**`المستديرة-كامل.html`** (CSS داخل `<style>`، JS داخل `<script>`). الباكند `Code.gs` **لم يُلمَس** في كل هذا.

### 🎨 إعادة التصميم الشاملة A→Z (8 أجزاء) — **مكتملة ومُتحقَّق منها حيًّا**
أُعيد بناء الواجهة كمنتج SaaS رياضي راقٍ بنظام تصميم موحّد، **دون كسر أي id/دالة/طلب API ودون لمس `Code.gs`**. الاستراتيجية: إعادة بناء التوكنات في CSS + **طبقة aliases** تُبقي كل class/id قديم يعمل بالباليتة الجديدة.
1. **الأساس:** توكنات light/dark بالباليتة المطلوبة (`--light-*`/`--dark-*` + semantic + aliases قديمة)، خلفية SaaS متحركة (mesh+grid+orb، مخفّضة على الجوال + `prefers-reduced-motion`)، منع وميض الثيم (سكربت inline أول `<body>`) + اكتشاف `prefers-color-scheme`، `color-scheme`/`theme-color`، انتقال ثيم بـ`:where()`.
2. **أزرار/حقول:** `--ring` موحّد، نظام أزرار (`btn-ghost/outline/danger/icon/text`)، **إظهار كلمة السر** (`togglePass`) + **مسح البحث** (`clearSearch`).
3. **تجميع الأوقات** صباحاً/ظهراً/مساءً (`TIME_PERIODS` في `renderDetailTimes`).
4. **توحيد ألوان الحالات** بتوكنات حبر (`--ok/danger/warning/info-ink`) عبر الشارات/الإشعارات/`booking-strip`/`stat-card`/`reason-box` (تباين صحيح بالدارك + حدود `border-inline-start`) + **Sidebar لوحة المالك على الكمبيوتر** (≥1024px) + تنقّل أسهم ↑↓.
5. **شاشة نجاح الحجز** (`showBookingSuccess`/`showSimpleSuccess`/`resetSuccessCard`): علامة ✓ متحركة + ملخّص + «بانتظار تأكيد الملعب» + رقم الحجز + زرّا حجوزاتي/الرئيسية.
6. **الملف الشخصي** (`#page-account`): Avatar + بطاقة تعديل + بطاقة تفضيلات (تبديل ثيم/لغة داخل الحساب).
7. **أقسام هبوط جديدة:** المناطق (data-driven عبر `renderLandingRegions`) + آراء (توضيحية — تُستبدل بحقيقية) + لأصحاب الملاعب.
8. **صقل:** نوافذ مركزية على الكمبيوتر (≥700px)، زر لغة بالفوتر، فحص استجابة 320→1400 (صفر overflow).

**التحقّق:** كل جزء `node --check` ✓ ثم دُمج ثم عُوين حيًّا على 6 ملاعب حقيقية في الوضعين — صفر أخطاء/تحذيرات كونسول. خادم المعاينة: `koora` (في `.claude/launch.json`) ⇒ `localhost:8000/_preview_1.html`.

**طريقة الدمج (كل مرة بعد التعديل):**
```powershell
$dir="C:\Users\malik\OneDrive\Desktop\koora\app"; $root="C:\Users\malik\OneDrive\Desktop\koora"
$h=[IO.File]::ReadAllText("$dir\index.html");$c=[IO.File]::ReadAllText("$dir\style.css");$j=[IO.File]::ReadAllText("$dir\app.js")
$m=$h.Replace('<link rel="stylesheet" href="style.css">',"<style>`r`n$c`r`n</style>").Replace('<script src="app.js"></script>',"<script>`r`n$j`r`n</script>")
$e=New-Object System.Text.UTF8Encoding $false;[IO.File]::WriteAllText("$root\المستديرة.txt",$m,$e);[IO.File]::WriteAllText("$root\المستديرة-كامل.html",$m,$e)
```
المعاينة: ملف مؤقت `app/_preview_X.html` (مدموج) ثم `python -m http.server 8000` ⇒ `localhost:8000/_preview_X.html`. (لقطة الشاشة تتعطّل بيئيًا — اعتمد `preview_eval`/`preview_inspect`.)

### ✅ مكتمل ومُتحقَّق منه حيًّا
- **م1 تهذيب المؤثرات (~35%):** أُوقفت auroraDrift/shineText/pulseGlow/pulseDot/floatY + لمعان الأزرار/الصور؛ خُفّض blur وHover (محصور بـ`@media hover`).
- **م2 شعار SVG** (ملعب مبسّط) بدل ⚽ في الـNavbar/الهيدر/الفوتر + أيقونات رؤوس الدخول SVG.
- **م3 وضع داكن أخضر فحمي** (`--cream:#07110c …`).
- **م4 صفحة هبوط:** 3 مميزات مدموجة، "تصفّح" هو الإجراء الرئيسي.
- **م5 نصوص تسويقية مصحّحة** (لا "حجز فوري"/"واتساب" كاذبة).
- **م7 الخدمات والمرافق** في التفاصيل عبر `parseAmenities()` (بطاقة=2، تفاصيل=الكل) + "مواقف" بدل "مصفات".
- **م8 نافذة مراجعة فقط** + مصدر حالة واحد (`State.detail`) — لا تكرار اليوم/الوقت؛ "تغيير الموعد" يرجّع لقسم الموعد.
- **م9 i18n كامل عربي/إنجليزي:** قاموس `I18N{ar,en}` + `t()` + `setLanguage()` + `applyTranslations()` + 226 وسم `data-i18n`؛ تبديل بلا reload؛ Intl للتاريخ/العملة (`د.أ`/`JOD`)؛ `API_MESSAGE_MAP`؛ زر لغة في الـNavbar/الهيدر/المالك؛ المفتاح `mustadaira_language`.
- **م10 جلسة بالمفاتيح القديمة:** `Session` يقرأ/يكتب `player_token`/`owner_token` **نص مباشر بلا TTL** (لا تسجيل خروج للمستخدمين الحاليين). الثيم/اللغة فقط تحت namespace `mustadaira:`.
- **م11 Timeout + AbortController:** `fetchWithTimeout()` (14ث) + `requestControllers` (آخر طلب يفوز) + `handleApiError`/`isAbort`/`isTimeout`.
- **م12 وصولية (شبه مكتمل):** كل النوافذ `role=dialog`+`aria-modal`+`aria-labelledby`؛ Focus Trap+Escape+قفل تمرير+إعادة تركيز؛ نجوم Radio؛ Skip link؛ `ico()` يضيف `aria-hidden`؛ بطاقة الملعب الداخلي/التنقّل السفلي = `button`+`aria-pressed`/`aria-current`؛ أزرار اليوم/الوقت `aria-pressed`+`aria-label` بالحالة؛ تبويبات المالك `role=tablist/tab/tabpanel`+`aria-controls`+**أسهم كيبورد**.
- **م17 عملة موحّدة `د.أ`/`JOD` + حالات حجز موحّدة.**

### 🔴 عطل قيد المتابعة — لوحة المالك (ابدأ من هنا في الجلسة الجديدة)
**العَرَض:** تبويب "الملاعب" بلوحة المالك يظهر فارغًا، وتبويب "الحجوزات" يظهر "تعذّر جلب البيانات". الملاعب تظهر للاعب (العام) بشكل طبيعي، وإضافة ملعب جديد تنجح وتظهر للاعب لكن ليس للمالك.

**التشخيص المؤكَّد (تم):** الباكند المنشور **سليم وسريع** (اختُبر بـ`curl` على `getInitialData`/`getOwnerData` ⇒ JSON صحيح، ~4s، أقل من مهلة 14s). فالمشكلة **استثناء JavaScript أثناء `renderOwnerDashboard()`** في الواجهة، كان يُمسك بنفس `try/catch` الخاص بالجلب فيظهر كرسالة مضلِّلة "تعذّر جلب البيانات".

**ما عُمل (تم ودُمج):** في [app.js](app/app.js) `loadOwnerDashboard`: فُصل الجلب عن الرسم (فشل الشبكة فقط ⇒ "تعذّر جلب البيانات"؛ خطأ الرسم ⇒ `⚠️ صار خطأ بعرض اللوحة` + نص الخطأ). وأُضيف `safeRender(label,fn)` يعزل كل قسم رسم (`stats/econ/fieldFilter/bookings/fields/today`) ويطبع بالكونسول `owner render [القسم] failed: ...`. هذا يُظهر بقية الأقسام رغم عطل قسم واحد، ويكشف المذنب.

**الخطوة التالية (مطلوبة من المستخدم):** يفتح الكونسول (F12) بعد الدخول للوحة المالك على البناء الجديد، ويعطينا سطر `owner render [____] failed: ____` ⇒ يحدّد القسم والسبب الجذري بالضبط ⇒ يُصلَح. (تعذّرت إعادة الإنتاج محليًا: تحتاج توكِن مالك صالح غير متوفر. `API_URL` المنشور في [app.js:17](app/app.js:17).)

### ⏭️ المتبقّي (ابدأ من هنا)
0. **نشر `Code.gs`:** إصلاحان بانتظار النشر على Apps Script + Deploy جديد: (أ) إلغاء اللاعب لحجزه، (ب) إظهار ملاعب المالك الموقوفة (`getOwnerData` صار يقرأ Places+Fields مباشرة بلا فلتر `active` العام). لا تغييرات Sheets.
1. **م13 Utility classes:** الـclasses مُضافة (`mb-sm/mb/mb-md/mb-lg/mb-xl/mb-card`,`text-center/start/end`,`flex-between`,`danger-outline-btn`,`readonly-field`…) وطُبّق `mb-card` على بطاقات المالك. **يتبقّى الاستبدال المنهجي الأوسع** للأنماط المضمّنة (هوامش `finput` بـ`index.html`، وflex inline في بطاقات الحجز بـ`app.js`) — **منخفض القيمة/عالي المخاطرة على التباعد، اختياري وتدريجي.**
2. ~~(اختياري) ترجمة رسائل واتساب~~ → انتقل إلى "أُنجز حديثًا".

### ✅ أُنجز حديثًا (كان متبقّيًا)
- **🌍 اتجاه حقيقي للّغتين + نظام تصفّح مضغوط + إصلاح تداخل الهيرو:**
  1. **الاتجاه:** حُذف `direction:rtl` المثبّت من `html,body` ⇒ صار التخطيط يتبع السمة `dir` التي يضبطها `setLanguage` (ar⇒rtl · en⇒ltr). البنية التحتية لـLTR كانت موجودة سلفًا (6 قواعد `html[dir="ltr"]` + خط Inter) لكن التثبيت كان يُبطلها. حُوّلت البقايا الفيزيائية لمنطقية: `.detail-back` (`right`→`inset-inline-start`)، `.price-unit` (`text-align:left`→`end`)، `.bk-step::after` (`left`→`inset-inline-start`). **تحقّق:** إنجليزي يبدأ من 20 وعربي من 380 على 320/400/1280 — صفر overflow.
  2. **نظام التصفّح الجديد:** الرياضات صارت **حبوب أفقية** (أيقونة 26px + اسم على سطر) بدل عمود أيقونة‑50px‑فوق‑اسم ⇒ **109px→60px**، وشارة «قريباً» انتقلت داخل الحبّة (تعديل صغير في `renderSportTabs`). ودُمج صفّا **المناطق (62px) + الفلاتر (56px)** في `.browse-bar` واحد **60px**: زر الفلاتر مثبّت على `var(--s-5)` + المناطق تنزلق بجانبه بقناع تلاشٍ على **الطرف البعيد فقط** (تلاشي الطرفين كان يقصّ حافة الحبّة النشطة). و`.sort-current` انتقل لصفّ `.sec-title` الموجود أصلًا (`.sec-title-end`). **الإجمالي 268→162px (توفير ~107px).** `updateSportSections` صار يخفي `#browseBar` بدل `#regionTabs`+`.filter-bar`.
  3. **تداخل الهيرو:** المسافة بين `.lp-hero-search` وأزرار CTA كانت **صفرًا** (+ ظلّ `--shadow-md` ينزل فوقها) ⇒ هامش سفلي 22px. وهامش `.lp-hero-cta` رُفع 16→**24px** لأن `fadeUp` يبدأ بـ`translateY(20px)` مع `fill:both` فكانت الأزرار تتداخل مع `.lp-owner-link` بـ4px خلال تأخير 120ms في كل تحميل.
  4. **إضافة:** `.home-brand` صار يتقلّص ويُقصّ نصّه و`.home-tools` لا تتقلّص — كان صفّ الهيدر يفيض 24px بالإنجليزية ومع اسم مستخدم طويل («أهلاً مالك مؤيد المصري»).
  **تحقّق حيّ:** اللغتان × الثيمان × 320/400/1280 — صفر overflow، صفر أخطاء كونسول، `node --check` ✓، ودُمج. **ملاحظة بيئية:** جدول الأنيميشن في متصفّح المعاينة **مجمّد** (`currentTime:0`)، فتبدو الـtransitions/animations عالقة عند حالة البداية — تحقّق من قيم `transform` بتعطيل الـtransition مؤقتًا لا بالقراءة المباشرة.
- **📐 صقل تخطيط (5 تعديلات CSS فقط — `style-new.css`، بلا لمس HTML/JS/`Code.gs`):** قيست الأبعاد حيًّا قبل/بعد عبر `getBoundingClientRect`.
  1. `.lp-hero-search`: `--shadow-sm`→`--shadow-md` و`--line`→`--line-2` (تباين الحدّ .16→.30) لبروز أعلى على الخلفية الفاتحة.
  2. زرّا الهيرو: توكن جديد `--cta-h:56px` + قاعدة `.lp-hero-cta .lp-btn-primary,.lp-hero-cta .lp-btn-ghost{height/min-height:var(--cta-h);padding-block:0}` — كان التساوي (56px) **صدفة** ناتجة عن اختلاف حجم الخط والحدّ؛ صار مضمونًا. مقصورة على `.lp-hero-cta` كي لا تتأثر `.lp-owners-card .lp-btn-primary`.
  3. `.bnav`: الحشوة `9px 12px max(12px,…)`→`6px 12px max(8px,…)` + `.nicon` height 32→28px + مؤشّر `.nitem.active::before` من `top:-9px`→`-6px` ليبقى على خطّ الحدّ. **الارتفاع 84→72px** مع بقاء `.nitem` = 57px (فوق `--tap:48px`).
  4. `.search-bar` الداخلي: **السبب كان `max-width:680px` في `@media(min-width:1024px)`** (على الجوال كان محاذيًا أصلًا). صار `max-width:none;margin-inline:0` ⇒ يطابق عرض `.places-list` تمامًا (قيس: 680→1050px على 1280px).
  5. محاذاة الفلاتر: **المُختلّ لم يكن `.filter-bar`** (كان على `var(--s-5)` سلفًا مثل `.ftab.active-tab`) بل **`.sport-tabs`** بحشوة `var(--s-4)`=16px ⇒ فقاعة الرياضة النشطة الخضراء تقع على 28px. صارت `calc(var(--s-5) - 12px)` (12px = 10px حشوة `.sport-tab` + 2px توسيط الأيقونة في `min-width:74px`) ⇒ الحافة على `var(--s-5)` بالضبط.
  **تحقّق حيّ:** الحواف الخمس (بحث/بطاقة/فقاعة رياضة/تبويب منطقة/زر فلاتر) متطابقة على 320 و400 و1280؛ صفر overflow؛ فاتح/داكن؛ صفر أخطاء كونسول؛ ودُمج في `المستديرة محدث.txt`. (~~ملاحظة: التطبيق RTL دائمًا حتى بالإنجليزية~~ — **بطلت**: صار الاتجاه يتبع اللغة، راجع بند «اتجاه حقيقي للّغتين» أعلاه.)
- **✨ خلفية هندسية مُحسّنة + أشكال عائمة لصفحة الترحيب (CSS فقط):** (1) أُعيد ضبط `#app::before` ليكون أوضح: blur 60→36px (جوّال 28px)، ألفا نُحاس داكن ~0.12-0.13 / نهاري ~0.08، أشكال أكبر (بوكيهان دائريان + مثلثان conic بحافة حادّة + انطباع سداسي 6 قطاعات + هالة نعناع + إضاءة رخامية)، `luxeDrift` 26s (transform/opacity فقط). `#app::after` (الشبكة) لم يُلمس. (2) أُضيفت طبقة زخرفية واحدة `<div class="lp-floating-shapes" aria-hidden>` كأول ابن لـ`#page-welcome` فيها 7 أشكال `.lp-float-shape` (سداسي/مثلث/دائرة/حلقة/قوس مداري/خط ملعب مجرّد) بكلاسات `fs-1..fs-7`، نُحاس~70%/بلاتيني~20%/نعناع~10%، clip-path + color-mix + حدود رفيعة + blur خفيف، 3 أنماط حركة `floatShapeA/B/C` (27/21/16s، تأخير سالب، transform فقط). z-index:-1 خلف المحتوى داخل `isolation:isolate` على `#page-welcome`. **مهم:** القصّ على `.lp-floating-shapes{overflow:hidden}` وليس على `#page-welcome` (وضع overflow:hidden على الصفحة يكسر ثبات الـnav اللاصق — جُرّب وأُزيل). الجوال ≤480px يُخفي fs-4/6/7 (يبقى 4). reduced-motion يوقف الحركة ويُبقي الأشكال ساكنة. **تحقّق حيّ (بعد restart للمعاينة لتجاوز تعطّل المُصيّر):** nav لاصق سليم، الأشكال خلف المحتوى وCTA قابل للنقر، صفر overflow 360/480/1024/1400، فاتح/داكن مقروءان، scroll-reveal يعمل (16 عنصر)، صفر أخطاء كونسول، ودُمج.
- **🌐 توطين شامل (ar/en) + إصلاح ظِلّ `t` بلوحة المالك:** نُقلت كل النصوص المتبقّية إلى `I18N` (toasts/فراغات/تحميل/اقتصاد/قوالب واتساب/تواريخ/مرافق/وصولية) بمفاتيح موصوفة وتكافؤ ar/en كامل + `validateI18nParity()` (تحذير كونسول) + `t()` آمن للـ`{var}` (يحفظ 0، لا HTML). الأوقات: `label` يبقى القيمة الكنسية المُرسلة للخادم (عربي)، والعرض الإنجليزي يُولَّد من `hour` عبر `slotDisplay()`/`enSlotLabel()`. `AMENITY` صار `labelKey`. **إصلاح جذري مهم:** دوال المالك (`renderOwnerStats/renderOwnerToday/renderOwnerCalendar/renderOwnerBookings`) كانت تُعرّف `const t=today()` فتُظلِّل دالة الترجمة `t()` ⇒ استثناء عند `t('...')` = على الأرجح **سبب عطل لوحة المالك** المذكور أعلاه. أُعيدت التسمية إلى `td`. دُمج. (يبقى التحقّق بتوكِن مالك حقيقي.)
- **☀️ ثيم نهاري (Marble) + نقر الشعار:** (1) أُضيف **ثيم نهاري** — أُعيد بناء `:root` كباليتة رخامية فاتحة (خلفية `#F4F1EC`/`#EBE5DC`، أسطح زجاج فاتح `rgba(255,255,255,.72)`، نص `#1C1814`، أكسنت نص **برونز نُحاسي عميق** `#7A4F2E`=`--emerald` للتباين على الفاتح، نعناع نهاري **جِيد عميق** `#0B8457`، ظلال دافئة ناعمة) مع إبقاء **النُحاس للأزرار** `#B88E70`=`--g-btn` بنص أسود في الوضعين. `body.dark` يبقى **أوبسيديان** (نعناع كهربائي `#4FFFB0` + ظلال سوداء عميقة أُضيفت كاملةً فيه + توكنات `--mint-*`/`--copper-*` مُعرّفة فيه لأن قيم `:root` صارت نهارية). زر الثيم الآن **يبدّل فعليًّا** نهاري↔داكن. الهيدر وبطاقات الميزات (`.app-header`/`.lp-callout-inner`/`.lp-owners-card`) تبقى داكنة في الوضعين (لمسة فاخرة مقصودة، كما كان الهيدر داكنًا أصلًا). (2) **نقر الشعار** (`.home-brand`) ⇒ صفحة الترحيب عبر `data-go="welcome"` (نفس الـdelegation) + `role/tabindex/aria-label` + مُعالِج `keydown` صغير (Enter/Space على `[data-go]`/`[data-nav]` ذات `role=button`) — بلا تغيير منطق موجود. **تحقّق حيّ:** نهاري وداكن صحيحان (توكنات + مكوّنات حقيقية)، التبديل يعمل، الشعار ينقل home→welcome، صفر أخطاء كونسول، صفر overflow 320→1400، `node --check` ✓، ودُمج في `المستديرة.txt`+`المستديرة-كامل.html`.
- **🖤 ثيم «Obsidian Marble» (Modern Dark Luxury):** تحويل كامل للهوية إلى داكن فاخر **عبر CSS فقط** (توكنات + aliases + pseudo-elements، دون لمس أي id/class/دالة/`Code.gs`/HTML). (كان وقتها **موحّدًا داكنًا** في الوضعين؛ صار لاحقًا **ثنائي الثيم** — `:root`=نهاري رخامي · `body.dark`=أوبسيديان، راجع بند الثيم النهاري أعلاه.) الباليتة (الداكنة): خلفية أوبسيديان `#080809`/`#0A0A0C`، أكسنت أساسي **نُحاس مصقول** `#B88E70` (hover `#D6A98A`) عبر `--copper-primary`/`--primary`/`--g-btn`، أكسنت ثانوي **نعناع كهربائي** `#4FFFB0` (`--mint-electric`/`--accent`، للمتاح/النجاح فقط)، أسطح **زجاج داكن** `rgba(20,20,22,.65)`، حدود نُحاسية خافتة `rgba(184,142,112,.15)`، نص بلاتيني `#EAEAEA` + `--muted` نُحاسي `#A09085`. الـlegacy (`--emerald`/`--forest`/`--neon`/`--gold`) مُعاد توجيهها للنُحاس/النعناع/الأوبسيديان فتعمل كل الكلاسات القديمة بمظهر جديد كليًّا. **Neo-glassmorphism** (`backdrop-filter:blur(20px) saturate(180%)`) على البطاقات/النافبار/المودال/الـsticky/التبويبات/الحقول + ظل فاخر `0 25px 50px -12px rgba(0,0,0,.7)`. **أزرار** نُحاس صلب بنص **أسود** + `font-weight:900` + `letter-spacing:.05em` + `radius:12px` + توهّج نُحاسي بالـhover. **خلفية** متحركة فاخرة (هالات نُحاس/نعناع `opacity:.05` + `blur(100px)` + انجراف `luxeDrift` 20s) بدل الـmesh الأخضر. شارات المتاح/النجاح نعناع، وكنس شامل لكل الأخضر المتبقّي → نُحاس. **تحقّق حيّ:** صفر أخطاء كونسول، صفر overflow 320→1400، 6 ملاعب حقيقية، ودُمج في `المستديرة.txt`+`المستديرة-كامل.html`. (يَستبدل بصريًّا الـVisual Calm Pass أدناه.)
- **🎨 إعادة ضبط بصري (Visual Calm Pass):** تهدئة شاملة عبر التوكنات فقط دون لمس أي id/class/دالة/`Code.gs`. أخضر واحد عميق مخفّض التشبّع كأكسنت وحيد + إزالة الذهبي البنيوي (`--gold`/`--accent`؛ أُبقي الذهبي للنجوم فقط). تسطيح كل التدرّجات (`--g-btn`/`--g-gold` + الأزرار/الشارات/الـchips/نصوص العناوين المتدرّجة → ألوان صلبة، زر primary صلب واحد)، وتوحيد أزرار المالك (`primary` صلب للتأكيد · `danger` واحد للرفض/الإلغاء عبر توكن جديد `--danger-btn` بتباين أبيض جيد بالوضعين · `outline` للتعديل/واتساب). ظلال محايدة طبقة واحدة (`--shadow-green`/`-green-lg`/`--gold-glow` → ناعمة)، إيقاف `bgDrift` نهائياً + شبكة `#app::before` شبه مختفية + خلفية body مسطّحة ساكنة، حذف رفعات الـhover (إبقاء press scale و`:focus-visible` فقط) وتقصير مدد الانتقال، أوزان العناوين 900→800، ومقياس radii أصغر (sm8/md12/lg16/xl20/2xl24/3xl28). **تحقّق حيّ:** فاتح/داكن على 6 ملاعب حقيقية، صفر أخطاء كونسول، صفر overflow من 320→1400، `node --check` ✓، ودُمج في `المستديرة.txt`+`المستديرة-كامل.html`.
- **🛡️ تحصين لوحة المالك (تشخيص العطل أعلاه):** `loadOwnerDashboard` فصل الجلب عن الرسم + `safeRender` يعزل كل قسم ويطبع المذنب بالكونسول. (راجع "🔴 عطل قيد المتابعة".) دُمج في الملفات الكاملة.
- **🐛 إظهار ملاعب المالك الموقوفة (`Code.gs`):** `getOwnerData` كان يستعمل `getPlaces()` العام الذي يُسقط الملاعب `active=false` والمكان كله إن لم يبقَ ملعب نشط. أُعيد كتابته ليقرأ المكان من شيت Places وكل الملاعب من شيت Fields **مباشرة بلا فلتر** ⇒ المالك يرى ويدير الموقوفة. `node --check` ✓. **يحتاج Deploy.**
- **🐛 إصلاح توافق Code.gs ↔ الواجهة (إلغاء اللاعب):** كان `updateBookingStatus` في `Code.gs` يقبل `owner_token` **فقط**، بينما الواجهة ([app.js:1129](app/app.js:1129)) تستدعيه بـ`player_token` عند إلغاء اللاعب لحجزه ⇒ كان يفشل دائمًا بـ"انتهت جلستك". أُصلح `Code.gs`: يدعم المسارين — المالك (أي حالة لحجوزات مكانه) أو اللاعب (**`cancelled` فقط** ولحجزه هو، يُتحقَّق عبر عمود `player_id`). فُحص `node --check` ✓. **يحتاج نسخ `Code.gs` على Apps Script + Deploy.** (كان مُصلَحًا سابقًا في `apps-script-improved.gs` غير المستخدمة فقط، لا في `Code.gs` المعتمَد.)
- **✅ ترجمة رسائل واتساب:** `sendWhatsApp()` في `app.js` يفرّع على `State.lang` (عربي/إنجليزي) لرسائل التأكيد/الرفض/الإلغاء، والسعر صار عبر `formatCurrency` (د.أ/JOD) بدل "دينار" المضمّن. فُحص `node --check` ✓ ودُمج في `المستديرة.txt`+`المستديرة-كامل.html`.
- **✨ تأثير لمعان الزجاج + تأخير ظهور البطاقات:** في `app/style.css` أُضيف `::before` على `.place-card/.card/.scard/.subfield-card/.stat-card/.econ-item` يُحاكي لمعة زجاجية تنزلق عند الـhover (استُخدم `inset-inline-start` للتوافق مع RTL/LTR). في `app/app.js`: `renderPlaces` يضيف `animationDelay = i*0.05s` على كل بطاقة، و`renderDetailTimes` يضيف `animationDelay = i*0.04s` على أزرار الأوقات. دُمج في `المستديرة.txt`+`المستديرة-كامل.html`.
- **🐛 إصلاح ظهور لوحة المالك في كل الصفحات:** في `app/style.css` بـ`@media(min-width:1024px)` تم تغيير `#page-owner{display:grid}` إلى `#page-owner.active{display:grid}` لمنع `display:grid` من تجاوز `display:none` الافتراضي. دُمج في `المستديرة.txt`+`المستديرة-كامل.html`.
- **م6 إيموجي←SVG:** غلاف الكرة الاحتياطي + أيقونة هيرو التفاصيل + رؤوس صفحات الدخول — كلها SVG الآن. (يبقى `📍` الصغيرة و🎉/الحالات الفارغة/الـToast — مسموحة.)
- **م14 خطوط:** أُسقط وزن Cairo 500 غير المستخدَم (Tajawal 800 مُبقًى لأنه مستخدَم بكثرة في التسميات).
- **م15:** ارتفاع غلاف البطاقة على الجوال صار 168px (`@media max-width:480px`).
- **✅ تحسينات Apps Script الخادمية (`Code.gs`):** بُنيت فوق الباكند الأصلي (نفس الدخول/Base64/كلمات السر تمامًا). أُضيف في `createBooking`: تحقّق أن **التاريخ ليس قديماً**، أن **الملعب تابع للمكان**، أن **الوقت ضمن slots الملعب**، و**اشتقاق السعر والتسمية واسم المكان/الملعب/المدينة من الشيت** (لا ثقة بسعر العميل). وغُلِّف `doGet/doPost` بـ try/catch. (LockService + منع التكرار + تبعية الملعب للمالك + `getPublicBookings` بلا بيانات شخصية كانت موجودة أصلًا.) فُحص بناء الجملة بـ`node --check` ✓. **يحتاج: نسخ `Code.gs` على Apps Script + Deploy جديد. لا تغييرات Sheets مطلوبة** (يستخدم نفس الأعمدة، وعمود `booking_id` كان مكتوبًا أصلًا).

> **بروتوكول المستخدم:** يقول "اكمل" ⇒ أكمل من المتبقّي أعلاه. لو نفد الرصيد يكمل من نفس السطر دون إعادة تفكير من الصفر.

---

## 📁 الملفات المهمة

| المسار | الوصف |
|--------|-------|
| **`app/index-new.html`** | ✅ **هيكل HTML الحالي** — المصدر الفعلي للتطوير |
| **`app/style-new.css`** | ✅ **نظام التصميم الحالي** (هوية Teal+Lime، أقسام passes مذيّلة) |
| **`app/app.js`** | ✅ كامل المنطق — IIFE، I18N، NavStack، مشترك بين القديم والجديد |
| **`المستديرة محدث.txt`** | ✅ **المخرَج النهائي الحالي** (دمج المصادر الثلاثة أعلاه) |
| `app/index.html` | ⚠️ قديم — لا تطوّر عليه |
| `app/style.css` | ⚠️ قديم — لا تطوّر عليه |
| `المستديرة-كامل.html` | ⚠️ مخرَج قديم (من المصادر القديمة) |
| `المستديرة-موقع.html` | نفس الكامل + Navbar علوي + Footer — تجربة Desktop |
| **`Code.gs`** | ✅ **الباكند المعتمد للنشر** — الأصلي + **تحقّق خادمي** (سعر/Slot/تاريخ/تبعية الملعب) + try/catch؛ **نفس Base64/كلمات السر/Tokens الأصلية بلا تغيير**. هذا ما يُنسَخ على Apps Script. |
| `apps-script-improved.gs` | ⚠️ نسخة قديمة بـSHA-256/HMAC — **لا تُستخدم** (تخالف قيد "لا تغيّر كلمات السر/Tokens"). |
| `README.md` | توثيق GitHub جاهز |
| `المستديرة-backup.txt` | النسخة الأصلية قبل أي تعديل |
| `جوجل-شيت-كود (1).txt` | باكند الأصلي (للمراجعة فقط) |

---

## ✅ ما تم إنجازه

### معمارية الكود
- [x] فصل كامل: `index.html` + `style.css` + `app.js`
- [x] IIFE pattern — صفر global variables
- [x] `h()` function آمنة من XSS (textContent للبيانات، innerHTML للـ SVG الثابتة فقط)
- [x] Event delegation عبر `data-action / data-nav / data-go`
- [x] `Session` module — **مفاتيح قديمة `player_token`/`owner_token` نص مباشر بلا TTL** (توافق كامل، لا تسجيل خروج)
- [x] `API` module يلف كل fetch calls
- [x] `State` object مركزي

### UX/Performance
- [x] Aurora background (transform/opacity فقط — 60fps)
- [x] Modal swipe-to-close (touchstart/touchmove/touchend)
- [x] Progressive disclosure: أوقات الحجز تظهر فقط بعد اختيار اليوم
- [x] `withLoading(btn, fn)` — منع double-submit + spinner
- [x] Debounce 300ms على البحث
- [x] Empty states جذّابة مع أيقونة + نص + زر
- [x] Tap targets ≥ 44×44px
- [x] Dark/Light mode

### ملفات الإخراج
- [x] `المستديرة-كامل.html` — ملف واحد للـ GitHub
- [x] `المستديرة-موقع.html` — نسخة Desktop (Navbar + Footer + Grid 3 أعمدة)
- [x] `apps-script-improved.gs` — باكند محسّن كامل
- [x] `README.md` — توثيق GitHub

### تحسينات الباكند
- [x] كلمات سر مُجزّأة SHA-256 + salt (ترقية تلقائية من plaintext)
- [x] توكنات HMAC-SHA256 + TTL (مع backward compatibility للقديمة)
- [x] السعر يُشتق من الشيت في الخادم (لا ثقة بالعميل)
- [x] قراءة واحدة للشيت بدل مزدوجة
- [x] Route maps بدل if-chains
- [x] try/catch حول doGet/doPost
- [x] إصلاح خلل: إلغاء اللاعب لحجزه الخاص كان يفشل

---

## ⏭️ الخطوات التالية المقترحة (لم تُنفَّذ بعد)

1. **نشر على GitHub** — رفع ملفات `app/` + `README.md` + الملفات الكاملة
2. **تطبيق الباكند المحسّن** — نسخ **`Code.gs`** (لا `apps-script-improved.gs`) على Apps Script + Deploy جديد
3. **PWA support** — manifest.json + service worker للعمل offline وتثبيت كتطبيق
4. **`.gitignore`** — إضافة ملف gitignore مناسب للمشروع
5. **PropertiesService** — إخفاء `SPREADSHEET_ID` من الكود إلى Properties

---

## 🔑 قرارات معمارية مهمة (لماذا X لا Y)

- **Vanilla JS لا React/Vue**: المشروع SPA بسيط، لا يحتاج framework، التبعيات صفر.
- **IIFE لا ES Modules**: لتشغيل مباشر في المتصفح بلا bundler أو build step.
- **`h()` builder لا innerHTML**: XSS prevention — كل بيانات المستخدم تمر عبر textContent.
- **localStorage + TTL لا cookies**: سهولة التطوير؛ للإنتاج الكامل يُفضّل httpOnly cookies.
- **Google Apps Script**: الباكند المجاني الجاهز المرتبط بـ Google Sheets.

---

## 🔧 كيفية استئناف العمل

```bash
# معاينة محلية (خادم koora في .claude/launch.json يخدم مجلد app/ على :8000)
cd C:\Users\malik\OneDrive\Desktop\koora\app
python -m http.server 8000
# ثم افتح http://localhost:8000/_preview_mohdath.html
```

في جلسة Claude جديدة قل:
> "اقرأ CLAUDE.md — قسم «🚦 آخر حالة» أولًا — وكمّل من المعلّق"

---

## ⚙️ CONFIG الأساسي (في app.js)

```js
const CONFIG = {
  API_URL: 'https://script.google.com/.../exec',  // الرابط الحقيقي موجود — لا تغيّره
  CACHE_KEY:'mustadaira:places_cache_v8', CACHE_MS: 10*60*1000, AUTO_REFRESH_MS: 90*1000,
  SEARCH_DEBOUNCE: 300, COMMISSION: 0.10, API_TIMEOUT: 14000,
};
```

**مفاتيح localStorage:** `player_token` · `owner_token` (جلسات، نص مباشر) · `mustadaira:theme` · `mustadaira_language` (ar/en) · `mustadaira:places_cache_v8` · `mustadaira:pending` (حجز ضيف معلّق).
