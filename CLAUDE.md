# CLAUDE.md — المستديرة | حالة المشروع

> **اقرأ هذا أوّلًا في أي جلسة جديدة.** خريطة الملفّات في [`README.md`](README.md).
> السجلّ التاريخي (الدفعات والقرارات القديمة) في [`docs/history.md`](docs/history.md) — لا تقرأه إلّا عند الحاجة.
> **آخر تحديث: 2026-08-25.**

---

## 📌 ما هو المشروع

منصّة حجز **ملاعب رياضية** في الأردن — خمس رياضات في التطبيق (كرة القدم · بادل · كرة السلة ·
تنس · كرة الطائرة). **المفتوح للحجز اليوم كرة القدم وحدها** لأنها الملاعب المسجَّلة في القاعدة،
وما عداها يحمل شارة «قريبًا» في الاثنين. **وجهان اثنان لا ثالث لهما:**

| | ما هو | أين | الباكند |
|---|---|---|---|
| **التطبيق** | أندرويد مدمج (Capacitor) — **الحجز يجري هنا وحده** | `app/` ⇒ APK | Postgres/Supabase |
| **الموقع** | موقع تسويقي ثابت + `/admin` | `site/` ⇒ `public/` ⇒ Vercel | يُولَّد وقت البناء |

Vanilla JS/HTML/CSS بلا أي مكتبة. ثنائي اللغة (عربي RTL / إنجليزي LTR)، ثيمان نهاري/ليلي،
هوية **Teal `#0F4B53` + Lime `#8CC63E`** مسطّحة.

**أمر البناء (يبني الاثنين):**
```bash
powershell -ExecutionPolicy Bypass -File build.ps1
```

---

## 🔴 معلَّق على المالك

> ### ✅ أُغلق تصعيد الصلاحيات — **`24`…`27` شُغِّلت 2026-08-10**
>
> مراجعةٌ خارجية كشفت **تصعيد صلاحيات كاملًا**، ومعه ستّ ثغرات أصغر. فُحصت كلّها
> بقراءة الملفّات لا بالثقة بالتقرير، ثمّ **شُغِّلت الترحيلات كلّها بنجاح** (أكّده
> المالك 2026-08-10) ومعها إعادة `22` بعد تحريره. **لم يعد منها شيء معلَّقًا.**
> والجدول أدناه يبقى لأنه يشرح *ما* كان مفتوحًا و*لماذا* بُني كلّ حارس كما بُني:
>
> | | ما كان يمرّ | أُغلق بـ |
> |---|---|---|
> | 🔴 | `PATCH /profiles {"role":"admin"}` على صفّي ⇒ **القاعدة كلّها**: كل الحجوزات بأرقام هواتفها، سجلّ التدقيق، الكتابة على `places`/`fields` — **وكلّ حارسٍ كُتب قبله**، لأن كلًّا منها يبدأ بـ`if is_admin() then return new` | [`24`](migration/24_integrity_guards.sql) |
> | 🔴 | `PATCH /profiles {"active":true}` ⇒ الموقوف يعيد تفعيل نفسه، ففحصُ `inactive` في `join_open_game` بلا معنى | [`24`](migration/24_integrity_guards.sql) |
> | 🔴 | `POST /bookings {"status":"confirmed"}` ⇒ حجزٌ يؤكّده صاحبه. وينكسر معه القيد ① في المباريات المفتوحة: العرض يشترط `confirmed` وهو شرطٌ كان بيد العميل | [`24`](migration/24_integrity_guards.sql) |
> | 🔴 | تاريخ ماضٍ · ملعبٌ من مكان آخر · ساعة خارج `slots` — **ثلاث فحوصات كانت في `Code.gs`** وسقطت في الترحيل بلا بديل، ورسائلها الثلاث ما زالت في `API_MESSAGE_MAP` تنتظر ردًّا لا يأتي | [`24`](migration/24_integrity_guards.sql) |
> | 🔴 | حجزٌ على **خانة مغلقة**: `17` يحرس اتّجاهًا واحدًا (إغلاقٌ يبتلع حجزًا)، والعكس مفتوح — والسباق واقعي: المالك يُغلق السادسة، ولاعبٌ يضغط «احجز» في الثانية التالية | [`24`](migration/24_integrity_guards.sql) |
> | 🔴 | `PATCH {"status":"cancelled","cancel_kind":"expired"}` ⇒ اللاعب يصم إلغاءه «طلبٌ لم يردّ عليه المالك». وهو **بالضبط** التمييز الذي بُني `15` لأجله، و`/admin` تُخدَع فعلًا (التطبيق محصَّن) | [`24`](migration/24_integrity_guards.sql) |
> | 🟠 | مباريات مكانٍ **أخفاه الأدمن** تبقى معروضة للانضمام (العرض `security definer` فلا يمرّ بـ`places_read`) · ومضيفٌ ينضمّ إلى مباراة أخرى في ساعته (فحص `clash` أعمى عن المضيف) | [`22`](migration/22_open_games.sql) — أُعيد تشغيله |
> | 🟠 | `reviews` بلا سقف طول ولا حدّ معدّل ولا تطهير — وهو **المصدر الوحيد** للنجوم منذ إسقاط `rating_seed` في `06` | [`25`](migration/25_review_limits.sql) |
>
> **الترتيب الذي شُغِّلت به: `24` ⇐ إعادة `22` ⇐ `25` ⇐ `26` ⇐ `27`.** وكلّها آمنة
> لإعادة التشغيل، فالترتيب نفسه يصلح لأي قاعدة أخرى (نسخة اختبار مثلًا).
> ثمّ تحقّق بأمر واحد:
> ```bash
> node tools/security-matrix.mjs --url https://<ref>.supabase.co --anon <key> --phone 07… --pass …
> ```
> بتوكن **لاعب عادي** لا `service_role`: اختبارٌ يحتاج المفتاح الرئيسي لا يختبر ما يستطيعه المهاجم.
> ✅ **وصار بناء APK إصدار آمنًا من هذه الجهة**: المفتاح العام في كل APK، وقبل `24`
> كان ذلك المفتاح مع أي حساب مجّاني طريقًا إلى القاعدة كاملةً. لم يعد كذلك.

> ### 🟢 حالة الترحيلات — **أُعيد قياسها حيًّا 2026-08-18** بالمفتاح العام
>
> ✅ **الخلاصة: `08…33` كلُّها مُشغَّلة، ولا ترحيلَ معلَّقًا اليوم.**
> وأحدثُها [`33_booking_note.sql`](migration/33_booking_note.sql) (الدفعة ٣٩) — شُغِّل
> 2026-08-21 و**مُتحقَّقٌ حيًّا بالمفتاح العام**: `bookings?select=player_note` و
> `bookings_full?select=player_note` يردّان **200** كلاهما. والثاني هو الفحص الذي
> يهمّ: العمود في **العرض** لا في الجدول وحده — وهو ما سقط ثلاث دفعات قبل الترحيل ٣٠.
> وآخرُها [`32_owner_demand.sql`](migration/32_owner_demand.sql) — شُغِّل 2026-08-18
> (أكّده المالك) **ومُتحقَّقٌ حيًّا بفحوصه الثلاثة**: العرض يردّ `200 []` بلا توكن،
> و`profile_id`/`id`/`order=profile_id.desc` تردّ **400 `42703`** في الثلاثة ⇒
> لا عمود يُعرِّف أحدًا يخرج منه.
> ⚠️ **و`[]` هنا هي الحالة الصحيحة لا عطل**: الحصر بـ`owns_place` **داخل** العرض،
> فمن لا جلسةَ له لا يخرج له صفّ — والمالك يرى صفوفَ مكانه وحدها.
> 🔴 **وثلاثة صفوفٍ في هذا الجدول كانت خاطئة** لأن فحص الدوالّ كان بجسمٍ فارغ،
> وهو يردّ `PGRST202` عن دالّةٍ **موجودة** لها وسائط — نفس ردّ الغياب. التفصيل
> في «مزالق التحقّق»، والفحص في `/admin` أُصلح.
>
> **الجدول أدناه كان قديمًا ومضلِّلًا: معظم الترحيلات مُشغَّلة فعلًا.** قِستُها بسؤال
> القاعدة نفسها (`?select=<عمود>&limit=0` لكل جدول/عمود) لا بقراءة هذا الملفّ:
>
> | | |
> |---|---|
> | **مُشغَّلة ✅** | `08` `10` `11` `12` `13` `14` `15` `16` `17` `19` `20` `21` `22` `23` |
> | **شُغِّلت 2026-08-10 ✅** | `24` (حرّاس السلامة) · `22` (أُعيد بعد تحريره) · `25` (حدود التقييمات) · `26` (تسمية العاشرة صباحًا) · `27` (أدنى إصدار) |
> | ~~غير مُشغَّل ❌~~ | 🔴 **هذا الصفّ كان خاطئًا كلَّه — صُحِّح 2026-08-13.** `29_field_gender` **مُشغَّل** (`fields?select=gender` ⇒ 200)، و`18_pricing_rules` **مُشغَّل بتمامه**: `resolve_field_price` و`place_price_grid` تردّان **200** حين تُنادَيان **بأسماء وسائطهما**. والخطأ الذي أنتج الصفّ: نداءُ دالّةٍ بجسمٍ فارغ يردّ `PGRST202` — وهو **نفس ردّ «غير موجودة»**، فقُرئ غيابًا. انظر «مزالق التحقّق» |
> | **شُغِّل 2026-08-10 ✅** | **`28_reply_speed`** — مُتحقَّق حيًّا بالمفتاح العام: العرض يردّ **200**، و`customer_phone`/`player_id`/`order=customer_phone.desc` تردّ **400** في الثلاثة (لا عمود يُعرِّف أحدًا يخرج منه)، و`n=lt.7` يردّ `[]` ⇒ العتبة داخل العرض تعمل، و`bookings.replied_at` موجود. ⚠️ **والعرض يردّ `[]` اليوم** لأن لا مكان بلغ سبعة ردود بعد ⇒ لا سطر يُعرَض — وهي الحالة الصادقة لا عطل: الرقم يظهر مكانًا بعد مكان كلّما تراكمت الردود |
> | **حُسم ✅ 2026-08-13** | **`09_player_reschedule` مُشغَّل.** النداء بوسائطه الحقيقية (`p_booking`/`p_date`/`p_hour`) يردّ **`42501 permission denied`** بمفتاح anon — ورفضُ صلاحيةٍ **دليلُ وجود**. وبجسمٍ فارغ كان يردّ `PGRST202` فقُرئ غيابًا، ومنه جاءت شارة «١ ترحيل ناقص» في `/admin` — **إيجابيّةٌ كاذبة**، وقد أُصلح الفحص |
> | ✅ **شُغِّل 2026-08-13** | **[`30_bookings_full_refresh.sql`](migration/30_bookings_full_refresh.sql)** — مُتحقَّق حيًّا: `bookings_full?select=cancel_kind,no_show,visibility,players_needed,players_brought` يردّ **200** بعد أن كان `42703`. — العرض `bookings_full` مُعرَّف بـ`select b.*`، و`*` **تُفكّ عند إنشاء العرض لا عند قراءته** ⇒ تجمّدت قائمة أعمدته على 01، فبقيت `cancel_kind`(15) و`no_show`(16) و`visibility`/`players_needed`/`players_brought`(22) **في الجدول وخارج العرض**. مقيس: `bookings?select=no_show` ⇒ 200 و`bookings_full?select=no_show` ⇒ `42703`. **والتطبيق يقرأ العرض** ⇒ لا زرّ «لم يحضر» ولا شارة مباراة مفتوحة ولا تمييز «انقضت المهلة» — **بلا عطلٍ ظاهر** لأن الجلب يتراجع ثلاث جولات، و`/admin` سليمة لأنها تقرأ `bookings` خامًا |
>
> ✅ **وخبرٌ سارّ في الطريق:** `app_settings` يردّ **401** لا 200 ⇒ الجدول موجود **ومحجوب**،
> فتصادُم الاسم بين `11` و`15` الموصوف أدناه **لم يترك سياسة قراءة عامّة** على جدول
> مزوّد الرسائل. لا تسريب.
>
> ⚠️ **وبنودُ الجدول التالي التي تصف «بدونه لا يعمل كذا» صارت تاريخًا لكلّ ما هو ✅ أعلاه** —
> أُبقيت لأن نصوصها تشرح *لماذا* بُني كلّ شيء كما بُني. **صدِّق الصفّ أعلاه لا الجدول.**

| # | ما هو | الأثر |
|---|---|---|
| 1 | **شغّل [`migration/08_realtime_audit.sql`](migration/08_realtime_audit.sql)** | بدونه سجلّ `/admin` يتحدّث بالاستطلاع كل 20ث لا حيًّا. **اللوحة تعمل**، وتقول ذلك صراحةً |
| 1ب | **شغّل [`migration/09_player_reschedule.sql`](migration/09_player_reschedule.sql)** | بدونه **زرّ «تعديل» في «حجوزاتي» لا يعمل**: PostgREST يردّ 404/`PGRST202` والتطبيق يقولها صراحةً («تعديل الموعد غير مُفعّل على الخادم بعد»). مقيس حيًّا 2026-07-30 |
| 1ج | **شغّل [`migration/10_admin_place_owners.sql`](migration/10_admin_place_owners.sql)** | بدونه **ربط مالك بمكان من «إضافة مكان» يفشل بـ403**: `place_owners` له سياسة قراءة ولا سياسة كتابة ⇒ حتى الأدمن ممنوع. **إنشاء المكان وملاعبه يعمل بدونه**، واللوحة تقول سبب فشل الربط وتشير إلى الملفّ |
| 1د | **شغّل [`migration/11_phone_verification.sql`](migration/11_phone_verification.sql)** | بدونه **شاشة «أكّد رقمك» تعرض لوحًا صادقًا** ("تأكيد الرقم غير مُفعّل على الخادم بعد") وزرّ «أكمل». التسجيل والحجز يعملان بدونه تمامًا |
| 1هـ | **اشترِ مزوّد رسائل ثمّ املأ `sms_provider`** | **لا توجد وسيلة مجانية موثوقة** (قيست: Twilio تجريبي = أرقام موثَّقة فقط · قوالب مصادقة واتساب مدفوعة · Firebase = نظام هوية ثانٍ). ما دام المفتاح فارغًا فـ`request_phone_code` تردّ `sent:false, reason:'no_provider'` **ولا تولّد كودًا ولا تكتب في الطابور**. الخطوات الأربع في §6 آخر الملفّ |
| 1و | **شغّل [`migration/12_payment_method.sql`](migration/12_payment_method.sql)** (اختياري) | **التطبيق لا يعتمد عليه إطلاقًا** — لا يرسل `payment_method` والافتراضي `'cash'`. يسجّل مسار المال ويهيّئ للبطاقة. بدونه كل شيء يعمل |
| 1ز | **بوّابة دفع للفيزا** | حتى تُوصَل، «فيزا» في نافذة المراجعة **معطَّلة بشارة «قريباً»** والنقر عليها يقول السبب. ⚠️ **لن يُكتب حقل بطاقة داخل التطبيق أبدًا** — الرقم يُدخَل عند البوّابة (PCI-DSS)، ولا يصلنا إلا مرجع العملية وآخر أربعة أرقام. والقرار التشغيلي المعلَّق: حجزُ مبلغ عند الطلب وقبضُه عند تأكيد المالك (الحجز طلبٌ ينتظر ردًّا، فالسحب المبكّر = ردُّ مبلغ عند كل رفض) |
| 1ح | **شغّل [`migration/13_field_sports.sql`](migration/13_field_sports.sql)** | بدونه **لا يستطيع الأدمن تسجيل ملعب بادل ولا تنس**: العمودان `fields.sport` و`fields.attrs` غير موجودين، والقاعدة ترفض الإدراج بـ`PGRST204`. واللوحة **تقول ذلك قبل أن تملأ النموذج** لا بعد رفضه. وكلّ ما عداه يعمل: كل ملعب يُقرأ كرة قدم كما كان |
| 1ط | **شغّل [`migration/14_notifications.sql`](migration/14_notifications.sql)** | بدونه **لا إشعارات إطلاقًا** (لا للمالك عند وصول طلب، ولا للاعب عند التأكيد): الجدول غير موجود فيردّ PostgREST 404/`PGRST205`، ومركز الإشعارات **يقول سببه صراحةً** بدل قائمة فارغة تُوهم بأنه لا جديد. الحجز والتأكيد والإلغاء تعمل كما هي |
| 1ك | **شغّل [`migration/15_booking_expiry.sql`](migration/15_booking_expiry.sql)** — ⚠️ **بعد 14 لا قبله** (السبب في [`migration/README.md`](migration/README.md)) | ملفٌّ واحد لقاعدتين لأنهما حقيقة واحدة: **للخانة ساعة**. بدونه ① **مهلة إلغاء اللاعب غير مفروضة**: الواجهة تُخفي الزرّ قبل ٦ ساعات من الموعد وتعرض لوحًا صادقًا برقم الملعب، لكن نداءَ REST مباشرةً **يمرّ** — الإخفاء ليس أمانًا · ② **لا انقضاء تلقائي لطلبٍ لم يُردَّ عليه**، وهو يحجز خانته (‏`bookings_no_double_idx` يشمل `pending`) فيمنع غيره ثمّ يضيع. والعدّاد يظهر في لوحة المالك على أي حال، **وتحته حاشيةٌ تقول إن الانقضاء غير مُفعّل** بدل الوعد بما لا يقع · ③ `/admin` لا يفصل «انقضت المهلة» عن «رفض المالك». **وكل ما عدا ذلك يعمل بلا نقص** |
| 1ل | **شغّل [`migration/16_no_show.sql`](migration/16_no_show.sql)** (مستقلّ عن 15) | بدونه **لا تُسجَّل واقعة «لم يحضر»** — وهي بندٌ في الاتفاقية (المادّة ٩: الحجز يبقى مكتملًا والعمولة مستحقّة) بلا آليّة تنفّذه. الزرّ موجود في لوحة المالك ويردّ `PGRST204` **باسم الترحيل الناقص**، وبلاطة «لم يحضروا» تُخفى: صفرٌ لا مصدر له يُقرأ «لا يحدث هذا عندنا» وهو ادّعاء لا قياس |
| 1م | **شغّل [`migration/17_closures.sql`](migration/17_closures.sql)** | بدونه **لا بدائيّة لـ«هذا اليوم غير متاح»**، والمالك يُغلق يومًا بتزوير حجز وهمي في كل خانة — فتدخل إيرادَه وإشغالَه ومتوسّطَ سعره، ويقرأ يومَ الصيانة يومًا ناجحًا. اللوحة تعرض الزرّ **وتقول اسم الترحيل الناقص** عند الضغط، ولوح اليوم يقول «هذا اليوم مفتوح». وكل ما عداه يعمل |
| 1ن | **شغّل [`migration/18_pricing_rules.sql`](migration/18_pricing_rules.sql)** | بدونه **لا تسعير بالساعة ولا بالموسم**: كل خانة بسعر `fields.price` كما اليوم، ونافذة «التسعير» تقول اسم الترحيل وتُخفي نموذجها. ⚠️ **وبعده يصير السعر بيد الخادم**: مُشغِّل يفرض `resolve_field_price` على إدراج اللاعب، فعميلٌ معدَّل يرسل `price:0` يُكتب له السعر الصحيح — وقبله كان يُكتب صفرًا. والمالك مستثنًى (حجزه اليدوي متّفَقٌ عليه) |
| 1س | **شغّل [`migration/19_place_applications.sql`](migration/19_place_applications.sql)** | بدونه **نموذج «سجّل ملعبك» على `/owners` لا يجد جدولًا**: يردّ 404 ويقول ذلك ويحيل إلى `/contact`، وصندوق الوارد في `/admin` يقول اسم الترحيل بدل قائمة فارغة تُوهم بأنه لا طلبات. ⚠️ **الصفوف تحمل أرقام هواتف حقيقية** ⇒ نفّذ فحص التسريب الأربعة في ذيل الملفّ بالمفتاح العام قبل الإعلان عن النموذج |
| 1ع | **شغّل [`migration/20_slot_watch.sql`](migration/20_slot_watch.sql)** — 🔴 **بعد 14 وإلّا توقّف** | بدونه **زرّ «نبّهني إذا فضيت» لا يظهر إطلاقًا** في لوح البدائل: النيّة التي لا نستطيع الوفاء بها لا تُقبَل. والملفّ نفسه **يرفض التشغيل** إن غاب 14 ويقول لماذا — جدولُ رغباتٍ بلا وسيلة إشعارٍ جمعُ بياناتٍ بلا فائدة |
| 1ف | **شغّل [`migration/21_events.sql`](migration/21_events.sql)** | بدونه **لا يُقاس ما لم يحدث**: لا بحثٌ فارغ ولا قمعُ تحويل ولا خانةٌ مطلوبة. التطبيق يحاول مرّة ثمّ يكفّ صامتًا، ولوح «الطلب الذي لم يُلبَّ» في `/admin` يقول اسم الترحيل. ⚠️ **والقياس بلا أثر رجعي** — كل يوم تأخير يومٌ لا يعود |
| 1ص | **شغّل [`migration/22_open_games.sql`](migration/22_open_games.sql)** | بدونه **لا مبدّل «مباريات» ولا خيار «مباراة مفتوحة»**: التطبيق يسأل القاعدة مرّةً عند أوّل تحميل بجلسة (‏`open_games?limit=0`) ويُخفي الاثنين إن لم يجد — لا يُعرَض بابٌ ثمّ يُسحَب. وكل حجز خاصّ كما اليوم بالحرف. ⚠️ **لا مقعد يُنشَر قبل تأكيد الملعب** (شرطٌ في العرض نفسه لا في الواجهة)، و**لا رقم هاتف في أيٍّ من العرضين** — نفّذ الفحوص الأربعة في ذيل الملفّ بالمفتاح العام |
| ~~1ط-دفع~~ | ✅ **إشعارات الدفع تعمل من طرف إلى طرف — 2026-08-14** | مشروع Firebase موجود، `google-services.json` في `android/app/`، سرّ `FCM_SERVICE_ACCOUNT` في Supabase، دالّة `push` منشورة، ترحيل ٣١ مُشغَّل، ومفتاحا `app_settings` مملوءان. **ومُتحقَّق حيًّا على الإنتاج**: صفّ إشعارٍ حقيقي أُدرج فأطلق المُشغِّل، ووصل إلى الدالّة، وردّت `200 {sent:false, reason:"no_token"}` — وهي **الإجابة الصحيحة**: لا جهاز سجّل رمزًا بعد. ⚠️ **والحلقة تكتمل فقط بعد أن يُثبَّت هذا الإصدار على جهاز**: يفتح التطبيق ويمنح إذن الإشعارات فيُسجَّل الرمز، وعندها يصل الإشعار فعلًا |
| ~~1ي~~ | ✅ **الـAI منشورٌ ويعمل — 2026-08-13** | الدالّة منشورة، و`GEMINI_API_KEY` و`AI_MODEL=gemini-flash-latest` في أسرار Supabase، و**اللوحتان تعملان** (مؤكَّد من المالك على جهازه). التفصيل الكامل والأخطاء الأربعة التي مرّ بها النشر في «الدفعة الثلاثون» أدناه. ⚠️ **والاستهلاك على حساب Google الخاصّ بالمالك**: لوحتان تستدعيان النموذج (المستشار بشرط ٧ أيام حجزٍ فعلية · التقييمات بشرط تقييمٍ واحد)، والطقس **لا يستدعيه إطلاقًا**. والنتيجة تُحفَظ في **الجلسة وحدها** ⇒ كلّ إعادة فتحٍ للتطبيق = استدعاءان. القياس الحقيقي في `aistudio.google.com/usage` لا في تقديرٍ هنا |
| ~~1ن-٣٢~~ | ✅ **شُغِّل ترحيل [`32_owner_demand`](migration/32_owner_demand.sql) — 2026-08-18** (أكّده المالك) | **ومُتحقَّق حيًّا بالمفتاح العام**، بالفحوص الثلاثة المكتوبة في ذيل الملفّ: العرض يردّ **200 و`[]`** بلا توكن (لا `auth.uid()` ⇒ لا مالك ⇒ لا صفّ — وهي الحالة الصحيحة لا عطل)، و`profile_id` و`id` و`order=profile_id.desc` تردّ **400 `42703`** في الثلاثة ⇒ **لا عمود يُعرِّف أحدًا يخرج منه**. ولوح «طلبٌ وصلك وما لقى مكان» صار يظهر في التقارير حين توجد رغبات |
| ~~2~~ | ✅ **اختُبر على جهاز حقيقي — 2026-08-18** (أكّده المالك) | ⚠️ **وهذا تأكيدُ مستخدمٍ لا قياسُ أداة**: يغطّي ما جرّبه فعلًا. وما يبقى خارجه صنفٌ لا يراه الاستعمال العادي — **TalkBack** (بروتوكول الإتاحة في [`docs/qa/accessibility-protocol.md`](docs/qa/accessibility-protocol.md) وجداولُه ما زالت فارغة)، و**تنميط الأداء** على جهازٍ متوسّط |
| 3 | **أرقام المُلّاك الحقيقية** | حسابات المُلّاك ما زالت بالبيانات المستورَدة |
| ~~4~~ | ✅ **المفتاح موجود والإصدار موقَّع** | مقيس: `android/mustadeera-release.jks` على القرص وخارج git، والبناء يطبع `[apk] copied from the signed build → /downloads/mustadaira-1.0.apk`. والدليل في [`android/SIGNING.md`](android/SIGNING.md). ⚠️ **وفقدانه يعني استحالة التحديث** — لا يُستبدَل بعد أوّل نشر |
| ~~5ب~~ | ✅ **أُغلق 2026-08-24 — والصفّ كان بائتًا** | 🔴 **ما كُتب هنا لم يعد صحيحًا**: مقيس أنّ **الهيرو ولوح الختام** يقرآن `multisport-courts-1100.jpg` منذ الدفعة ٢١، وأنّ **بلاطات الرياضات الخمس لكلٍّ صورتُها** (`padel` · `basket` · `tennis` · `volley` في `site/static/assets/photos/`). فالباقي **ثلاثة مواضع لا أربعة**: شريط «كيف يعمل» ولوح المُلّاك (`players-kick` · `players-run`) وشريحة الترحيب الثانية. **وقرار المالك 2026-08-24: تبقى كرة قدم** — وهي **المفتوح للحجز فعلًا** (قرار ٧)، والوعدُ العريض تسنده الهيرو وبلاطات الرياضات؛ فصورةُ لاعبين تركل الكرة هنا **صادقة** لا متناقضة. ⚠️ ويبقى المبدأ: **لا تُخترَع صورة ولا يُكتب نصّ بديل لما ليس فيها (م5)** |
| 6 | ⏳ **بطاقة مشاركة واتساب للمقعد المنشور** — **مؤجَّلة بقرار المالك 2026-08-13 إلى «قُبيل الإطلاق»** | الحلقة الوحيدة ذات انتشارٍ حقيقي في المنتج، وغير مسوَّقة: مضيفٌ ينشر مقاعده فيجنّد تسعة من مجموعاته ⇒ تسع تثبيتات لكل مباراة. **بلا ترحيل جديد** — كل ما تحتاجه موجود في `open_games` (الملعب · الوقت · الحصّة التقديرية · المقاعد المتبقّية). ⚠️ وقيدان يحكمانها من الدفعة ١٥ ولا يُخرَقان: **لا رقم هاتف يعبر البطاقة** (العرضان لا يحملانه أصلًا)، و**لا مقعد يُشارَك قبل أن يؤكّد الملعب** — فالزرّ لا يظهر على حجزٍ معلّق. والحصّة تُوسَم «تقديري» في نصّ الرسالة كما تُوسَم في التطبيق (م5). ولمّا يقترب الموعد: راجع أن الرابط العميق يقع على مسارٍ يفتحه من لا يملك التطبيق (‏`/download` ثمّ الاستئناف) لا على شاشةٍ بيضاء |
| ~~5~~ | ~~**قرار P5:** الـAI~~ | ✅ **حُسم 2026-08-06**: نُقل إلى Supabase Edge Function، و`Code.gs` **لم يعد يُنادى من التطبيق إطلاقًا** (نُقل إلى `_refs/legacy-backend/` في ترتيب 2026-08-22) ⇒ يُوقَف. انظر 1ي أعلاه للنشر |

✅ **كل الدفعات حتى السابعة والعشرين مدفوعة** — مقيس 2026-08-13 بـ`git rev-list --count origin/main..HEAD` = **0**.
⚠️ **وهذا السطر كان يقول عكس ذلك** («الدفعات ١٢–٢٠ غير مدفوعة» · «العشرون غير مُلتزَمة»)
وقد صار خطأً بعد أن دُفعت، فبقي يُضلّل جلساتٍ لاحقة. **حالةُ الدفع تُقاس بأمرٍ لا تُكتب بيد** —
والسطر يبقى هنا مثالًا: أي جملة حالةٍ في هذا الملفّ تُصدَّق فقط بعد أن يؤكّدها أمر.

⚠️ **تحقّق من لوحة `vercel.com` أن آخر بناء نجح** — الموقع محجوب بفلترة SNI فلا يُفحَص حيًّا من هنا. ثلاثة أشياء تحديدًا لم تُتحقَّق إلّا محلّيًّا:
1. **`/build/site.<hash>.css|js`** يُخدَمان فعلًا (لو 404 تظهر الصفحات بلا أنماط ولا سلوك). افتح الرئيسة وتأكّد أن الشكل سليم.
2. **`Cache-Control: immutable`** على `/build/*` — القاعدة أُضيفت في `vercel.json` ولم تُفحَص على الخادم.
3. **`/assets/photos/*.jpg`** تصل (604KB مجموعها).

وما زال قائمًا من قبل: حُذف `index.html` من الجذر وهو **لم يكن يُخدَم** (`outputDirectory: "public"`) لكنّه لم يُتحقَّق حيًّا.

---

## ✅ ما اكتمل

**التطبيق** — الترحيل إلى Postgres تامّ (صفر طلب إلى Apps Script عدا AI) · تغيير كلمة السرّ ·
تقييمات حقيقية فقط (`hasRating`: مكان بلا تقييم بلا نجوم) · haptics أصلية · انتقالات صفحات
وإيماءة رجوع · ثيم سلس · إعداد التوقيع جاهز في `build.gradle`.

**الموقع** — 28 صفحة (8 مسارات × لغتين + 6 ملاعب × لغتين) + `/admin`. `/places` **مولَّد من
Postgres وقت البناء** مع كاش `site/data/places.json` يمنع كسر البناء عند انقطاع الشبكة.

**تطوير الموقع (2026-07-29)** — الـCSS والـJS خرجا إلى `/build/site.<hash>.css|js`
(الرئيسة كانت 41KB لأن الورقة تتكرّر في 28 صفحة؛ صارت 26KB وتُخزَّن الأصول سنة `immutable`).
وعليه: **درج تنقّل للجوّال** (لم يكن هناك أيّ تنقّل تحت 900px)، كشف عند التمرير
بـIntersectionObserver (كان `animation-timeline` ⇒ Chromium وحده)، تبديل ثيم بقصّ دائري عبر
View Transitions، انتقال بين الصفحات (`@view-transition`) فتبديل اللغة مزج لا وميض، شريط
أرقام حقيقية من القاعدة، أسئلة شائعة، وتصفية فورية في `/places` **بالمنطقة لا بالمدينة**
(الملاعب الستّة كلّها في مدينة واحدة وستّ مناطق ⇒ تصفية المدينة لا تصفّي شيئًا).

**الدفعة الثانية (2026-07-29)** — هاتف الهيرو صار جهازًا واقعيًّا بالـCSS (إطار معدني ·
جزيرة بعدسة · أزرار جانبية · شريط حالة · مؤشّر سفلي) · **أُزيل أنيميشن اللمعة الثلاثة**
(الأزرار · الشاشة · زرّ الإرسال) بطلب المالك، وبديله ارتفاع وتوهّج ونبض حيّ · صور حقيقية
(`site/tools/make-photos.ps1` يصغّرها وينسخها، والموقع يملك نسخته) في الهيرو ولوح المُلّاك
ولوح الختام · **`/admin` صارت تربط أصول الموقع نفسها**، وجداولها أوضح ومعها بيانات لم تكن
معروضة: **المعلّقة** (مشتقّة، ومجال `status` أربع قيم فيصحّ الاشتقاق) · صفوف مجاميع ·
حصّة كل مكان من الإيراد · معرّف الكيان في سجلّ التدقيق · وقت نسبي بجموع عربية صحيحة.

**الدفعة الثالثة (2026-07-29، `b975c1c`)** — **هيكل الهاتف (٨٨ سطر CSS) حُذف**، ومحلّه
**لقطتان حقيقيتان** من شاشة الحجز في التطبيق: `assets/app-mockup-{ar,en}.png` عبر
`{{lang}}`. لقطتان لا واحدة لأن الهيكل القديم كان يقرأ نصّه من `strings/` فيتبع اللغة
مجّانًا. تُعرَضان على لوح `.hero-shot` (هالة Lime + حافّة + طفو 7.5ث) **بلا أي دوران** —
`rotateY` على لقطة مسطّحة يُقرأ اعوجاجًا. · **الأزرار المغناطيسية أُزيلت** وبديلها بقعة
ضوء لا تحرّك الزرّ · درجات التحويم تُضيء لا تُعتِم (`--teal-lift` · `--lime-lift`)،
والثانوي يمتلئ ويقلب نصّه · ظلال مصبوغة بالهوية + حافّة زجاج · صورة ممزوجة في بلاطة
Bento · كتلة عائمة ثانية · انزياح صورة المُلّاك بمرورها.

**الدفعة الرابعة (2026-07-30، `8a93a95`)** — **إطار جهاز حول اللقطة** (معدن · حافّة لامعة ·
ثلاثة أزرار جانبية منطقية · جزيرة ومؤشّر فوق الصورة) فلم تعد تُقرأ مربّعًا أسود؛ وبلا دوران
كما قبل. · **زرّ «نزّل التطبيق» يشير إلى الـAPK مباشرة** حين يُملأ `site/release.txt`، ومعه
رابط إلى `/download/` (البصمة وخطوات التثبيت) في تلك الحالة وحدها؛ وما دام الملفّ فارغًا
فالزرّان يذهبان إلى `/download/` كما كانا.

**الدفعة الخامسة — سبع ترقيات في التطبيق (2026-07-30)** — ① **تعديل موعد الحجز** للمعلّق وحده
(نافذة تعيد استعمال منطق الوقت نفسه، والحفظ عبر دالّة في القاعدة لا PATCH — انظر المزلق أدناه) ·
② **لافتة ندرة** تظهر حين يبقى وقت أو وقتان، وعددها = عدد الأزرار القابلة للنقر بالضبط ·
③ **حذف الحساب** = `profiles.active=false` (حذف مستخدم Auth يحتاج `service_role` ⇒ مرفوض كقرار ٥)،
ونصّ التأكيد يقول ما يحدث بالضبط ولا يَعِد بمحو البيانات · ④ **طبقة انقطاع الاتصال** بحدثَي
`offline`/`online` ومعها زرّ خروج إجباري (‏`navigator.onLine===true` ليس دليل إنترنت) ·
⑤ **تحذير التعديلات غير المحفوظة** على أربع نوافذ ومن **كل** مسارات الإغلاق ·
⑥ **السحب للتحديث** على الرئيسية والحجوزات ولوحة المالك · ⑦ **شرائح الفلاتر المفعّلة** اللاصقة.

**الدفعة السادسة — المنتج صار «ملاعب رياضية» لا «كرة قدم» (2026-07-30)** — الوصف كان يقول
كرة القدم في كل مكان بينما التطبيق فيه **خمس رياضات** منذ البداية، فكان العميل يقرأ شارات
«قريبًا» عطلًا لا خطّة. الآن: **الشعار والتذييل وعناوين البحث** كلّها «حجز الملاعب الرياضية» ·
**قسم «الرياضات» في الرئيسة** — خمس بلاطات بأيقونات التطبيق نفسها، حالة كل واحدة مكتوبة عليها
(«متاح للحجز» / «قريبًا») لا في حاشية · **سؤال شائع يُعرَض أوّلًا** («هل التطبيق لكرة القدم
فقط؟») · **حاشية في `/places`** تقول لماذا الدليل كلّه كرة قدم · **لوح في `/about`** ولوح في
`/owners` يجيب «ملعبي ليس لكرة القدم — هل تقبلونه؟» · وفي **التطبيق**: أيقونات الترحيب صارت
خمسًا لا أربعًا (كانت تعِد بأقلّ ممّا فيه)، ونصّ فرعي يقول «خمس رياضات في تطبيق واحد»،
وحاشية `sportsHint` تحت مبدّل الرياضة في ورقة الفلاتر.
⚠️ وأُصلح في الطريق: `metaHomeDesc` كان لا يزال يعِد بـ«نسخة تعمل على المتصفّح» وقد سُحبت (قرار ١).

**الدفعة السابعة — الصورة تلحق بالنصّ: مخطّطات ملاعب (2026-07-30)** — الدفعة السادسة قالت
«خمس رياضات» **نصًّا**، وبقيت كل صورة على الوجهين كرة قدم، فالزائر يقرأ وعدًا ولا يرى ما يسنده.
والمخرج ليس صورة مخترَعة (م5) بل **مخطّط الملعب**: رسمٌ هندسي لأبعاد منشورة، لا ادّعاء بأن
عندنا ملعبًا كهذا. الآن:
① **قسم «الرياضات» في الرئيسة** — كل حبّة صار فوقها **لوح مخطّط** بدل الأيقونة وحدها،
والأيقونة شارة على زاويته (فتبقى الصلة بالتطبيق). ② **حالة «قريباً» في التطبيق** — مخطّط
الرياضة المختارة بدل الرمز التعبيري ⏳ (الساعة الرملية تقول «انتظر» ولا تقول **ماذا** تنتظر).
③ اللوح نسبته ثابتة 16/10 في كل الحبّات فتبقى الشبكة منتظمة، أمّا **الملعب داخله فبنسبته
الحقيقية** (بادل 20×10 · قدم 105×68 · تنس 23.8×11 · سلة 28×15 · طائرة 18×9) ⇒ الأشكال تخرج
مختلفة فعلًا، **والاختلاف نفسه هو الرسالة**. ④ المخطّطات نسخة واحدة على الوجهين
(`COURT` في [`app/src/app.js`](app/src/app.js) = ما في قسم الرياضات بالموقع).
⚠️ **الصور الفوتوغرافية ما زالت كلّها كرة قدم** (الهيرو · لوح المُلّاك · لوح الختام ·
`app/assets/onb/`) — ولا تُخترَع (م5). ✅ **وأُغلق البند 5ب 2026-08-24** — الهيرو ولوح الختام صارا متعدّدَي الرياضات في الدفعة ٢١، وما بقي كرةَ قدم يبقى **بقرار المالك**.

**الدفعة السابعة — الرئيسة تُعاد على مرجع بصري (2026-07-30)** — ① **عنوان الهيرو ثلاثة أسطر**
بثلاث درجات صوت: أبيض ⇒ Lime ⇒ **محيطي** (`-webkit-text-stroke` داخل `@supports`) ·
② **الجهاز يميل فعلًا**: منظور على الحاوية وpreserve-3d والزجاج مرفوع 3px، فالإطار والشاشة
كتلةٌ واحدة تدور — لا لقطة مسطّحة تُشوَّه (انظر المزلق أدناه) · ③ **لوحتان طائرتان** حول
الجهاز بمحتوى بنيوي لا حدث حيّ · ④ **بلاطة الرقم العملاق** `0` ⇒ «مكالمة لتحجز» ·
⑤ **بلاطة التسعير** ومداها `{{priceSpan}}` **مقروء من القاعدة وقت البناء** لا مكتوب بيد ·
⑥ **قسم المُلّاك صار لوحًا عاجيًّا بعرض الشاشة** بشارة على حدّه وقائمة `٠١..٠٤` (يبقى عاجيًّا
في الثيمين كما يبقى الهيرو داكنًا — لو تبع الثيم لضاع «قلب الجهة») · ⑦ **صور رمادية تتلوّن
عند التحويم** بطبقتين تتقاطعان بالـopacity · ⑧ **جملة ختام كبيرة بلونين**.
⚠️ ورُفض من المرجع كلّ رقم مخترَع: «40+ ملعبًا · 18k حجزًا · 4.8★ · 28 ثانية»، والشهادات،
و«9:00 م — فضي الآن»، و«١٤ لاعبًا يراقبون» (م5).

**الدفعة الثامنة — التطبيق (2026-07-31)** — ① **حالة كل يوم على زرّه**: «ممتلئ» أو «باقي n»
مشتقّة من `State.bookedSlots` بلا طلب شبكة (الجلبة الواحدة تبني الأيام السبعة)، وارتفاع السطر
محجوز دائمًا فلا يتسنّن الشريط · ② **عدد أوقات اليوم على بطاقة المكان** — `placeAvailability`
كانت تحسب `todayFree` وترميه · ③ **بديل ذكي حين يمتلئ اليوم**: يقترح أقرب وقت متاح فعلًا
(ملعب آخر نفس اليوم، ثمّ الأيام التالية) وينقل إليه بضغطة — محلّي بالكامل، بلا جدول ولا هجرة ·
⑥ **«حسابي» أربع بطاقات معنونة** بأيقونات (بلا بطاقة «دعم»: لا رقم دعم في التطبيق) ·
⑧ **هالة تركيز** حول الحدّ · **صورة الملعب تذوب** في لون الصفحة بقناع تدرّج بدل حدّ مقصوص ·
**الشريط السفلي عائم** والعنصر النشط **حبّة ممتلئة** تتمدّد بـ`max-inline-size` (لا `width:auto`
فهي لا تُنعَّم).

**الدفعة التاسعة — الحساب: تأكيد الرقم · شاشتا الدخول · وسيلة الدفع (2026-07-31)**

① **تأكيد رقم الهاتف** — التوليد والمقارنة **في القاعدة وحدها** ([`11_phone_verification.sql`](migration/11_phone_verification.sql)):
كود مُجزَّأ sha256 بملح · صلاحية عشر دقائق · خمس محاولات ثمّ يُحرَق · حدّان للإرسال
(‏60ث بين رسالتين · 5 في الساعة) **في القاعدة لا في الواجهة** (عدّاد الواجهة يُتجاوَز بإعادة
التحميل). الجدولان بلا أي سياسة RLS ⇒ لا `anon` ولا `authenticated` يرى منهما صفًّا.
الشاشة: ست خانات تتصرّف كحقل واحد (تقدّم · تراجع بالمسح · لصق يوزّع · اكتمالٌ يُرسل بلا زرّ)،
والأرقام الهندية تُطبَّع إلى لاتينية. **ولا تحجز أحدًا**: «أكمل، وأكّد لاحقاً» ظاهر دائمًا،
و**الضيف الذي جاء في منتصف حجز يُستأنف حجزه ولا يمرّ بالشاشة أصلًا** (جاء ليحجز لا ليؤكّد).
⚠️ وم5 حرفيًّا: جملة «أرسلنا كوداً إلى {رقمك}» **لا تُكتب إلا بعد أن يؤكّد الخادم الإرسال**؛
قبلها — وطوال حالة «لا مزوّد» — السطرُ يقول لماذا نؤكّد الرقم ولا يدّعي إرسالًا لم يقع.

② **شاشتا الدخول والتسجيل** — ثلاثة شروط لكلمة السرّ **مكتوبة قبل أن تُخالَف** وتُشطب وهو
يكتب، ومقياس قوّة بأربع شرائح تُملأ بـ`scaleX` (وصفٌ لا شرط). **وأُصلح تعارض مقيس**: النائبة
كانت تقول «4 خانات على الأقل» بينما الخادم يرفض ما دون **الستّ** ⇒ من يكتب خمسًا يُرَدّ بلا
سبب ظاهر. المصدر الآن واحد (`PW_MIN`). والبدائل («حساب جديد» · «تصفّح بدون حساب») خلف فاصل
فلا ينافس الزرَّ الأساسيَّ زرّان بوزنه. الحركة: هالة تنبض وأيقونة تطفو وظهور متدرّج —
`transform`/`opacity` وحدهما، وكل إزاحة أفقية **متناظرة** فلا تحتاج ضربًا في اتجاه.

③ **وسيلة الدفع** — خياران في نافذة المراجعة: «نقداً في الملعب» (مختار) و«فيزا» **معطَّلة
بشارة «قريباً»**، والنقر عليها يقول السبب بدل أن يُبتلع بصمت. وبطاقة «بطاقاتي» في «حسابي»
تقول الحقيقة بدل قائمة فارغة تُوهم بأن الحفظ ممكن. و[`12_payment_method.sql`](migration/12_payment_method.sql)
يسجّل `payment_method/status/ref` **ومعه مُشغِّل يمنع غير الأدمن و`service_role` من كتابة
حقول الدفع أصلًا** — لأن `bookings_insert_player` تسمح للاعب بإدراج صفّه، والسياسة تحكم
الصفوف لا الأعمدة، فقيدُ «البطاقة تستلزم مرجعًا» وحده يتجاوزه بمرجع مخترَع.

④ **وأُصلح في الطريق:** `getPlayerBookings` كانت لا تردّ `player`، و`init()` يُسند
`State.player = res.player` ⇒ **`undefined` بعد كل إعادة فتح**: الاسم يختفي من سطر الترحيب.
(‏`sbCreateBooking` كان يتراجع إلى `session.name` فلم يظهر الأثر في الحجز نفسه.)

⚠️ **قياس تبديل الثيم واللغة (مقيس، وغير محسوم):** خطّ الأساس الساكن **صفر** إطارات ساقطة،
والنقرة الخاملة **صفر**. تبديل **اللغة سليم**: إطار واحد ساقط (دفعة إعادة الرسم مخفيّة خلف
لقطة الانتقال). تبديل **الثيم يُسقط 9-12 إطارًا** (ذروة 50ms) موزّعة على كامل الـ560ms.
والسبب **الانتقالات لا الحركات**: `*{transition:none}` مضافةً قبل الضغطة تُنزلها إلى **2**،
و`*{animation:none}` لا تغيّر شيئًا (14). لكنّ **العلاجين المجرَّبين فشلا**: توسيع قائمة
`.theme-swap` إلى `*`، وإبقاؤها مرفوعة طوال الانتقال بدل reflow واحد — كلاهما أبقى الرقم
(12 و9)، فأُعيدا كما كانا. **لا تُكرَّر المحاولتان**؛ الخطوة التالية تنميط حقيقي (profiler).

**الدفعة العاشرة — الشريط السفلي: حبّة واحدة تنزلق (2026-07-31)** — الشريط كان يُنعِّم ما لا
يُنعَّم: الحبّة خلفيةُ الزرّ نفسه، والنصّ يتمدّد بـ`max-inline-size` **وهو داخل التدفّق** ⇒
كل فريم من الـ170ms **يُعيد تخطيط الصفّ كلّه**، ومعه `background-color` تُطفَأ في زرّ وتُشعَل
في آخر. لا منحنى ولا مدّة تُصلح ذلك — الخاصّية نفسها هي العطل. الآن:
① **حبّة واحدة خارج التدفّق** (`.nav-pill`) تنزلق بين الأزرار بـ`transform`+`width` بنابض
حقيقي `--sp-pill` (‏ζ=0.72 · ω=8.2 · 420ms · تجاوز 3.8%)، والأزرار لا تتحرّك إطلاقًا ·
② **النصّ ظاهر دائمًا** لا في النشط وحده (بلا تمدّد لا حاجة إلى إخفائه)، ولونه صار `--muted`
‏(6.21:1 على الأبيض · 9.08:1 على الداكن) لا `--soft` ‏(2.87:1): نصٌّ دائم يحمل معلومة ·
③ **نبضة الأيقونة** صارت `--sp-pop` (‏ζ=0.50 · 420ms) وتجاوزها من المنحنى نفسه لا من إطار
وسيط مكتوب بيد. **والحالة ما زالت محمولة على الشكل** (حبّة ممتلئة) لا على اللون وحده.
مقيس: الحبّة تطابق الزرّ النشط **بالبكسل** (dx=dy=dw=0) في RTL وLTR وعبر ثلاث تبديلات لغة،
وبلا تجاوز أفقي عند 375px.

**الدفعة الحادية عشرة — الرياضات الخمس تصير بيانات، والحجز يصير حوارًا (2026-08-06)**

**① الرياضة على الملعب لا على المكان** ([`13_field_sports.sql`](migration/13_field_sports.sql)) —
`fields.sport` + `fields.attrs jsonb`. **على الملعب** لأن الوحدة القابلة للحجز هي الملعب،
ومجمّعٌ واحد قد يضمّ ملعب كرة قدم وملعبَي بادل: لو كانت على المكان لاستحال تسجيله إلّا
بتمزيقه إلى مكانين بنفس العنوان والهاتف.

**② `SPORTS[].ready` حُذفت — الحالة تُشتقّ من القاعدة.** كانت `{football:true, …:false}` مكتوبةً
في الكود، ففتحُ البادل كان يستلزم **نشر نسخة جديدة من التطبيق** بعد تسجيل ملاعبها، وبينهما
نافذةٌ يرى فيها اللاعب «قريبًا» والملاعب جاهزة (أو أسوأ: العكس). الآن `sportHasVenues()`:
رياضةٌ لها ملعب نشط واحد **هي** المفتوحة ⇒ **«إضافة مكان» في `/admin` هي الفعل الذي يفتح
الرياضة**، بضغطة وبلا نشر. والموقع مرآةٌ لنفس الصفوف وقت البناء (`$SportLive` في
`build-site.ps1`) ⇒ **قرار ٧ صار مضمونًا بالبناء لا بالانضباط**.

**③ `applySportScope()` — الرياضة تقصّ البيانات لا العرض.** `State.allPlaces` ما وصل من الخادم،
و`State.places` **مشتقّة** منها بملاعب الرياضة الحالية وحدها. البديل المرفوض: أن تفحص كل دالّة
عرض `State.sport` بنفسها — سبعة مواضع (`renderPlaces` · `placeAvailability` · `distinctSizes`
· `distinctTypes` · تبويبات المناطق · شريط الأرقام · التفاصيل)، ونسيانُ واحد يُظهر ملعب بادل
في تصفية كرة القدم بلا أن يصرخ شيء.

**④ المواصفات لكل رياضة** (`FIELD_SPECS` في [`app/src/app.js`](app/src/app.js)) — مفردات **مغلقة**،
ونسختها في `admin.html` مرآة. و**ما لا ترجمة له في اللغتين لا يُعرَض إطلاقًا** (درسُ المرافق:
«بأجرة» و«حتى 20 سيارة» دخلتا القاعدة فقرأهما مستخدم الإنجليزية بالعربية). و`size` **أرقام لا
كلمات** في كل رياضة (بادل `20×10` · سلة `28×15`) لأن التطبيق يعرضه حرفيًّا ويبني منه شرائح تصفية.
⚠️ **حارس الانحراف:** `node tools/check-spec-mirror.js` يقارن الجدولين ويتحقّق أن لكل خيار
تسميتين. (و`tools/check-i18n-parity.js` لمفاتيح `I18N`.)

**⑤ الإشعارات** ([`14_notifications.sql`](migration/14_notifications.sql) + `@capacitor/local-notifications`) —
جدول يكتبه **مُشغِّل داخل نفس معاملة الحجز**: طلبٌ جديد ⇒ للمُلّاك · تأكيد/رفض/إلغاء ⇒ للّاعب ·
نقل موعد ⇒ للمُلّاك. **ولا سياسة `insert` لأحد** ⇒ يستحيل تلفيق إشعار. و**النصّ لا يُخزَّن**:
الصفّ يحمل `kind` ومعطياته، والجملة تُكتب في الواجهة بلغة المستخدم **الحالية** — نصٌّ مخزَّن
يُجمَّد على لغة لحظة كتابته. ولا يُشعَر أحدٌ بفعلٍ فعله بنفسه (حجز أدخله المالك · إلغاء ألغاه
اللاعب). الإذن يُطلَب **بعد أوّل حجز** لا عند الإقلاع: أندرويد لا يعيد السؤال بعد الرفض.
⚠️ **ولا يوقظ هاتفًا مغلقًا** — إشعارات محلّية لا دفع؛ الصفّ باقٍ ويُقرأ عند أوّل فتح.

**⑥ لوح متابعة الحجز على الرئيسية** — السؤال بعد الحجز ليس «أين ألعب؟» بل «هل ردّوا؟ ومتى
موعدي؟»، وكان جوابه خلف نقرتين. حجزٌ واحد (الأقرب زمنًا) بحالته وعدّاده، والعدّاد من `hour`
لا من نصّ الوقت. ولا يُعرَض شيء بلا حجز (م5).

**⑦ الـAI: من Apps Script إلى دالّة حافّة** ([`supabase/functions/ai/index.ts`](supabase/functions/ai/index.ts)) —
**لم يكن بطيئًا، كان مستحيلًا**: `validateOwnerToken` تنتظر التوكن القديم بينما التطبيق يحمل
توكن Supabase ⇒ كل طلب يُردّ بـ«انتهت جلستك» مهما كانت طازجة؛ ولو مرّ لقرأ من Sheets التي
توقّفت عن استقبال الكتابة يوم انتقل الحجز إلى Postgres. عطلان مستقلّان، والواجهة تعرض عليهما
«جرّب بعد قليل» — وهي كذبة. الآن: الدالّة تتحقّق من JWT، **وتعيد فحص الملكية في القاعدة**
(لا تثق بـ`place_id` القادم في الطلب)، وتقرأ **بتوكن المستدعي** فلا تملك `service_role` أصلًا،
والمفتاح في أسرار Supabase. **ولا شيء يتعلّق ما دامت غير منشورة:** الطقس من Open-Meteo مباشرةً
(بلا مفتاح)، والتوصيات **محسوبة من أرقام المالك** وموسومة «بلا ذكاء اصطناعي» — تسميةُ الحساب
ذكاءً اصطناعيًّا تُفقد كل شارة في التطبيق معناها.

**⑧ `/admin` — نموذج إضافة مكان يغطّي كل عمود**: الرياضة (قائمة) · الأبعاد (تتبدّل بالرياضة) ·
المواصفات (تُبنى من الرياضة) · **الصور** (`image_url`، وتُفحَص `http(s)` بنفس شرط `fieldImages`
فيُقال المرفوض بدل أن يُحفَظ ثمّ يختفي صامتًا) · وبطاقات الأماكن تعرض رياضاتها.

⚠️ **أُصلح في الطريق (كلاهما ظهر في الفحص لا في القراءة):** ① `spec_court_single` كان
«فردي (20×6)» — أبعاد **البادل** مطبوعةً على ملعب **تنس**؛ المقاسات محلّها `size` وحده.
② «اليوم الأضعف» في التحليل المحسوب كان **يستحيل أن يرى اليوم الميّت**: يومٌ بلا حجز واحد
لا يدخل جدول العدّ فلا يُرشَّح — فتقول اللوحة «السبت أضعف أيامك بثلاثة» وفي البيانات ثلاثاءُ
صفرٌ تامّ، وهو اليوم الذي جاء المالك يسأل عنه. الأيام السبعة تُبذَر بصفر الآن.
③ لوح المتابعة والإشعارات لم يكونا يُعاد بناؤهما عند تبديل اللغة (نصّهما من معطيات لا من
`data-i18n`) ⇒ يبقيان بلغة سابقة إلى الجلبة التالية.

**`/admin` — قوائم مغلقة بدل الكتابة الحرّة في «إضافة مكان» (2026-07-31)** — التطبيق يبني
شرائح التصفية من **القيم المميّزة كما هي في القاعدة** (‏`distinctSizes`/`distinctTypes`)،
فحرفٌ واحد مختلف يصنع شريحتين — وفي القاعدة الآن `8x8` بإكس لاتينية بينما البقيّة `×`.
والـ`datalist` **يقترح ولا يمنع**. فصارت المدينة والمنطقة ونوع الأرضية والحجم **قوائم**،
ولكلٍّ خيار «أخرى…» يفتح حقلًا حرًّا ⇒ القيمة الجديدة **قرار** لا زلّة لوحة مفاتيح
(وحارسٌ يرفض «أخرى…» الفارغة). والمدن والمناطق **من `places` نفسها**، وقيمةٌ وحيدة في
القاعدة كلّها تُختار افتراضًا (لا بديل تخفيه).
**والمرافق أهمّها:** `amenityValueText` في التطبيق لا يعرف إلّا **أربع** قيم
(`Free`⇒«مجاناً» · `Paid`⇒«برسوم» · `Available`⇒«متوفرة» · والفارغ لا أيقونة له)، وما عداها
**يُعرَض حرفيًّا وبالعربية وحدها** — فيقرؤه مستخدم الإنجليزية عربيًّا، ويختلف مكانان في وصف
الخدمة نفسها. فصارت قائمةً بأربعة خيارات لا حقلًا حرًّا.

**`/admin` — أُعيدت على أسئلة صاحب العمل (2026-07-31)** — كانت خمسة تبويبات تعرض ما تعطيه
العروض المجمَّعة، فصارت **سبعة** مرتَّبة على السؤال لا على الجدول: **الملخّص · المال ·
الملاعب · العملاء · التشغيل · ➕ إضافة مكان · السجلّ** (بـARIA و`tabindex` متنقّل وتنقّل
بالأسهم، والمفتوح في `location.hash`).

**المفتاح: اللوحة صارت تقرأ `bookings` و`fields` خامًا** لا العروض وحدها. RLS يسمح للأدمن
بذلك أصلًا (`bookings_read` · `fields_read`) ⇒ **بلا ترحيل ولا عرض جديد**، والجلبة الواحدة
فتحت كل ما لم يكن يُحسَب: الإشغال · العملاء العائدون · ساعات الذروة · مهلة الحجز المسبق ·
أسباب الإلغاء · الإيراد الضائع.

- **الملخّص:** بطاقة صدارة = **دخلك (العمولة)** لا حجم المبيعات — كان الإيراد هو البطل
  والعمولة حاشية، والعكس هو الصحيح · ثمّ **«ماذا تقول الأرقام؟»**: ٦ جُمل مولَّدة كلٌّ منها
  من رقم مقيس ومعها ما يترتّب عليها (تركيز الاعتماد · المعلّق الذي فات موعده · حساسيّة
  الإشغال · العائدون)، وما تعجز عنه البيانات يُقال عجزه لا يُخمَّن (م5).
- **المال:** جدول **شهري** بالنموّ (والشهر «الجارٍ» و«القادم» بلا نموّ — مقارنة شهر ناقص
  بشهر كامل بلا معنى) · **«أين يتسرّب المال؟»** يفصل ثلاثة أرقام تُخلَط عادةً: ما ضاع فعلًا ·
  ما زال معلّقًا · والطاقة الفارغة **سقفًا نظريًّا مكتوبًا أنه ليس هدفًا** · **حساسيّة
  الإشغال**: كم دينارًا تساوي نقطةٌ مئوية واحدة.
- **الملاعب:** + أعمدة الملاعب والخانات/يوم والدخل · لوح **تركيز الاعتماد** (حصّة أعلى مكان،
  وأماكن بلا مبيعة واحدة).
- **العملاء:** العميل = **رقم هاتف** لا حساب مسجَّل · عائدون/جدد وحصّتهم من المبيعات ·
  توزّع الولاء · أفضل ١٠ · **معتادون انقطعوا** · ومعها بحث الحسابات وإيقافها.
- **التشغيل:** **قائمة عمل** بالمعلّق الأقدم أوّلًا مع الهاتف، ولافتتان: قادمٌ بلا ردّ >٢٤س،
  ومعلّقٌ فات موعده — **والمعلّق يحجز خانته** (قيد `bookings_no_double_idx` يشمل pending)
  فالطلب الذي لا يُردّ عليه يمنع غيره ثمّ يضيع · ساعات الذروة · مهلة الحجز · أسباب الإلغاء.

**بقي كما كان:** أربعة رسوم SVG بلا مكتبة · توقّعات بانحدار خطّي بمداها وطريقتها ·
**بثّ حيّ لسجلّ التدقيق** (عميل Phoenix مكتوب باليد + مؤشّر حالة صادق + استطلاع احتياطي +
تجديد جلسة). **تربط `site.css` نفسه** ⇒ كل تغيير في التوكنات يصلها تلقائيًّا، وحافّة الزجاج
على `.ad-bar` مثل `.hd`. لا صور ولا كتل عائمة فيها.

**➕ إدارة الأماكن (2026-07-31)** — تبويب واحد يجمع كل الكتابة على الأماكن:

**① الأماكن الموجودة** — بطاقة لكل مكان (والمخفيّ أوّلًا، فهو ما جئت من أجله):
- **إخفاء/إظهار** (`active`) — الفعل الافتراضي، قابل للرجوع، وأثره في التطبيق فوري.
- **حذف نهائي** — الزرّ **معطَّل مسبقًا** على مكان عليه حجوزات (`bookings.place_id` مرجعٌ
  بـ`on delete restrict` ⇒ القاعدة ترفضه)، **والخطأ 23503 مُمسَك أيضًا** لأن العدّ المعروض
  قد يتأخّر عن الواقع والقاعدة هي الحكم. والتأكيد **داخل البطاقة لا في نافذة المتصفّح**:
  `fields` و`reviews` و`place_owners` كلّها `on delete cascade` فتذهب صامتةً، والتأكيد
  **يعدّها بالاسم** قبل السؤال (ولهذا يُجلَب `reviews?select=place_id`).

**② ربط المالك — الحساب واحد و`role` هو الفارق.** لا يوجد «حساب لاعب» و«حساب مالك»
منفصلان: من يسجّل في التطبيق يُنشَأ `player`، و`sbLogin` عند «دخول المالك» يرفض كل دور
غير `owner`/`admin` (‏[`app/src/app.js:878`](app/src/app.js)) ⇒ **الربط وحده يترك صاحبه
أمام «الرقم أو كلمة السر غلط» بلا سبب مفهوم**. لذلك النموذج يربط **ويرفع الدور** (بعلامة
ظاهرة، فهو تغيير صلاحية)، ودورا `owner`/`admin` لا يُمَسّان. وفي تبويب «العملاء» زرّ
`player ⇄ owner` — **ولا يُرفَع أحد إلى `admin` ولا يُنزَّل عنه من الواجهة**: الأوّل يَهَب
اللوحة كلّها بضغطة، والثاني يقفل آخرَ أدمن خارج لوحته بلا طريق إلّا محرّر SQL.

**③ إضافة مكان جديد** — نموذج واحد ينشئ المكان **وملاعبه معًا**، لأن التطبيق
**يُسقط كل مكان بلا ملعب** (`filter(p => p.fields.length > 0)` في `sbGetInitialData`) ⇒ مكانٌ
بلا ملعب عملٌ ضائع لا يراه أحد، فآخر ملعب في النموذج لا يُحذَف. المرافق نصّ حرّ (الفارغ =
غير متوفّر) · الأوقات ثماني شرائح + ثلاث مجموعات جاهزة، **بنفس تسميات `SLOT_SETS`** في
`build_import.mjs` كي يفهمها `parseSlots` · المدن والمناطق **datalist من القاعدة** (الكتابة
الحرّة تصنع «عمّان» و«عمان» مدينتين، **وتصفية الموقع بالمنطقة**). حرّاس قبل أي كتابة: اسم
مكرّر · ملعب بلا وقت · منطقة فارغة. **وإن فشلت الملاعب حُذف المكان** فلا يبقى فارغًا.
ونتيجة العملية تُسرَد بندًا بندًا (نجح/فشل)، ومنها صراحةً: **ظاهر في التطبيق فورًا، وغير
ظاهر على الموقع حتى إعادة البناء** (‏`/places` مولَّدة وقت البناء).

⚠️ **قواعد التوقّع في اللوحة — امتداد م5:** لا توقّع قبل **7 أيام** فيها حجز فعلي (وإلّا
تُكتب العلّة صراحةً) · كل توقّع معه **مداه** (تشتّت البواقي، لا هامش أمان مضاف) **وطريقته** ·
`Math.max(0,…)` على كل قيمة فلا سالب · وخطّ التوقّع **متقطّع** فالشكل نفسه يقول «ليس قياسًا».

**الأمان** — تسريب `reviews.phone` أُغلق ومُتحقَّق حيًّا بالمفتاح العام: `select=phone` و`select=*`
و`order=phone.desc` و`phone=like.*` ⇒ **401**، والأعمدة العامّة ⇒ **200**.

**الدفعة الثانية عشرة — للخانة ساعة: مهلة إلغاء · مهلة ردّ · لم يحضر (2026-08-08)**

ثلاث قواعد تحمي **المورد الوحيد في هذا العمل**: خانةٌ تُباع مرّة وتموت في موعدها.
وكلّها في القاعدة أوّلًا والواجهة ثانيًا — الواجهة تُخفي ما سيُرفَض، والقاعدة هي الحكم.

**① مهلة إلغاء اللاعب — `CONFIG.CANCEL_WINDOW_H = 6`** ([`15_booking_expiry.sql`](migration/15_booking_expiry.sql)) —
كان `canCancel` يفحص الحالة وحدها، فيُلغي اللاعب قبل خمس دقائق من الصافرة وتموت الخانة بلا
مشترٍ. الآن الشرطان **مفصولان**: `cancellableByStatus()` (هل يصلح للإلغاء أصلًا؟) و
`withinCancelWindow()` (هل الوقت يسمح؟). ودمجُهما في راية واحدة كان يُخفي الزرَّ في الحالتين
**بلا كلمة**، فيبقى اللاعب يبحث عن زرٍّ حُذف تحت عينيه. بعد المهلة: لوحٌ يقول السبب ومعه
**رقم الملعب** (اتصال + واتساب) مقروءًا من `State.allPlaces` — `bookings_full` تحمل هاتف
**العميل** لا هاتف المكان. وقبل المهلة: القاعدة مكتوبةٌ **في جسم نافذة التأكيد نفسها** لا في
حاشية — من ضغط «تأكيد» لم يعد أمامه قرار تنفعه الحاشية.
⚠️ والفرض في القاعدة **مُشغِّل لا توسيع سياسة**: `with check` تعطي العميل «صفًّا فارغًا مع
200» — أسوأ ردّ ممكن (العملية «نجحت» ولم يحدث شيء). المُشغِّل يرفع `cancel_window_closed:<n>`
**ومعه رقم الخادم**، فالواجهة تعرض الرقم الذي رُفض به فعلًا لا الذي كانت تظنّه.
والمالك والأدمن خارج القيد عمدًا: الاتفاقية تُلزمهما بالإلغاء عند العطل القاهر مهما قرب الموعد.
والبند في [`docs/legal/venue-agreement-ar`](docs/legal/venue-agreement-ar.tex) مُلئ بالرقم نفسه.

**② مهلة ردّ المالك وانقضاؤها** — `bookings_no_double_idx` يشمل `pending` ⇒ **الطلب الذي لا
يُردّ عليه يمنع غيره ثمّ يضيع**، و`/admin` كانت تحمل لافتة عنه بينما المالك — الوحيد الذي
يستطيع الفعل — لا يرى عدّادًا. الآن عدّاد بثلاث حالات على كل بطاقة معلّقة، ونصّه من
`Intl.RelativeTimeFormat` (المعدود العربي مجّانًا). والموعد النهائي = **الأقرب** من
«الوصول + ٢٤س» و«بدء الخانة» — والثاني ليس تفصيلًا: طلبٌ لمباراة الليلة لا يملك حتى الغد
ليُردّ عليه، وبدونه يبقى معلّقًا **بعد** أن يمرّ موعده.
⚠️ **والترتيب صار بالمهلة لا بالساعة**: كان الأقدم يقع **تحت** طلبٍ وصل قبل دقيقة لأن مباراته
أبكر. والانقضاء `rejected` + **`cancel_kind='expired'`** لا حالة خامسة — فيبقى اشتقاق
«المعلّقة» في `/admin` (`bookings − confirmed − lost`) صحيحًا كما هو.

**③ لم يحضر** ([`16_no_show.sql`](migration/16_no_show.sql)) — بندٌ موقَّع بلا آليّة تنفّذه:
المادّة ٩ تقول إن الحجز يبقى مكتملًا والعمولة مستحقّة، والمنتج لا يملك وسيلة تسجيله.
مالك المكان أو الأدمن فقط · على `confirmed` فقط · **بعد انتهاء الخانة** فقط · وقابل للرجوع
(علامةٌ لا تُرفَع يتجنّبها صاحبها فيموت المقياس). والطوابع يكتبها الخادم — قيمةٌ قادمة في
الطلب تُتجاهَل. ونصّ التأكيد **لا يَعِد بتحصيل**: التطبيق لا يمسّ مالًا، والتحصيل بين المالك
واللاعب. **ولا سمعة للاعبين** — لا عدّاد على ملفّ، ولا رؤية للاعبٍ في سجلّ آخر.

**والتقارير صارت تفصل خمس حقائق** بدل ثلاث: مؤكّد · ملغى · مرفوض · **انقضت المهلة** ·
**لم يحضر**. والأخيرتان تظهران **فقط حين تُقاسان**: قبل الترحيلين لا عمود يقيسهما، وصفرٌ
لا مصدر له يُقرأ «لا يحدث هذا عندنا» وهو ادّعاء. واللوحة **تسمّي الترحيل الناقص** بدل
الصمت. ولم يُبنَ أي ترتيب «أكثر/أقلّ» على `no_show` — عمدًا: أيّ ترتيب كهذا يحتاج بذر
مجاله بصفر أوّلًا (درس «الصفر لا يُرشَّح»)، ولا داعي له قبل أن توجد بيانات.

**الدفعة الثالثة عشرة — سيطرة المالك: إغلاق · تسعير · تسجيل ذاتي (2026-08-08)**

**① إغلاق يوم (أو ساعات منه)** ([`17_closures.sql`](migration/17_closures.sql)) — كان المالك
يُغلق يوم صيانةٍ **بتزوير حجز وهمي في كل خانة**، وتلك الحجوزات تدخل إيراده وإشغاله ومتوسّط
سعره: يقرأ يومَ الصيانة يومًا ناجحًا، والمقياس يتلف. **والإغلاق ليس حجزًا** فلا يُخزَّن كحجز:
الحجز بيعُ خانة، والإغلاق سحبُها من العرض — و«كم بعتَ؟» لا يجوز أن يشمل ما لم يُعرَض.
**والقراءة عامّة** لأن اللاعب لا بدّ أن يرى **لماذا** اليوم مظلم: خانةٌ تختفي بلا سبب تُقرأ
عطلًا، ومكتوبٌ عليها «مغلق — صيانة» معلومةٌ تُحترَم. ولذلك للمغلق **حالة ثالثة** لا لونٌ ثانٍ
للمحجوز: `freeCountFor` تُرجع `'closed'`، والزرّ مخطَّطٌ مائلًا لا رماديًّا.
⚠️ **والنطاق تداخلٌ لا احتواء**: الخانة ساعتان، فإغلاق `[18,22)` يُغلق خانتَي 18 و20 ويترك 16
(تنتهي عند 18) و22. نفس الشرط حرفيًّا في `slotClosure` و`fn_closure_guard`.
⚠️ **وإغلاقٌ يبتلع حجزًا مؤكّدًا مرفوض**، ولا يُلغى نيابةً عن المالك — واللوحة تعرض المتعارضين
**بالاسم والوقت داخل النافذة** لا توستًا يختفي. و`owner_close_field` تُرجع القائمة مفصّلةً
بينما المُشغِّل يحرس REST المباشر: الأولى للاستعمال، والثاني للأمان.
⚠️ **وكل حساب طاقة يطرح المغلق**: `openSlotsFor` بدل `fieldSlots` في سبعة مواضع
(‏`isFieldAvailable` · `placeAvailability` · `soonestSlotTs` · `freeCountFor` · `findAlternative`
· إشغال اللوحة · أوقات اليوم). بلا ذلك تُقرأ جمعةُ الصيانة «إيرادًا ضائعًا» إلى الأبد.

**② التسعير بالساعة والموسم** ([`18_pricing_rules.sql`](migration/18_pricing_rules.sql)) — اللوحة
تقول «أكثر وقت طلب — مفيد للتسعير» منذ دفعات ولا تُمكِّن منه. الآن قواعد فوق السعر الأساسي،
و`fields.price` يبقى **رقم الدليل** (‏`/places` يعرض «من كم يبدأ» لا سعر ساعة).
**والحسم في دالّة واحدة** (`resolve_field_price`) ينادِيها الطرفان — لا منطق أسبقية في `app.js`
ولا في `admin.html`، فهذا بالضبط ما جعل `FIELD_SPECS` تنحرف حتى لزمها حارس مرآة.
والأسبقية **رقمٌ صريح** لا «الأكثر تحديدًا يفوز»: الثاني يصير مصادفةً عند تساوي نطاقين، ولا
يستطيع المالك أن يشرح لنفسه لماذا خرج هذا السعر.
⚠️ **والشبكة تُرجع المختلف وحده** (`place_price_grid`) — الغياب معناه محدَّد: «السعر الأساسي».
والسعر يظهر **على زرّ الوقت قبل النقر**، لأن «السعر قبل الطلب» وعدٌ على الموقع: سعرٌ يظهر أوّل
مرّة في نافذة المراجعة هو المفاجأة التي وُضع الوعد لمنعها.
⚠️ **ومُشغِّل يفرض السعر على إدراج اللاعب**، وبدونه كان كلُّ ما سبق زينةً: `bookings_insert_player`
تسمح للاعب بإدراج صفّه، والسياسة تحكم الصفوف لا الأعمدة ⇒ عميلٌ معدَّل يكتب `price:0`.
والمالك مستثنًى (حجزه اليدوي متّفَقٌ عليه خارج التطبيق). وإن اختلف ما كُتب عمّا عُرض، **يُقال**.

**③ تسجيل ذاتي للملاعب** ([`19_place_applications.sql`](migration/19_place_applications.sql)) —
نموذج على `/owners` يكتب مباشرةً بالمفتاح العام. **ولا سياسة `select` لغير الأدمن**: الصفوف
تجمع الاسمَ والرقمَ والمنشأة، وسياسةُ قراءةٍ واحدة هنا = تسريب قاعدة أرقام (أخطر من
`reviews.phone`). وحدّ المعدّل في **مُشغِّل** لا في `with check`: التعبير في السياسة يُنفَّذ
بصلاحية المستخدم، والجدول بلا سياسة قراءة ⇒ استعلامٌ فرعي داخلها لا يرى صفًّا فيمرّ الحدّ دائمًا.
والقوائم **مغلقة ومصدرها القاعدة** (‏`$PlaceModel` وقت البناء) لا نصّ حرّ، وإلّا صارت «عمّان»
و«عمان» مدينتين وانشقّت تصفية الموقع بالمنطقة. و**القبول في `/admin` لا يكتب مكانًا بطريق ثانٍ**:
يملأ نموذج «إضافة مكان» القائم فيمرّ بحرّاسه كلّها وبتراجعه، ويُعلَّم الطلب مقبولًا **بعد** أن
يوجد المكان لا عند الضغط. والرفض يُبقي الصفّ مع سببه — «قدّمنا ولم يردّ أحد» شكوى لا جواب لها
إن مُحي الصفّ. وصفحة الخصوصية تذكر ما يُجمَع، وبما هو صحيح فقط.
⚠️ **والتحسين التدريجي معكوس عمدًا**: النموذج **مخفيّ افتراضًا** وطريقُ الاتصال ظاهر، ثمّ
يقلبهما سكربتُ الصفحة نفسه. ولا يُعلَّق على `.has-js` لأنها تُزال حين يتأخّر `site.js` — وهو
ملفّ لا شأن للنموذج به، فكان سيُخفي نموذجًا يعمل.

**الدفعة الرابعة عشرة — الرئيسية تُفرَغ، والبدائل تُعرَض، والطلب يُقاس (2026-08-08)**

**① الرئيسية: أربعُ أدوات تصير واحدة (٣.١)** — **مقيس عند 375px، لوح المتابعة ظاهر:**
المسافة إلى أوّل بطاقة **٤٨٨ ⇐ ٤٤٧** بالعربية (−41px) و**٤٨٠ ⇐ ٤٤٠** بالإنجليزية (−40px).
مصدرها: الرأس ١٧٦⇐١٤٨ وعنوان القسم ٧٤⇐٦١.
• **دبّوس الموقع و«عمّان» المكتوبة حُذفا** — التطبيق لا يعرف أين المستخدم (لا إذن موقع ولا
اختيار مدينة)، فالأيقونة تدّعي معرفةً لا نملكها ويقرؤها من هو خارج عمّان تحديدًا خاطئًا. ولم
تُستبدل بمصفّي مدينة: المدن كلّها واحدة في القاعدة اليوم، ومصفٍّ لا يصفّي شيئًا م5.
• **«تحديث» حُذف** — السحب للتحديث موجود على الصفحة نفسها، والزرّ تكرارٌ في أزحم صفّ.
• **الترتيب ومبدّل العرض** انتقلا إلى ورقة الفلاتر (وفيها الترتيب أصلًا منذ البداية).
والصفّ الذي فرغ **هو الذي يدفع ثمن مبدّل «ملاعب/مباريات»** في الدفعة التالية.

**② بدائل خانةٍ بعينها (٣.٢)** — «البديل الذكي» لا يعمل إلّا حين يمتلئ اليوم كلّه، بينما
الواقع أن **الساعة** قليلة لا اليوم: من يريد الخميس ٨ مساءً يصطدم بزرٍّ ميّت. الآن نقرُ وقتٍ
محجوز يفتح لوحًا بأربعة بدائل مرتَّبة من الأقرب إلى ما طلب: ملعب آخر بنفس الساعة ⇐ أقرب ساعة
نفس اليوم ⇐ نفس الساعة أقرب يوم ⇐ مكان آخر في نفس المنطقة. ولا يُعرَض إلّا الموجود فعلًا.
⚠️ **والمحجوز يُنقَر والمغلق لا**: محجوزٌ يعني «سبقك غيرك» وله جواب، ومغلقٌ يعني «لا أحد
يلعب هنا» فلا بديل يُشتقّ منه. والزرّ القابل للنقر يحمل `aria-disabled` لا `disabled` —
الثاني يبتلع الحدث فلا يصل الأب أصلًا.

**③ قياس ما لم يحدث (٣.٣)** — كل ما تقيسه اللوحة اليوم هو النجاح. خمسة أحداث في قائمة
**مغلقة** مرآتها قيدٌ في القاعدة، ولوحُ «الطلب الذي لم يُلبَّ» في التشغيل: القمع بمقامه ونافذته
· أعلى بحثٍ فارغ (خريطة توسّع) · الخانات المطلوبة المحجوزة. ولا رقم هاتف ولا اسم ولا موقع،
و`profile_id` يكتبه الخادم. والإرسال دفعةً بلا انتظار ويُبتلع فشلُه: حجزٌ يتعثّر لأن سطر
تحليلات فشل ليس مقبولًا.

**④ `/admin` يفتح على «التشغيل» (٣.٤)** — الملخّص أثرٌ شهري، وسؤالُ كل زيارة «هل في شي
مستنّي؟». ومعه **شريط حالة فوق التبويبات** يُرى من كلّها: المعلّق · ما فات وقته · طلبات
الانضمام · **وصحّة الترحيلات ١٤ منها مقيسةً بسؤال القاعدة نفسها** (‏`limit=0` للجداول
والأعمدة، ونداءٌ فارغ للدوالّ يُقرأ رمزُ ردّه لا نتيجته). كان كل نموذج يقول نقصه بنفسه،
فصار الجواب في مكان واحد. والروابط العميقة السبعة تعمل كما هي.

**الدفعة الخامسة عشرة — المباريات المفتوحة (2026-08-09)**

عند الحجز يختار اللاعب **خاصّة** (الافتراضي، ولا شيء يتغيّر) أو **مفتوحة**: يقول كم لاعبًا
تحتاج المباراة وكم أحضر، والفرق يُنشر مقاعدَ يأخذها غيره. ([`22_open_games.sql`](migration/22_open_games.sql))

**والقيود الثلاثة هي التصميم كلّه:**

**① لا مقعد يُنشَر قبل أن يؤكّد الملعب.** الحجز **طلبٌ**، ونشرُ مقاعد في مباراة قد تُرفَض
بيعُ مكانٍ في شيء لا وجود له. والشرط **في العرض نفسه** (`status='confirmed'` داخل
`open_games`) لا في الواجهة — فلا يُنشر مقعدٌ ولو نادى أحدٌ الـREST مباشرةً. وبطاقة الحجز
المعلّق تقول ذلك حرفيًّا: «ما انعرض ولا مقعد بعد». وإن رُفض أو انقضت مهلته (15) فلم يُعرَض
مقعدٌ قطّ ولا أحد ليخيب ظنّه.

**② لا مال يمرّ من هنا.** لا بوّابة دفع أصلًا، فأقصى ما يُعرَض **حصّةٌ تقديرية** = سعر
الخانة ÷ العدد، موسومةً «تقديري» ومعها «تتدفع في الملعب لإدارته». ولا عمود دفع ولا رصيد ولا
دَين: تحصيلُ مالٍ لا نستطيع تحويله يجعلنا وسيطًا ماليًّا بلا أدوات وسيط.
⚠️ والحصّة من **لقطة** `bookings.price` لا من قاعدة التسعير الحيّة (18) — ما نُشر يبقى صحيحًا.

**③ لا رقم هاتف يعبر هذا الباب.** القراءة من **عرضين** لا من `bookings`: `open_games` لا
يحمل `customer_phone` ولا `player_id` ولا الاسم الكامل (‏`split_part` على أوّل مسافة)،
و`open_game_players` لا يراه إلّا المضيف ومن انضمّ. وما ليس مكتوبًا في العرض لا يخرج مهما
كان الاستعلام. ولا محادثة داخل التطبيق — التنسيق عند الملعب.

⚛️ **والتزامن يُحسم بقفل صفّ** (`for update`) لا بقراءةٍ ثمّ إدراج: لاعبان يضغطان آخر مقعد
في الثانية نفسها هو **الحالة العادية** عند التاسعة مساءً. والمقاعد **مشتقّة لا مخزَّنة**
(المطلوب − المُحضَر − عدد المنضمّين): عدّادٌ مخزَّن وجدولُ انضمامٍ يختلفان خلال أسبوع.

**والحوافّ الحادّة كلّها مُعالَجة صراحةً:** إلغاء المضيف يمرّ بمهلة ١.١ ونصُّه يسمّي عدد
المتأثّرين · إلغاء الملعب أو رفضه يُشعر المنضمّين (**الملعب لا يعرف أنهم موجودون** — التطبيق
وحده يستطيع) · الانسحاب يعيد المقعد فورًا · خفضُ العدد تحت من انضمّ مرفوضٌ **ومعه الرقم**
(«انضمّ ٢، وأقلّ عدد مسموح ٦») · والعودة إلى خاصّة ممنوعة بوجود منضمّين · و«لم يحضر» (١.٣)
تبقى على **الحجز** لا على المشاركين — سمعةُ لاعبين نظامٌ غير مصمَّم وخارج النطاق.

**وللمالك خبرٌ واحد جديد** (٤.٥): شارةٌ تقول إن هذا الحجز مباراة مفتوحة وعددها، **ومعها
سطرٌ يقول إن مسؤوليّته لم تتغيّر**: حجزٌ واحد، صاحبُ حجزٍ واحد، وهو الدافع. بلا ذلك السطر
يظنّ أنه صار مطالَبًا بتحصيل عشر حصص عند البوّابة.

⚠️ **و«المباراة تبدأ قريبًا» غير موجودة عمدًا**: لا cron في الخطّة المجانية فلا شيء يُطلقها
في وقتها، ونوعُ إشعارٍ لا يكتبه أحد شارةٌ كاذبة في القائمة. الأنواع الأربعة المكتوبة كلّها
مدفوعةٌ بحدث يقع فعلًا.

**والمُستبعَد عمدًا من هذه المرحلة:** المستوى والمركز («حارس مطلوب») · المحادثة · التكرار
الأسبوعي · الإلغاء التلقائي إن لم تكتمل · تقييم اللاعبين · قائمة انتظار للممتلئة. كلٌّ منها
يُدافَع عنه لاحقًا، ومجتمعةً تحوّل انضمامًا بضغطتين إلى خمس شاشات.

**الدفعة السادسة عشرة — النصّ يُختصَر، والشرط يُسحب من العلن، وثلاث ثغرات تُغلق (2026-08-09)**

**① النسبة لا تُطبع.** كانت مكتوبةً بالنصّ في **أربع تسميات معروضة** في
[`site/admin.html`](site/admin.html) (ثلاثة عناوين أعمدة · حاشية الجدول الشهري · بطاقة
الصدارة) وفي **شارة** على «ملخّص الإيراد» في لوحة المالك بالتطبيق. والوجهان **ملفّان ثابتان
يقرؤهما أي أحد**: `/admin` يُخدَم بلا مصادقة على الملفّ نفسه (الأمان في RLS بعد الدخول
لا في الملفّ)، والـAPK يُنزّله الجميع ويُفكّ بأمر واحد. التسميات صارت «دخلك» و«ملخّص
الإيراد» بلا شارة، والأرقام تُحسَب كما كانت.
🔴 **وما بقي يُقال صراحةً لا يُدّعى إخفاؤه.** النسبة **ما زالت مقروءة في ثلاثة مواضع**،
وكلّها ثوابت يلزمها الحساب: `RATE` في `admin.html` · `CONFIG.COMMISSION` في `app.js` ·
والضرب في `admin_daily` داخل [`01_schema.sql`](migration/01_schema.sql). **والمستودع عامّ**
⇒ من يفتح GitHub يجدها بلا مجهود. ما تغيّر أنها لم تعد **معروضةً** على شاشةٍ تُصوَّر
وتُشارَك. وإخفاؤها فعلًا يحتاج قرارًا من المالك بين ثلاثة: **① مستودع خاصّ** ·
**② نقل «ربح المنصّة/صافيك» إلى عرضٍ في القاعدة** فيُحذف الثابتان من العميلين (ترحيل
جديد) · **③ حذف البطاقتين من لوحة المالك** (أرخصها، وثمنه أن المالك يفقد صافيه المحسوب).

**⑤ 🔴 «المباريات» كانت تختفي عن اللاعب طوال الجلسة — والسبب ليس الترحيل.**
`GAMES_OK` يُسأل عنه في `sbProbeGames`، ولا يُنادى إلّا من `updateModeSeg`، ولا تُنادى
إلّا من `loadData`. و`playerLogin`/`playerRegister` تنادِيان `loadData` **بشرط**
`if(!State.places.length)`. فالمسار الذي يعرضه التطبيق نفسه — **«تصفّح بدون حساب» ⇒ اختر
وقتًا ⇒ سجّل دخولك ⇒ استئناف الحجز** — يُحمِّل الأماكن **وأنت ضيف** (فلا توكن ⇒ لا سؤال)،
ثمّ يتخطّى `loadData` بعد الدخول لأنها محمَّلة ⇒ **`GAMES_OK` يبقى `null` بقيّة الجلسة**:
لا مبدّل «مباريات» ولا خيار «مباراة مفتوحة» في نافذة الحجز. **والميزة سليمة على الخادم
تمامًا** — لذلك بدت «ناقصة ترحيلًا» وهي ليست كذلك.
العلاج موضعان: `await updateModeSeg()` بعد الدخول والتسجيل مباشرةً، **و**سؤالٌ متأخّر عند
فتح نافذة الحجز إن كان العلَم `null` يكشف الخيار حين يصل الجواب. والاتجاه **مخفيّ ⇒ ظاهر**
وحده؛ العكس بابٌ يُعرَض ثمّ يُسحَب.
⚠️ **والدرس أعمّ من هذا العطل:** كلّ علَمٍ يُسأل عنه **مرّةً بشرط وجود جلسة** يحتاج سؤالًا
عند **كل** لحظة تتغيّر فيها الجلسة — لا عند تحميل البيانات وحده. الجلسة تتغيّر بعد التحميل
الأوّل في كل مسار «ضيفٌ يصير مستخدمًا».

**② نصّ الموقع اختُصر ٢٠ مفتاحًا** (‏`privUsageBody` ٩٣٣⇐٤٣٠ · `privJoinBody` ٨٢١⇐٣٥٠ ·
`faqA7` ٥٦٧⇐٢٣٠ · `sportsLead` ٤٦٥⇐٢٣٠ …) **بلا حذف حقيقةٍ مُفصَح عنها**: صفحتا الخصوصية
والشروط تصفان ما يفعله الكود، فالاختصار حذفُ حشوٍ لا حذفُ بند. وفي التطبيق: أسطر ورقة
الانضمام الأربعة وحوارات «حذف الحساب» و«لم يحضر».
⚠️ **وأُصلح تناقضٌ كان قائمًا:** `ownersS4V` تقول «ولا حصّة نقتطعها من سعرك» بينما
`ownCostBody` تقول إن هناك عمولة — والأولى تُقرأ «لا عمولة». الصحيح أن اللاعب يدفع سعرك
كاملًا في الملعب ولا نمسّه، وهو ما تقوله الآن. **وحُذف من `ownCostBody` وصفُ النموذج**
(«ولا شيء على حجوزاتك المباشرة») — جملةٌ تُقرأ خريطةَ التفافٍ على العمولة قبل أن تُقرأ ميزة.

**③ ثلاث ثغرات في المباريات المفتوحة** ([`22_open_games.sql`](migration/22_open_games.sql)،
ولم يُشغَّل بعد فالتعديل فيه مجّاني):
- 🔴 **حسابٌ موقوف كان ينضمّ.** `profiles.active=false` هو **كل** ما يفعله «إيقاف الحساب»
  في `/admin` و«حذف الحساب» في التطبيق، **ولا سياسة واحدة في القاعدة تقرؤه** ⇒ الموقوف
  يحجز مقاعد الناس ولا يحضر، وهو سبب إيقافه. `join_open_game` تفحصه الآن (`inactive`).
- 🔴 **مقعدان في الساعة الواحدة.** لا شيء كان يمنع حسابًا من أخذ مقعد في **كل** مباراة
  عند التاسعة مساءً — فتظهر كلّها ممتلئة ولا يحضر أيًّا منها. أرخصُ تخريبٍ ممكن هنا لأنه
  بلا مالٍ ولا سمعة (ولا نظام سمعة أصلًا). القيد صحيحٌ في ذاته: لا أحد يلعب مباراتين معًا (`clash`).
- 🔴 **`set_open_game` كانت تقبل حجزًا ملغى أو مرفوضًا**: لا يُنشر مقعد (العرض يشترط
  `confirmed`) لكن يُكتب `visibility='open'` وعددان على صفٍّ ميّت يقرؤه كل تقرير.

**④ والمالك يرى «مباراة مفتوحة» قبل أن يقبل لا بعده.** كانت الشارة مشروطة بـ`confirmed`
⇒ يضغط «قبول» وهو **لا يعرف** أن الطلب مباراة يأتيها حتى `players_needed` لاعبًا، فيكتشف
العدد بعد أن يصير التزامًا. والمعلومة التي تُغيّر قرارًا مكانها **قبله**. ونصّ الشارة صار
«حتى {n} من اللاعبين» — العدد الذي يعني المالك واحد: كم إنسانًا يدخل ملعبه؛ وتفصيلُ
المقاعد شأن المضيف. ولون المعلّقة `--warning-ink` (مقيس ٥٫٨٣ نهاري · ٨٫٢٦ ليلي).
⚠️ **وأُصلح رقمٌ كاذب على بطاقة المضيف:** `gmCardLive` كانت تقول «باقي مقاعد: {n}»
و`{n}` هي `المطلوب − المُحضَر` — **المنشور لا الباقي**، لا تطرح من انضمّ. مضيفٌ نشر ٧
وانضمّ ٧ كان يقرأ «باقي ٧». النصّ صار «نشرتَ {n} مقعد» وهو ما يحسبه بالضبط، والباقي
الحيّ في «إدارة المباراة» (تجلب المنضمّين فتعرفه).

---

**الدفعة السابعة عشرة — مراجعة خارجية: تصعيد صلاحيات، وطبقة اختبار لم تكن (2026-08-09)**

راجع مدقّقٌ خارجي البندل كاملًا وأخرج ٢٣ بندًا. **فُحص كلٌّ منها بقراءة الملفّ لا
بتصديق التقرير**: ٢٢ صحيحة، وواحدة خاطئة (‏`privAiBody` **يذكر** إرسال نصوص
التقييمات إلى Gemini فعلًا، وبدقّة تطابق `index.ts`). وثلاث صياغات صُحّحت — أهمّها
أنّ تزوير «انقضت المهلة» يخدع `/admin` وحدها لا «كل تقرير»: `isExpiredBooking` في
التطبيق يشترط `rejected` معها.

**① القاعدة — الحرّاس الستّة أعلاه** (‏[`24`](migration/24_integrity_guards.sql)).
والمبدأ الواحد الذي يحكم الملفّ: **لا يُوسَّع أيّ `with check`.** تعبيرٌ في سياسة
يردّ الخرق «صفًّا فارغًا مع 200» فلا يملك العميل رمزًا يقرؤه؛ فكلّ حارسٍ إمّا مُشغِّل
يرفع اسمًا (‏`booking_date_past` · `slot_closed` …) وإمّا إسنادٌ صامت للقديم حين لا
يكون للمستخدم قرارٌ أصلًا. وحارس الإلغاء **قائمة بيضاء مبنيّة من الصفّ نفسه**
(‏`jsonb_populate_record`) لا قائمة أعمدة مكتوبة بيد — تلك تنسى أوّل عمودٍ يضيفه
ترحيلٌ قادم، وتعمل هذه على قاعدةٍ شُغِّل عليها `16`/`22` وعلى قاعدةٍ لم يُشغَّلا عليها
بلا فرعٍ واحد.
⚠️ **والقيد على `profiles.active` اتّجاهيّ لا مطلق**: «حذف الحساب» في التطبيق هو
`PATCH {active:false}` حرفيًّا، فتجميد العمود كان سيكسر المسار الوحيد لإغلاق الحساب.
المسموح `true ⇒ false`، والممنوع العكس.

**② الأمانة في الواجهة — ٢٢ رسالة كانت تصل مستخدم الإنجليزية بالعربية.**
(«الرقم أو كلمة السر غلط» · «انتهت جلستك» · «تم حفظ الحجز» · «شكراً، تقييمك وصل» …)
⚠️ **و`check-i18n-parity.js` كان يمرّ ٩٣٩/٩٣٩ وهو محقّ**: هذه النصوص لا تمرّ بـ`I18N`
أصلًا، بل تُكتب حرفيًّا في `message:` وتُترجَم بـ`API_MESSAGE_MAP`. **اختبارٌ يمرّ دائمًا
أخطر من غياب اختبار — يشتري ثقةً بلا تغطية**، والعمى كان في النطاق لا في التأكيد.
الحارس الجديد `tools/check-api-messages.js` يقارن الخريطة بكل `message:'…'` عربية.
ورموز حرّاس القاعدة تُترجَم من **`I18N` لا من الخريطة**: هي أسماء آلية لا عربية،
فالجملة تُكتب بلغة المستخدم الحالية — نفس مبدأ `14` و`15`.

**③ الهويّة — حسابان لشخص واحد.** `validPhone` كانت «تسعة أرقام فأكثر»
و`normalizePhone` لا تعرف `790123456` (بلا صفر، وهي صيغة شائعة) ⇒ تمرّ بلا لمسة
فيُشتقّ لها بريدُ دخولٍ مستقلّ. **والتحقّق والتوحيد وجهان لقاعدة واحدة**: الأوّل
يفحص **الناتج الموحَّد** الآن (`^9627[789]\d{7}$`)، و`sbPhone` — التي يُشتقّ منها
البريد ويُكتب بها `customer_phone` — صارت نداءً لـ`normalizePhone` بدل نسخةٍ ثانية.
⚠️ و`venuePhone` **لا** تُفحَص بها: رقم الملعب قد يكون أرضيًّا، والسؤال هناك «هل
يُطلَب؟» لا «هل هو حساب؟».

**④ الروابط — `map_link` بلا فحص مخطَّط** بينما `fieldImages` تفرض `http(s)` منذ
دفعات، والالتزام مكتوب في هذا الملفّ. و`href` ليس أهون من `src` داخل WebView كابستور
ومعه جسر أصلي. الآن `isHttpUrl` واحدة للاثنين، ونفس الشرط في `build-site.ps1`
وحارسٌ في نموذج «إضافة مكان» (‏`type="url"` **يقترح ولا يمنع**: المتصفّح يقبل
`javascript:` عنوانًا صالحًا).

**⑤ `esc()` في `/admin` لم تكن تهرّب `"`** — تسلسلُ عقدةٍ نصّية يهرّب `& < >` وحدها،
وهي مستعملة في عشرة مواضع **داخل سمات**. والسلسلة ليست نظرية: `place_applications`
يكتبه anon، ثمّ يقبل الأدمن الطلب فيملأ منه اسمَ المكان ⇒ XSS مخزَّن **بجلسة أدمن**.

**⑥ دالّة الحافّة: `withDeadline` لم تكن تُلغي شيئًا.** الـ`signal` لا يصل أي `fetch`،
والوعد مُقيَّم قبل الدخول ⇒ `ctl.abort()` يطلق في الفراغ، **وتعليق رأس الملفّ يقول
"It never hangs"**. صارت تأخذ مصنعًا لا وعدًا. و`place_id` يُفحَص uuid قبل أن يُحشَر
في مسار PostgREST. ⚠️ **وتعليقٌ يصف نيّةً لا سلوكًا أخطر من غياب التعليق** — القارئ
التالي يدقّق النثر بدل الكود.

**⑦ الموقع: ملفّان ميّتان كانا يَعِدان بحماية غير موجودة.** `site/static/_headers`
و`_redirects` بصيغة Cloudflare على استضافة Vercel ⇒ لا يقرؤهما أحد، **وفيهما HSTS
و`/ar/* → /:splat` وليسا في `vercel.json`**. حُذفا، ونُقل ما فيهما إلى المصدر النافذ.
والقاعدة: ملفّان يصفان العقد نفسه يختلفان يومًا، **والذي لا يُنفَّذ هو الذي يكذب**.

**⑧ أندرويد: `allowBackup="true"`** — والتطبيق يحفظ **جلسة Supabase كاملة** (‏`at`+`rt`)
في `localStorage` داخل مجلّد الـWebView ⇒ تدخل نسخة Drive وتُستخرَج بـ`adb backup`:
توكن تحديثٍ في نسخة سحابية = جلسة قابلة للاستعادة على جهاز آخر بلا كلمة سرّ. صار `false`.

**⑨ لا مسار تحديث، والمخطّط يتحرّك تحته.** `versionCode 1` بلا متجر يدفع تحديثًا.
الآن `min_app_version` في `booking_rules` (‏[`27`](migration/27_min_app_version.sql))
يقرؤه التطبيق **بلا توكن** (‏`br_read using(true)` ⇒ حتى الضيف يعرف)، ويعرض شريطًا
**ناصحًا لا حاجبًا** يشير إلى `/download/`. وإغلاق تطبيقٍ يُوزَّع يدويًّا عقابٌ على
عطلٍ ليس عطل صاحبه؛ و`block_app_version` هي الخطّ الأحمر وتبقى صفرًا حتى يوجد سبب.

**⑩ الخطوط صارت محزومة** (‏`tools/fetch-fonts.mjs` ⇒ ٢٦٨KB · ٢٩ وجهًا في ١٤ ملفًّا).
كانت من `fonts.googleapis.com` **داخل تطبيق أصلي**: أوّل إقلاع بلا شبكة = خطّ النظام،
وIP كل مستخدم يصل طرفًا ثالثًا، **وهو جذر مزلق `document.fonts.ready`** المسجَّل.
والتقليل بالتجزئة لا بالاسم: الخطوط المتغيّرة تُعيد **نفس البايتات** لكل وزن، فالتسمية
بالوزن نزّلت Montserrat أربعًا وInter خمسًا (٧٨٤ ⇐ ٢٦٨KB).

**⑪ الوصولية: ٦١ حقلًا، ٢٧ منها بلا اسم منطوق** — ومعظمها له `<label class="form-label">`
**مرئية وغير مرتبطة**، والربط سمةٌ واحدة. صفرٌ الآن (مقيس في المتصفّح بـ`el.labels`).
والمفارقة أن الملفّ يقيس التباين إلى منزلتين عشريتين ويترك أرخص فوزٍ في الوصولية.

**⑫ وطبقتا اختبار حيث لم يكن `npm test` إلّا `exit 1`:**
- `tools/test-pure.mjs` — ٧٣ تأكيدًا على الدوالّ النقيّة، **تُقرأ من `app.js` نفسه
  وتُقيَّم معزولةً** لا تُنسَخ: النسخة تنحرف، والمنحرفة تمرّ بينما الإنتاج يكسر.
- `tools/security-matrix.mjs` — كل حارسٍ يعتمد عليه المشروع، **بتوكن لاعب عادي**،
  والتأكيد على **النتيجة لا على رمز الحالة** (‏PostgREST يردّ «200 وصفًّا فارغًا»).
- `tools/check-mirrors.js` — الثوابت المكرّرة بالضرورة: العمولة في ثلاثة مواضع
  (وأخطرها الضربُ داخل عرض `admin_daily`: تغييرها يستلزم `create or replace view`،
  ومن ينسى يحصل على لوحةٍ تقول رقمين للربح نفسه بلا خطأ) · `versionCode` · المهلتان ·
  تسمية العاشرة.

⚠️ **وثلاثة أعطال لم يذكرها التقرير، كشفها العملُ نفسه:**
① **`slotsToKeyword` كانت تقرأ الملعب المسائي «كامل اليوم»** — الشرط `s.includes('22=10:00')`
يصدق على المسائي وهو مفحوصٌ أوّلًا. **والعطل يكتب لا يعرض فقط**: زرّ إظهار/إخفاء الملعب
يعيد إرسال `slots: slotsToKeyword(f.slots)` ⇒ ضغطةٌ واحدة تمنح الملعبَ المسائيَّ خمسَ
خاناتٍ صباحية لم يعرضها صاحبه قطّ. الحسم الآن **بمجموعة الساعات** لا بمطابقة نصّية،
وكشفه `test-pure.mjs` لا القراءة.
② **`url()` داخل ورقة أنماط تُحسَب من موضع الورقة لا من المستند** ⇒ `assets/fonts/assets/fonts/…`
وثلاثة عشر 404 صامتًا **والصفحة تُرسَم** بخطّ احتياطي بلا خطأ واحد. كشفته المعاينة لا الكود.
③ **`countNoun(0)` كانت تعطي «0 ملعبًا»** والصواب الجمع: «0 ملاعب».

**والتسمية الملتبسة** (‏`10:00 - 12:00 م` لخانتَي العاشرة صباحًا وليلًا) أُصلحت في
مصادرها الأربعة ومعها [`26`](migration/26_slot_label_fix.sql) للبيانات القائمة —
**والحجوزات الماضية لا تُمَسّ**: `time_label` لقطةٌ كـ`price`، وتعديلها بأثر رجعي يجعل
السجلّ يقول ما لم يُعرَض. ⚠️ و`enSlotLabel` كانت صحيحةً في الإنجليزية منذ البداية،
**فاللغة الأساسية وحدها كانت المعطوبة** — ومن فحص الثانية مرّرها.
---

**الدفعة الثامنة عشرة — ثلاثة تعديلات بصرية، اثنان منها عطلٌ بنيوي (2026-08-09)**

**① لوح «حجزك القادم»: الوزنان مقلوبان.** الدفعة 11 بنت هذا اللوح على جملة صريحة
(«السؤال بعد الحجز ليس *أين ألعب؟* بل *هل ردّوا؟ ومتى موعدي؟*»)، ثمّ جعل التنسيقُ
**اسمَ الملعب أكبر عنصر فيه** (‏15.5px · 800 · `--ink`) والموعدَ سطرًا باهتًا تحته
(‏13px · `--muted`) — أي أن البطاقة تجيب بأعلى صوتها عن السؤال الذي قال تصميمُها
نفسُه إنه ليس السؤال. الآن **الموعد عنوان** (‏19px · 900 · `--ink` · `tabular-nums`
كي لا يرتجف السطر مع عدّاد الدقيقة تحته) **والمكان حاشية** (‏13px · 600 · `--muted`).
والترتيب في الـDOM قُلب معه لا التنسيق وحده: قارئ الشاشة يقرأ ما يراه المبصر بالترتيب
نفسه. مقيس في الثيمين: كل سطر ≥ عتبته (نهاري 15.59 · 6.21 · 11.97 · 9.76 · 6.21).

**② شاشة الترحيب: النصّ كان يقع تحت زرَّي «لاعب» و«صاحب ملعب» — والسبب رقمان لا
يعرفان بعضهما.** ارتفاعُ اللوح فوق الهيرو مكتوب مرّتين (‏`margin-top:-20px` ثمّ
`-26px` تعلوها بترتيب المصدر) وحاجزُ الهيرو ثلاث مرّات (‏34 · 40 · و**30** داخل
`@media(max-height:760px)`) ⇒ عند أي شاشة ≤760px يخرج الحاصل **٤ بكسلات** بين آخر
سطر وحافّة اللوح. وأي زيادة — تكبير الخطّ في أندرويد، سطرٌ ثالث في الوصف العربي —
تدفع النصّ تحت الأزرار. الآن الارتفاع **متغيّر واحد** (`--onb-lift`) والحاجز
**مشتقٌّ منه** (`--onb-clear`)، والضغطُ على الشاشات القصيرة يقع على الارتفاع لا على
الحاجز. مقيس عند 640 · 720 · 844 وفي الاتجاهين وعند خطّ 130%: الفسحة تساوي
`--onb-clear` بالضبط دائمًا (12 قصيرة · 18 طويلة)، وصفر تقاطع مع أي زرّ.
⚠️ **والدرس هو نفسه المسجَّل في حرّاس المرآة**: رقمان يجب أن يتّفقا مكتوبان في خمسة
مواضع سيختلفان — والحلّ اشتقاقُ أحدهما من الآخر لا ضبطُهما بيد.

**③ مبدّل شكل البطاقات عاد إلى صفّ العنوان.** الدفعة 14 نقلته إلى ورقة الفلاتر
لإفراغ الصفّ الأزحم، والثمن كان أكبر ممّا بدا: **تفضيلُ عرضٍ يُبدَّل بضغطة صار خلف
نقرتين وداخل ورقة**، فلم يُعثَر عليه (بلاغ المالك حرفيًّا: «بطّلت أقدر أغيّره»).
وهو ليس مرشِّحًا — لا يغيّر النتائج ولا يُعدّ في «تطبيق (n)» — فمكانه فوق البطاقات
التي يغيّرها. وأيقونتان لا كلمتان: مقيس عند 360px في الاتجاهين، الصفّ **٦١ بلا
المبدّل و٦٤ معه** — ثلاثة بكسلات، فلا يسترد شيئًا يُذكَر ممّا حرّرته الدفعة 14 (‏41px).
والنسخة داخل ورقة الفلاتر **تبقى**: `updateViewToggle` تزامن كل `.vt-btn` من
`State.view`، فالمصدر واحد ولا انحراف.

⚠️ **وقياسان كادا يكذبان في الطريق:** ① قراءة ألوان اللوح مباشرةً بعد قلب الثيم
أعطت «‏2.05:1 راسب» على العدّاد — وهي قيمةُ **منتصف انتقال** الثيم لا القيمة
النهائية (المزلق مسجَّل في اللقطات، وينطبق على `getComputedStyle` كذلك). بإضافة
`*{transition:none}` قبل القياس خرجت **9.76 ناجحة**. ② محاكاة ارتفاع الشاشة بحقن
`min-height` على الحاوية **لا تُطلق `@media(max-height)`** فبقيت المتغيّرات على فرع
واحد وقرأتُ رقمًا واحدًا لكل الارتفاعات؛ القياس الصحيح بتغيير النافذة فعلًا.

---

**الدفعة التاسعة عشرة — التنقّل يصير تنقّلًا، والانتظار يصير رقمًا (2026-08-10)**

**① الشريط السفلي: للّاعب أربعة، وللمالك أربعة بدل واحد.**
`favGet/favToggle/favOnly` مبنيّة بالكامل وقلبٌ على كل بطاقة، ومدخلها الوحيد
**شريحةٌ بقلب داخل مسار انزلاق أفقي بين أسماء المناطق** ⇒ من ضغط قلبًا لا يجد
أين تذهب. صارت زرًّا رابعًا و**مسارًا** (`favorites`) لا علَمًا: `showPage`
يفصل `navKey` عن `name` — الأوّل يضيء الزرّ والثاني يختار `#page-*` —
و`home` يفرض `favOnly=false`. **وشريحة القلب حُذفت**: بابان لميزةٍ واحدة
انحرافٌ مؤجَّل. ومعها سقطت شريحة «المفضّلة» من شرائح التصفية: شريحةٌ تزيلها
تترك الحبّة على الزرّ وتحته كلّ الملاعب.
و`#nav-owner` كان **زرًّا واحدًا وحبّةً لا تنزلق إلى شيء**، يأكل ~64px من كل
شاشة يراها المالك ويقول «أنت هنا» وهو المكان الوحيد الممكن — بينما تنقّله
الحقيقي **خمسة تبويبات تنزلق أفقيًّا** أعلى الصفحة. الآن التبويبات الأربعة
(اليوم · الحجوزات · التقويم · الملاعب) في الشريط، و`showOwnerTab` تبقى
**الدالّة الوحيدة** التي تُحدّث الحالة (والأسهم وARIA انتقلت إليها).
و«التقارير» **لم تُدفَن**: بطاقة بسهم في نهاية «اليوم» — التبويبات أسئلةٌ
يوميّة والتقارير أثرٌ شهري. وسقط Sidebar الكمبيوتر: أداة تنقّل واحدة لا
اثنتان تتفرّعان بالعرض.
⚠️ **والصفّ صار عموديًّا (أيقونة فوق نصّ) لأن العدد تغيّر لا لأن الذوق تغيّر** —
انظر المزلق أدناه. **مقيس عند 375px**: الحبّة تطابق الزرّ النشط بالبكسل
(dx=dy=dw=dh=0) في RTL وLTR وعبر ثلاث تبديلات لغة، وصفر تجاوز أفقي في
الشريطين، والنصّ ظاهر دائمًا بـ`--muted`.

**② صفحة الملعب: مقياس خطوات واحد، والمعلومة التي تصنع القرار قبله.**
كانت الصفحة تعدّ **1·2·3** (ملعب · يوم · وقت) ثمّ تفتح نافذةً تعدّ **1·2·3**
أخرى (ملعب · موعد · تأكيد): من أنهى «الخطوة ٣» يقرأ أنه في «الخطوة ٣»، ورقم ٢
يعني «اليوم» في شاشة و«اليوم+الوقت» في الأخرى. صار المقياس **أربع خطوات تمتدّ
عبر الشاشتين** بتسميات عناوين الصفحة نفسها (`stepTime` مفتاح جديد).
**والتبويبات أربعة صارت اثنين**: «احجز» يحمل المسار كلّه، و«عن الملعب» يجمع
الثلاثة التي لا يزورها أحد وهو في منتصف اختيار الساعة. وما كان محبوسًا خلفها
صعد إلى حيث يُسأل عنه: **صفّ مرافق مضغوط** (ثلاثة و«+n» يفتح «عن الملعب») ·
**سطر تقييم بجانب الاسم** قابل للنقر (ومكانٌ بلا تقييم **لا يعرض شيئًا** — لا
«0 (0)») · **وزرّا الاتصال والخريطة ظاهران دائمًا** قرب الرأس، بحارس
`isHttpUrl` كما هو. وصار `.detail-actions` فليكس لا شبكةَ عمودين: أحد الزرّين
يختفي كثيرًا، وفي الشبكة يبقى الباقي بنصف العرض وبجواره فراغٌ يُقرأ عطلًا.

**③ «عادةً يردّ خلال ن» — أكبر قلقٍ في المنتج، وأوّل رقمٍ يجيب عنه**
([`28_reply_speed.sql`](migration/28_reply_speed.sql)) — الحجز **طلبٌ ينتظر
ردًّا**، وعندنا لوح متابعة وعدّاد وإشعار وكلّها تقول «ننتظر» ولا شيء يقول
**كم**. والرقم في البيانات أصلًا: الزمن بين الوصول وأوّل ردّ ⇒ **قياسٌ لا
اختراع**، يمرّ من م5 بشرط ألّا يُعرَض قبل أن يوجد.
عمودٌ `replied_at` يكتبه **مُشغِّل** عند أوّل خروج من `pending`، وعرضٌ عامّ
`place_reply_speed` بثلاثة أعمدة لا غير (‏`place_id` · `median_minutes` · `n`)،
**وعتبة `n>=7` داخل العرض نفسه** لا في الواجهة: ما لا يُقاس لا يخرج من القاعدة.
والنافذة 90 يومًا (رقمٌ من العام الماضي لا يصف الملعب اليوم)، والوسيط لا
المتوسّط (ردٌّ متأخّر ثلاثة أيام يسحب المتوسّط وحده).
🔴 **والانقضاء التلقائي مستثنًى في المُشغِّل وفي العرض معًا** — انظر المزلق أدناه.
🔴 **وإلغاء اللاعب نفسه كذلك**: من سحب طلبه لم يتلقَّ ردًّا.
وفي التطبيق: علَمٌ **ثلاثيّ الحالات** بنفس نمط `GAMES_OK` بالحرف، وجلبةٌ داخل
دفعة `place_stats` لا جلبة جديدة، وموضعان اثنان (بطاقة الملعب · **فوق زرّ
التأكيد** حيث يُتّخذ القرار). و`replySpeedText(minutes, lang)` **دالّة نقيّة
تأخذ اللغة وسيطًا** كي تُختبَر معزولةً — ومعها ٢٠ تأكيدًا جديدًا في
`tools/test-pure.mjs` (‏53 ⇐ 73). **مقيس قبل التشغيل**: سؤالٌ واحد يردّ 404،
فيُنزَّل العلَم ولا يُسأل ثانيةً، ولا سطر ولا رسالة ولا فراغ.
✅ **وشُغِّل الترحيل 2026-08-10 ومُتحقَّق حيًّا بالمفتاح العام** (الفحوص الستّة
في ذيل الملفّ). والعرض يردّ `[]` اليوم لأن لا مكان بلغ سبعة ردود بعد ⇒ لا سطر
يُعرَض، وهي الحالة الصادقة: **الرقم يظهر مكانًا بعد مكان كلّما تراكمت الردود**،
ولا يُعرَض لأحدٍ قبل أن يُقاس.

**④ «حسابي»: بطاقة كلمة السرّ تُطوى** — ثلاثة حقول وثلاثة أزرار عين وشرحٌ من
سطرين، لفعلٍ يقع مرّة في السنة، تدفع ما بعدها تحت الطيّة. **مقيس: 467 ⇐ 94px.**
والعلامة الافتراضية لـ`<summary>` مثلّثٌ لا ينقلب مع RTL في كل محرّك، فالبديل
سهمٌ عموديّ ينزل عند الفتح — لا اتجاه له في اللغتين.

**⑤ `/admin` على الهاتف: العمود الأوّل يثبت** — جدول «الملاعب» اثنا عشر عمودًا
و«العملاء» و«التشغيل» سبعة، وكلّها تُقرأ بالانزلاق الأفقي **فيختفي الاسم عند
أوّل تمريرة وتصير الأرقام بلا هويّة** — والوصول اليومي من مفضّلة المتصفّح، أي
من الهاتف غالبًا. قاعدتان في CSS (‏`position:sticky` + خلفية معتمة + ظلٌّ
فاصل)، ومعهما أولويّة أعمدة تحت 700px: أربعة أعمدة لا تحمل قرارًا على الهاتف
**تُخفى ولا تُحذَف من الـDOM**. مقيس: بعد الانزلاق إلى النهاية يبقى العمود
الأوّل عند نفس البكسل، والخلفية مطابقة للتظليل المتناوب في الثيمين.

**⑥ الرسوم كانت تقول «أنا صورة» ثمّ تختفي** — `role="img"` مع
`aria-hidden="true"` **في العنصر نفسه**، في `/admin` وفي رسمَي لوحة المالك.
حُذفت الثانية، وصار الاسم **مولَّدًا من نفس المصفوفة المرسومة** (الأعلى
بتسميته · المتوسّط · ومدى التوقّع **موسومًا توقّعًا لا قياسًا**). و`drawBars`
و`emptyChart` لم تُمَسّا: تُخرجان نصًّا مقروءًا أصلًا.

**⑦ الموقع** — `/places` صار فيه **ترتيب ومدى سعر** كما في التطبيق وعلى نفس
البيانات (‏`data-price` دائمًا، و`data-rating` **فقط عند `hasRating`** — صفرٌ
هنا يرتّب غير المقيَّم تحت صاحب النجمة الواحدة، وهو ادّعاء لا تقوله البيانات).
والترتيب **مستقرّ**: التساوي يبقي الأبجدي. والأدوات **خاملة لا مخفيّة** بلا JS.
· وصفحة التنزيل صار لها **آليّة** شريط لقطات: `build-site.ps1` يقرأ
`site/static/assets/shots/shot-<n>-<lang>.png` ويولّد القسم كلّه — **مجلّدٌ
فارغ يعني أن القسم غير موجود**، لا عنوانًا فوق فراغ. **ولا تُخترَع صورة ولا
يُكتب وصفٌ لما لم يُرَ (م5)**: النصّ البديل يقول ما **هو** الملفّ لا ما فيه،
وسطرٌ في نهاية البناء يقول للمالك كم لقطة وُجدت وأين تُوضَع البقيّة.
· والهيرو **درجتان لا ثلاث**: المعالجة المحيطية سقطت (أضعفهنّ بصريًّا وأعلاهنّ
كلفةً في الحرص)، **والنصّ باقٍ**. واللوحان الطائران سقطا لأن نصّهما تكرارٌ
حرفي لـ`hero-note` فوقهما — وهو ما اعترف به `aria-hidden` عليهما في الوسم نفسه.
ومعهما حُذفت مفاتيحهما الأربعة من ملفّي اللغة. **مقيس: 27,174 ⇐ 26,885 محرفًا.**

---

**الدفعة العشرون — حركة التطبيق وشكله: أربع مراحل (2026-08-10)**

دفعةٌ على **التطبيق وحده**، ومعيارها القاطع: **بصمة `public/` قبلها وبعدها واحدة
(`520c168f…fae30f`)** و`git status --porcelain public/` فارغ ⇒ صفر تسرّب إلى الموقع.
والأقسام المضافة في ذيل [`app/src/app.css`](app/src/app.css): **§24** أعطال صامتة ·
**§25** البطاقة · **§26** انتقال الصفحات · **§27** الافتتاح. وكلّها منطَّقة بـ`body.native`
**عدا §27** (السبب في ③ أدناه).

**① بطاقة الملعب: تضارب كاسكيد كان يُلغي الضغطة.** `animation:fadeUp … forwards` تثبّت
`transform`, و`:active{transform:scale(.99)}` انتقالٌ يعلو عليها **أثناء جريانها فقط** —
وعند انتهائها تعود قيمة الأنيميشن ⇒ البطاقة تنضغط ثمّ **ترتدّ والإصبع ما يزال عليها**.
والعلاج **فصل المحاور** لا ترقيع الأولوية: الدخول على `translate` والضغطة على `scale`،
وهما خاصّيتان مستقلّتان ⇒ يستحيل أن تتنازعا مهما كان `fill-mode`. و`opacity:0 + forwards`
استُبدلت بـ`@starting-style`: الحالة الافتراضية **مرئية** والإخفاء في حالة البداية وحدها
⇒ أسوأ فشلٍ صار «بلا حركة» بدل «بطاقة غير مرئية».
مقيس: الضغطة تبقى `scale 0.985` عند **1200ms** (العطل القديم يرتدّ خلال ~240ms).

🔴 **وكاد الإصلاح كلّه يكون ميّتًا: [`app/src/native.css`](app/src/native.css) ورقةٌ ثانية
يحقنها `build.ps1` بعد `app.css` فتفوز عند تساوي التخصيص.** كانت تحمل
`body.native .place-card{transition: transform, box-shadow, opacity}` — **بلا `translate`**
⇒ تُلغي حركة الدخول صامتةً، و`:active{transform:scale(.975)}` تتضاعف مع `.985` = **0.96**.
وثالثةٌ ظهرت معهما: ظلّ الضغط فيها بلا `!important` أمام `.place-card{box-shadow:…!important}`
⇒ **لم يُطبَّق قطّ**. نُقلت ملكيّة حركة البطاقة إلى §24 وحده. ولم يكشفها `grep` على
`app.css` — كشفها سؤال **CSSOM في المتصفّح**.

**② نافذة الحجز كانت تظهر فجأةً بلا انزلاق.** `display:none ⇒ flex` لا يُنشئ حالة
قبل‑التغيير ⇒ لا `opacity` ولا انزلاق يُنفَّذان — والنمط الصحيح مستعمل في سبعة مواضع أخرى
وغائبٌ عن **أكثر نافذة تُفتح في التطبيق**. العلاج **CSS خالص**: `transition-behavior:
allow-discrete` + `@starting-style`، **وبلا لمس `Modal.open`** — مسارات الإغلاق أربعة
وثلاثة منها تزيل `.show` مباشرةً، فتعليق السلوك على **الصنف نفسه** يجعل الأربعة تتصرّف
بلا استثناء. والسحب لأسفل يكتب `transform` سطريًّا فيبقى محورًا ثالثًا مستقلًّا.
مقيس: الفتح `100% ⇐ −3.75%` (تجاوز النابض) `⇐ 0` · **عشر دورات ⇒ صفر نافذة عالقة**.

**③ الافتتاح: 1760ms ⇐ 1021ms.** معامل واحد `--intro-k` (‏`1` أوّل إقلاع · `.58` بعده)
يضرب كل مدّة وتأخير بـ`calc` **بلا تغيير أي رقم أصلي** ⇒ الهوية تبقى والانتظار يقصر
(النسبة **0.58** مقيسة بـ`getComputedStyle` في الحالتين). والخروج صار **ثقبًا دائريًّا
يتّسع من قلب الشعار** (`@property --hole`) — الدائرة هي العلامة نفسها — ومعه `opacity`
في نفس الـkeyframes **مسار سلامة**: متصفّح بلا `@property` يُخرج اللوح بالتلاشي.
والزوم 1.5 ⇐ 1.22 (عنصران يتباعدان بسرعتين = تشويش)، واللمعة تدرّجٌ متحرّك لا عنصر
منزلق داخل `overflow:hidden`، و`introGlow` رُبط بالحلقة الأولى (940/190) بدل 1760ms
لا تُدرَك. والتسليم إلى `onbPhotoIn` سليم بلا تعديل: **1900 − 1760 = 140ms**.
⚠️ **وراية `intro-quick` في السكربت المضمَّن في `<head>` لا في `app.js`**: الأخير
مُحمَّل عند `</body>` بينما اللوح يُرسَم قبله ⇒ تبدأ الحركة بالمعامل القديم ثمّ تقفز.
مكانها بجوار سطر منع وميض الثيم وللسبب نفسه — **ولهذا §27 وحدها غير منطَّقة بـ`body.native`**
(اللوح يبدأ الحركة قبل أن يضيف `native.js` الصنف).

**④ انتقال الصفحات: حركة مزدوجة.** كانت المغادِرة تُقصّ بـ`display:none` فورًا فيتحرّك
طرفٌ واحد. الآن الداخلة تصل من الحافّة **كاملةً** (100% لا 22% — النسبة الصغيرة تُقرأ
قفزة) والمغادِرة تنزاح **14%** معاكسةً، والفارق بين السرعتين هو ما يُقرأ عمقًا. والتظليل
بطبقة `::before` لا بـ`filter` (فلترٌ على صفحة كاملة = طبقة دمج بحجم الشاشة كل فريم).
⚠️ والمؤقّت 360ms **لا يُلغى** عند تنقّل متتالٍ: التنظيف المكرّر غير ضارّ، وإلغاؤه يترك
صفحةً عالقة مرسومة. مقيس: **عشرون تنقّلًا سريعًا ⇒ صفر `.pg-leave`**، و`.bnav` باقٍ
`fixed` عند `bottom = innerHeight`.

**⑤ البطاقة صارت ثلاث كتل بدل قائمة مكدّسة.** الاسم وحده على عرض كامل (فيكتفي بسطر
غالبًا بعد خروج التقييم من صفّه) ⇒ **سطر ميتا واحد** يجمع التقييم والموقع وعدد الملاعب
بنقاط وبوزن واحد (ثلاث معلومات ضعيفة كانت تأخذ ثلاثة صفوف بوزن عنوان) ⇒ **المرافق
أيقونات 16px** لا شرائح بارتفاع 30px لنصّ 10px ⇒ صفّ الإجراء كما هو. والسعر **سطران**:
تسمية «حسب الملعب» حُذفت لأن **الشرطة نفسها تقولها**، والمدى داخل `<bdi dir="ltr">`.
ومكانٌ بلا تقييم **لا يعرض فاصلًا يتيمًا** (م5).
🔴 **واصطدام اسم:** `.place-meta` **موجود أصلًا** في `app.css:896` — قاعدةٌ **يتيمة** من
تصميم قديم لا يُصدرها أي كود، فأحياها اسمي ومعها `justify-content:space-between` التي لم
أكتبها: النقاط تباعدت إلى أطراف السطر. أُعيدت التسمية إلى `pcard-meta`. **ولم يكشفه إلّا
`getComputedStyle`** — نفس زلّة `pl-card` المسجّلة أدناه.

**⑥ الإقلاع البارد على أندرويد.** 🔴 `splash.png` كان **لوح Capacitor القياسي: شعارٌ أزرق
على أبيض** ⇒ كل إقلاع بارد يعرض علامة طرفٍ ثالث قبل علامة التطبيق، ومعها الأبيض الذي
يصطدم بلوح الافتتاح. صار لوحًا مسطّحًا `#0F4B53` — **أوّل لون في تدرّج `.intro` حرفيًّا**
لا درجةً قريبة، فالتسليم غير مرئي. ⚠️ **وخلفية النافذة وحدها لا تكفي**:
`android:windowBackground` تُرسم **خلف** الـWebView والأخير يرسم أبيضه فوقها ⇒ لزم
`android.backgroundColor` في `capacitor.config.json` أيضًا. وشريط الحالة صار يفحص وجود
`#intro` (نفس الحالة البصرية لهيرو الترحيب)، ومعه إيقاظٌ صريح بعد `el.remove()` لأن
المراقب يرصد **صنف `body`** لا حذف عنصر.

**أرقام مقيسة:** ارتفاع البطاقة **346** وسيطًا للشبكة (‏322…394، وكان التقدير 392)
و**152** للقائمة (كان 172) ⇒ صُحّح `contain-intrinsic-size` · **صفر تجاوز أفقي في 24
حالة** (‏320/360/390 × نهاري/ليلي × ar/en × شبكة/قائمة) · التباين **6.21 فأعلى** في كل
نصّ جديد في الثيمين · `npm run check` يمرّ و`npm test` **73/73**.

**⚠️ معلَّق بقصد:** `--sport-accent` خطّافه في CSS **بلا ألوان لكل رياضة** — تخصيصها اليوم
اختراعُ هويّة، والمفتوح كرة القدم وحدها (قرار ٧) فلن يراه أحد (م5). **ودَينٌ مسجَّل:**
`.stat-card` و`.game-card` ما زالتا على نمط `opacity:0 + animation forwards`؛ لا ضغطة
عليهما اليوم فلا عطل ظاهر، **وأوّل ضغطة تُضاف لأيّهما تُعيد عطل ① حرفيًّا**.
✅ **واختُبرت على جهاز حقيقي — 2026-08-18** — و⑥ تحديدًا **لا يُرى في أي معاينة على المتصفّح إطلاقًا**، فالجهاز هو مقياسُه الوحيد — وهذا القيد يبقى مكتوبًا لأنّه يحكم كلّ تعديلٍ قادم على الإقلاع.

---

**الدفعة الحادية والعشرون — إتاحة وطباعة وصدق بصري: تسع دفعات في واحدة (2026-08-11)**

مراجعةٌ خارجية ثانية أخرجت وثيقة تسع دفعات. **وثلاث من فرضياتها كانت قائمةً على كودٍ
تغيّر تحتها**، فُحصت كلّها قبل التنفيذ:

| الوثيقة تقول | الواقع |
|---|---|
| `introOut` يخرج بـ`clip-path` | **لا** — §27 (الدفعة ٢٠) أعادت تعريف الاسم نفسه بقناع `--hole`، و**آخر `@keyframes` بالاسم يفوز** ⇒ التعريف الأوّل ميت |
| خلفية النصّ `#FAFAF7` / `#081D22` | **لا** — قسم `NATIVE APP REDESIGN` يعيد تعريفها: **`#F4F5F6` / `#0A0E11`**، والأسطح ثلاثة لكلٍّ |
| `.ss-eyebrow` هو استثناء العربية الوحيد | **لا** — كانت `html[lang="ar"] :is(…٢٦ محدِّدًا…)` **مكتوبةً بيد** |
| `.card`/`.detail-sticky`/`.finput` تحمل `backdrop-filter` | **لا** — الثلاثة **مُبطَلة** بـ`none!important` منذ إعادة التصميم |

**🔴 ثلاثة أعطال صحّة بيانات وإتاحة:**
① **إيصال النجاح كان يقرأ `field.price`** (سعر الدليل) بينما التأكيد يستعمل `slotPrice`
والخادم هو الحكم. **كامنٌ اليوم** (‏18 غير مُشغَّل فالقيمتان متطابقتان) و**ينفجر يوم
تشغيله فيبدو عطل تسعير في الخادم**. الآن `res.price ⇐ slotPrice`، وبحارس صريح قبل
التحويل (‏`Number(null)=0` و`isFinite(0)` صحيحة ⇒ الغياب كان سيخرج «0 د.أ» واثقًا).
② **الإيصال كان صامتًا عن المباراة المفتوحة** — نفس الشاشة لمن اختار «خاصّة» ولمن نشر
ثمانية مقاعد. الآن الشارة والعدد و«ما انعرض ولا مقعد» **بصفر مفتاح جديد** (نفس
`gmBadgeWaiting`/`gmCardWaiting` وصنف `.bk-open` المستعملة في «حجوزاتي»).
③ **`opacity` لا تضرب نسبة التباين بل تهدمها**: تُركّب النصّ **وخلفيته معًا** نحو الأب.
`.tbtn.taken` قاست **1.63:1** نهارًا و**1.98** ليلًا (لا 2.45 و3.23 كما يُظنّ) ⇒ حُذفت.

**التوكنات صارت مصدر الحقيقة في ثلاثة محاور:**
- **التتبّع**: ٨٣ قيمة حرفية ⇒ **٦ توكنات + ٤٤ تحويلًا**، والعربية **تصفّرها من مكان واحد**.
  والقائمة اليدوية أغفلت **٢٦ محدِّدًا حيًّا** (‏`h1,h2,h3` · `.onb-title` · `.intro-name` …).
  🔴 و`@keyframes introWipe*` كانت تنتهي بـ`-.02em` فتُلغي التصفير **على اسم العلامة نفسه**.
- **الوزن**: `--fw-black` (‏900 · **800 عربيًّا**) — مقيس من `fonts.css`: **Montserrat وحده**
  يملك 900؛ Changa وTajawal وInter تتوقّف عند 800 ⇒ كل 900 عربي كان يُرسَم 800.
- **اللمس**: `--tap-min:44px` بجانب `--tap:48px`، وطبقة شبح `inset-inline:50%`+`margin`
  سالب — **متمركزة بلا `transform` وبلا فرعٍ لكل اتجاه**.

**🔴 وCairo كانت غير قابلة للوصول — مُثبَتة لا مُقدَّرة:** `unicode-range` المُعلَن لها
(عربي **ولاتيني**) مطابقٌ حرفًا بحرف لنطاق Changa وTajawal وMontserrat، والمتصفّح يبتّ
عند مستوى `@font-face` لا عند الغلايف ⇒ لا يستشيرها لأيّ محرف. حُذفت: **٢٦٨ ⇒ ٢٠٥KB**.

**وثلاثة إصلاحات كاسكيد، كلّها «قاعدةٌ أحدثُ تغلب قاعدةً أصحّ»:**
- 🔴 **«تقليل الحركة» في الافتتاح كان بلا أثر على التوقيت**: قاعدة التقليل مختصرة،
  ولاحقتا §27 أحدثُ بنوعيّة متساوية ⇒ ١٧٦٠ms كاملة لمن طلب التقليل. أُصلح في **§29 آخر
  الملفّ — وموضعُه هو الإصلاح**.
- 🔴 **`body.dark .tbtn.taken` (0,3,1) تغلب `.tbtn.taken.current` (0,3,0)** ⇒ خانة «حجزك
  الحالي» تفقد لون الهوية ليلًا. **2.70 ⇒ 6.79:1**.
- **انتقال الثيم كان حيًّا في كل لحظة** (٣٥ وسمًا × ٦ خاصّيات) فيرث `:hover` مدّةَ 450ms.
  صار `html:where(.theming)` — **داخل `:where` عمدًا** كي تبقى النوعية (0,0,1) فلا تغلب
  المكوّنات. ومكسبٌ جانبي: «تقليل الحركة» صار **تبديلًا فوريًّا**.

**والخطوط صارت مملوكةً للوجهين من مولّد واحد** (‏`TARGETS` في `fetch-fonts.mjs` بثلاثة
مخرَجات وتنزيلٍ مشترك): `site/admin.html` والموقع كانا يجلبان من Google ⇒ **صفر طلب خارجي
الآن**، وCSP ضاق (`font-src 'self'`). **ولا نسخ يدوي**: مصدرا حقيقة يعنيان أن وزنًا يُضاف
على وجهٍ يترك الآخر نسخةً وراءه.

**وميزة واحدة جديدة — شاشات الترحيب** (‏`#obs`): ثلاث شرائح بـ`transform` و`--nav-dir`،
**والثالثة تُحذف من الـDOM إن لم يكن `GAMES_OK === true` حرفيًّا** — وعند أوّل إقلاع لا
جلسة ⇒ العلَم `null` ⇒ **شريحتان فعليًّا**، وهي الحالة الصحيحة لا نقص. والمفتاح
`mustadaira:onb='1'` **رقم إصدار** لا قيمة منطقية، **وفشل القراءة تخطٍّ** لا تكرار.
وحُذف معها توست `firstVisitWelcome` ومفتاحه — مسارٌ واحد للحقيقة الواحدة.

**والصور صارت تسند الوعد:** الهيرو ولوح الختام وهيرو ترحيب التطبيق كانت **كرة قدم في سياق
«خمس رياضات»** ⇒ صارت **ملاعب متعدّدة الرياضات**، وبلاطات الرياضات الخمس صار خلف مخطّطها
صورةٌ حقيقية — **إضافةً لا استبدالًا**: المخطّط يبقى فوقها لأنه يقول **الأبعاد**، وشارة
«متاح/قريبًا» في نفس البلاطة تمنع أن تُقرأ الصورة وعدًا (قرار ٧ + م5). المصدر Pexels،
وهو **نفس مصدر الصور القائمة** (مكتوب في رأس `embed-onb-photo.ps1`).
⚠️ **وأُصلح في الطريق**: `embed-onb-photo.ps1` كان يشير إلى `app\native\app.css` — مسارٌ
مات في إعادة ترتيب 2026-07-29 ⇒ **كان يرمي استثناءً في كل تشغيل**. صار يُشتقّ من `$PSScriptRoot`.

**وحُذف ما لا يراه أحد:** كتلة `.lp-*` كلّها — **٢٠٧ قاعدة حُذفت و١٥ قُلّمت**، ومعها دالّتا
JS ميتتان وكتلة الكشف عند التمرير. `app.css` **649,155 ⇒ 624,565** بايت.

**والوثيقة الميتة حُذفت:** `docs/design-system/al-mustadira/` (‏MASTER.md + ٣ صفحات) كانت
مولَّدةً 2026-06-18 تصف منتجًا آخر — أزرق `#1E3A8A` وبرتقالي `#F97316` وRubik/Nunito
و`@import` من Google، ومعها social-proof وtestimonials وstat-counters **تحرّمها م5**.
محلّها [`docs/design-system/README.md`](docs/design-system/README.md) مشتقٌّ من الكود وحده،
والتوصيات المستقبلية معزولة في `## مقترَح — غير منفَّذ`.
**وأُضيف** [`docs/qa/accessibility-protocol.md`](docs/qa/accessibility-protocol.md) — أداة
تُشغَّل بجداول نتائج **فارغة**: ٤ مسارات (لاعب · مباراة مفتوحة · مالك · لوحة مفاتيح).

⚠️ **ولوحة المالك:** التقارير صارت ثلاث مجموعات (نظرة عامّة · الأداء · الاتجاه والتحليل)
**بلا نقل بطاقة واحدة** — الترتيب كان يطابق التصنيف أصلًا والناقص كان قولَه. ومعها **نطاق
تاريخ** هو **مرشِّح مُدخَل لا تعديل حساب**: الدوالّ الثلاث تأخذ مصفوفة، فـ`reportScoped()`
تقصّها قبلها ⇒ صفر لمسة على رياضيات العمولة والصافي. **ولا مقارنة فترات على نافذة جزئية.**

🔴 **ولم يُقَس شيء من التخطيط في هذه الدفعة**: كل رقم تباين **محسوب** بمعادلة WCAG، وكل
بُعد **مقروء من CSS**. والقياسات الحقيقية (كثافة الرئيسية · الطيّة · رأس المالك · تجاوبه ·
مسح أهداف اللمس · ارتفاع الشرائح) **سكربتات DevTools معلّقة على المالك**، ومعها فرضية
قناع الافتتاح وجرد `backdrop-filter` (‏١٤ قاعدة حيّة، أخطرها `.skeleton-card` و`.stat-card`
و`.fav-btn` — متكرّرة في قوائم).

---

**الدفعة الثانية والعشرون — بلاغات المالك على الجهاز (2026-08-11، بعد ٢١ بساعات)**

سبعة بلاغات من **استعمالٍ حقيقي على الهاتف**، وأهمّها أنّ بندين ظنّهما هذا الملفّ مُغلقَين
كانا مفتوحين، وثالثًا حسم بوّابةً معلّقة:

**① 🔴 قناع الافتتاح: الفرضية تأكّدت — والعلاج إزالة البنية لا إصلاح الآليّة.**
قال المالك: «لسا في مشكلة زي قبل لكن **أخفّ**». و«أخفّ» هي الدليل: `pointer-events:none`
(الدفعة ٢٠…٢١) أزال **ابتلاع اللمس** وبقي **النزيف البصري** ⇒ العلّة ليست الآليّة بل
البنية: قناعٌ متحرّك على عنصر `fixed` بداخله ابنٌ يتحرّك بـ`transform` وخلفه طبقة ثالثة.
`introOut` صار **تلاشيًا مسطّحًا**، و`@property --hole` وقاعدة `mask-image:none` في تقليل
الحركة سقطتا معه. **ولا مدّة ولا تأخير تغيّرا** — التزامن مع `onbPhotoIn` كما هو.

**② «التقارير» كانت مدفونة فعلًا.** الدفعة ١٩ كتبت أنها «لم تُدفَن — بطاقة بسهم في نهاية
اليوم»، وسأل المالك حرفيًّا **«وين صارت الأرباح والتحليلات والمساعد الذكي»**. صارت **أوّل
عنصر في تبويب اليوم**، وتسميتها الفرعية تسمّي ما بداخلها بالاسم لا بالوصف.
⚠️ **والدرس:** «ليست مدفونة» ادّعاءٌ لا يُثبته ترتيبُ DOM بل عثورُ المستخدم عليها.

**③ رأس المالك بُسِّط** (طُلب في وثيقة ٢١ وبقي بوّابةَ قياس): كان **أربعة أزرار** بجانب
عنوانٍ من سطرين. الفرز **بالتكرار لا بالأهمّية**: الجرس مباشر، واللغة والثيم والخروج خلف
زرّ واحد. **مقيس حيًّا**: الإجراءات 88px مقابل عنوانٍ 238px في رأس 375px.

**④ الرئيسية:** لوح «حجزك القادم» **تحت الشريط اللاصق لا فوقه** — الفلتر أداةٌ دائمة لكل
من يفتح الصفحة، واللوح يخصّ صاحب الحجز وحده، وبالترتيب القديم كان **صاحبُ الحجز هو من
يفقد أداة التصفّح**. والبطاقة **٦ كتل ⇒ ٣** (مقيس **144px**): العدّاد صار حبّةً بجانب
الموعد لا سطرًا تحته بفاصلٍ أفقي، والتلميح للمعلّق وحده.
ومبدّل «ملاعب/مباريات» خرج من صفّ العنوان إلى **صفٍّ مستقلّ في الغلاف اللاصق** (زرّان
164×44) — فصفّ العنوان صار عنصرين بدل أربعة، **وصفر ارتفاع** حين يكون ترحيل 22 مُطفأً.

**⑤ صورة الترحيب استُبدلت** بأخرى من Pexels (ملاعب متعدّدة بألوان حيّة) — والأولى كانت
باهتة. `embed-onb-photo.ps1` هي الأداة، ومسارها أُصلح في ٢١.

**⑥ وهدفا لمس أُغلقا بقياسٍ حيّ لا بتقدير**: جرس الرأس (38×38) وزرّ الفائض (42×38)
والفجوة بينهما **8px** ⇒ الشبح 44×44 يترك **3.8px** بينهما (مقيس في المعاينة بعد البناء).

⚠️ **وزلّة أداة سُجِّلت:** استعملتُ `regex` غير جشع (`.*?</span></span>`) لاقتطاع عنصرٍ من
سطرٍ طويل في `app.html` فابتلع الوسمَ التالي كلَّه و**كسر الصفحة بصمت** — والـ`grep` بدا
سليمًا. الاستعادة بـ`git checkout` ثمّ تحريرٌ صريح بنصٍّ كامل. **لا تقتطع وسمًا بـregex من
ملفٍّ مُصغَّر السطور.**

**الدفعة الثالثة والعشرون — مراجعة UX خارجية: ما صحّ منها وما كان منفَّذًا (2026-08-11)**

خمس ملاحظات. **اثنتان كانتا منفَّذتين بالكامل**، وثلاث صحيحة ونُفِّذت:

| الملاحظة | الحكم |
|---|---|
| مسار الحجز: أخرِج المواصفات والصور والإحصائيات من المسار | **نصفها منفَّذ**: `#placeStats` مطويّ في `<details>` أصلًا. والمواصفات والصور كانتا لوحين مفتوحين **بين «اختر الملعب» و«اختر اليوم»** ⇒ طُويتا في نفس المكوّن |
| ازدواج CTA: أخفِ اللاصق حين يظهر الداخلي | **منفَّذ بالكامل منذ قبل**: `.detail-book-inline{display:none}` تحت 700px أصلًا، و`initStickyDedup()` بـIntersectionObserver، و`renderDetailSticky` يعرض «اختر اليوم والوقت» قبل الاكتمال. **لم يُبنَ شيء** |
| كثافة الرئيسية | المناطق **شرائح أفقية أصلًا**، والباقي (Search+Sport+Filter) كما اقترحتَ. و«احذف شبكي/قائمة» **علّقتَه أنت على Analytics** — والدفعة 18 أعادته لأنه صار غير قابل للاكتشاف داخل الورقة ⇒ **لا حذف** |
| ترتيب رئيسة الموقع | صحيحة ونُفِّذت: `[هيرو][أرقام][كيف أحجز][الفوائد][الرياضات][المُلّاك][FAQ][تنزيل]` |
| شكل مبدّل «ملاعب/مباريات» | صحيحة ونُفِّذت — أدناه |

**① المواصفات والصور خلف طيّة.** محتوًى **يسند القرار** لا يُنجز المعاملة ⇒ مكانه اختياري،
والمسار المعاملاتي (ملعب ⇐ يوم ⇐ وقت) يبقى متّصلًا. ونفس مكوّن `.disclosure` المستعمل
للإحصائيات — نمطٌ واحد لنفس المعنى. و`syncFieldExtras()` تُخفي الطيّة إن خلا الاثنان:
عنوانٌ يفتح على فراغ أسوأ من غيابه.

**② مبدّل «ملاعب/مباريات» — حبّةٌ تنزلق.** كان الزرّ النشط يأخذ **تدرّج CTA** (‏`--g-btn`)
بعرض نصف الشاشة، أي بوزن زرّ «احجز» نفسه وهو مبدّل نطاق لا إجراء. الآن سطحٌ محايد وحبّة
تنزلق والنشط يتميّز **باللون والوزن** — نفس درس الشريط السفلي (الدفعة العاشرة).
🔴 **ومزلق جديد مقيس:** الحبّة بـ`::before` **لا تنزلق إطلاقًا**. الإعلان نفسه حرفيًّا يعطي
`matrix(…,-100,0)` على عنصر حقيقي و`matrix(…,0,0)` على `::before` لنفس العنصر — **مع أن
`--seg-i` و`--nav-dir` تُقرآن صحيحتين على الاثنين** (مقيس بـ`getComputedStyle`). العلاج:
عنصر حقيقي `.mode-thumb`، وهو **النمط القائم في المشروع أصلًا** (`.onb-seg-thumb` · `.nav-pill`).
مقيس بعد الإصلاح: الحبّة تطابق الزرّ النشط **بالبكسل** (`dx=dy=dw=dh=0`) في الخانتين.
⚠️ **ودرسٌ في القياس تكرّر:** قرأتُ `transform` مباشرةً بعد تغيير المتغيّر فحصلتُ على
**منتصف الانتقال** — وهو المزلق المسجَّل في هذا الملفّ حرفيًّا. القياس الصحيح بعد
`*{transition:none!important}`.

**③ ترتيب رئيسة الموقع** صار معاملاتيًّا: «كيف أحجز» قبل «الفوائد»، والرياضات بعدهما.
وتناوب `sec-alt` أُعيد ضبطه فلا يتكرّر سطحان متجاوران.
⚠️ **واعتراضي السابق سقط**: كنتُ أُبقي «الرياضات» ثالثةً لتسند وعد الهيرو («خمس رياضات»)،
وقد صارت **صورة الهيرو نفسها ملاعب متعدّدة** (الدفعة ٢٢) فالوعد مسنودٌ في مكانه.

**إصلاح عاجل — مصيدة «مباريات» (2026-08-11)**

🔴 **عطلٌ أدخلَته الدفعة ٢٢ بنفسها.** `setMode` كانت تحمل منذ الدفعة ١٥:
```js
const st=$('#homeSticky'); if(st) st.hidden=games;   // «الشريط اللاصق يخصّ تصفّح الملاعب وحده»
```
وكان صحيحًا **حتى نُقل مبدّل «ملاعب/مباريات» داخل هذا الغلاف** (الدفعة ٢٢، لفكّ ازدحام صفّ
العنوان). فصار اختيار «مباريات» **يُخفي المبدّل الذي به وحده يُرجَع إلى «ملاعب»** ⇒ مصيدة
كاملة: لا طريق للعودة إلّا إعادة تشغيل التطبيق. بلّغ عنه المالك من الجهاز.

الآن يُخفى **ما يخصّ تصفّح الملاعب وحده**: `#browseSticky` (الفلاتر · المناطق · الرياضة)
و`#fchipsBar`، ويبقى صفّ المبدّل ملتصقًا في الوضعين. والشرائح عند العودة **تُعاد بناؤها**
بـ`renderFilterChips()` لا تُظهَر عمياءً — هي وحدها من يعرف أفارغةٌ هي أم لا.

⚠️ **والدرس أعمّ من العطل:** *أيّ عنصرٍ يُنقَل داخل حاويةٍ تُخفى بالكامل يرث إخفاءها.*
`grep` على `.hidden` **للحاوية** قبل نقل أيّ شيء إليها — والنقل بدا آمنًا لأن الحاوية
والمنقول كلاهما سليم على حدة.

✅ **ومقيس بعد الإصلاح** (معاينة 375px، ثلاث مراحل): «ملاعب» ⇒ الفلاتر ظاهرة والمبدّل ظاهر ·
«مباريات» ⇒ الفلاتر مخفيّة **والمبدّل ظاهر** وقائمة المباريات ظاهرة · العودة ⇒ كل شيء يرجع.
وترتيب الرئيسية مُتحقَّق بالبكسل: المبدّل 148 · **الفلاتر 210 · بطاقة الحجز 269** · العنوان 427.

**الدفعة الرابعة والعشرون — القياسات المعلَّقة شُغِّلت (2026-08-11)**

🔴 **تصحيحٌ لقاعدة كتبتُها في هذه الجلسة نفسها.** قلتُ «لا أملك محرّك عرض» فعلّقتُ سبعة
قياسات على المالك — **وأنا أملك لوحة معاينة تُنفّذ JS وتقيس التخطيط**. استعملتُها فعلًا
للحبّة والرأس، ثمّ نسيتُ أنّ ما بقي معلَّقًا يُقاس بها. القاعدة تبقى صحيحة لما لا تُنفّذه
المعاينة (TalkBack · WebView أندرويد · الأداء على جهاز)، **وباطلة لقياس التخطيط**.

**① أهداف اللمس: من ٨ محدِّدات ⇒ صفر.** مسحٌ آليّ على **٧٧ عنصرًا تفاعليًّا ظاهرًا** عبر
الصفحات العشر عند 360px، ثمّ فحصُ تراكبٍ لكلٍّ منها مع كل جارٍ تفاعليّ:

| | كان | العلاج |
|---|---|---|
| `.dtab-btn` · `.search-btn` · `.settings-toggle` | 40–42 ارتفاعًا | **ارتفاع حقيقي** — الفرق لا يُرى |
| `.amen-row-btn` · `.vf-badge` · `.detail-rating` | 25–31 ارتفاعًا | شبح بعرض الزرّ (النموّ يكسر الصفّ) |
| `.ftab` | 41×40 | شبح متمركز |
| `.onb-tool` | 40×40 | **حجم حقيقي 44** — انظر أدناه |
| `.vf-cell` (‏`<input>`) | 39×48 | عرض حقيقي: الغلاف يخرج 6px عن حشوة البطاقة ⇒ 44.2px بفجوة 5px |
| `.remember-box` | 20×20 | **لا شيء** — أبوه `<label class="remember-row">` بارتفاع **48** فالنقر عليه يبدّل. إيجابيّةٌ كاذبة في المسح |

🔴 **و`::after` ليست أداةً موثوقة دائمًا.** على `.onb-tool` و`.settings-toggle` حسب المتصفّح
`auto×auto` للشبح **مع أن القاعدة تُطبَّق**، و`elementFromPoint` يفشل على ثلاث جهات من أربع.
وعلى زرٍّ آخر بنفس القاعدة حسبها 44×44. العلاج: **حجم حقيقي حيث النموّ غير مؤذٍ**.
والقاعدة: *الشبح حيلةٌ لحالةٍ واحدة — أن يكون الحجم البصري مقيسًا عمدًا (‏`.view-ico`).
وحيث لا يكون كذلك، **الأبسط الذي يعمل يغلب الأذكى الذي لا يعمل**.*
⚠️ **و`inline-size:100%` صراحةً** لا اشتقاقًا من `inset-inline:0`: الثانية تُعيد `auto`.

**② ٨-أ كثافة الرئيسية — مقيسة، والنتيجة «لا عمل».** عند 360px: الهيدر 148 · الشريط اللاصق
120 · صفّ العنوان 64 · **ومن أسفل الهيدر إلى أوّل بطاقة 184px بصفّين اثنين**. ليست
«لوحة تحكّم» — والقلق كان مشروعًا والقياس نفاه. ⚠️ ويبقى **١٥ محطّة تركيز** قبل أوّل بطاقة،
معظمها شرائح المناطق السبع — وهي مرشِّحٌ حقيقي لا زينة.

**③ ٤-ز الطيّة — القرار للمالك.** أوّل زرّ يوم عند **853px** والشاشة 800 ⇒ **٥٣px تحت الطيّة**
(بملعبين فرعيين؛ بملعبٍ واحد يعلوها). والتفصيل: هيرو 230 · اسم وتقييم 78 · اتصال 48 ·
تبويبات 56 · «اختر الملعب» 162 · **طيّة المواصفات 52**. ونقل الطيّة إلى «عن الملعب» يوفّر
الـ53 بالضبط — **لكنه يفصل المواصفات عن الاختيار الذي تصفه**، فلم أفعله بلا قرارك.

**④ ٩-ج تجاوب المالك — منفَّذ أصلًا.** `--max` للمالك **1100px** لا 480: عند 1024 يملأ
`#app` **1014px** والمساحة البيضاء **١٪**. لا عمل.

**⑤ ٩-أ و٩-ب** نُفِّذتا في الدفعتين ٢١ و٢٢ (رأس المالك · مجموعات التقارير + نطاق التاريخ).

**وما يبقى معلَّقًا على جهازٍ حقيقي لا على قياس:** بروتوكول الإتاحة (‏TalkBack) ·
جرد `backdrop-filter` (يحتاج تنميط أداء لا قياس تخطيط) · واختبار الافتتاح على WebView.

---

**الدفعة الخامسة والعشرون — وثيقة سبع دفعات نُفِّذت دفعةً واحدة (2026-08-12)**

**🔴 عطلان صامتان أوّلًا، وكلاهما «الحارس صحيح والتوزيع خاطئ»:**

**① شاشات الترحيب كانت تعرض شريحتين دائمًا، لا ثلاثًا.** الشريحة المشروطة
بـ`GAMES_OK === true` كانت **الثالثة**، والعلَم `null` عند أوّل إقلاع (لا جلسة
⇒ لا سؤال) ⇒ تُحذَف من الـDOM في المسار الوحيد الذي تُعرَض فيه هذه الشاشات
أصلًا. الحارس **لم يُلمَس** (ميزةٌ خلف علَمٍ مُطفأ لا تُعلَن)؛ ما تغيّر أن
غير المشروطة صارت **ثلاثًا** والمشروطة صارت **الرابعة**. والثالثة الجديدة
(«السعر والوقت الفاضي قدّامك») تقول القيمة الفعلية للمنتج وكانت غائبة عن المسار.

**② مالكُ مكانين كان يرى واحدًا بلا كلمة.** `place_owners` علاقةٌ متعدّدة،
و`sbLogin` كان يأخذ `[0]` ويرمي الباقي. الآن `place_ids` في الجلسة، و`getOwnerData`
يأخذ المكان من **حالة قابلة للتبديل** (‏`State.ownerPlaceId`، محفوظة) لا من
الجلسة مباشرةً — ومعها **قائمة بيضاء على الخادم**: `place_id` يصل من العميل، وما
ليس في جلسته يعود إلى المختار (‏RLS يحرسه على أي حال، لكنّ ردّه حينها «ما لقينا
المكان تبعك» — تشخيصٌ خاطئ لخطأ برمجي).

**الحجز صار طلبًا في تسميته لا في حاشيته:** «تأكيد الحجز» ⇒ **«أرسل الطلب»**،
والعنوان «مراجعة الطلب». · **خطوات المؤشّر الثلاث الأولى صارت أزرارًا تعمل**
(‏`updateBookingStep` كان يرسم الحالة نفسها دائمًا، فيبدو المؤشّر تفاعليًّا وهو
صورة). · **الإيصال ١١ خليّة ⇒ أربع حقائق** (الموعد · المدّة · الملعب · الإجمالي)
و«الاسم/الهاتف/الحالة/المقاس» خلف طيّة `.disclosure` — تلك بياناتُ الحساب، أي ما
**لا** يُراجَع قبل الإرسال. · **الدفع سطرٌ مثبَّت** لا مجموعة راديو بخيارٍ واحد،
وتفسير غياب البطاقة **خلف طيّة في مكانها** لا نافذةً ثانية (‏`Modal.open` ينادي
`closeAll` ⇒ نافذةٌ تشرح تُخفي التي تشرحها — مزلقٌ مسجَّل).

**والمباراة المفتوحة صار لها مدخل قبل الالتزام لا بعده:** بطاقة «ناقصك لاعبين؟»
في صفحة الملعب بعد اختيار الوقت (نفس النافذة، و«مفتوحة» مختارة سلفًا)، **وشرط
«لا مقعد يُنشَر قبل أن يؤكّد الملعب» صار قبل الحقول** — كان المضيف لا يقرؤه إلّا
في شاشة النجاح، أي بعد أن التزم. والعددان صارا عدّادَي ± ومعهما **شريط اكتمال
واحد** يُعاد استعماله في ثلاثة مواضع (مراجعة · بطاقة · انضمام) لا ثلاث نسخ.
وشيت الانضمام صار يقول **المقاعد المتبقّية** وماذا يحدث عند رفض الملعب أو إلغاء
المضيف وأن **الانسحاب بلا مهلة** — وكلّها مقروءة من `22_open_games.sql` لا مخترَعة.

**لوحة المالك:** مبدّل أماكن (لا يظهر إطلاقًا لمالكِ مكان واحد) · اسم المكان على
البطاقة **عند التعدّد وحده** · **مخطّط زمني** ليوم واحد: شبكة CSS لا `<table>`
(هذه خريطة إشغال لا بيانات جدولية)، عمودٌ أوّل مثبَّت، وخمس حالات **بلونٍ ونمطٍ
معًا** بألوان `statusLabel` نفسها · شرائح فلاتر الحجوزات بنفس مكوّن `.fchip` جهةِ
اللاعب (والمصدر عناصر HTML لا مرآةٌ في `State`) · وتجزئة التقارير بين الأماكن.

**والترحيب صار صفحةً لا طبقة:** `#page-onboarding` داخل `showPage` ⇒ يدخل الـhistory
ويرث انتقالات الصفحات وإيماءة الرجوع الأصلية، والرجوع داخله يرجع **شريحةً** قبل أن
يغادر (الحارس في `navigateBack` لا في الجسر وحده ⇒ الإيماءة والزرّ سلوكٌ واحد).
ومعه: سحب أفقي · شريط تقدّم بدل النقاط (أربعة أهدافِ لمسٍ 8px لا يقصدها أحد،
وعددها يتغيّر بتغيّر العلَم) · سهم رجوع مرئي · **لغة رسم واحدة** في الأربع ·
و`OBS_VER = '2'`.
🔴 **وكُشف بالقياس عطلٌ لا يظهر إلّا بالعربية ومن الشريحة الثانية فصاعدًا:**
`overflow:hidden` **يصنع حاوية تمرير**، والمسار يُزاح في RTL إزاحةً **موجبة**
(‏`--nav-dir = -1`) فيفيض إلى اليمين ⇒ يرسو التمرير على الحافّة اليمنى ويُزيح
اللوح كلّه بعرض شاشة: «تخطٍّ» وشريط التقدّم والزرّ الأساسي تخرج جميعًا.
و`overflow:clip` تقصّ بلا تمرير فلا يوجد ما يرسو. **مقيس:** بعد الإصلاح تقع
الشرائح الثلاث داخل `[0,375]` والرأس والذيل عند `x=0` في الثلاث.

**الموقع:** الـAPK يُستضاف **من هذا الأصل** لا من GitHub — `/downloads/mustadaira-<v>.apk`
بكاش أبدي (النسخة في الاسم) و`/app.apk` **ملفًّا حقيقيًّا** لا تحويلًا (التحويل كان
سيلزمه ذكر النسخة في `vercel.json` ⇒ مصدران لحقيقة واحدة). ويُجلَب في البناء مرّةً
ولا يُلتزَم (`*.apk` في `.gitignore`؛ المستودع عامّ و٧MB لكل إصدار). · زرّ يقرأ
الجهاز · شريط تنزيل لاصق للجوّال مع لوح خطوات التثبيت (ولقطة Play Protect تُقرأ من
القرص، فإن غابت سقط الشكل كلّه). · **والرئيسة قُلِّمت `27,014 ⇒ 23,066` محرفًا**
والترتيب صار معاملاتيًّا: هيرو ⇒ كيف يعمل (بلا فقرات) ⇒ **الملاعب المتاحة الآن**
(أقوى محتوى على الموقع، وكان مدفونًا) ⇒ خمس بلاطات لا تسع ⇒ المُلّاك ⇒ أربعة
أسئلة ⇒ الختام. والرياضات وثلاثة أسئلة انتقلت إلى `/about`.
⚠️ **وبلاطة «مباريات مفتوحة» مشروطة بردّ القاعدة**: البناء يسأل `open_games`
بالمفتاح العام، فلا يَعِد الموقعُ بما لا يعطيه التطبيق (قرار ٧) — نفس منطق `GAMES_OK`.

**`/admin`:** التبويب الافتراضي كان **الخامس** في الشريط وهو يحمل
`aria-selected="true"` — تناقضٌ يقرؤه قارئ الشاشة قبل أن يراه أحد؛ صار الترتيب
بالتكرار (التشغيل أوّلًا). · «إدارة الأماكن» **إجراء لا قسم** ⇒ زرّ في الرأس. ·
**أرقام الشريط صارت قابلة للنقر** تقفز إلى قسمها **بالفلتر مطبَّقًا**. · شرائح
تصفية لقائمة العمل. · تصدير CSV للجدولين (يُقرأ من الجدول المرسوم لا من البيانات
الخام، وبـBOM وإلّا شوّه إكسل كل اسم عربي). · خطّ اتجاه بجانب رقم الصدارة **ولا
يُرسَم على أقلّ من ٧ نقاط** (والأيام تُبذَر بصفر أوّلًا — درس «الصفر لا يُرشَّح»). ·
نموذج «إضافة مكان» بثلاث خطوات **وحفظٍ واحد**: تقسيم الحفظ كان سيصنع مكانًا بلا
ملعب، والتطبيق يُسقط كل مكان بلا ملعب ⇒ صفٌّ لا يراه أحد. · الشروح الطويلة خلف
(i) بقياسٍ حيّ (`scrollHeight`) لا بعدد محارف. · قفل خمول **لا يمسّ الجلسة**. ·
زرّ نسخ للهاتف. · ولون «المعلّق» وُحِّد (‏`#8C5F16` هنا مقابل `--warning-ink` هناك).

**مقيس:** تباين كل نصّ جديد **٥٫٦٩ فأعلى** في الثيمين (بعد تعطيل الانتقالات —
القراءة أثناءها تعطي منتصف الانتقال) · صفر تجاوز أفقي · `npm run check` يمرّ
و`npm test` **73/73** · الموقع ٣٠ صفحة بلا متغيّر غير محلول · وبصمة `+9,699 native`
كما هي (حقنة واحدة).

⚠️ **وثلاثة بنود لم تُنفَّذ، وسببها لا وقتها:**
① **رمز QR** على الموقع — كتابة مُرمِّز QR من الصفر بلا وسيلة **فكّ** ترميز
للتحقّق تعني شحن رمزٍ قد لا يُقرأ، وهو وعدٌ بما لا يقع (م5). البديل المنفَّذ:
لوحٌ للحاسوب يعرض **الرابط الثابت** `/app.apk` بخطّ كبير مقروء.
② **تقسيم `app.js` إلى وحدات** — الوثيقة نفسها تشترط «دفعة مستقلّة تمامًا بلا أي
تغيير سلوكي معها»، وتنفيذها مع سبع دفعات يُبطل شرطها.
③ **قياس خسارة iOS** — قائمة `events.kind` **قيدٌ في القاعدة** (ترحيل 21)، ونوعٌ
جديد يحتاج ترحيلًا معلَّقًا على المالك؛ والموقع اليوم لا يكتب في القاعدة إطلاقًا.
🔴 **وبندٌ سقطت فرضيّته:** «الفلاتر شرائح لا قوائم منسدلة في جدولي العملاء
والأماكن» — **لا قائمة منسدلة في أيٍّ منهما أصلًا** (الوحيدة في نموذج الإضافة،
وهي حقول إدخال لا مرشِّحات). الشرائح أُضيفت حيث يوجد مرشِّح فعلًا: قائمة العمل.

---

**الدفعة السادسة والعشرون — سبعة بلاغات من المالك (2026-08-12)**

**① الشريط اللاصق فقد ما لا يُلمَس.** مبدّل «ملاعب/مباريات» خرج من `#homeSticky`:
قرارٌ يُتَّخذ مرّةً عند دخول الصفحة لا أداةٌ تُلمَس أثناء التمرير، والالتصاق كان
يأكل شريطًا من الشاشة طوال التصفّح مقابل زرّين. واللاصق باقٍ للفلاتر والمناطق
والرياضة — وهي ما يُلمَس فعلًا وأنت تنزل.

**② المخطّط الزمني صار لكل يوم.** كان مقصورًا على `today()`، والمالك يسأل «شو
وضع الخميس؟» بقدر ما يسأل «شو وضع اليوم؟». شريط يوم بسهمين و«رجّعني لليوم».
⚠️ و«راح» تُقاس **بساعة اليوم المعروض** لا بالساعة الحالية مطلقًا: بلا هذا
الفرق كان الغد يُرسَم نصفه رماديًّا لأن الساعة عندنا الآن الثامنة.

**③ `/places` صار أسماء لا بطاقات.** كان فيه بحثٌ وشرائح مناطق وترتيبٌ ومدى
سعر فوق بطاقاتٍ تحمل ستّ حقائق — **ستّ أدوات وستّ حقائق على سبعة ملاعب**.
والصفحة ليست مكان الحجز (قرار ١)، ومهمّتها أن يجد الزائر اسم ملعبه ويفتحه.
ولا حقيقة حُذفت: كلّها مطبوعة في `/places/<اسم>/`. والقائمة **مكوّن واحد** تشاركه
الرئيسة، فلا بطاقتان تنحرفان.

**④ الشرح قُلِّم في الثلاثة.** التطبيق **22,414 ⇒ 21,142** محرفًا في جدول النصوص ·
الموقع **17,122 ⇒ 15,888** · و`/admin` **6,184 ⇒ 3,931** في فقرات الشرح (‏−36٪)،
ومعها طيّة (i) تُخفي ما بقي طويلًا. والقاعدة المطبَّقة: **يسقط ما يشرح ما قاله
العنوان، ويبقى ما لا يُستنتَج** — «الحجز طلب» · «الدفع في الملعب» · ما لا يستطيعه
الخادم · وأسباب الرفض. ولا بند إفصاح واحد حُذف.

**⑤ شاشات الترحيب على مرجع المالك.** صورة ملء العرض أعلى الشاشة، ولوحٌ بحوافّ
علوية دائرية تحتها بعنوان وسطرين، وذيلٌ فيه رجوع · نقاط · التالي (والأخيرة زرٌّ
ممتلئ). والصور **من المستودع** (‏`app/assets/onb/`) تُقرأ من `assets/` بجوار
`index.html` — أربع صور base64 كانت ستضيف ~مليون محرف إلى ورقةٍ تُحلَّل عند كل إقلاع.
🔴 **ومزلقان مقيسان في الطريق:** `--g-btn` تساوي **Lime** في الثيم الليلي،
فالأبيض عليها 2.05:1 — والحلّ أن يأخذ الزرّ صنف `.sbtn` نفسه (يعرف أن يقلب النصّ
إلى حبرٍ داكن ليلًا): **9.76 نهاري · 8.07 ليلي**. و`[hidden]{display:none!important}`
**عامّة في الورقة**، فإبقاء زرّ «رجوع» في التدفّق بـ`visibility` خسر أمام
`!important` ⇒ النقاط تنزاح عن المنتصف في الشريحة الأولى وحدها (مقيس: 329 مقابل 188).

**⑥ لمن الملعب: رجال · سيدات · مشترك** ([`29_field_gender.sql`](migration/29_field_gender.sql)) —
عمودٌ على **الملعب** لا المكان (نفس سبب `sport` في 13: مجمّعٌ واحد قد يخصّص ملعبًا
للسيدات ويترك الباقي مشتركًا).
🔴 **والقيمة الافتراضية `null` لا `'mixed'`**: كتابة «مشترك» على صفوفٍ لم يقل عنها
أحدٌ شيئًا اختراعُ بيانٍ لم يُدلِ به صاحبه، وضررُه حقيقي — لاعبةٌ تذهب إلى ملعبٍ
للرجال على وعدٍ لم يقطعه أحد (م5). فالغياب غياب: لا شارة، ولا يدخل الملعبُ تصفيةً
بجنسٍ بعينه، و**قسم التصفية كلّه يُخفى** ما لم يصرّح ملعبٌ واحد.
⚠️ **ولا حقل جنس على اللاعب ولا فحص صلاحية**: المنتج يقول **سياسة المكان** ولا
يصنّف مستخدميه، والمنع عند البوّابة شأن إدارة الملعب لا تطبيقِ حجز.
⚠️ والتطبيق يعمل بلا الترحيل: `select=*` ⇒ العمود `undefined` ⇒ لا شارة ولا مرشِّح.
و`/admin` يحذف المفتاح من الإدراج عند «لم يُحدَّد» فيعمل النموذج على القاعدتين بلا فرع.

**⑦ تسجيل الملعب صار طريقًا واحدًا.** كان في `/owners` زرّان يذهبان إلى `/contact`
ونموذجٌ ثالث في الأسفل — **ثلاثة أبواب لفعلٍ واحد**، وأحدها (واتساب) لا يُسجَّل في
جدول ولا يظهر في صندوق `/admin` فيضيع بلا أثر. الزرّان صارا يقودان إلى النموذج،
و`/contact` بقي في `<noscript>` وحده (النموذج يرسل بـfetch).

---

**الدفعة السابعة والعشرون — التطبيق كان معطَّلًا بالكامل، وحارسٌ لم يكن (2026-08-13)**

**① 🔴 العطل: الدفعة ٢٦ شحنت خمسة استعمالات بلا تعريف.**
بلاغ المالك: «الملاعب مش ظاهرة وبعطيني خلل في الإشارة». والسبب **ليس الشبكة ولا
القاعدة** — قِستُ ثمانية مسارات بالمفتاح العام قبل قراءة سطرٍ واحد من الكود،
وكلّها **200** (‏`places` · `fields` · `place_stats` · `booked_slots` ·
`field_closures` · `place_reply_speed` · `open_games` · `booking_rules`)،
والحمولة كلّها ٧ أماكن و١٤ ملعبًا و٢٧ خانة — لا مهلة ولا حجم.
العطل أنّ ترحيل ٢٩ أدخل **استعمالات** `GENDERS` و`genderLabel` و`genderDeclared`
و`fieldGender` و`fx.genders` ولم يُكتب لأيٍّ منها **تعريف**:
`activeFilterCount()` تقرأ `fx.genders.length` على `undefined` ⇒ ترمي داخل
`updateFilterBar()` ⇒ والسطر `renderRegionTabs(); updateFilterBar(); renderPlaces();`
يموت **قبل** `renderPlaces()` ⇒ **لا بطاقة ملعبٍ واحدة تُرسَم**، ورسالةُ الخطأ
الموحَّدة تقول «حدث خطأ في الاتصال» — فيقرأ المستخدم عطلَ شبكةٍ لا وجود له.
⚠️ **و`node --check` يمرّ**: الاسم غير المعرَّف خطأ **تشغيل** لا صياغة.
الإصلاح: التعريفات الأربعة (‏`GENDERS` مفرداتٌ مغلقة مرآتها `fields_gender_chk`،
و`genderDeclared` من **`State.places`** لا `allPlaces` — نفس أساس `distinctSizes`
بالضبط، فمرشِّحٌ مبنيٌّ على ملاعبَ مقصوصةٍ بالرياضة يعرض خيارًا لا يطابق شيئًا)،
و`genders:[]` في المواضع الثلاثة (‏`State.fx` · `resetAllFilters` · `fxDraft`)،
وشرطُ الجنس في `passesFilters` **«أيٌّ من المختار» لا «كلّها»** (مكانٌ فيه ملعب
سيدات وآخر مشترك يطابق الاثنين)، و`gender` في `sbField`.
✅ **مُتحقَّق حيًّا في المعاينة**: الملاعب السبعة تُرسَم · ورقة الفلاتر تفتح ·
صفحة الملعب تُرسم · **صفر خطأ في الـconsole** · و«لمن الملعب» **مخفيّ صحيحًا**.
⚠️ **وسببُ الإخفاء صُحِّح 2026-08-13**: كُتب هنا «لأن ترحيل ٢٩ لم يُشغَّل»، وقياسٌ
لاحق أثبت أنه **مُشغَّل** (‏`fields?select=gender` ⇒ 200). والسبب الصحيح أنّ
**الملاعب الأربعة عشر كلَّها `gender = null`** — لم يصرّح ملعبٌ واحد بعد. والنتيجة
المرئية واحدة، والفرق أنّ الأولى تقول «ميزةٌ تنتظر ترحيلًا» والثانية تقول «ميزةٌ
تنتظر أن يملأها المالك من `/admin`» — وهما فعلان مختلفان تمامًا.

**② الحارس الذي كان غائبًا** ([`tools/check-globals.js`](tools/check-globals.js)) —
الحرّاس الأربعة تفحص **النصوص والمرايا**، و`test-pure` يُقيّم دوالَّ بعينها معزولةً،
فلا واحد منها يرى **«اسمٌ استُعمل ولم يُعرَّف»** — وهو الصنف الذي أوقف المنتج كلَّه.
الأداة بلا تبعية تحليل: تجريدُ التعليقات والنصوص، ثمّ جمعُ التعريفات (بما فيها
المعامِلات، وإلّا صار كل `cb()` بلاغًا كاذبًا)، وطرحُها من الاستعمالات.
⚠️ **وأُصلح فيها بلاغان كاذبان قبل تسليمها**، وكلاهما درسٌ في ذاته:
① `closeAll(except){` **شكلها شكل النداء تمامًا** — الفارق ما بعد القوس المُغلِق
المقابل، ولا يُعرَف إلّا بموازنة الأقواس فعلًا. ② `const A='x', B='y'` تُعرّف
اسمين، والregex الساذجة تلتقط الأوّل ⇒ الثاني بلاغٌ كاذب. **وبلاغ كاذب واحد
يكفي ليُعطَّل الحارس كلُّه**، فالتسليم كان بعد **صفر بلاغ** على الشجرة السليمة.
✅ **ومُتحقَّق بالتجربة لا بالافتراض**: شُغِّل على نسخة ما قبل الإصلاح فأخرج
**الأربعة بأسمائها وأسطرها**، وعلى النسخة المُصلَحة فمرّ نظيفًا. وصار أوّل
حلقة في `npm run check`.

**③ ٢٩٨KB من base64 خرجت من مسار التحليل.** مراجعةٌ خارجية كشفت تناقضًا داخليًّا:
`build.ps1` يشرح أن الخطوط **لا تُضمَّن** لأن «٢٦٨KB من base64 تُدفع عند كل
تحليل»، وفي `app.css` **٢٩٨,٩٣٢ بايتًا** من الشيء نفسه — أي **٤٥٪ من الورقة** —
تُحقَن في `index.html` وتُحلَّل عند كل إقلاع بارد. والحجّة التي أسقطت الخطوط
تنطبق حرفيًّا، و«الرابط يفشل بلا إنترنت» **لا يصحّ**: `assets/` داخل الـAPK.
خمس صور صارت ملفّات في `app/assets/img/`، والبناء ينسخها إلى الوجهتين أصلًا.
**مقيس: `index.html` من 1,279,073 ⇐ 981,455 محرفًا (−23٪)**، و`+9,699 native`
كما هي (الحقنة واحدة).
🔴 **وكشف الفحصُ ميتًا لم يذكره أحد:** `--logo-mark` **مُعلَن ولا يستهلكه أحد**
(‏`var(--logo-mark)` صفر ظهور) — ٢٥,٨١٤ بايتًا تُشحَن في كل APK بلا أن تُرسَم
مرّةً. حُذف ومعه ملفّه.
⚠️ **والفحص بالشبكة لا بالقراءة** (مزلق `url()` المسجَّل: ١٣ × 404 والصفحة
تُرسَم بلا خطأ واحد): كل الطلبات **200** في الثيمين، والشعار والصورة مرئيّان.
⚠️ **وSVG للشعار لم يُنفَّذ**: تحويل PNG إلى SVG تتبُّعٌ يخترع شكلًا لم يرسمه
صاحبه — والمكسب المقيس (خروج ٢٩٨KB من التحليل) تحقّق بلا ذلك.

**④ النصوص: ما يقوله المنتج عن نفسه حين لا يعمل شيء.**
🔴 **أحد عشر نصًّا كان يصل المستخدم بأسماء ترحيلاتٍ وأوامرِ طرفية**:
«شغّل الترحيل 22» · «ترحيل ١٤ لم يُشغَّل» · `supabase functions deploy ai` ·
`GEMINI_API_KEY`. والقارئ **لاعبٌ أو صاحبُ ملعب لا مطوِّر**: لا يستطيع تشغيل
ترحيل، ولا يتعلّم من الرقم شيئًا — ويقرأ منتجًا نصفَ مبنيّ. **ولا حقيقة حُذفت**:
كلّها ما زالت تقول إن الميزة لا تعمل، لكنّها صارت تقول **ما يستطيعه هو** بدلها
(«الإشعارات لسّه ما اشتغلت — حالة كل طلب وردّ الملعب في «حجوزاتي»»). والتفصيل
التقني بقي حيث يُقرأ فعلًا: شريطُ صحّة الترحيلات في `/admin` (الدفعة ١٤).
⚠️ **والحالات الفارغة لم تُمَسّ لأنها سليمة أصلًا** — ملاحظة المراجعة عنها لا
تنطبق هنا: «كن أوّل من يقيّم» · «ملعبك الأول بانتظارك» · «لم يفز ملعبٌ بقلبك»
كلّها دعوةٌ لا اعتذار. **الفارغ الوحيد الذي كان طريقًا مسدودًا** («لا توجد ملاعب
متاحة حالياً») صار يقول المخرج: اسحب لتحديث، أو جرّب رياضة ثانية.
· وعلى الموقع: «الإصدار الموقَّع لم يُنشَر بعد» ⇒ «التطبيق على وشك النزول»
(والوعد نفسه في جسمه بلا نقصان) · «نسخة الآيفون ليست جاهزة» ⇒ «على آيفون؟
سجّل اهتمامك» — **الجدار صار فعلًا يستطيعه الزائر** · وحُذف تحوّطٌ دفاعي من
**رسالة نجاح** («لا نستطيع أن نعدك بموعد محدّد للردّ») يُقلق من فرح للتوّ،
بلا أن يُستبدَل بوعدِ موعدٍ لا نملكه.

**⑤ شاشات الترحيب: قصّة لا قائمة حقائق.** كان الترتيب **فائدة ⇒ قيد ⇒ فائدة**،
فيقع «الحجز طلب ينتظر» مطبًّا في منتصف القمع؛ وكانت الشريحتان ① و③ تقولان
الشيء نفسه («شوف الفاضي والسعر») مرّتين. صار: **تصفَّح ⇒ ابعث ⇒ يوصلك الردّ**.
ولا حقيقة سقطت — «الملعب يوافق» ما زالت بالحرف، لكنّها صارت **خاتمة المسار**:
الردّ يصلك، لا أنت تنتظره.

**⑥ وثيقة التصميم صُحِّحت في ثلاثة بنود** (‏[`docs/design-system/README.md`](docs/design-system/README.md)):
🔴 **§١٤.٤ كان يمنع `--sport-accent` بم5 — وهو توسيعٌ خاطئ للقاعدة**: م5 تحكم
*الادّعاءات* لا الألوان، ولونٌ ليس وعدًا يُنقَض. **والقاعدة المطبَّقة خارج نطاقها
تُفقِد نفسَها قوّتها حيث تصحّ.** السبب الصحيح أقوى: الهويّة لونان، و`--g-btn` لون
**الفعل** وحده، فخمسة أكسنتات تنزعه وتصطدم بالثلاثي الدلالي. · §١٤.٥ صار يقول
**٢٧٨٨ `px` مقابل ٥ `rem`** (مقيس) ومعه أنّ التشخيص نفسه غير محسوم: الأرجح أن
`textZoom` يكبّر النصّ **مهما كانت الوحدة** فيكون العطل «النصّ يكبر والصناديق لا»
لا «التكبير معطَّل» — والأوّل **يبدو سليمًا** حتى تُرفع نسبة الخطّ، فهو أخطر.
· §١٤.٦ أُغلق: `.lp-*` صفر محدِّد حيّ (بقي اسمٌ في تعليق).

**⑦ الحرّاس التصميمية بُنيت، وكشفت مخالفةً حيّةً واحدة**
(‏[`check-css-rules.js`](tools/check-css-rules.js) + [`check-contrast.js`](tools/check-contrast.js)):
- **الخصائص المنطقية:** المراجعة قالت «٢٢ فيزيائية بلا سبب موثَّق، ومنها
  `.place-top-badges`». **القياس نفاها**: الإحدى والعشرون كلّها مشروعة — خمسٌ
  تضبط `left` و`right` **معًا** (شريطٌ بعرض الحاوية بلا اتجاه، ومنها
  `.place-top-badges` نفسها)، وستٌّ هي مثال التوسيط `left:50%`+`translate`
  — **وهو فيزيائيّ صحيح**، والخطأ المسجَّل هو خلطُ منطقيٍّ بفيزيائي — وأربعٌ
  موثَّقة (حبّة تُوضَع بقياسٍ حقيقي · بقعة ضوءٍ عند إحداثيّ المؤشّر · لمعةٌ
  غير دلالية). فالحارس يحرس **الجديد** لا يُصلح قائمًا.
- 🔴 **التتبّع: ثلاث عشرة قيمةً حرفيةً فلتت من تصفير العربية.** الدفعة ٢١
  حوّلت ٤٤ ووضعت ستّة توكنات، **و٤٥ قاعدةً تستعمل التوكن مقابل ١٣ بقيت حرفيةً**
  ⇒ `html[lang="ar"]{--tr-*:0}` لا تطالها، فيبقى تتبّعٌ سالب على نصٍّ عربيٍّ
  متّصل. وهي **نفس زلّة القائمة المكتوبة بيد** المسجَّلة هنا حرفيًّا. حُوِّلت،
  ⚠️ **واستُثني ثلاثة بقياس لا بحدس**: `.rd-stars{2px}` تباعدُ رموز ★ لا
  تتبّعُ نصّ · و`.18em` داخل `@keyframes` عند `opacity:0` (حالةٌ عابرة لا قيمة
  تصميمية) · و`.1em` مقصورةٌ على `html[lang="en"]` أصلًا.
- **التباين:** ١٢ زوجًا في الثيمين محسوبةً من التوكنات نفسها بحلّ سلاسل `var()`
  وتركيب الشفّافية. والأرقام **تطابق ما هو مسجَّل في هذا الملفّ** (‏15.59 ·
  6.21 · 8.50) ⇒ الحارس مُعايَر على قيمٍ معروفة لا يقول ما يحلو له.
- ⚠️ **وفحص الارتفاع ضُبط بعد ٣٩ بلاغًا معظمها كاذب**: `height` ثابتة على أيقونة
  SVG أو مربّع اختيارٍ **صحيحةٌ ومقصودة**، والعطل الحقيقي «نصٌّ في صندوقٍ لا
  ينمو» ⇒ الشرط صار **وجود `font-size` في القاعدة نفسها**. فبقي بلاغان حقيقيّان
  (‏`.tab-badge` · `.filter-count`) صارا `min-height`. **وحارسٌ صاخب حارسٌ مُعطَّل.**

**⑧ `rem` للطباعة — ٣٧٣ قيمةً، وصفر بكسل تغيّر.** `html` بلا حجمٍ مصرَّح ⇒
الجذر 16px ⇒ `N/16` **تنتهي دائمًا** (كل القيم أنصافٌ أو صحاح) فالتحويل متطابقٌ
حسابيًّا. ✅ **ومقيس في المتصفّح لا مُفترَضًا**: `.onb-title` 42px · `.sbtn`
15.5px · `body` 14px — نفس الأرقام قبل التحويل بالضبط. ثمّ رُفع الجذر إلى 20px
فتبع النصُّ (‏14 ⇐ 17.5)، **وثبت العنوان** لأنه `clamp(31px,8.8vw,42px)` ⇒
حدّاه صارا `rem` والوسط بقي `vw` (عرضُ الشاشة لا حجمُ الخطّ).
⚠️ **والحشوات الرأسية بقيت `px` عمدًا**: الأمان جاء من `min-height` لا من تحويلٍ
شامل، وقياسُ الجهاز (§١٤.٥) لم يقع بعد.

**⑨ شريط المالك: التقويم شكلُ عرضٍ لا قسمًا.** أربع خانات على 375px،
واحدةٌ منها للتقويم — **وهو الحجوزات نفسها بشكلٍ آخر** — و«التقارير» (جواب
سؤال «هل يربحني هذا؟») بلا خانةٍ إطلاقًا. الآن: **اليوم · الحجوزات · التقارير ·
الملاعب**، والتقويم مقطعٌ داخل الحجوزات بنفس مكوّن `.ov-seg` المستعمل في «اليوم».
والحبّة تبقى على «الحجوزات» في الشكلين — المستخدم فيها فعلًا.
⚠️ **وثلاثة أخطاء وسمٍ أُصلحت في الطريق**: `ownerTab-reports` كان `role="region"`
بلا `aria-labelledby` ⇒ صار `tabpanel` مربوطًا · و`ownerTab-calendar` بقي يشير
إلى زرٍّ حُذف (**مرجعٌ معلَّق في شجرة الوصول**) · والمقطعان حملا **مُعرّفًا واحدًا
مكرّرًا** ⇒ صارا مُعرّفَين وصنفًا مشتركًا تُزامَن به النسختان.
✅ **مقيس في المعاينة**: أربعة أزرارٍ بمقاصدها · خمسة ألواحٍ كلّها `tabpanel` ·
**صفر مُعرّفٍ مكرّر وصفر مرجعٍ معلَّق في المستند كلّه**.
⚠️ **ولم تُقَس لوحة المالك حيّةً** — خلف تسجيل دخول، و`javascript_tool` معزول عن
نطاق الصفحة. التحقّق بنيويّ، والتفاعل يبقى على أوّل دخول للمالك.

**⑩ فرع `min-width:1400px` حُذف.** أكبر لوح أندرويد يخرج ~٩٦٠ بكسلًا منطقيًّا ⇒
الفرع غير قابل للوصول على جهازٍ يُشحَن له، وكان يُقرأ في معاينة سطح المكتب وحدها
فيُوهم بتخطيطٍ لا يراه مستخدم.

**⑪ `/admin` على الهاتف: صفٌّ يصير بطاقة — والشبكة المموَّهة صارت أداةً دائمة**
(‏[`tools/admin-harness.mjs`](tools/admin-harness.mjs)) — الوصول اليومي إلى اللوحة
من مفضّلة المتصفّح، أي من الهاتف غالبًا. **مقيس عند 375px قبل التغيير:** جدول
«الملاعب» يخرج **631px داخل حاوية 310px = ‏321px انزلاقًا أفقيًّا**، فالاسم يختفي
عند أوّل تمريرة وتصير الأرقام بلا هويّة. وتثبيتُ العمود الأوّل (الدفعة ١٩) عالج
**العرَض لا العلّة**.
الآن تحت 700px: كل صفٍّ بطاقة، عنوانُها العمود الأوّل بلا تسمية (الاسم يقول نفسه)
وتحته أسطرٌ «تسمية ⇠ قيمة». **مقيس بعده:** التجاوز **321 ⇐ صفر** في الجداول الخمسة
(‏`months` · `daily` · `places` · `topCust` · `pend`) ومعها **صفّ المجاميع في
`tfoot`**، وارتفاع البطاقة **270px**. **والحاسوب لم يتغيّر بشيء** (مقيس عند 783px:
`display:table` · الرأس ظاهر · العمود الأوّل ما زال `sticky` · ولا تسمية تُعرَض).
⚠️ **والتسمية تُشتقّ من `<thead>` وقت الرسم** (‏`stampLabels` + مراقب `MutationObserver`
على `#panel`) **لا تُكتب في كل `<td>`**: عشرات المواضع في خمسة جداول، وأوّل عمودٍ
يُضاف أو يُزاح يجعل التسميات **تكذب بصمت** — وهو الانحراف الذي بُنيت له حرّاس
المرآة أصلًا. والمراقب لا يُنسى كما يُنسى نداءٌ بعد كل `innerHTML`.
🔴 **وعطلا نوعيّةٍ وشبكةٍ كُشفا بالقياس لا بالقراءة:**
① `.ad-scroll td{display:flex}` نوعيّتها (0,1,1) **تغلب** `.ad-c2{display:none}`
(0,1,0) ⇒ الأعمدة الأربعة التي أخفتها الدفعة ١٩ عادت، فارتفعت البطاقة **435px**؛
وبقاعدةٍ أخصّ عادت إلى **270**. ② وبذرة الشبكة سمّت عمود `admin_daily` باسم `d`
وهو **`booking_date`** ⇒ خليّة تاريخٍ فارغة و«undefined قادم»، فبدت اللوحة معطوبةً
وهي سليمة تمامًا. **شبكةٌ لا تطابق المخطّط تصنع أعطالًا وهمية تُهدَر عليها دفعة** —
فاقرأ أسماء الأعمدة من الكود الذي يقرؤها، لا من الحدس.
⚠️ **والصفحة تُحذَف بعد الفحص دائمًا** (‏`--rm`): `public/` مخرَجٌ متتبَّع، وصفحةٌ
تتخطّى تسجيل الدخول لا تُشحَن. ولا خطر تسريب — بياناتها **مولَّدة بالكامل**.

⚠️ **وما لم يُنفَّذ، وسببُه لا وقتُه:**
① **تقسيم نافذة المراجعة** — مسارُ المال، وقد أُعيد بناؤه في الدفعة ٢٥؛ تعديله
في دفعةٍ تُصلح عطلًا حرجًا خلطٌ لا يُبرَّر.
② **توحيد التوكنات الجذرية** — ورقة الموقع مولَّدة من `site/styles/`، والتوحيد
يمسّ الوجهين بأمر بناءٍ واحد ⇒ دفعةٌ قائمة بذاتها بمعيارها الخاصّ (بصمة `public/`).
③ **SVG للشعار** — تتبُّع PNG اختراعُ شكلٍ لم يرسمه صاحبه.

---

**الدفعة الثامنة والعشرون — مراجعة واجهة خارجية: التسلسل الهرمي، وميتٌ ظُنّ حيًّا (2026-08-13)**

مراجعةٌ خارجية قالت إن المنتج «يبدو رخيصًا» وعزت ذلك إلى **فائض تزيين وعجز تسلسل
هرمي**. فُحص كل بند بقراءة الملفّ ثمّ **بقياس CSSOM في المتصفّح** — والفحص قلب أهمّها.

**🔴 ① أقوى بند في المراجعة كان يصف كودًا ميتًا.** قالت إن `#app::before` يحمل
**ثمانِ طبقات تدرّج** (بوكيه + مثلثان conic + وردة سداسية بستّ قطاعات + هالة نعناع
+ إضاءة رخامية) فوقها `filter:blur(36px)` وحركةُ انجراف **٢٦ ثانية بلا توقّف**،
وفوقها شبكة ٦٠px في `::after` — وأنها «إعادة تركيب GPU في كل فريم». **والوصف صحيح
حرفًا بحرف في المصدر، وخاطئ تمامًا في المُنتَج**: قاعدةٌ لاحقة اسمها «خلفية مسطّحة
نظيفة» كانت تقتلهما معًا:
```css
#app::before,#app::after{content:none!important;background:none!important;display:none!important}
```
مقيس في المتصفّح: `content` تُحسب **`none`** للاثنين ⇒ **صفر بكسل مرسوم وصفر كلفة
GPU منذ تلك الدفعة**. حُذف المصدر (٢٫٧KB) لأنه **مضلِّل** لا لأنه بطيء، وحُذفت معه
القواعد الأربع التي كانت تقتله وقد صارت بلا هدف.
⚠️ **والدرس هو أهمّ ما في الدفعة:** كتلةٌ تُقرأ في المصدر قد تكون مطفأةً بقاعدةٍ
بينك وبينها ألف سطر — و`grep` على تعريفها **لا يراها**. أيّ تشخيص أداءٍ مبنيّ على
قراءة CSS يُصدَّق بعد `getComputedStyle` وحده. (نفس عائلة مزلق `native.css` المسجَّل:
ورقتان لا واحدة، والثانية تفوز.)

**② لون الفعل ثبت تيلًا في الوضعين** — كان `--act-bg` تيلًا `#0F4B53` نهارًا
و**لايم `#8CC63E` ليلًا**: هويّة الزرّ الأساسي تنقلب بتبديل الوضع. صار الليلي
**تيلًا فاتحًا `#5FB7C4`** بحبر `#062126` — وهو **لونٌ في الباليتة أصلًا** (حلقة
التركيز الليلية `--ring`) لا لونٌ مخترَع. **مقيس: 9.76:1 نهاري · 7.23:1 ليلي**
للنصّ، و**7.49:1** لخلفية الزرّ على خلفية الصفحة الليلية (والتيل العميق كان سيذوب
فيها — وهو سبب اللايم الأصلي). واللايم صار **لهجةً**: الحبّة النشطة وشارة «متاح».

**③ التسلسل الهرمي في شاشة الترحيب** — ثلاثة أزرار **بعرض كامل وارتفاع ٥٢ ووزن ٨٠٠**،
والثاني بحدّ **بلون الهوية نفسه** ⇒ فعلان بلون واحد، والعين لا تعرف أيّها الفعل.
الآن: ① أساسي مملوء (اللون الوحيد على الشاشة) · ② **سطح محايد** (وهو ما كان معرَّفًا
في `.onb-btn-ghost` أصلًا قبل أن تقلبه تمريرة §20) · ③ **رابط نصّي بعرض محتواه**.
⚠️ و**الفجوة هي ما يصنع الرتبة لا اللون**: مقيس بعد التغيير **١٢px** بين الزرَّين
و**٢٠** قبل الرابط، والرابط **١٤٠٫٤×٤٤** (هدف لمس سليم بعرضٍ لا يدّعي أنه زرّ).
التباين مقيس في الثيمين: **9.76 · 13.31 · 11.97** نهاري · **7.23 · 13.80 · 12.62** ليلي.

**④ `.onb-grabber` حُذف — مقبضٌ يَعِد بسحبٍ لا يقع.** اللوح ثابت ولا مستمع سحب عليه.
والأفوردانس الوهمي أوضح علامة قالبٍ جاهز، وهو من جنس م5: عنصرُ واجهةٍ يدّعي قدرة
لا نملكها.

**⑤ حركتان خاملتان حُذفتا** — `authOrb` (هالة تنبض **٧٫٥ث بلا توقّف**) و`authFloat`
(أيقونة تطفو ٥٫٦ث)، ومعهما `@keyframes pulseGlow` الميت. **مقيس بعدها: صفر حركة
لا نهائية جارية في التطبيق كلّه** (مسحٌ على كل عنصر و`::before`/`::after`).
والمبدأ: **لا شيء يتحرّك ما لم يحرّكه المستخدم.**

**⑥ الضغطة: `scale(.96)` بنابض مرتدّ ⇒ `.985` بمنحنى خروج** في `.btn`/`.sbtn`/`.cbtn`/
`.onb-btn` — نفس الرقم الذي ثبّتته الدفعة ٢٠ لبطاقة الملعب، فصار المشروع كلّه على
لغة ضغطٍ واحدة. و`--ease-spring` بقيت للعناصر الصغيرة، وخرجت من الأزرار الكبيرة.

**⑦ الحافّة اللامعة المزيّفة** (`inset 0 1px 0 rgba(255,255,255,.25)`) حُذفت من خمسة
مواضع. ⚠️ وكانت **ميتةً في أربعة منها** أصلًا: `box-shadow:…!important` في تمريرة
لاحقة يسبقها — نفس صنف ① بالضبط، والحذف هنا تنظيفٌ لا تغييرٌ بصري.

**⑧ الوزن: ٩٠٠/٨٠٠ على الأزرار ⇒ ٧٠٠.** توكن `--fw-black` **لم يُمَسّ** (هو يقول
حقيقةً مقيسة عن الحزمة)، وما تغيّر استعمالُه على الأزرار وحدها: الأسود العريض
إشارة «تطبيق شعبي»، والفخامة وزنٌ متوسّط بحجم صحيح ومسافةُ تنفّس.

**⑨ زجاجٌ على عناصر تتكرّر في القوائم** — `.skeleton-card` و`.stat-card` و`.fav-btn`
و`.pill` كانت تحمل `backdrop-filter` **بعد** أن أزالته §13 عن الأسطح المعتمة: أي
طبقة دمج GPU **لكل بطاقة في القائمة**. **مقيس على الرئيسية بسبع بطاقات: من ١٤ طبقة
حيّة ⇒ صفر، في الثيمين.** والشفّافيات عُوِّضت برقمٍ أعلى (`.45 ⇒ .62` في `.fav-btn`).

**⑩ الظلال الليلية**: `rgba(0,0,0,.82)` و`.78` و`.66` تُوحل ولا ترفع على سطحٍ داكن
⇒ نزلت إلى `.55`/`.5`/`.45`. والارتفاع يبقى محمولًا على **لون السطح** (`--surface-solid`).

**⑪ حبيبات ساكنة بشفافية ٢٫٥٪** بديلًا عن كل ما حُذف — بلاطة ١٢٨px من `feTurbulence`
تُرسم مرّة وتتكرّر. **بلا حركة وبلا `blur` وبلا `mix-blend-mode`** (الأخير يفرض طبقة
دمج بحجم الشاشة، وهو بالضبط ما تخلّصنا منه في ⑨).
⚠️ **وعلى `body::after` لا `#app::after`**: الأخير محكومٌ بقاعدة الإطفاء في ①، ومحاربتُها
بـ`!important` ثانٍ إحياءٌ لما قرّرت الدفعةُ السابقة إطفاءه. والحبيبات **ليست من جنس
ما أُطفئ** (أشكالٌ وشبكة) بل نسيجُ سطح — فمحدِّدٌ آخر، لا معركةُ أولويّة.

**وأربعة بنود رُفضت — بسببها لا بوقتها:**
① **تبديل الخطوط إلى عائلة واحدة بعربية حقيقية** (‏Montserrat بلا عربية ⇒ العناوين
العربية Changa واللاتينية Montserrat = شخصيتان). **قرار المالك: تبقى كما هي** —
والتبديل يمسّ التطبيق والموقع و`/admin` من مولّد واحد ⇒ دفعةٌ قائمة بذاتها.
② **حذف صورة الهيرو لصالح رسمٍ هندسي من خطوط الملعب.** الفكرة قوية (اسم المنتج =
دائرة المنتصف، وتحلّ أن كل صورنا كرة قدم بينما نعِد بخمس رياضات)، **لكنّها تنقض
طلبًا صريحًا للمالك** في الدفعتين ٢٢ و٢٦. **قراره: تبقى الصورة.**
③ **حذف مبدّل «لاعب/صاحب ملعب»** (وزن ٥٠/٥٠ لانقسام واقعه ٩٥/٥). **قراره: يبقى.**
④ **إعادة تسمية طبقة الـalias** (`--emerald` تيل، `--gold` و`--copper-primary` لايم —
أسماءٌ تكذب على قارئها). صحيحة، **ومكسبها صفرٌ للمستخدم ومخاطرتها حقيقية** على ٣٤+
موضعًا ⇒ دفعةُ تنظيفٍ بمعيار «صفر تغيّر بصري مقيس»، لا ذيلُ دفعةٍ أخرى.

**⚠️ وثلاث فرضيات في المراجعة كانت خاطئة** (رابعتها ① أعلاه):
`backdrop-filter` على `.card` — **مُبطَل بـ`none!important` منذ §13** · `.finput` و
`.detail-sticky` كذلك · و«الزجاج على كل بطاقة» يصف مصدرًا لا مُنتَجًا. والصحيح كان
في العناصر الأربعة في ⑨ وحدها — وهي التي لم تذكرها المراجعة.

**مقيس:** `npm run check` يمرّ · `npm test` **73/73** · **صفر تجاوز أفقي** على
المستند (‏375/375) في الثيمين · **صفر خطأ console** · سبع بطاقات ترسم · بصمة
`public/` **لم تتغيّر بايتًا** (‏`git status --porcelain public/` فارغ) · و`+9,699
native` كما هي (حقنة واحدة). ✅ **واختُبرت على جهاز حقيقي — 2026-08-18، أكّده المالك.** (‏تأكيدُ استعمالٍ لا قياسُ أداة: TalkBack وتنميطُ الأداء خارجه.)

**الدفعة التاسعة والعشرون — تسعة عشر بلاغًا من المالك، وعطلٌ صامت في القاعدة (2026-08-13)**

بلاغاتٌ من استعمالٍ حقيقي على الجهاز والموقع. **وكلُّ تشخيصٍ هنا مقيس في المتصفّح
أو بسؤال القاعدة** — لا واحدَ منها من قراءة الكود وحدها، وثلاثةٌ منها قلبت ما كان
مكتوبًا في هذا الملفّ نفسه.

**🔴 ① تقويم المالك: ثلاثة أعمدة تنتفخ وأربعة تنكمش — واصطدامُ اسم هو السبب.**
بلاغ المالك «آخر يوم بالسطر مربّعه صغير جدًّا». والخانات الفارغة قبل أوّل الشهر
كانت `class="cal-cell empty"`، و`.empty` في الورقة **حالةُ فراغٍ عامّة**
بـ`padding:56px 28px` ⇒ عرضُها الأدنى 56px يفرض عرضَ عمودها. **مقيس عند 375px:**
`56 · 56 · 56 · 34.3 · 34.3 · 34.3 · 34.3`. صارت `cal-blank` ⇒ **43.6 للسبعة كلّها**
(قيمةٌ واحدة لا سبع). ونفس عائلة `pl-card` و`place-meta` المسجّلة: **فضاء الأصناف
مشترك، والاسم العامّ يسرق قاعدةً لم تكتبها.**

**② «أكّد + واتساب» على حجزٍ مؤكَّد.** الحالات الثلاث غير المعلّقة كانت تعرض
الأزرار الأربعة نفسها ⇒ يرى المالك زرَّ تأكيدٍ على ما أكّده هو، وضغطُه يعيد كتابة
الحالة نفسها فلا يحدث شيء. الآن: **مؤكّد ⇒ إلغاء** (ومعه «لم يحضر» بعد انتهاء
الخانة) · **ملغى/مرفوض ⇒ أكّد** (المخرج الوحيد من رفضٍ بالخطأ) · والواتساب في
الحالتين. والقاعدة: لا يُعرَض زرٌّ لا يُغيّر شيئًا.

**🔴 ③ قائمة فائض المالك: ثلاثة أسطر بثلاثة أشكال — والسببان حارسان يعملان.**
«مش مرتبة ومظهرها مش فخم» وصفٌ دقيق لما يقع فعلًا:
- **بند اللغة كان «EN» عاريةً بلا أيقونة ولا تسمية**: `setLanguage` تكتب
  `b.textContent = t('langSwitch')` على **كل** `[data-action="toggleLang"]`، وهذا
  البند زرٌّ **مركَّب** فيه svg وspan ⇒ يُمحى الاثنان.
- **وبند الثيم كان أيقونةً عارية**: حارس «الشمسين» (‏`> :not(.thm-ic){display:none
  !important}` + `font-size:0!important`) صحيحٌ لزرٍّ أيقونتُه كلُّ محتواه، وهذا
  صفٌّ فيه تسمية.
**والعلاج لم يُضعِف الحارس**: البندان خرجا من نطاقه باسمَي فعلٍ آخرين
(`themePick`/`langPick`) يستدعيان **الدالّتين نفسيهما**. ثمّ صار البند ثلاثة أعمدة:
أيقونة في مربّع ليّن ⇐ تسمية ⇐ **قيمةٌ حالية** («عربي» · «ليلي») تُكتب عند كل فتح،
وفاصلٌ قبل «الخروج». **مقيس:** الأيقونات الثلاث عند `x=197`، والتسميات الثلاث تنتهي
عند `186`، والقيمتان تبدآن عند `37`.

**🔴 ④ أسماء أيام الرسم البياني تتداخل — لأن `weekday:'short'` بالعربية ليست مختصرة.**
‏Intl يردّ «الخميس» و«الأربعاء» كما هما (‏`short` = `long` في العربية)، والإنجليزية
تردّ «Thu». والخانة في `createBarChart` عرضها **38 وحدة**، و«الخميس» تقيس **45.5**
عند 12px ⇒ ثلاثٌ من سبع تتداخل فعلًا. العلاج شقّان: مصفوفة اختصارٍ صريحة بلا أداة
تعريف (أحد · اثنين · … · سبت) و`.bar-x` إلى **10px**. **مقيس بعده: صفر تداخل،
وأصغر فجوة 8.8 وحدة.** ⚠️ ولا قصَّ محرفين من مخرَج Intl: يكسر عند أوّل لهجة تختلف.
⚠️ **وتصغيرُ الخطّ يكبّره على الشاشة**: الـSVG يتمدّد إلى عرض حاويته، فviewBox أضيق
⇒ مقياسٌ أعلى. الرقم المكتوب في الورقة ليس الرقم المرسوم.

**⑤ المساعد الذكي: الدالّة غير منشورة — والتطبيق يقول ذلك صراحةً.**
مقيس: `POST /functions/v1/ai` ⇒ **404**. لا عطلَ في الكود؛ البند 1ي في «معلَّق على
المالك» هو الجواب (`supabase functions deploy ai` ثمّ `secrets set GEMINI_API_KEY`).
والتوصيات المعروضة اليوم **محسوبة من أرقام المالك** وموسومةٌ بذلك (م5).

**🔴 ⑥ انتقال الصفحات «بطيء وبعلق» — والحركة كانت تبدأ قبل أن تُبنى الصفحة.**
كان صنفُ الاتجاه يُضاف ثمّ تجري خطّافاتُ الصفحة (`renderPlaces` ·
`renderOwnerDashboard` · `loadPlayerBookings`) وهي تبني عشرات العقد **أثناء
الانتقال** ⇒ كلّ تعديلٍ يُبطل تخطيطَ الطبقة المتحرّكة فتُعاد رسمتُها كلَّ فريم.
ولهذا كان العطل **متقطّعًا**: التبويبات الخفيفة سلسة والرئيسية ولوحة المالك تتلعثم.
الآن الصنف يُضاف **بعد** الخطّافات، في المهمّة نفسها فلا وميض — ومكسبٌ ثانٍ: خطّافٌ
يُحوّل إلى صفحةٍ أخرى (`return showPage(...)`) لم يعد يُشغّل انتقالًا لصفحةٍ تُهجَر
في السطر التالي. **وأصنافُ المغادرة تُمسح في حلقة إعادة الضبط** لا بالمؤقّت وحده.
**مقيس: عشرون نقرة متتالية ⇒ أقصى عددٍ يحمل `pg-leave` في أي لحظة = 1، وصفرٌ بعد
الاستقرار.**

**⑦ مبدّل «ملاعب/مباريات» ملتصقٌ بشريط المناطق — بالبكسل.** `.mode-row` بحشوة
سفلية `0` والغلاف اللاصق تحته سطحٌ معتم ⇒ **الفجوة صفر** (مقيس)، فتُقطَع حوافّ
المبدّل ويُقرأ الاثنان كتلةً واحدة. صارت **10px** (مقيس بعده).

**⑧ «عن الملعب»: قسم «الموقع» حُذف** — كان يطبع `placeLocation(place)`، وهي **نفس**
الجملة تحت اسم المكان في الرأس، وفوقها زرُّ «الخريطة» الذي يفتحه فعلًا. ثلاثةُ
مواضع لحقيقةٍ واحدة، اثنان منها لا يفعلان شيئًا. (بقي القسمان: المرافق · التقييمات.)

**⑨ الدفع صار حقلًا لا حاشية** — سطرُ «نقداً في الملعب» كان يطفو بلا تسمية بين
«نوع المباراة» وحاشية الطلب. الآن فوقه **«طريقة الدفع»** بنفس مكوّن `.pay-pick-title`
المستعمل فوقه حرفيًّا (مقيس: الفجوة 9px).

**⑩ «رقم الحجز» حُذف من الإيصال.** المعرّفات صارت `uuid` يوم انتقلت القاعدة إلى
Postgres ⇒ كان يُطبَع سطرٌ من ستّةٍ وثلاثين محرفًا لا يحفظه أحد ولا يُطلَب في أيّ
مسار: الملعب يعرف صاحبه بالاسم والهاتف والموعد، و`/admin` تبحث بالهاتف. رقمٌ لا
يُستعمَل في محادثة ليس مرجعًا.

**⑪ «حسابي» رُتِّبت بالتكرار لا بالتصنيف.** كان ما يُلمَس كل يوم (الثيم واللغة)
رابعًا تحت طيّةٍ وبطاقةٍ لا فعلَ فيهما. الآن: بيانات ⇐ **تفضيلات** ⇐ كلمة السرّ
(طيّة) ⇐ **بطاقاتي (صارت طيّة)** ⇐ الجلسة. و«شوف شاشات الترحيب» **حُذف بقرار
المالك**: سطرٌ لفعلٍ لا يفعله مستخدمٌ مرّةً في عمره، وُضع لتيسير الاختبار ويدفع
ثمنَه كلُّ لاعب. **مقيس: خمس بطاقات، ارتفاع الصفحة 1453px، و«بطاقاتي» 94px بدل ~200.**

**🔴 ⑫ نقاط شاشات الترحيب تنزاح في الشريحة الأخيرة وحدها.** التعليق في الورقة كان
يَعِد بأن «الطرفين `flex:1` ⇒ النقاط في المنتصف مهما اختلف العرضان» — وكان صادقًا
حتى تُسقط `.obs-go.is-last` تلك القاعدة لتصير زرًّا ممتلئًا بعرض نصّه. **مقيس على
375px:** مركز النقاط **161.4** ومركز الذيل **187.6** ⇒ انزياح **26.2px**، وفي
الشريحة الأولى **2px بالمصادفة** (‏127.6 مقابل 131.6). صار الذيل شبكة
`1fr auto 1fr` ⇒ المساران الطرفيان متساويان **بالتعريف**. **مقيس بعده: الانزياح
`0.0` في الأولى والأخيرة معًا** رغم أن عرض الزرّ يقفز 69.2 ⇐ 103.4.
⚠️ **والدرس مسجَّلٌ من قبل بصيغةٍ أخرى**: تعليقٌ يصف نيّةً لا سلوكًا أخطر من غيابه.

**⑬ انتقال الشرائح صار بعمق.** الصورة تتخلّف عن اللوح **بخُمس** المسافة (‏`--s` لكل
شريحة بـ`nth-child`، والإزاحة من **بُعدها** عن الحالية لا من رقم الحالية وحده)،
والعنوان والسطر يصلان بعدها بفارقٍ صغير. والحالة الافتراضية **مرئية** والإخفاء على
غير النشِطة ⇒ أسوأ فشلٍ «بلا حركة» لا «عنوانٌ غير مرئي». و«تقليل الحركة» يوقفها كلَّها.

**🔴 ⑭ الموقع: أرقام الخطوات فوق نصّها — و`@media` لا تضيف نوعيّة.**
`.steps-lean .step{padding-block:18px}` نوعيّتها (0,2,0) تغلب `.step{padding-top:66px}`
(0,1,0) **ولو كانت داخل `@media`** ⇒ فوق 820px يقع العنوان **داخل** دائرة الرقم.
**مقيس عند 1400px:** الدائرة 1352.6…1400.6 والعنوان 1370.6…1396.2 — أي أن السطر
كلَّه داخل الرقم. بقاعدةٍ مكافئة داخل الاستعلام: **فسحة 18px** (مقيس بعده).

**⑮ صفحة الملعب على الموقع صارت اسمًا وموقعًا وفعلًا واحدًا** (قرار المالك): حُذفت
شبكةُ الحقائق (عدد الملاعب · مدى السعر · الأرضية) وجدولُ الملاعب وقائمةُ المرافق
والنجوم. **ولا فراغ صامت**: سطرٌ يقول إن الأسعار والأوقات والمرافق **داخل التطبيق
تتحدّث لحظةً بلحظة** (م5). وصفحةٌ ثابتة تطبع سعرًا وقت البناء نسخةٌ ثانية من الحقيقة
تنحرف بين النشرتين، وأقصى ما تستطيعه «نزّل التطبيق» على أي حال.

**🔴 ⑯ أشرطة `/admin` بأطوالٍ مختلفة — والمقارنة البصرية نفسها تكذب.** كان كلّ
`.fn-row` شبكةً مستقلّة بعمودٍ أوّل `minmax(84px,auto)` ⇒ يتّسع بمقدار **تسميته هو**.
**مقيس في «ساعات الذروة»:** التسميات 84 · 88.3 · **103.8** فالمسارات 365.4 · 361.1 ·
**345.6**. والأثر ليس شكليًّا: شريطٌ ممتلئ ١٠٠٪ في مسارٍ أقصر يبدو **أقلَّ** من
شريطٍ أقلّ في مسارٍ أطول. صار `.funnel` هو الشبكة و`.fn-row{display:contents}` ⇒
**عرضٌ واحد وxٌ واحد لكل رسم** (مقيس في الثلاثة، وعلى 375px كذلك بصفر تجاوز أفقي).

**🔴 ⑰ «١ ترحيل ناقص» في `/admin` كانت إيجابيّةً كاذبة.** الفحص ينادي الدالّة
بجسمٍ **فارغ**، وPostgREST يحسم الدالّة **باسمها وأسماء وسائطها معًا** ⇒ دالّةٌ لها
وسائط إلزامية تردّ `PGRST202`، وهو **نفس ردّ «غير موجودة»**. النداء بالوسائط
الحقيقية أعطى `42501 permission denied` — **دليلَ وجود**. صار كلُّ فحصِ دالّةٍ يحمل
وسائطه بقيمٍ لا تطابق شيئًا (uuid أصفار) فلا أثرَ يقع، وفحصُ 18 انتقل من **الجدول
إلى الدالّة** (‏`resolve_field_price`) لأن الجدول وحده لا يُسعّر شيئًا. وأُضيف
إلى القائمة: 22 · 28 · 29 · **30**.

**🔴 ⑱ والعطل الحقيقي الذي كشفه الفحص: `bookings_full` ينقصه خمسة أعمدة.**
انظر الصفّ في جدول الترحيلات أعلى الملفّ و[`30_bookings_full_refresh.sql`](migration/30_bookings_full_refresh.sql).
**وهو أثقل ما في الدفعة أثرًا**: «لم يحضر» والمباريات المفتوحة وتمييز «انقضت
المهلة» كلّها معطَّلة في التطبيق منذ تشغيل 15/16/22 — **بلا أن يصرخ شيء**، لأن
`sbBookingsQuery` تتراجع ثلاث جولات فتنجح الأخيرة.

**⑲ «لون البطاقة في التطبيق» حُذف من نموذج الإضافة.** سؤال المالك «شو بأثّر
بالزبط؟» جوابُه: **لا شيء**. العمود `places.color` يصل التطبيق (`sbPlace`) ثمّ لا
يقرؤه أحد — صفر استعمال في الرسم أو الورقة، بقيّةُ تصميمٍ قديم. حقلٌ يطلب قرارًا بلا
أثر هو بالضبط ما ترفضه م5. والعمود يبقى في القاعدة بقيمته الافتراضية ⇒ **بلا ترحيل**.

**مقيس:** `npm run check` يمرّ · `npm test` **73/73** · صفر تجاوز أفقي على 375px ·
صفر خطأ console من كود التطبيق (والـ400 الباقية هي تراجع أعمدة `bookings_full`
نفسه، ويُنهيه الترحيل 30) · وسبع بطاقات تُرسَم. ✅ **واختُبرت على جهاز حقيقي — 2026-08-18، أكّده المالك.** (‏تأكيدُ استعمالٍ لا قياسُ أداة: TalkBack وتنميطُ الأداء خارجه.)

---

**الدفعة الثلاثون — تشغيل الـAI: أربعة أعطال متتالية، كلٌّ منها يخفي الذي تحته (2026-08-13)**

اللوحتان (المستشار · ملخّص التقييمات) تعملان الآن على جهاز المالك. والطريق إليهما
مرّ بأربعة أعطال **مختلفة الأسباب ومتطابقة الأعراض**: كلّها تُعرَض للمالك جملةً
واحدة — «تعذّر توليد التحليل حالياً» — والسببُ الحقيقي في كل مرّة كان في حقل
`detail` من ردّ الدالّة، لا يراه إلّا من فتح كونسول المتصفّح.

| # | العرَض | السبب الحقيقي | العلاج |
|---|---|---|---|
| ① | `404` على الدالّة | لم تُنشَر أصلًا | `functions deploy ai --use-api` |
| ② | `404 no longer available` | جوجل **أوقفت** `gemini-2.0-flash` (الافتراضي) | `AI_MODEL` |
| ③ | `404 no longer available **to new users**` | `gemini-2.5-flash` **موجودٌ ومحجوب** عن الحسابات الجديدة | `gemini-flash-latest` |
| ④ | `401 Expected OAuth 2…` | المفتاح بصيغة `AQ.Ab8…` لا `AIza…` — تُقبل من الجهاز وتُرفض من خادم | مفتاح قياسي جديد |
| ⑤ | `unparseable` | `maxOutputTokens: 1024` يقطع الردّ العربي في منتصفه | `8192` |

**🔴 ① وأخطر ما في السلسلة: «موجود» لا يساوي «مسموح».** `ListModels` بنفس المفتاح
كان يُرجع `gemini-2.5-flash` في قائمته، ثمّ `generateContent` عليه يردّ 404. فحصُ
الوجود في الكتالوج **لا يُثبت الإذن بالاستدعاء** — والفحص الصحيح أن تستدعي فعلًا.
وهو أخو المزلق المسجَّل في فحص الدوالّ (‏`PGRST202` عن دالّةٍ موجودة): **في
الحالتين ردٌّ واحد لمعنيين، والتمييز يحتاج نداءً حقيقيًّا لا استعلامَ فهرس.**

**🔴 ② واسمُ النموذج أصلٌ مؤرَّخ لا ثابت.** ماتَ افتراضيّان في يومٍ واحد، بطريقتين
مختلفتين (إيقافٌ تامّ · حجبٌ عن الحسابات الجديدة). فصار الافتراضي **لاحقة**
`gemini-flash-latest` لا لقطةً مثبَّتة: تنجو من النوعين لأنها تُحلّ دائمًا إلى ما
هو مسموحٌ لحسابك. **والثمن مقبولٌ هنا وحده** — مخرَجُ هذه اللوحات فقرةٌ استشارية
قصيرة، لا شيء ينكسر إن تبدّلت صياغتها؛ ومكوّنٌ حسّاسٌ لانحراف المخرَج يستحقّ
التثبيت مع مراقبةٍ لا اللاحقة.

**🔴 ③ وسقف التوكنات يفشل كخطأ صياغة لا كخطأ طول.** عند 1024 كانت **التقييمات
تنجح والمستشار يفشل**، والفرق هو الدليل: الأولى تطلب ملخّصًا ≤٥٠ كلمة، والثاني
**٣–٥ بنودٍ كلٌّ ≤٤٦ كلمة**. والعربية أثقل بالتوكنات من الإنجليزية التي قُدِّر
عليها السقف بالعين ⇒ يُقطع الردّ في منتصف كائن، فيجد `parseModelJson` قوسَ إغلاقٍ
داخليًّا من عنصرٍ اكتمل، ويُسلّم `JSON.parse` جسمًا مبتورًا ⇒ `null` ⇒ «غير قابل
للتحليل». **لا شيء في السلسلة يقول «طويل».** ومعه سببٌ ثانٍ يتراكم: اللاحقة تُحلّ
إلى نموذجٍ **مفكِّر**، وتوكنات تفكيره تُخصَم من **نفس** السقف — فقد يُستهلك كلُّه
قبل أن يخرج محرفُ إجابةٍ واحد.
⚠️ ومعها أُصلح افتراضٌ ثانٍ: كان يُقرأ `parts[0].text` وحده، والنموذج المفكِّر قد
يُعيد جزءَ تفكيرٍ **قبل** جزء الجواب ⇒ يُسلَّم المحلّلُ نثرًا. صارت **تُجمَع
الأجزاء كلُّها**.

**④ والخطأ صار يشخّص نفسه.** `unparseable` عاريةً كلّفت جولةً كاملة مع المالك،
والجوابان اللذان يحسمانها كانا في الردّ الذي نملكه أصلًا. صارت ترجع
`unparseable finish=<سبب> len=<طول> head=<أوّل ٩٠ محرفًا>` — و`finishReason`
وحده يفصل «قُطع لطوله» عن «رُفض لسلامة المحتوى» عن «ردّ نثرًا».
⚠️ **وهذا هو درس الدفعة كلّها**: أربع مرّات متتالية كانت الرسالة المعروضة صادقةً
وعديمةَ الفائدة، والسببُ خطوةً واحدة تحتها. **رسالةٌ لا تسمّي سببها تُحوّل العمل
من قراءة سطر إلى إعادة إنتاج عطل** — وهي نفس القاعدة المسجّلة في «صدقُ الرسالة لا
يكفي»، مطبَّقةً هذه المرّة على الرسائل التي يقرؤها **المطوّر** لا المستخدم.

**⑤ وأدواتٌ لا علاقة لها بالمشروع أوقفت العمل مرّتين** — تستحقّ التسجيل لأنها
ستتكرّر: `npx` في PowerShell يشغّل `npx.ps1` فيصطدم بسياسة تنفيذ السكربتات
(`UnauthorizedAccess`) ⇒ **`npx.cmd` يتجاوزها بلا تغيير أي إعداد نظام**. و`curl`
في PowerShell **ليست curl** بل اسمٌ مستعار لـ`Invoke-WebRequest` لا يعرف `-s`
فيتعلّق سائلًا عن `Uri`. وأمرٌ متعدّد الأسطر يُلصَق في الطرفية ينكسر سطرًا سطرًا
⇒ **كل أمرٍ يُعطى للمالك يُكتب سطرًا واحدًا بفواصل `;`**.

**⑥ وزلّةُ مسافةٍ واحدة**: `GEMINI_API_KEY=…--project-ref` بلا مسافة ⇒ يبتلع
العلَمَ في قيمة السرّ. والتحقّق لا يحتاج كشفَ السرّ: `secrets list` يُرجع
**بصمة sha256** لكل قيمة، فحسابُ بصمة القيمة المتوقّعة محلّيًّا ومقارنتُها يُثبت
المخزَّن **بلا أن يخرج المفتاح من الجهاز**. وبها ثبت أن المفتاح والنموذج كليهما
مخزَّنان نظيفَين، فانتقل البحث إلى المفتاح نفسه — وهناك كان العطل.

**والاستهلاك على حساب Google الخاصّ بالمالك.** لوحتان تستدعيان النموذج لا ثلاث
(الطقس من Open-Meteo بلا مفتاح)، وكلٌّ بشرط: المستشار يحتاج **٧ أيام حجزٍ فعلية**
والتقييمات تحتاج **تقييمًا واحدًا** — وما دونهما يردّ بلا استدعاء. والنتيجة
تُحفَظ في **الجلسة وحدها** (‏`State.ai` في الذاكرة لا في التخزين) ⇒ كلُّ إعادة
فتحٍ للتطبيق استدعاءان جديدان. ولعشرين مالكًا بمعدّل فتحتين يوميًّا ≈ **٣٠٠٠
استدعاء شهريًّا** على طبقة `flash` — حملٌ خفيف. ⚠️ **ولا رقم بالدينار هنا**:
الأسعار والحصص تتغيّر، والقياس الوحيد الصادق `aistudio.google.com/usage`.

---

**الدفعة الحادية والثلاثون — مراجعتان خارجيتان: حارسان، وعطلُ إتاحةٍ لم يذكره أحد (2026-08-14)**

مراجعتان (كلود · جيميناي) فُحص كلُّ بندٍ فيهما **مقابل الكود**. والخطّة المنفَّذة
كلُّها في [`docs/plans/PLAN-3-reviews.md`](docs/plans/PLAN-3-reviews.md) — **اقرأها
قبل أي عمل**، وفيها قرارات المالك الأربعة (2026-08-14): **عمولة** كما هي الاتفاقية
(⇒ لا بوّابة دفع ولا اشتراك) · **التطبيق وحده** (⇒ لا ويب ولا `/game/<id>`) ·
**قناة الإشعار مؤجَّلة** حتى يقرأ المقارنة · ونطاق التصميم مفتوح بأربعة بنود.

⚠️ **وثلاثة بنودٍ في المراجعتين خاطئة ولا تُنفَّذ:** ① **ES Modules** (جيميناي) —
الـAPK يحمّل `index.html` من `file://` والوحدات من أصلٍ `null` **يحجبها WebView**،
والهدف صحيح والآليّة قاتلة ⇒ الصيغة الصحيحة تقسيمُ المصادر ودمجُها في `build.ps1`.
② **قوائم الانتظار** — مبنيّة ومُشغَّلة منذ `20_slot_watch`. ③ وبندان أعاد كلود
طرحهما **وقد رفضهما المالك في الدفعة ٢٨** (مبدّل «لاعب/صاحب ملعب» · صورة الهيرو).

**① [`tools/check-view-parity.mjs`](tools/check-view-parity.mjs)** — العطل الذي
كلّف ثلاث دفعات (`select b.*` تتجمّد عند الإنشاء) أُصلح بترحيل `30`، **وكُتب
العلاج قاعدةً في وثيقة**. صار حارسًا: يقرأ `SB.URL`/`SB.KEY` من `app.js` نفسه،
ويستخرج الأعمدة من `migration/*.sql` **بالجملة حتى الفاصلة المنقوطة** لا سطرًا
سطرًا (‏`16` و`22` يكتبان عدّة `add column` تحت رأسٍ واحد — وهي بالضبط الأعمدة
التي غابت). ومُتحقَّق في ثلاثة مسارات: يمرّ (٢٧ عمودًا) · وُجِّه إلى `open_games`
فأخرج **١٨ باسمها** · وبمضيفٍ لا يُحلّ **تخطّى بصوتٍ عالٍ وخرج بصفر** (‏`check`
يُشغَّل بلا إنترنت كثيرًا، وحارسٌ يفشل لانقطاع الشبكة حارسٌ يُعطَّل بعد أسبوع).

**② 🔴 وكشف العملُ نفسه عطلَ إتاحةٍ لم تذكره أيّ مراجعة:** كتلة
`@media(prefers-contrast:more)` كانت عند السطر ٧٧٧، ثمّ يعيد قسم `NATIVE APP
REDESIGN` تعريفَ `--line`/`--line-2` على `:root` **بعدها**. و`@media` لا تضيف
نوعيّة ⇒ **التجاوز ميتٌ تمامًا**: من يطلب تباينًا أعلى من نظامه كان يحصل على
حدودٍ بعتامة `.09` بدل `.42`. مقيس في المتصفّح: بتفعيل الشرط قسرًا لم تتغيّر
`--line` بمحرف. والعلاج **موضعُ الكتلة** (نُقلت إلى §30) — نفس عائلة عطل «تقليل
الحركة» (الدفعة ٢١، §29) حرفيًّا.

**③ تسطيح التوكنات: ٤٥ تصريحًا ميتًا على ٢٨ توكنًا.** طبقةٌ أصلية وطبقةُ
`NATIVE APP REDESIGN` تعيد تعريف الأسماء نفسها فوقها. حُذف كلُّ ما يعلوه لاحقٌ
تحت نفس المحدِّد (عمليةٌ صفريّة الأثر بالتعريف)، **والحذف بفراغٍ بنفس الطول ⇒
أرقام الأسطر لم تتزحزح**. وعائلة `--radius-*` كلُّها سقطت (خمسةٌ بصفر مستهلك،
و`--radius-full` بمستهلكَين صارا `999px` كما في ٦٦ موضعًا) ⇒ **عائلةٌ واحدة**.
وحارسٌ رابع في `check-css-rules` يمنع العودة، **ويستثني صنفين لأن التكرار فيهما
مقصود**: قاعدةٌ داخل `@media`، وتصريحان متتاليان في نفس الكتلة (احتياطيّ
`cubic-bezier` ثمّ `linear()`). مُتحقَّق: أخرج **٤٥ مخالفة** على الشجرة القديمة.

🔴 **وزلّةُ قياسٍ وقعت وصُحّحت — وهي المزلق المسجَّل حرفيًّا:** قِستُ «صفر فرق»
**مرّتين والصفحة تخدم CSS قديمًا**، لأن `_preview_app.html` **يضمّن** الورقة وقت
البناء ولا يربطها ⇒ لا إعادةُ تحميلٍ ولا `?v=` تُحدِّثها. كشفه أنّ القرص خلا من
`--radius-` بينما `getComputedStyle` ما زال يردّ `999px`. **أيّ قياسٍ للتطبيق
يقع بعد `build.ps1` لا بعد حفظ الملفّ.** وبعده: **صفر فرق في العناصر** — ٢١
عنصرًا × ١٧ خاصّية × ثيمين = ٧١٤ قياسًا.

**④ شاشة التنبيهات (طلب المالك).** كانت **قائمةً لا شاشة**: صفوفٌ مسطّحة تفصلها
شعرة، ونقطةٌ 9px هي كلُّ ما يميّز المقروء، و**أحد عشر نوعًا تُرسَم بالشكل نفسه**.
الآن: أيقونةٌ لكل نوع في مربّعٍ مصبوغ (‏`bell` من **نفس مسار جرس الرأس حرفيًّا**) ·
**ثلاث نبرات** من توكناتٍ مقيسة أصلًا بلا لونٍ جديد (`ok`/`no`/`wait` — والأخيرة
لأفعالٍ تنتظر ردًّا ولا تحسم) · الصفّ بطاقةٌ بفجوة · وغيرُ المقروء على **الشكل**
لا اللون (سطح + شريطٌ جانبي + نقطة) · والوقت انتقل إلى سطر العنوان · وعناوينُ
أيام لاصقة تُحسَب من `created_at` **بتاريخٍ محلّي لا بفرق ساعات** · وتلاشٍ في آخر
٢٢px يقول «تحت هذا مزيد» (مقيس أنّ الرأس اللاصق ما زال يلتصق بعده).
⚠️ **ولم يُمَسّ منطق الإشعارات** — دفعةُ عرضٍ خالصة.

**⑤ البيانات المنظّمة على الموقع.** `SportsActivityLocation` على كل صفحة مكان
(‏١٤ عقدة، كلُّها JSON صالح) ومعها `BreadcrumbList`، و`FAQPage` على الرئيسة.
🔴 **وأربعة حقولٍ لا تُبَثّ، وسببُها لا نسيانُها:** `geo` (لا نخزّن إحداثيات،
و`map_link` رابطٌ مختصر) · `priceRange` و`openingHoursSpecification` (الدفعة ٢٩
حذفتهما من الصفحة بقرار المالك، والبيانات المنظّمة ليست استثناءً بل أسوأ —
سعرٌ بائت يجلس في نتيجة بحثٍ أسابيع) · و`aggregateRating` **كُتب ثمّ حُذف بعد
القياس**: نفس الدفعة أزالت النجوم، فكان سيُوسَم تقييمٌ **لا يُرى في أي مكان** —
خرقٌ لقاعدة جوجل نفسها. ⚠️ و`JsonEnc` لا `HtmlEnc`: الثانية تحوّل `"` إلى
`&quot;` وهو صحيحٌ في سمة وخاطئ داخل `<script>` (المحتوى يمرّ خامًا).

**⑥ الخصوصية: ثلاثة بنود** — نقلُ البيانات خارج الأردن (قانون ٢٤ لسنة ٢٠٢٣
يشترط الإفصاح والموافقة) · الأساس القانوني · وهويّة المسؤول («راسلنا من صفحة
التواصل» لا يكفي: القانون يسأل عن جهةٍ لا عن نموذج).
⚠️ **ولم تُذكر منطقة الخوادم بالاسم لأنها غير مقيسة**: `CF-Ray` تعطي حافّة
كلاودفلير الأقرب **للقارئ** (‏AMM) لا موقع القاعدة. المكتوب «خارج المملكة» وهو
مؤكَّد، والمنطقة بالاسم على المالك من لوحة Supabase.

**⑦ `security-matrix` في CI** على كل دفعةٍ تمسّ `migration/**`، **ويتخطّى بصوتٍ
عالٍ إن غابت الأسرار ولا يفشل** (فحصٌ أحمرُ دائمًا فحصٌ يُتجاهَل).
🔴 **ولا يُشغَّل على الإنتاج**: يُنشئ حجزًا ويُلغيه ويترك الحساب `active=false`
⇒ **معلَّق على المالك: مشروع Supabase اختباري وأربعة أسرار في GitHub.**

**⑧ عمود «عادوا عبرنا»** في جدول أماكن `/admin` — من حجز هنا كم عاد عبرنا.
🔴 **ولا يقيس التسرّب ولا يستطيع أحد**: لا نرى إلّا ما مرّ بنا؛ وحاشيةٌ تحته
تقول ذلك حرفيًّا، وبلاها يُقرأ «تسرّبك = كذا» وهو ادّعاءٌ لا تقوله البيانات.
وعتبةُ ثمانية عملاء قبل أن يظهر رقم، ولا مجموع له في `tfoot`. وبلا ترحيل.

**مقيس:** التباين في الثيمين — العنوان 14.28/15.8 · الجسد والوقت 5.69/8.92 ·
عنوان اليوم 6.21/8.5 · والأيقونة على صبغتها **4.59…11.1** (العتبة غير النصّية 3:1)
· صفر تجاوز أفقي عند 375px **والمرآة تامّة** في الاتجاهين · `npm run check` يمرّ
و`npm test` ٧٣/٧٣ · و`+9,699 native` كما هي · وجدول الأماكن **١٣/١٣/١٣** عمودًا
وارتفاع بطاقته **٢٧٠px كما هو** (بالشبكة المموَّهة، والصفحة تُحذف بعدها).
✅ **واختُبرت على جهاز حقيقي — 2026-08-18، أكّده المالك.** (‏تأكيدُ استعمالٍ لا قياسُ أداة: TalkBack وتنميطُ الأداء خارجه.)

---

**الدفعة الثانية والثلاثون — التصميم: السلّم والنبرة (2026-08-14)**

**① الطباعة: ٣٣ حجمًا ⇒ ١٦.** العطل لم يكن «الأساس 14px» وحده — في الورقة
**٣٣ حجمًا مختلفًا** بين 8.5 و66px، و**٢٨٥ من ٣٧٣** تصريحًا محشورة بين 11 و14
تؤدّي وظائف متقاربة. فرفعُ رقمٍ واحد لا يفعل شيئًا (كل عنصرٍ يكتب حجمه)،
والعلاج **ضغطُ السلّم ورفعُه معًا**: ٣٦٦ تصريحًا حُوِّلت بجدولٍ صريح
(‏12⇒13.5 · 13⇒15 · 14⇒16 …)، **والأحجام العرضية (32px فأعلى) لم تُمَسّ** —
كبيرةٌ أصلًا، ونموّها يأكل ارتفاعًا بلا مكسبٍ في القراءة.
⚠️ و`line-height:1.75` **لم يُمَسّ**: قرارٌ موثَّق لأجل الحركات العربية،
والمراجعة اقترحت خفضه ولم تكن تعرف سببه.
**مقيس: صفر تمرير أفقي للمستند** عند 320 و360 في الاتجاهين، وصفر تجاوز في
البطاقات. (والمتبقّي `.onb-photo` نزيفٌ متناظر مقصود، و`.ftab` داخل مُمرِّر
أفقي — كلاهما بالتصميم، والمستند لا يمرّر.)

**② 🔴 حالة «مختار»: نمطان متعايشان، والمصدر يكذب.** `grep` على `--g-btn`
أعطى ٣٥ موضعًا — و**`getComputedStyle` قال غير ذلك**: `.ftab.active-tab`
مُعرَّفة **خمس مرّات** وآخرُها `background:transparent` ⇒ أربعُ قواعد ميتة.
والقياس كشف الحقيقة: `.ftab` و`.mode-btn` **محايدتان أصلًا**، وسبعٌ أخرى
(‏`.tbtn.sel` · `.day-btn.active` · `.fx-chip.on` · `.sport-tab.active` ·
`.dtab-btn.active` · `.cal-cell.sel` · `.field-switch.on`) ما زالت **تُملأ
Lime كاملةً بظلٍّ أخضر ووزن ٨٠٠**.
والنتيجة أنّ لون الفعل على «احجز» وحده بينما Lime يملأ كلَّ خانةٍ ويوم ⇒
**الفعل الأساسي أضعفُ ما على الشاشة لا أقواه**. وُحِّدت السبعة في **§31**
على نمطٍ واحد: صبغةٌ من لون الفعل + حبرٌ منه + حدٌّ منه، بلا تعبئة وبلا ظلٍّ
ملوَّن ⇒ **المملوءُ الوحيد في الشاشة هو الفعل**. ونفس بنية نبرات الإشعارات.
⚠️ ونسخة `body.dark` مكتوبةٌ بنفس التصريحات **لتساوي النوعيّة** لا لاختلافها:
`body.dark .tbtn.sel` (0,3,0) تغلب `.tbtn.sel` (0,2,0) مهما تأخّرت.
**مقيس في الثيمين: صفر Lime وصفر ظلٍّ ملوَّن، والتباين 6.49 نهاري · 8.46 ليلي.**

**③ «كم فاضي اليوم؟» على البطاقة.** `placeAvailability` تحسب `free` منذ
الدفعة ٨ ثمّ **كانت البطاقة ترميه**: لا يظهر إلّا نقيضُه («غير متاح») عند
الامتلاء. وهو أنفع رقمٍ عليها — من يتصفّح مساءً يسأل «في فاضي الليلة؟».
⚠️ **وقياسٌ لا وعد** (م5): خاناتُ الملعب ناقصَ المحجوز ناقصَ المغلق، ولا
يُعرَض إلّا فوق الصفر — والصفرُ حالتُه الشارةُ القائمة لا رقمٌ يقول «صفر».
⚠️ و`countNoun` لا نصٌّ بعلامة نائبة: **مقيس** «8 أوقات فاضية اليوم» و
«24 وقتًا فاضيًا اليوم» — والصيغة الساذجة كانت ستُخرج «1 أوقات».

**④ الترحيب: أربع شرائح ⇒ واحدة.** المحذوفتان تصفان ما يراه المستخدم بعينه بعد
ثانيتين من التصفّح، والباقية تقول ما **لا يُستنتَج من الشاشة**: أنّ الحجز طلبٌ
يوافق عليه الملعب ويصلك خبره. والمشروطة بـ`GAMES_OK` تُحذف عند أوّل إقلاع (لا
جلسة ⇒ `null`) ⇒ **شريحةٌ واحدة فعلًا**، ثمّ الملاعب الحقيقية مباشرةً.
🔴 **وزلّة تستحقّ التسجيل:** حذفتُ أوّلًا الشريحتين **الخطأ** لأنّي قرأت النصّ
الاحتياطي في `data-i18n` — والدفعة ٢٧ أعادت كتابة الشرائح قصّةً ولم تُحدَّث تلك
النصوص معها، فبقي الوسم يقول «طلبك ينتظر ردّ الملعب» على شريحةٍ نصُّها الحيّ
«ابعث طلبك بضغطة». **النصّ المعروض من `I18N` لا من الاحتياطي، والاحتياطي يبلى
بلا أن يصرخ شيء.** كشفه أنّ العنوان المرسوم خالف الوسم.
ومعها `OBS_VER` ٢⇒٣، وحُذفت ثمانية مفاتيح، والنقاط تُخفى عند شريحةٍ واحدة
بـ`visibility` لا `hidden` (‏`[hidden]` بـ`!important` عامّة، والذيل شبكة
`1fr auto 1fr` يحتاج بقاء الصندوق). مقيس: انزياح المنتصف **0.0**.

**⑤ مسار الحجز في مصفوفة الأمان: ٢٧ ⇒ ٣٢ تأكيدًا.** المراجعة طلبت أربعة،
و**واحدٌ منها كان مغطًّى أصلًا** (`status` يُفرَض `pending`). أُضيف:
**الخانة لا تُباع مرّتين** (‏`bookings_no_double_idx` يشمل `pending`، ولاعبان
على خانة التاسعة هو الحالة العادية) — ويُنظَّف الصفّ بعده · و**الإلغاء داخل
المهلة يُرفَض باسمه** (`cancel_window_closed`) لأن الفرض مُشغِّل لا `with check`.
⚠️ والثاني **زمنيّ بطبعه** (يحتاج خانةً تبدأ داخل النافذة) ⇒ **يتخطّى بصوتٍ
عالٍ** بدل أن يفشل الخامسة فجرًا. و«الانضمام إلى مباراة مكانٍ مخفيّ» لم يُضَف:
يحتاج إخفاء مكانٍ وهو فعلُ أدمن، وتوكن المصفوفة **لاعبٌ عادي عمدًا**.

⚠️ **وبندٌ واحد لم يُنفَّذ: تقسيم `app.js`** — والخطّة نفسها تشترط له «دفعةً
مستقلّة تمامًا بلا أي تغيير سلوكي معها» ومعيارَ «بصمة المخرَج متطابقة بايتًا
ببايت»، وتنفيذُه مع دفعةٍ تغيّر الطباعة واللون والترحيب يُبطل شرطه.

---

**الدفعة الثالثة والثلاثون — لوحة المالك: الفعل أوّلًا، والرقم بمقارنته (2026-08-18)**

مراجعةٌ خارجية على لوحة المالك في ثلاثة محاور: شكل الواجهة · طريقة عرض البيانات ·
الأرقام الناقصة. ونُفِّذت كلُّها، **ومعها أوّل قياسٍ حيّ للوحة المالك في تاريخ المشروع** —
كان مكتوبًا هنا أنها «لم تُقَس حيّةً» لأنها خلف تسجيل دخول، فبُنيت لها شبكةٌ مموَّهة.

**🔴 ① والقياس كشف أنّ الحقن المباشر مستحيل: التطبيق مغلَّف في IIFE.**
جرّبتُ أوّلًا أن أُسند `State.ownerData` من سكربتٍ لاحق فأعطى `State is not defined`
**بلا خطأٍ واحد في الـconsole**، والصفحة تبدو سليمة على شاشة الترحيب. والسبب مكتوبٌ
في رأس `app.js` حرفيًّا: «وحدة واحدة مغلّفة (IIFE) — صفر متغيرات عامة في window».
فالمدخل الصادق **الشبكة**: [`tools/owner-harness.mjs`](tools/owner-harness.mjs) يستبدل
`fetch` بردودٍ **بشكل صفوف القاعدة الخام**، فيجري المسار الحقيقي كلُّه — `sbSession`
ثمّ `sbBookingsQuery` بجولات أعمدتها ثمّ `sbBooking`/`sbField`. والصفحة تُحذف بعد
الفحص (`--rm`) كما تُحذف صفحة `admin-harness`.

**② تبويب «اليوم»: الفعل أوّلًا.**
• **بطاقة «التقارير» المكرّرة حُذفت** — «التقارير» خانةٌ في الشريط السفلي منذ الدفعة ٢٧،
  وبقيت معها بطاقةٌ **تعلو الطلبات**. والمراجعة طلبت أحد اثنين (خانة في الشريط أو أيقونة
  في الرأس) **والأولى قائمة فعلًا**، فالمطلوب حذف الثانية لا إضافة ثالثة.
• **شريط فعلٍ لاصق** («طلبان بانتظار ردّك — ردّ الآن») **يُخفى تمامًا عند الصفر**: لا
  «٠ طلبات» تأكل شريطًا لاصقًا من كل شاشة. مقيس: ضغطةٌ واحدة تنقل التمرير من `0` إلى
  `992` فتصير أوّل بطاقة معلّقة على بُعد 230px من أعلى الشاشة.
• **أربع بلاطات ⇒ بطلٌ واحد + ثلاثةٌ مضغوطة**، و«بانتظار ردّك» **زرٌّ لا عرض**.
• **«القادم الآن»** — سطرٌ واحد يجيب سؤال الواقف عند البوّابة: «خلال ساعة · الاسم ·
  الملعب · الوقت · الهاتف»، وثلاث حالات لا اثنتان (**جارية الآن** حالةٌ قائمة بذاتها،
  فالخانة ساعتان). ولا يُعرَض شيء بلا حجزٍ قادم (م5).
• **المخطّط انقلب: ساعاتٌ صفوفًا وملاعبُ أعمدة**، **وصار الافتراضي لمن له أكثر من
  ملعبين** (والاختيار يُحفَظ ويعلو على الاستنتاج). والسبب سؤالٌ لا ذوق: السطر الواحد
  صار «الثامنة مساءً عبر ملاعبك كلّها» — وهو ما يُسأل — وقراءتُه مسحٌ أفقيّ واحد.
  🔴 **وكان الافتراضي ميتًا**: `State.ownerView` مكتوبةً `'cards'` ثابتةً تُخرج
  `resolveOwnerView` فورًا فلا يُستنتَج شيء أبدًا. صارت `null` = «لم يُحسَم بعد».
• **ورقة قرارٍ سفلية** بدل زرَّين متجاورين على بطاقةٍ في أعلى قائمة طويلة: «قبول»
  مملوءٌ (334×56) و«رفض» زرٌّ نصّي هادئ (334×48) ومعهما واتساب — مقيسة.
  ⚠️ **وبلا نقرةٍ إضافية**: «قبول» كان يمرّ بنافذة تأكيدٍ على أي حال، فالورقة **هي**
  تلك النافذة وقد صارت تحمل القرارين. و«رفض» يبقى بسببٍ إلزامي (`cancel_reason`).
• 🔴 **و`showOwnerTab` كانت تُصعّد إلى رأس الصفحة بعد كل إعادة جلب** — وهي تُنادى من
  `renderOwnerDashboard` عقب **كل** ردّ ⇒ المالك يردّ على طلبٍ في منتصف القائمة فتقفز
  به الصفحة إلى أعلاها، وهو يردّ على عشرة. صار الصعود **عند تبديل التبويب وحده**.

**③ الأرقام: لا نسبة بلا مقامها، ولا نسبة تحت العتبة.**
`OWNER_MIN_N = 7` — **نفس** عتبة `place_reply_speed` (ترحيل ٢٨) التي كانت مطبَّقةً في
القاعدة وحدها ومتروكةً في ستّة مواضع هنا. و`pctRow` تفرضها: تحت العتبة يُقال العدد
وسببُ الامتناع، لا نسبةٌ تُقرأ ثقةً.
• **المقارنة بنفس اليوم من الأسابيع الأربعة الماضية** لا بالأمس (موسميّة الملاعب
  أسبوعية حادّة)، ولا تُرسَم بأقلّ من عيّنتين، **ولا يُعدّ يومٌ سابقٌ لأوّل أثرٍ للمكان
  صفرًا** — عدُّه يجعل كلّ مكانٍ جديد يقرأ «أعلى من المعتاد بـ٤٠٠٪».
• **خريطة حرارية ٧ أيام × ساعات محلّ دونات الإشغال** — رقمٌ واحد يخلط ملعبًا ممتلئًا
  ٨–١١ مساءً وفارغًا نهارًا بملعبٍ نصفِ ممتلئ طوال اليوم: نفس الأربعين بالمئة وقراران
  متعاكسان. ⚠️ والخليّة **عددُ حجوزاتٍ مؤكّدة لا نسبة**: تاريخُ الإغلاقات الماضية غير
  محفوظ في العميل، ونسبةٌ مقامُها مجهول تكذب بثقة. والتمييز الذي يهمّ محفوظ: ساعةٌ **لا
  يعرضها أيّ ملعب** تُرسَم شرطةً لا صفرًا.
• **الرسوم رُبطت بالنطاق** بدلوٍ متكيّف (يومي ٣٠ · أسبوعي ١٣ · شهري ٦) واسمُ الدلو على
  الشارة، ومعها `every` في `createBarChart` فلا تتداخل التسميات. مقيس: أصغر فجوة بين
  تسميتين **16.5** وحدة في الشهري و**58.7** في الأسبوعي و**170.8** في اليومي.
• 🔴 **«الإيراد الضائع» حُذف** — كان (كلّ خانة فارغة × السعر) فيحسب خانة التاسعة صباحًا
  التي لن يحجزها أحد في أيّ كون؛ رقمٌ مخيف بلا فعل يُدرِّب صاحبه على تجاهله. محلُّه
  **ما طُلب فعلًا**: «راحت بلا ردّ» (المنقضية بقيمتها) · «رفضتها» · «ألغاها اللاعب» ·
  «ما حضروا». **والمنقضي ليس المرفوض**: الرفض قرارُك والانقضاء إهمالُك، وجمعُهما في
  «إلغاء/رفض ٪» كان يطمس الفرق الوحيد الذي يستطيع صاحبه أن يفعل شيئًا حياله.
  والطاقة الفارغة تبقى **مقصورةً على الذروة وموسومةً سقفًا نظريًّا لا هدفًا**.
• **المجموعات على السؤال لا على النوع**: «شو صار؟ · وين بضيّع؟ · شو أعمل الأسبوع الجاي؟».
• **تصدير CSV** للحجوزات وللعملاء بـBOM (بدونه يشوّه إكسل كل اسم عربي).
  ⚠️ **والمسار يُختار بحسب أين نعمل، ولا يُدّعى ما لا يقع**: `<a download>` لا يفعل شيئًا
  في WebView أندرويد وWeb Share غير مطبَّقة فيه ⇒ على الجهاز **نسخٌ إلى الحافظة**
  والرسالة تقول «نسخنا» لا «نزّلنا»، وفي المتصفّح تنزيلٌ حقيقي. والدالّة تُرجع ما حدث
  فعلًا (`share`/`download`/`copy`/`abort`/`fail`) لا ما نتمنّاه.

**④ سبعة أرقام كانت في القاعدة ولا تُعرَض للمالك:**
**سرعة ردّك** (وسيط ٢٨ + **أثرُه**: نسبة التأكيد لمن رُدّ عليه ≤١٠د مقابل >ساعة —
والانقضاء التلقائي مستثنًى فعدُّه ردًّا يجعل المُهمِل يبدو أسرعَ كلّما أهمل) ·
**زمن الاستباق** (وسيط `booking_date − created_at`، وحجوزات المالك اليدوية مستثناة:
يُدخلها بعد أن تقع) · **المنقضي ≠ المرفوض** · **الإيراد لكل خانة معروضة** (نسخة
الملاعب من RevPASH) **ذروةً وغيرَ ذروة**، والذروة **تُحسَب ولا تُفترَض** (الساعات التي
تحمل نصف الحجوزات، وتُكتب أسماؤها فيستطيع المالك أن يخالفها) · **عملاء لا حجوزات**
(فريد · عائد بمقامه · أعلى ١٠ بالقيمة · **ومنقطعون منذ ٣ أسابيع بأرقامهم** — والقائمة
هي القابلة للفعل لا النسبة) · **أثر المباريات المفتوحة** · **و«لم تُحصَّل»** تحت
الإيراد ومعها «هذا التزام مش نقد — الدفع بصير عندك في الملعب» (لا بوّابة دفع، فما
يُعرَض وعدُ دفعٍ لا مبلغٌ في الجيب).
🔒 **ولا باب خصوصيةٍ جديد**: المالك يرى `customer_phone` لحجوزات ملعبه أصلًا عبر RLS،
وهذا تجميعٌ لما يراه لا كشفٌ لما لا يراه.

**⑤ وثلاثة أعطال صامتة كشفها العمل نفسه، لا المراجعة:**
① 🔴 **تعبيرٌ نمطيّ فيه علامة اقتباس أعمى `check-globals` عن الشجرة كلّها.**
`/[",\n]/` في `csvCell` جعلت الحارس يقرأ `"` بدايةَ نصٍّ ويبتلع ما بعده ⇒ **ثمانية عشر
بلاغًا كاذبًا** عن أسماء معرَّفة فعلًا (`askConfirm` · `showPage` · `buzz` …)، وكلُّها
معرَّفةٌ بعد موضع التعبير. **وبلاغٌ كاذب واحد يكفي ليُعطَّل الحارس كلُّه** — درسٌ
مسجَّل. والبديل بلا regex أوضح على أي حال.
② 🔴 **`--line-1` مستعمَل خمس مرّات وغير معرَّف قطّ** — الحدود تعود `currentColor`
صامتةً (خلايا المخطّط · الشرائح · شريط المقاعد). صار `--line`.
③ **`<body>` حرفيًّا داخل تعليقٍ في `app.5-actions.js`** — غير ضارٍّ اليوم، وهو من عائلة
المزلق المسجَّل في `build.ps1` (`</body>` في تعليق يحقن طبقة الهاتف مرّتين). أُبدل بـ«body».

**مقيس** (شبكة `owner-harness` على 375px، والانتقالات معطَّلة قبل كل قياس):
`npm run check` يمرّ · `npm test` **73/73** · **صفر تجاوز أفقي** في التبويبات الأربعة
وفي الاتجاهين · **صفر خطأ** في فخّ الأخطاء المحقون قبل سكربت التطبيق ·
**والتباين محسوبٌ بتركيب الشفّافية** على ٢٣ زوجًا: أدنى نهاري **5.41** وأدنى ليلي **7.23**.
🔴 **وزلّة قياسٍ وقعت مرّتين وصُحّحت**: قرأتُ «1.15:1» على شريط الفعل ثمّ «1.04» على
خليّة الخريطة — والأولى لأنّ الخلفية `rgba(…,.12)` **تحتاج تركيبًا** لا قراءةً، والثانية
لأنّي قِستُ خلفية **الأب** وتجاهلتُ خلفية العنصر نفسه. وبعد التصحيح ظهر خرقٌ **حقيقي**
واحد: `.heat-cell.l3` كانت **2.94** نهاريًّا ⇒ الرقم صار على الدرجة الرابعة وحدها
(‏9.76 نهاري · 7.23 ليلي)، واللونُ هو الكميّة والعددُ الدقيق في `title` وفي سطر الملخّص.
✅ **واختُبرت على جهاز حقيقي — 2026-08-18، أكّده المالك.** (‏تأكيدُ استعمالٍ لا قياسُ أداة: TalkBack وتنميطُ الأداء خارجه.)

⚠️ **وبصمة `+9,699 native` المكتوبة في هذا الملفّ صارت `+12,556`** — لا انحراف: `native.js`
كبر مع إشعارات الدفع (الدفعة ٣١)، والحارس يشترط **تطابقًا واحدًا** لـ`</body>` وهو يمرّ.

---

**الدفعة الرابعة والثلاثون — الرئيسية تصير شبكة Bento (2026-08-18)**

مراجعةٌ خارجية اقترحت تحويل المنتج إلى منصّة شاملة: شبكة Bento على الرئيسية بخمسة
أقسام (ملاعب · مباريات مفتوحة · متجر · أكاديميات · بطولات). **وقرارات المالك عليها
ثلاثة**، اتُّخذت بعد أن عُرض عليه القياس:

| سُئل | قراره |
|---|---|
| أيّ الأقسام حقيقيّ؟ (‏`products`/`orders`/`academies`/`coaches`/`tournaments`/`offers` **كلُّها 404** — مقيس) | **الثلاثة قادمة** |
| شبكة Bento تضيف شاشةً قبل التصفّح وتعاكس أربع دفعات جرّدت الرئيسية (‏١٤ · ٢٢ · ٢٥ · ٢٦) ومنها بلاغاته هو | **Bento كامل بخانات «قريبًا»** |
| «ملاعب إناث» شريحةً ظاهرة؟ (‏**صفر من ١٥ ملعبًا** صرّح بجنسه) | **تبقى في ورقة الفلاتر** ⇒ لا عمل |

**① و«قريبًا» هنا ليست خرقًا لم5 بل نمطُ المشروع نفسه.** الرياضات الخمس تحمل الشارة
منذ الدفعة السادسة، و«فيزا» تحملها منذ التاسعة — والقاعدة المشتركة أنّ الشارة **تقول
الحقيقة** ولا تَعِد بما لا يقع: البطاقة **لا تفتح على فراغ**، ونقرُها يقول ما هي
(«المتجر لسّه ما فتح — رح نخبرك أوّل ما يجهز»). الخرق أن تُعرَض بابًا يُفتَح على شاشةٍ
بيضاء أو على منتجاتٍ مخترَعة، ولا واحدة منهما هنا. **ولا وعدَ بموعد**: تاريخٌ لا نملكه
هو الكذبة لا الشارة.
⚠️ **ولا علَم قاعدةٍ لهذه الثلاثة** (خلافًا لقرار ٧ ظاهريًّا): قرار ٧ يمنع أن **يَعِد**
الوجهُ بما لا تعطيه القاعدة، وهذه لا تَعِد بشيء قابلٍ للحجز. وكلٌّ منها يحتاج **شاشةً
كاملة** يوم يوجد (كتالوج · حصص · جدول دوري)، أي إصدارًا على أي حال — فعلَمٌ يقلبها بلا
نشرٍ يفتح بابًا على لا شيء. تُبنى شاشتُها في دفعتها، وتسقط الشارة معها.

**② الشبكة صفحةٌ لا لوحٌ فوق القائمة.** `#page-hub` صار مهبط الجلسة (الضيف والداخل
والمسجَّل الجديد)، و«الرئيسية» في الشريط تشير إليه، **والتصفّح ابنُه**: `navKey='hub'`
حين تُعرَض `home` فتبقى الحبّة مكانها، والرجوع من التصفّح يعود إليها. و`backable()`
صار جذرُها `hub` بدل `home` — وإلّا صارت الشبكة مصيدةً بلا رجوع أو صار التصفّح جذرًا
لا يُرجَع منه. **مقيس**: بطل ⇒ تصفّح · رجوع ⇒ شبكة · بطاقة المباريات ⇒ تصفّح **في وضع
المباريات** · قسمٌ قادم ⇒ **يبقى مكانه** ويظهر التوست.

**③ الرتبة من الحجم لا من اللون:** البطل بعمودين ويحمل **لون الفعل وحده** (‏§31)،
وخلفه مخطّط الملعب — **نفس `COURT`** الذي يعرضه قسم الرياضات، لا رسمٌ ثالث لنفس المعنى.
والباقي أسطحٌ محايدة بحدّ، بلا `backdrop-filter` ولا ظلٍّ ملوَّن (خمس بطاقات × طبقة دمج
هو بالضبط ما أُزيل في §9 و§13).
🔴 **وعطلٌ كشفه النظر لا القراءة:** `fill:none;stroke:currentColor` مكتوبةٌ في
`.empty-court .court-svg` **وحدها**، فالمخطّط خارجها يُملأ أسودَ افتراضًا وخرج **كتلةً
صمّاء**. أُعيدت القواعد في محدِّر الشبكة، **ولم يُوسَّع محدِّر الحالة الفارغة**: توسيعه
يجرّ الرسم إلى كل موضعٍ لم يُقصَد.

**④ شارةٌ حيّة على «مباريات» — في المبدّل والشبكة معًا.** العدد مقروءٌ من `open_games`
داخل **نفس** نداء الفحص (`sbProbeGames` صار يجلب صفوفًا بدل `limit=0`) ⇒ صفر طلبٍ إضافي.
ويُصحَّح من الجلبة الكاملة حين تقع، **ويُخفى عند الصفر** (مقيس: ٣ من أربع مباريات —
تستثني الممتلئة — ثمّ يختفي كلُّ أثرٍ للشارة حين يردّ العرض `[]`).

**⑤ وثلاثة مزالق مسجَّلة وقعت في هذه الدفعة نفسها:**
① **نصٌّ مركَّب من معطيات لا يمسّه `data-i18n`** — بطاقة المباريات بقيت **عربيّةً في
الإنجليزية** حتى أُضيف `renderHub()` إلى مسار تبديل اللغة (نفس علاج `Notifs.paint`).
② **`opacity` تهدم التباين ولا تضربه — و`getComputedStyle(el).color` لا يراها**، أي
أنّ **القياس نفسه يكذب**. كانت `.86` على سطر البطل الفرعي فأُزيلت، واللون كامل والتراتب
من الحجم والوزن. مقيس بعدها: **صفر عنصرٍ نصّيّ بشفافية** في الشبكة.
③ **السهم فيزيائي والاتّجاه منطقي** ⇒ `scaleX(-1)` في RTL كـ`.own-entry-arw` بالحرف.

**⑥ وخمسة تعديلات على الشبكة نفسها (نفس اليوم، طلب المالك):**
• **مربّع «ملاعب سيدات»** — وجاهزيّته **من القاعدة لا من الكود** (قرار ٧، ونفس
  `sportHasVenues` بالحرف): ملعبٌ نشِط واحد يصرّح `gender='women'` يفتحه، وقبله يحمل
  «قريبًا» ونقرُه يقول السبب — فلا يفتح على قائمةٍ فارغة، **ولا يحتاج نشر نسخة** يوم
  يملأ المالك الحقل من `/admin`. ويفتح **نفس** صفحة الملاعب بمرشِّح الجنس في `State.fx`
  (مقيس: شريحة «سيدات» تظهر في صفّ الفلاتر المفعّلة فتُرفَع من حيث يُرفَع أيّ مرشِّح).
• **الشريط السفلي لا يظهر على الشبكة** — `hub` خارج `NAV_OF` عمدًا، والغياب هو الآليّة.
  ويبقى زرّ «الرئيسية» فيه **مخرجًا مرئيًّا** من التصفّح إلى الشبكة، وإلّا صارت العودة
  حكرًا على من يعرف إيماءة الرجوع (مصيدةٌ مسجَّلة).
• **«حسابي» صعد إلى رأس الشبكة** بجانب الجرس وخرج من الشريط ⇒ الشريط ثلاثة أزرار.
  والأفاتار **حرفُ الاسم** لا أيقونةُ شخصٍ عامّة.
• **مبدّل «ملاعب/مباريات» حُذف من صفحة التصفّح** — المباريات تُفتَح من مربّعها وحده،
  والقائمة `#gamesList` باقيةٌ في مكانها فلا صفحةَ ثالثة لنفس المحتوى. ودخولُ صفحة
  التصفّح يعود إلى وضع الملاعب دائمًا، وإلّا وجدها من زار المباريات سابقًا بلا مخرج.
• **لوح «حجزك القادم» انتقل إلى الشبكة** — يراه صاحبُ الحجز أوّل ما يفتح التطبيق بدل
  أن يكون خلف نقرة، ولا يُعرَض لمن لا حجز له (م5).

**🔴 وثلاثة أعطال أدخلها هذا التعديل نفسه وكُشفت بالقياس:**
① **حذفُ المبدّل أسقط الفحص معه.** `sbProbeGames` كان يُنادى من `updateModeSeg`، وهي
  تبدأ بـ`if(!seg) return;` ⇒ بحذف `#modeSeg` صارت تخرج فورًا، فيبقى `GAMES_OK = null`
  و**مربّع «مباريات» لا يظهر أبدًا** والميزة سليمة على الخادم تمامًا. أُخرج السؤال من
  عنصر الواجهة — وهو **نفس درس الدفعة ١٦ حرفيًّا**: علَمٌ يُسأل عنه من مسارٍ واحد يموت
  بموت ذلك المسار.
② 🔴 **حذفُ قاعدة CSS بـregex ترك محدِّرًا معلَّقًا فابتلع القاعدة التي تليه.**
  النمط طابق **داخل** `.mode-btn.active .mode-badge{…}` فحُذف جسمُها وبقي المحدِّر
  عاريًا، فالتقط أوّل كتلةٍ بعده — وهي `.bento{…}` ⇒ **انهارت الشبكة إلى عمود واحد**.
  **والبناء نجح والحرّاس مرّت كلُّها**، ولم يفضحه إلّا النظر إلى اللقطة. ونفس درس «لا
  تقتطع وسمًا بـregex من ملفٍّ مُصغَّر السطور» مطبَّقًا على CSS.
③ **رأس الشبكة داكنٌ في الثيمين** (مقيس `rgb(14,61,69)`)، فأعطيتُ الأفاتار حبرَ الهوية
  فخرج **شبحًا لا يكاد يُرى**. الطقم صار من **الجرس المجاور نفسه** — عنصران متجاوران
  في رأسٍ واحد لا يجوز أن يختلف أساسُ لونهما (مقيس بعده: **8.05:1**).

**مقيس:** `npm run check` يمرّ · `npm test` **73/73** · صفر تجاوز أفقي عند **320** و375
في الاتجاهين · صفر خطأ في فخّ الأخطاء · والتباين بتركيب الشفّافية: **أدنى نهاري 5.30
وأدنى ليلي 7.23** على ثماني نقاط. ✅ **واختُبرت على جهاز حقيقي — 2026-08-18، أكّده المالك.** (‏تأكيدُ استعمالٍ لا قياسُ أداة: TalkBack وتنميطُ الأداء خارجه.)

---

**الدفعة الخامسة والثلاثون — الفخامة: بطاقةٌ عائمة، وحرارةٌ في التحذير (2026-08-18)**

مراجعةٌ خارجية على «الفخامة» (‏politeness) في أربعة محاور. **وبندان منها كانا منفَّذين
بالكامل**، وبندان رُفضا بسببهما لا بوقتهما، والباقي نُفِّذ ومُقيس.

| الطلب | الحكم |
|---|---|
| **الورقة السفلية تصير بطاقةً عائمة** | ✅ نُفِّذ — هامش 12px من الجوانب والأسفل، ونصف قطر **24px** من الجهات الأربع |
| **تعتيمٌ زجاجي على الغطاء** | ✅ **موجودٌ أصلًا**: `backdrop-filter:blur(3px)` على `.modal-overlay` منذ دفعاتٍ سابقة (مقيس). الطلب كان 4px — فرقٌ لا يُرى، ولا يُلمَس رقمٌ يعمل |
| **إيماءةُ رجوعٍ محكومةٍ بالإصبع** | ✅ **منفَّذة بالكامل** في `edgeBackGesture`: تتبُّعٌ لإحداثيّ X، وحسمٌ بالمسافة **أو** بالسرعة، ووعيٌ بالاتّجاه، واهتزازٌ عند الاكتمال، وارتدادٌ بمنحنى عند الإفلات دون العتبة. **ولم يُبنَ شيء** |
| **حرارةٌ في الأزرار الثانوية الحاسمة** | ✅ نُفِّذ على «لم يحضر» |
| **الموعد بطلُ ورقة المراجعة** | ✅ نُفِّذ — حبّةٌ بصبغة الفعل و`tabular-nums` |
| **علامةُ نجاحٍ على زرّ الإرسال** | ✅ نُفِّذ |
| **دخولٌ متأخّر للإشعارات غير المقروءة** | ✅ نُفِّذ — 50ms بينها، وللجديد وحده |
| **إجراءاتٌ مضمَّنة في بطاقة الإشعار** | ❌ **لا تُنفَّذ** — انظر أدناه |
| **نافذةُ ملء شاشةٍ للمراجعة** | ❌ **لا تُنفَّذ** — انظر أدناه |

**① البطاقة العائمة.** الفرق ليس زخرفيًّا: الورقة الملتصقة تُقرأ **امتدادًا للإطار**،
والعائمة تُقرأ **طبقةً فوقه** فتُفهَم قرارًا مؤقّتًا يُغلَق.
⚠️ **والهامش السفلي يحترم الحافّة الآمنة** (‏`env(safe-area-inset-bottom)`)، وإلّا جلست
البطاقة تحت شريط الإيماءة في الأجهزة التي تحمله.
⚠️ **ولم يُلمَس `max-height`**: هي 88vh، والهامش يتركها داخل الشاشة (مقيس: أطول ورقة
**747px من 812**، والبطاقة `12/12/24px`). ورقمان يجب أن يوافق أحدهما الآخر يختلفان يومًا.

**② `--sp-pop` **لا** تصلح لارتداد الصفحة.** طُلبت بالاسم، وهي منحنى **تجاوز** (ذروته
1.16): صفحةٌ ترتدّ إلى الصفر بتجاوزٍ تنكشف من خلفها فجوةٌ عند الحافّة. و`--sp-snap`
هي الصحيحة وهي المستعملة فعلًا. **والمنحنى يُختار بما يرتدّ إليه لا باسمه.**

**③ الحرارة نبرةٌ لا صراخ.** التطبيق باردٌ كلُّه (تيل)، فالتحذير يضيع فيه ما لم يُكسَر
البرود. و«لم يحضر» صار صبغةً من `--warning-ink` بحدٍّ منه — **والأحمر لم يُمَسّ**: هو
للخطر الذي لا يُتراجَع عنه (حذف حساب · إلغاء حجز)، وخلطُ التحذير بالخطر يُفقد الأحمرَ
معناه. والعلامة هنا تُرفَع وتُنزَل.

**④ الموعد بطلُ الورقة.** كان خليّةً من أربع بنفس الوزن، واللاعب يفتحها ليتأكّد من شيءٍ
واحد: **متى ألعب؟** — نفس درس لوح «حجزك القادم» في الدفعة ١٨ حرفيًّا (الوزنان كانا
مقلوبين هناك أيضًا). مقيس: **11.86 نهاري · 15.52 ليلي** للقيمة، و4.72/8.76 للتسمية.

**⑤ علامةُ النجاح تُكتب بعد الجواب، ومن خارج `withLoading`.** الأخيرة تُعيد `innerHTML`
في `finally` فتمحو كلَّ ما كُتب داخلها (مزلق مسجَّل) ⇒ العلَم يخرج من الغلاف والحالة
النهائية تُكتب بعده. **ولا تسبق العلامةُ ردَّ الخادم** — وعدٌ بما لم يقع هو م5 حرفيًّا.

**⑥ 🔴 وبندان يُرفَضان بسببهما لا بوقتهما:**
• **الإجراءات المضمَّنة في الإشعار** بُنيت على مثالٍ **لا وجود له**: «المالك اقترح موعدًا
  بديلًا». أنواعُ الإشعارات كلُّها **إخبارٌ بنتيجة** (‏`booking_new`/`confirmed`/`rejected`/
  `cancelled`/`moved` ومعها المباريات والخانة المتحرّرة) — **ولا قرارَ للاعب في أيٍّ منها**،
  ولا يوجد في المنتج مسارُ «اقتراح موعد» أصلًا. وزرّا «قبول/رفض» على إشعارٍ لا يقابلهما
  فعلٌ في القاعدة هو بالضبط ما تمنعه م5.
• **نافذة ملء الشاشة للمراجعة** مشروطةٌ في المراجعة نفسها بـ«إن كانت الشاشة تحتوي خيارات
  كثيرة» — والدفعة ٢٥ **قلّصتها عمدًا** من إحدى عشرة خليّة إلى أربع حقائق وطوت الباقي،
  لسببٍ مقيس. فتوسيعها الآن نقضٌ لقرارٍ اتُّخذ بقياس، لا ترقيةٌ له.

**⑦ 🔴 وعطلٌ حقيقي من الدفعة ٣٤ كشفه هذا العمل:** `navigateBack` كانت تتراجع إلى
`'home'` عند خلوّ المكدّس — وقد صار الجذرُ `hub` في الدفعة السابقة، فمن يرجع من صفحةٍ
بلا تاريخٍ يهبط على **صفحة التصفّح** لا على الشبكة. صار المرجع `hub` في الموضعين.
⚠️ و`data-fallback="home"` على زرّ رجوع صفحة الملعب **صحيحٌ ويبقى**: من التفاصيل يُرجَع
إلى القائمة لا إلى الشبكة.

**مقيس:** `npm run check` يمرّ · `npm test` **73/73** · البطاقة العائمة `12/12/24` في
ورقتَي الفلاتر والإشعارات والمراجعة، **وصفر تجاوز أفقي داخلها** · الإشعارات: ثلاثةٌ غير
مقروءة بتأخير `0.12/0.17/0.22s` والمقروءة **بلا حركة إطلاقًا** · وصفر خطأ console.
✅ **واختُبرت على جهاز حقيقي — 2026-08-18، أكّده المالك.** (‏تأكيدُ استعمالٍ لا قياسُ أداة: TalkBack وتنميطُ الأداء خارجه.)

---

**الدفعة السادسة والثلاثون — مراجعة تخطيط: أربعةٌ منفَّذةٌ أصلًا، وثلاثةٌ نُفِّذت (2026-08-18)**

مراجعةٌ خارجية بثمانية بنود على الطباعة والبطاقات والأشرطة والمساحات. **وقِستُ كلَّ بندٍ
قبل تنفيذه** — فأربعةٌ منها كانت محقَّقةً في الكود فعلًا، وواحدٌ رُفض بقياس، وثلاثةٌ نُفِّذت.

| البند | الحكم — بالقياس |
|---|---|
| **الفراغ الخارجي ≥ الفجوة الداخلية** | ✅ **محقَّق أصلًا**: فجوة الشبكة **12px** وحشوة الصفحة **20px** ⇒ 20 > 12. والطلب كان «16 أو 24»، والقائم بينهما |
| **إخفاء الرأس عند التمرير** | ✅ **بلا معنى هنا**: الرأسان (الشبكة والتصفّح) `position:relative` — **يزولان بالتمرير أصلًا**، واللاصقُ هو شريطُ الفلاتر وحده وهو ما يجب أن يبقى. لا شيء يُخفى |
| **`aspect-ratio` يمنع قفزة التخطيط** | ✅ **القفزة ممنوعة أصلًا** بارتفاعٍ ثابت (‏168px)، والبطاقات متساويةٌ قبل وصول أيّ صورة. ⚠️ **لكنّ النسبة كانت تنزلق** — فنُفِّذ لهذا السبب لا لذاك (أدناه) |
| **الإيقاع العمودي: هامش العنوان الأسفل < الأعلى** | ✅ **محقَّق**: `.rep-group{margin-block:22px 10px}` ⇒ 10 < 22. وبقيّة العناوين هوامشها العلوية **صفر** والفراغُ من الحشوة، فالقاعدة لا تنطبق عليها أصلًا |
| **زرّ الحجز في مسار العين** | ✅ **محقَّق منذ الدفعة ٢٣**: `#detailSticky` **`fixed` أسفل الشاشة**، و`.detail-book-inline` **`display:none`** (إزالةُ الازدواج مقيسة) |
| **تعميم `clamp()` على العناوين الفرعية** | ❌ **رُفض بقياس** — أدناه |
| **تناسق حشوة بطاقات الشبكة** | ✅ **نُفِّذ** — كانت مخالفةً حقيقية أدخلتُها |
| **تلاشٍ خلف الشريط السفلي** | ✅ **نُفِّذ** |

**① تناسقُ الشبكة — مخالفةٌ أدخلتُها في الدفعة ٣٤.** بطاقتان من الأربع غير البطل كانتا
بحشوة **14px** وعنوانٍ **13.5px** مقابل **16/15** في أختيهما — وهنّ رتبةٌ واحدة (متجر ·
أكاديميات · بطولات · سيدات)، فالتفاوت يُقرأ اضطرابًا لا تراتبًا. صارت الستُّ **16px**،
والعناوينُ غيرُ البطل **15px**، والفرقُ في المحتوى وحده.
⚠️ ومعها `text-wrap:balance` على عناوين الشبكة: مقيسٌ أنّ «مباريات مفتوحة» و«متجر
المستديرة» تلتفّان سطرين عند 320px، فيُوزَّع السطران بالتساوي بدل حرفين في الثاني.

**② الغلاف بنسبةٍ لا بارتفاع.** والسببُ **ليس** قفزة التخطيط (ممنوعةٌ أصلًا)، بل أنّ
ارتفاعًا ثابتًا على عرضٍ متغيّر يجعل النسبة تنزلق: **مقيس قبله** 320px ⇒ 278×168 =
**1.66**، و375px ⇒ 334×168 = **1.99**، و430px ⇒ 389×168 = **2.32**. أي أنّ الغلاف يبدو
مربّعًا على الشاشة الضيّقة وشريطًا على الواسعة. الآن `aspect-ratio:2/1` ⇒ **2.00 في
الثلاثة** (مقيس: 278×139 · 334×167 · 389×194).
⚠️ **ومقصورٌ على الغلاف الممتدّ** (‏`:not([data-view="list"])`): في وضع القائمة الغلافُ
عمودٌ جانبيّ بـ`height:100%`، ونسبةٌ هناك تكسر الصفّ — **مقيس أنّ القائمة لم تتغيّر
بشيء** (‏152px وغلافٌ 118×150 كما كان).
⚠️ و`contain-intrinsic-size` أُعيد قياسه كما تقتضي القاعدة: **347px** مقابل المكتوب
**346** ⇒ يبقى صحيحًا بلا تعديل.

**③ تلاشٍ خلف الشريط السفلي.** 🔴 **وموضعُه هو الإصلاح**: الشريط عليه
`transform:translateX(-50%)`، والعنصرُ المحوَّل يصير الكتلةَ الحاوية لأيّ `fixed` بداخله
⇒ `.bnav::before` بـ`inset-inline:0` كان سيمتدّ **بعرض الشريط لا الشاشة**. و`body::after`
مشغولٌ بالحبيبات (‏§28) و`#app::before/::after` مقتولان بقاعدة الإطفاء (‏§28 ①) — فالجذرُ
هو الموضع الحرّ الوحيد. و`html:has(.bnav.show)::after` **لا يُرسَم بلا شريطٍ معروض**:
مقيس أنّه `none` على الشبكة (لا شريط فيها) و`""` بـ120px وz=99 على صفحة التصفّح.
⚠️ وتدرّجٌ لا `backdrop-filter`: طبقةُ دمجٍ بحجم الشاشة هي بالضبط ما أُزيل في §9 و§13 و§28.

**④ 🔴 والسلّم المرن رُفض بقياس لا بذوق.** المدى الفعلي لهذا التطبيق **320…430px**،
وفرقُ الحجم فيه **٢px** لعنوانٍ من 17 إلى 19 — لا يُرى. والأهمُّ أنّ الثمن حقيقي: خلطُ
`vw` في الطباعة **يُضعف استجابة النصّ لتكبير خطّ النظام** — وهو بعينه القلقُ المسجَّل في
§١٤.٥ من وثيقة التصميم («النصّ يكبر والصناديق لا»)، ولذلك بقي الهيرو وحده على `clamp`
بحدَّين `rem` ووسطٍ `vw`. **ومقيسٌ أنّ لا عنوان يُقصّ ولا يفيض عند 320px** (‏`docSW=320`
وصفر تجاوز)، فالمشكلة التي يحلّها البند غير موجودة.

**🔴 وزلّةُ قياسٍ وقعت وكشفت نفسها:** قِستُ البنود الثلاثة المنفَّذة فخرجت **كلُّها غير
مطبَّقة** — والملفّات على القرص تحملها. والسبب أنّ المتصفّح خدم **صفحةً من كاشه**: سؤالُ
CSSOM أثبته قاطعًا (‏`§36` غائبٌ عن الورقة المحمَّلة، وآخرُ قاعدةٍ محلَّلة هي آخرُ §35).
و`?v=` حسمه. **والمزلق مسجَّل في هذا الملفّ حرفيًّا** — ووقعتُ فيه لأنّي أعدتُ التنقّل
إلى نفس الرابط بلا بصمة. **أيّ قياسٍ بعد بناءٍ يمرّ برابطٍ جديد.**

**مقيس:** `npm run check` يمرّ · `npm test` **73/73** · الشبكة **٦ بطاقات بحشوة 16px**
وعناوينُ غير البطل **15px** · النسبة **2.00 عند 320 و375 و430** · القائمة **لم تتغيّر** ·
صفر تجاوز أفقي في الوضعين وعند العرضين · وصفر خطأ console.

---

**الدفعة السابعة والثلاثون — مراجعة بستّة محاور: خمسةٌ منها تصف كودًا ميتًا (2026-08-18)**

مراجعةٌ خارجية على الشكل والمسار والأزرار والمسافات. **وفُحص كلُّ بندٍ بـ`getComputedStyle`
قبل تنفيذه** — فخمسةٌ من أقوى بنودها كانت تقرأ المصدر لا المُنتَج، وثلاثةُ أرقامٍ فيها خاطئة:

| الوثيقة تقول | القياس |
|---|---|
| `.tbtn.sel` و`.dtab-btn.active` تُملأ Lime | ❌ **صبغة تيل منذ §31**: `color(srgb .059 .294 .326 / .14)` |
| Cairo تشحن ٦٤KB وغير قابلة للوصول | ❌ **محذوفة في الدفعة ٢١** (٢٦٨⇐٢٠٥KB) — و**وثيقة التصميم هي التي بليت** فأنفقت المراجعةُ بندًا على شيءٍ غير موجود |
| Montserrat أوّل السلسلة يكلّف مطابقةً بلا مقابل | ❌ نطاقها المُعلَن **لاتينيّ بحت** ⇒ لا تُستشار لمحرف عربي أصلًا |
| الشريط اللاصق `hidden` حتى يكتمل الثلاثي | ❌ **ظاهرٌ من فتح الصفحة**: «ملعب 1 • اختر اليوم والوقت» — والحالة الإرشادية المطلوبة قائمة |
| شبح `.fav-btn` لا يعمل داخل `overflow:hidden` | ❌ مقيس **44×44**، والشبح داخل البطاقة فلا يُقصّ |
| ‏١١٨ `!important` · `--s-*` ٣ مرّات · ٧ توكنات نصف | ❌ **٥٦** · **٥٤** (ثلاثٌ منها في `gap` وحده) · **٦** |

**والباقي صحيحٌ كلُّه ومقيس، ونُفِّذ.**

**① 🔴 أثقل بندٍ صحيح: زرُّ الإرسال كان خارج الشاشة.** مقيس عند 375×667 قبل الإصلاح:
محتوى ورقة المراجعة **1080px** والزرّ عند **y=980.6** ⇒ **٤٦٦px من التمرير** قبل أن يراه
أحد — على الشاشة التي يُتَّخذ عليها القرار الوحيد الذي يدرّ دخلًا في هذا المنتج. صار
تذييلًا لاصقًا داخل الورقة (‏`position:sticky` بخلفيةٍ معتمة وظلٍّ علوي يقول «تحت هذا مزيد»).
**مقيس بعده: الزرّ عند y=578.4 داخل الشاشة لحظة الفتح.**
⚠️ **ويُطبَّق على ورقتين بعينهما لا على `.modal` كلّها**: حوارُ تأكيدٍ من سطرين تذييلُه
اللاصق زخرفةٌ تأكل ارتفاعًا. والمكسب الثاني كان أكبر: **ورقة الفلاتر محتواها 1428px**.

**② 🔴 وعطلٌ لم تذكره المراجعة، كشفه القياس: مؤشّر الخطوات مزاحٌ خطوةً كاملة.**
الدفعة ٢٥ جعلت الخطوات **أربعًا** وبقيت `updateBookingStep` على ثلاث (`n<3` مكتملة ·
`n===3` نشِطة) ⇒ **«الوقت» يُعلَم نشِطًا وأنت في المراجعة، و«التأكيد» لا يُعلَم إطلاقًا**
— مقيس: نقطتُه تردّ `rgb(255,255,255)` أي خلفيّتها الافتراضية، و`aria-current` على الخطوة
الخطأ. **والوسم نفسه كان يقول الصواب** (`class="bk-step active" data-step="4"`) فينقضه JS
عند أوّل فتح. والعدد الآن **يُقرأ من الوسم** لا يُكتب رقمًا، فلا ينحرف يوم تُضاف خامسة.

**③ 🔴 والمسار السعيد كان صامتًا والخطأ موجَّهًا — وهو العكس.** `scrollToDetailSection`
مكتوبةٌ منذ دفعات ولا تُنادى إلّا عند **تحقّقٍ ناقص** أو من لوح البدائل. ومقيس أنّ قسم
«اختيار الموعد» يبدأ عند **y=767** ⇒ من اختار ملعبًا **لا يرى أيّ يومٍ أصلًا**. الآن:
اختيار الملعب يمرّر إلى «اختر اليوم»، واختيار اليوم يمرّر إلى شبكة الأوقات.
⚠️ **والتمرير مع الهيكل لا بعد الجلبة**: الهيكل يحجز الارتفاع، فانتظار الشبكة يقفز
بالصفحة تحت إصبعه بعد أن يكون قد بدأ يقرأ. **مقيس: `scrollY = 511` بعد اختيار اليوم.**

**④ السلّم: أربع رتبٍ لا تسعٌ وعشرون.** كان في الورقة **٣١٦ إعلان `border-radius`
على ٢٩ قيمة** و**١٦ قيمة فجوة**. والعين لا تسمّي ذلك لكنّها تقرؤه «غير منتهٍ».
الأنصاف: **10** تحكّم · **14** سطح · **24** ورقة · **999** حبّة. والفجوات مضاعفات أربعة:
**4 · 8 · 12 · 16 · 24**. والتوكنات الستّة بقيت بأسمائها (‏٣٤+ موضعًا يقرؤها) وأُسنِدت
إلى الرتب.
🔴 **وزلّةُ أداةٍ وقعت وكُشفت فورًا:** أوّل تمريرة للمطبِّع حوّلت **`999px` إلى `24px`
في ٧٢ موضعًا** — أي كلّ حبّةٍ في التطبيق — لأن الشرط كان `n > 18 ⇒ 24` بلا استثناءٍ
للقيمة الحارسة. أُعيدت الشجرة بـ`git checkout` وأُضيف `n >= 100 ⇒ لا تُمَسّ`.
**وأيّ تحويلٍ شاملٍ لقيمةٍ يحتاج قائمةَ ما لا يُحوَّل قبل أن يُشغَّل.**

**⑤ حالة «مختار»: §31 أُكملت.** وحّدت §31 سبعة محدِّدات على صبغة `--act-bg`، وبقي Lime
يملأ ثلاثة: نقطةَ الخطوة النشِطة · رقمَ قسم صفحة الملعب · وإبهامَ الترحيب. فكان في
الشاشة الواحدة **مملوءان** — الفعل بالتيل و«اخترتَ هذا» بالـLime — والعين لا تعرف أيّهما
تتبع. (قرار المالك 2026-08-18.) **مقيس: 7.69 نهاري · 5.85 ليلي.**

**⑥ الاتصال والخريطة خرجا من رأس القمع إلى «عن الملعب»** (قرار المالك). كانا عند
**y=371**، أي **فوق** التبويبات (435) وفوق «اختر الملعب» (508) — وهما **مخرجان خارج
المنتج**: المكالمة هي العادة التي يحاول التطبيق أن يحلّ محلّها، والخريطة تبويبٌ خارجي.
**مقيس بعده: «اختر الملعب» صعد من 508 إلى 444.**
⚠️ وقرار الدفعة ١٩ لم يُنقَض بل استُكمل: كانا مدفونَين خلف تبويبٍ لا يزوره أحد، وقد صار
فوق التبويبات **صفُّ مرافق مضغوط** يقود إلى «عن الملعب» — فالطريق إليهما قائم.

**⑦ شبكة Bento: الثلاثة القادمة في بطاقة واحدة** (قرار المالك). مقيس قبله: البطل
**112px** وأربعة مربّعات «قريبًا» **249px** ⇒ ما لا يعمل يأخذ **ضعفَي** ما يعمل في أوّل
شاشةٍ بعد الدخول. **ولا إفصاح نقص**: الثلاثة ما زالت `[data-soon]` بمفاتيحها الثلاثة
ونقرُ كلٍّ منها يقول ما هي — تغيّر **الوزن** لا الصدق.
🔴 **والدمج وحده كان يزيد ما جاء يقلّله**: `.bt-women` بقي بعرض عمودٍ واحد وجارُه
`.bt-games` مخفيّ (لا جلسة ⇒ `GAMES_OK=null`) ⇒ **نصف صفٍّ فارغ**، فارتفع المجموع إلى
**286**. بشرط `:has(.bt-games[hidden])` يمتدّ ويصير صفًّا أفقيًّا: **215.1 مقيسة**.

**⑧ 🔴 والقياس كشف خرقَ هدف لمسٍ لم تسمّه المراجعة: `.ftab` سبعُ شرائح بارتفاع 40px.**
طبقةُ الشبح التي وضعتها الدفعة ٢٤ ما زالت مكتوبةً، لكنّ `browse-rail` أعادت تعريف
`::after` **مؤشِّرًا سفليًّا بارتفاع 2.5px** ⇒ ورثت الشرائحُ المؤشِّر وفقدت الشبح.
🔴 **وأوّل إصلاحٍ لم يعمل**: كتبتُ `.ftab{min-height:var(--tap-min)}` فبقي **40px مقيسًا**،
لأن `.browse-rail .ftab` نوعيّتها (0,2,0) تغلب (0,1,0) **مهما تأخّرت الثانية** — عائلة
المزلق المسجَّل «النوعية قبل الترتيب». **مقيس بعد ذكرها صراحة: 44px، وصفر خرقٍ على الصفحة.**

**⑨ 🔴 السهم كان معلَّقًا على سبعة مفاتيح ترجمة** (`[data-i18n="enter"]` وأخواتها).
ومفتاحُ الترجمة اسمُ **نصّ** لا اسمُ **دور**: أوّل إعادة تسميةٍ في ملفّ اللغة تحذف السهم
صامتةً بلا خطأ ولا حارس. صار `.btn-go` على ثمانية أزرار.

**⑩ تبويبا الملعب: إبهامٌ ينزلق كبقيّة المشروع.** كانت خمسُ لغاتٍ لمعنًى واحد، و`.detail-tabs`
وحدها تبديلَ خلفية — وهي على مسار الحجز. **والإبهام عنصرٌ حقيقي** لا `::before` (مزلق
مسجَّل)، **وهندسته مشتقّة من `padding` و`gap` الحاوية** لا مكتوبة بيد.
🔴 **ومقيس أنّ أوّل صيغةٍ أخطأت أربعة بكسلات** (x=187.6 مقابل زرٍّ عند 191.6): `0` للعنصر
المطلق هو **صندوق الحشوة** لا صندوق المحتوى. **مقيس بعد الاشتقاق: `dx=dy=dw=dh=0` في
التبويبين وفي الاتجاهين معًا.**

**⑪ كودٌ ميت حُذف — بمطابقة نصٍّ كاملة لا بـregex** (مزلق الدفعة ٣٤: محدِّرٌ معلَّق ابتلع
`.bento`): الزجاج الميت **سبع مرّات** (مقيس `none` — تقتله §13) · شريط الهوية 3px أعلى
**ثمانية عشر حوارًا** (فيصير المدمِّر منها كالمحايد) · ظلّا `.place-card` الميتان ·
مكوّن `.mode-*` (حُذف من الوسم في الدفعة ٣٤) · و`.ftab.active-tab` الأولى.

**⑫ وطلبُ إذن الإشعارات كان يُطرَح مع ورقة النجاح في الثانية نفسها** ⇒ طبقتان تتنازعان،
ونافذة الإذن الأصليّة تحجب الخبر الذي جاءت من أجله. صار بعد إغلاق الورقة، **وبحارس مهلة**
فلا يبقى مؤقّتٌ يدور إن تُركت مفتوحة.

**⑬ ووثيقة التصميم صُحِّحت** — هي التي ضلّلت المراجعة: بقيت تقول إنّ Cairo محزومة **سبع
دفعاتٍ بعد حذفها**. ⚠️ **ووثيقةٌ تصف حالةً منقضية تُنفِق وقتَ من يقرؤها**، وهي من جنس
المزلق المسجَّل: وصفٌ لنيّة لا لسلوك أخطر من غياب الوصف.

**⚠️ وبندٌ واحد لم يُنفَّذ، وسببُه لا وقتُه:** «كتلة التحية على الرئيسية لا تساعد على
إيجاد ملعب». **صحيحة وظيفيًّا** — والدفعة ٣٤ نقلت مهبط الجلسة إلى الشبكة، فالتحية صارت
على رأسٍ لا يحمل بحثًا ولا فلاتر أصلًا (مقيس: الرأس 88px، وأوّل بطاقةٍ عند 88). أي أنّ
المشكلة التي يصفها البند **حُلَّت ببنيةٍ أخرى**، وحذفُ الاسم اليوم يحذف التحيّة الوحيدة
في التطبيق بلا مكسبٍ مقيس.

**مقيس:** `npm run check` يمرّ · `npm test` **73/73** · **صفر تجاوز أفقي** عند **320**
و375 (‏`docSW = vw` في الاثنين) · **صفر خرق هدف لمس** · **صفر خطأ console** · التباين
بتركيب الشفّافية: أدنى نهاري **5.30** وأدنى ليلي **5.85** على ١٤ نقطة · بصمة `public/`
**لم تتغيّر بايتًا** (‏`git status --porcelain public/` فارغ) · و`+12,556 native` كما هي.

---

**الدفعة الثامنة والثلاثون — مراجعة الموقع و`/admin`: تسعةٌ منفَّذةٌ أصلًا، وعطلان صامتان (2026-08-19)**

مراجعةٌ خارجية بمحورين (الموقع التسويقي · لوحة `/admin`) وأربعةِ بنودٍ لكلٍّ منهما.
**وقِستُ كلَّ بندٍ قبل تنفيذه** — فتسعةٌ من اثنين وعشرين كانت محقَّقةً في الكود فعلًا أو
مبنيّةً على فرضيةٍ خاطئة، وثلاثةٌ رُفضت بقياس، **وعطلان حقيقيّان لم تُسمِّهما المراجعة**
كشفهما القياسُ نفسه.

**🔴 ① شريط التنزيل كان يبتلع آخر التذييل — والرابط المبتلَع لا يُنقَر.**
`.dlbar` مثبَّت `bottom:0` بارتفاع **٦٩px**، و`.ft` حشوتها السفلية **صفر**. مقيس عند
375px في نهاية الصفحة: قمّة الشريط **٧٤٣** بينما «Switch to English» يقع **٧٦٤…٧٩٠**
⇒ **مغطًّى بالكامل**، و«© 2026» (‏726…752) مقصوصٌ نصفه. أي أنّ **مبدّل اللغة الوحيد في
أسفل الموقع كان غير قابل للوصول على الجوّال**.
والعلاج بتوكن واحد: `--dlbar-h` **يفرض** ارتفاع الشريط (`min-block-size`) **ويُحجَز به**
في التذييل — لا رقمان يتفارقان يومًا.
⚠️ و`body:has(.dlbar)` عمدًا: الشريط **لا يُبنى أصلًا** قبل وجود إصدار (البناء يحذف
الجزء كلّه) ومقصورٌ على الرئيسة (‏**صفحتان من ٣١**) ⇒ حشوةٌ غير مشروطة كانت ستترك
٨٢px من الفراغ تحت تسعٍ وعشرين صفحةً لا شريط فيها.
✅ **مُتحقَّق بالإصابة لا بالهندسة وحدها**: `covered: []` · فجوة **٣٤px** تحت آخر رابط ·
و`elementFromPoint` على مركزه يُرجع الرابطَ نفسه (`reachable`) · و`/about/` — ولا شريط
فيها — حشوتُها **0px** كما كانت.

**🔴 ② رأس الجدول اللاصق في `/admin` كان كودًا ميتًا منذ الدفعة ١٩.**
`.ad-scroll{overflow-x:auto}` — و**`overflow-y` تُحسَب `auto`** حين لا يكون المحور الآخر
`visible` ⇒ يصير **هذا الصندوق** هو مرسى الالتصاق لا الصفحة، وارتفاعه من محتواه
(`clientH === scrollH`) فلا يتمرّر رأسيًّا أبدًا ⇒ `position:sticky;top:0` **لا تُفعَّل قطّ**.
مقيس بعد تمرير 400px: **`thead.top = −204`** — الرأس خرج من الشاشة كأنّه غير لاصق،
والتعليق فوقه يشرح فائدةً لا تقع.
وسقفُ ارتفاعٍ واحد يُحييه (`max-block-size:72vh` فوق 700px): الصندوق يصير قابلًا للتمرير
رأسيًّا فيلتصق رأسه بأعلاه. ✅ **مقيس بعده**: الإزاحة **٣٣ ⇒ 0** وتبقى صفرًا عند 150 و180
و300، و`elementFromPoint` على أعلى الصندوق يُرجع **`TH` داخل `THEAD`** بنصّ الرأس الحقيقي.
⚠️ **والجداول القصيرة لا تتأثّر بحرف** (مقيس: 288 دون السقف بلا تمرير · 837 فوقه فيتمرّر)،
والعمود الأوّل ما زال لاصقًا أفقيًّا (`dx = 0` بعد `scrollLeft = 200`).
⚠️ **وفوق 700px وحدها**: تحتها تصير الصفوف بطاقات و`thead` مخفيّ أصلًا.

**③ اللصق على الجوّال صار تنقّلًا لا هويّة** (قرار المالك 2026-08-19). `.ad-bar` كان
**اللاصق الوحيد** وهو **١١٦px** عند 375px — أي أنّ المثبَّت طوال التمرير هو الشعارُ وزرُّ
الخروج، وكلاهما يُلمَس مرّةً في الجلسة. والتبويبات كانت تلتفّ إلى **ثلاثة صفوف = ٩٣px**
(‏٤٤ وحدها على الحاسوب) وتمشي مع المحتوى، وشرائح التصفية تمشي كذلك فوق جدولٍ طوله
**٨٢٥px** ⇒ تبديلُ مرشِّح يعني العودة ٨٠٠px. والمجموع **٢٩٠px من الأطر قبل أيّ محتوى**
على شاشة 812.
الآن تحت 700px: `.ad-bar` **تفقد اللصق**، والتبويبات **صفٌّ واحد ينزلق** يلتصق بالأعلى،
والشرائح تحته. ✅ **مقيس**: التبويبات ٩٣ ⇒ **٥٠** (‏`scrollW 573 > clientW 343`) · شرائح
الحالة ٨٣ ⇒ **٤٠** · الثابت **١٠٢px** بدل ١١٦ **وهو تنقّلٌ لا هويّة** · وصفر تراكب بين
الطبقتين (تبويبات 0…50 · شرائح 50…102) · وكلتاهما `reachable` بالإصابة لا بالهندسة.
⚠️ **والإزاحة مشتقّة لا مقدَّرة**: `--ad-tabs-h` تفرض ارتفاع الشريط وتُزيح الشرائح به معًا.
⚠️ **والشرائح لا تُعمِّر بعد جدولها**: `sticky` محكومٌ بصندوق الأب (`.ad-sec`) ⇒ مقيس أنّها
مثبَّتة عند 300 و600 و900، **وتغادر مع قسمها** عند 1200 (`chipsTop = −155`) — فلا مرشِّحٌ
معلّقٌ فوق رسمٍ لا يصفّيه.
⚠️ **وفوق 700px لم يتغيّر شيء**: `.ad-bar` تبقى لاصقةً كما كانت. والسبب أنّ إزاحة
اللاصق الثاني كانت ستُقرأ من ارتفاعٍ **متغيّر** (٧١px يكبر باسمٍ طويل) ⇒ رقمٌ يكذب
بصمت. وسقفُ الجدول في ② يغني عنها هناك.

**🔴 ④ ووقعتُ في المزلق المسجَّل في هذا الملفّ حرفيًّا: `@media` لا تضيف نوعيّة.**
كتبتُ كتلة الجوّال بعد `.panel[hidden]`، أي **قبل** تعريف `.ad-chips` بمئة سطر ⇒
`flex-wrap:wrap` الأساسي يغلب `nowrap` مهما كان داخل استعلامٍ أضيق. ولم يفضحه إلّا
القياس: شريط الحالة بقي **83px** بعد التعديل، أي بلا أثرٍ إطلاقًا بينما بقيّة الكتلة
تعمل. **وموضعُ الكتلة هو الإصلاح** — نُقلت إلى آخر الورقة، بعد كلّ ما تنقضه. (نفس
عائلة عطل «تقليل الحركة» §29 و`prefers-contrast` §30.)

**⑤ الرأس التكيّفي: خلفية صلبة بدل الضباب** (قرار المالك). كان `backdrop-filter:blur(12px)`
فوق خلفية بشفافية ٨٨٪ ⇒ **طبقة دمجٍ بعرض الشاشة تبقى حيّة طوال التمرير** لتُعيد ترشيح ما
يمرّ تحتها في كل إطار — وهو بعينه ما أُزيل من التطبيق في §9 و§13 و§28.
⚠️ **والفرق المرئي عند القمّة صفر**: الرأس في التدفّق **فوق** الهيرو لا فوقه، فالـ٨٨٪ كانت
تُركَّب على `--bg` نفسه. ✅ مقيس في الثيمين: `backdrop-filter: none` والخلفية
`rgb(8,29,34)` ليلًا و`rgb(250,250,247)` نهارًا — أي `--bg` بالضبط.
⚠️ **وحافّة الزجاج باقية**: هي `inset` ساكن لا مُرشِّح، ولا تكلّف طبقة دمج ⇒ الهوية تبقى
والكلفة تسقط.
وارتفاع الرأس صار يتقلّص: **٦٦ ⇒ ٥٢px** عند عتبة `is-scrolled` (‏y>8)، والظلّ وتصغير
الشعار كانا منفَّذين أصلًا. ⚠️ **والتقليص يقع مرّةً واحدة لا في كل إطار**، ولا يتذبذب عند
العتبة: تغيّر ارتفاع المستند لا يغيّر `scrollY` فالشرط لا ينقض نفسه.

**⑥ سؤال الأسئلة الشائعة كان عنوانًا في دوره وفقرةً في طباعته.** `<summary>` ليس
`h1..h3` فلا تطاله قاعدة العناوين، وكان يرث `--lh-body` (‏**1.85**) ⇒ **30.5px** لنصٍّ
16.5px. صار **1.22** إنجليزيًّا و**1.35** عربيًّا — وهي أرقام `h1,h2,h3` نفسها لا أرقامٌ
جديدة. ✅ مقيس: **30.53 ⇒ 22.28px** بالعربية و**20.13** بالإنجليزية.
⚠️ **والمكسب في الارتفاع ضئيلٌ ويُقال كما هو**: صفّ السؤال **64.5 ⇒ 62px** فقط، لأنّ
أيقونة الـ+ (‏28px) هي التي تفرض ارتفاع الصفّ بعد أن نزل صندوقُ السطر تحتها. الكسب
الحقيقي طباعيّ، ويظهر في الارتفاع وحده حين يلتفّ السؤال سطرين.

**⑦ هامش عنوان بلاطة Bento صار مشتقًّا** — `--bento-gap` توكن واحد تقرؤه الشبكة، وهامش
العنوان **نصفه** بـ`calc` (قانون التقارب: ما بين العنوان ووصفه أقرب ممّا بين بطاقةٍ
وأختها). مقيس **٩ ⇒ ٧px**، والقيمة الآن لا تنحرف عن الفجوة أوّل مرّة تتغيّر.

**⑧ الأرقام لا ترتجف**: `font-variant-numeric:tabular-nums` أُضيفت إلى تسميات محاور
الرسوم · رقمِ شريحة الحالة · نسبةِ حصّة المكان · سهمِ الاتجاه · عدّادِ التبويب. (‏`td`
و`.kpi .v` و`.fn-v` و`.fc-v` و`.mini .mv` كانت تحملها أصلًا.)

**⑨ استقرار الفراغ**: `.ad-fchips + .ad-scroll{min-block-size:200px}` — تصفيةٌ تُفرغ
الجدول كانت تُسقط ٨٢٥px من ارتفاع الصفحة دفعةً فيقفز التمرير تحت الإصبع. و٢٠٠px ≈
ثلاثة صفوف، ولا تمسّ جدولًا أطول منها. **وعلى الجدول الذي تحته مرشِّحٌ وحده.**

**⑩ عنوان بطاقة الجوّال** ١٤ ⇒ **١٥px**. ⚠️ والوزن يبقى **800** خلافًا لنصّ المراجعة
(٧٠٠): هو وزن كلّ عنوان بطاقة في اللوحة (`.ad-pl .nm` · `.usr-card .nm` · `.ad-pa-top b`)
و٧٠٠ هنا وحدها كانت تصنع شذوذًا في نفس الشاشة.

---

⚠️ **وأحد عشر بندًا لا يُنفَّذ — لأنّها منفَّذةٌ أصلًا أو فرضيّتها ساقطة، والقياس هو الحكم:**

| البند | القياس |
|---|---|
| **ارتفاع سطر العناوين 1.2–1.3** | **محقَّق**: `h1,h2,h3{line-height:1.22}` إنجليزيًّا و**1.35** عربيًّا بقرارٍ موثَّق (نفس سبب `1.75` للفقرات: الصاعدة والنازلة والتشكيل). الثغرة الحقيقية كانت `.faq-q` وحدها — ⑥ أعلاه |
| **`--muted` للنصّ الوصفي** | **محقَّق**: `.lead` و`.bt p` و`.step p` كلّها `--muted`. وما عداها استثناءات أسطحٍ داكنة (`.hero .lead` · `.cta-band .lead`) وهي صحيحة |
| **كسر نسبة 2/1 على الحاسوب** | 🔴 **الفرضية ساقطة**: **لا `aspect-ratio` على أيّ بطاقة في الموقع** — تلك الدفعة ٣٦ في **التطبيق**. والامتداد منفَّذٌ وأغنى: `span 3` من ستّة أعمدة فوق 620px، والبلاطة الكبيرة **صفّين** فوق 980px |
| **`scale(1.03)` لصور البطاقات عند التحويم** | 🔴 **بلا هدف**: مسحٌ على المستند ⇒ **صفر `<img>` داخل أيّ بطاقةٍ قابلة للنقر** (الوحيد رابطُ الشعار). و`.bt` عناصر `<article>` لا روابط ⇒ نموّها عند التحويم **أفوردانس كاذب** — وهو ما حُذف في §28 (`.onb-grabber`) |
| **قناع تدرّج تحت الصور ≥4.5:1** | **محقَّق ومقيس**: `background-blend-mode:soft-light` فوق تدرّج التيل يفعلها. أسطع ٢٪ تحت النصّ ⇒ **6.09:1** للعنوان و**9.47:1** للفقرة |
| **`max-width:1100px` للموقع** | **محقَّق**: `--wrap:1160px` مع `margin-inline:auto` |
| **زرّ الهيرو يقود للتنزيل والثانوي محايد** | **محقَّق**: الأساسي `href="/downloads/mustadaira-1.0.apk"` مملوءٌ بالـLime، والثانوي سطحٌ محايد `rgba(255,255,255,.07)` بحدٍّ شفّاف |
| **`max-width:1200px` للوحة** | **محقَّق**: `.ad-wrap{max-width:1240px;margin-inline:auto}` |
| **حقول «إضافة مكان» ≤480px على الحاسوب** | **محقَّق**: `.frm` شبكةُ `auto-fit minmax(230px,1fr)` ⇒ مقيس عند 1280: **أربعة أعمدة ٢٧٣px**، ولا حقل يمتدّ بعرض الشاشة |
| **إجراءات في نهاية البطاقة** | **محقَّق**: `.acts{margin-inline-start:auto}` في بطاقتَي المستخدم والمكان، وفي وضع البطاقات `td` بـ`space-between` |
| **فجوة رأسية 4–8px داخل بطاقة الجوّال** | **محقَّق وأضيق**: `padding:3px 0` مع `space-between` أفقيًّا |

**🔴 و«كيف أحجز» تبقى عمودية — رُفضت بقياس، ثمّ بقرار المالك.** المراجعة تعزو إليها
«مساحةً رأسية هائلة» تؤخّر «الملاعب المتاحة». مقيس عند 375px: **شبكة الخطوات ٢٢٥px**
من قسمٍ طوله ٦٤٢، و«الملاعب المتاحة» تبدأ عند **٢٥٦٩px** ⇒ التمرير الأفقي يوفّر
~١٥٠px = **٥٫٨٪**، وثمنه إخفاء خطوتين من ثلاث في **تسلسلٍ** يُقرأ دفعةً واحدة.
وأكبر مستهلكَين فعليَّين غيرُها: **الهيرو ١٤٠٥px** ثمّ **شريط الصورة ٢١١px** داخل القسم
نفسه. (‏قرار المالك 2026-08-19: تبقى كما هي.)

⚠️ **و«٣px بلون `--g-btn`» لمؤشّر التبويب لا يُنفَّذ حرفيًّا**: `--g-btn` **تدرّجٌ** لا لون
فلا يصلح `border-color`، و§31 أخرجت حالة «مختار» عن تعبئة الـLime أصلًا. الارتفاع صار
**٣px** والمؤشّر يبقى بتوكنه القائم.
⚠️ **وموضع المؤشّر انتقل من `bottom:-1px` إلى `0`**: الحاوية صارت صندوق تمرير، فبكسلٌ
واحد خارجها كان يصنع تمريرًا رأسيًّا ويقصّ المؤشّر. و`bottom:0` يقع على صندوق الحشوة،
أي فوق الحدّ مباشرةً.

**مقيس:** `npm run check` يمرّ · `npm test` **73/73** · **صفر تجاوز أفقي** على المستند
(‏`docSW === vw`) عند 375 في العربية والإنجليزية وفي `/admin` · صفر خطأ console ·
والفروق المعلنة أعلاه كلُّها مقروءة من المتصفّح بعد `build.ps1` **وبرابطٍ مبصوم**
(‏`?v=`) لا بعد حفظ الملفّ.
⚠️ **وبصمة `public/` تغيّرت هذه المرّة عمدًا** (‏٣٤ ملفًّا): الدفعة على الموقع، وبصمة
الورقة `site.<hash>.css` تتغيّر فتتبعها الصفحات الثلاثون كلُّها. والتطبيق لم يُمَسّ.

---

**الدفعة التاسعة والثلاثون — سبعة مراجع خارجية: ما كان في البيانات ولا يُعرَض (2026-08-21)**

قرأ المالك سبعة تصاميم من أدوات AI مختلفة (لاندنغ · لوحة مشغّل · تطبيق لاعب ·
تطبيق مالك) وطلب استخراج ما يصلح عندنا. **وأكثر ما فيها لم يكن ميزةً جديدة بل
رقمًا موجودًا في قاعدتنا ولا يُعرَض**، وأكثر ما رُفض منها كان أرقامًا مخترَعة
(«+١٢٤٠ ملعبًا» · «٤٨ ثانية للحجز» · «٩٦٪ توفّر لحظي» · «موثوق من UEFA») —
م5 حرفيًّا، ومعها كلُّ ما يَعِد بحجزٍ فوريّ فينقض نموذجنا نفسه.

**① لوحة المالك — ستّة، كلُّها بلا ترحيل:**
• ⭐ **تاريخ العميل على بطاقة الطلب** (`customerHistory`) — «سبق له ٢٨ حجزًا · بلا
  غياب» · «أول طلب — عميل جديد» · «سبق له ٣١ حجزًا · غاب ٣ مرّات». أقوى ما في
  المراجع، وبياناته كلُّها عند المالك أصلًا. والمطابقة على **الرقم** لا الاسم
  (العميل رقم هاتف، والاسم يُكتب «أحمد» و«احمد»)، ولا يُعدّ إلّا ما **لُعب فعلًا**
  (`isFinished`) وإلّا بدا كلُّ عميلٍ جديد قديمًا. و«أكثر عملائك» تحت `OWNER_MIN_N`
  لا تُقال — صدارةٌ بحجزين تصف قلّة البيانات لا العميل.
  🔴 **وخرجت أوّلًا «٢٨ حجزًا سابقة»** — الصفة بعد معدود متغيّر، وهو المزلق
  المسجَّل في هذا الملفّ حرفيًّا («٥ حجوزات مؤكّدًا»). العلاج المكتوب هناك:
  **انزع الصفة** ⇒ «سبق له {n}». كشفه القياس في المعاينة لا القراءة.
• **أسباب رفض مغلقة** بدل حقلٍ حرّ (`REJECT_REASONS`/`CANCEL_REASONS`)، **وتُخزَّن
  رمزًا لا جملة**: نصٌّ مخزَّن يُجمَّد على لغة لحظة كتابته (درس ترحيل ١٤)، والرمز
  يُترجَم عند العرض. ⚠️ والفائدة الثانية أكبر من الترجمة: النصّ الحرّ **لا يتجمّع**
  («مش متاح» و«غير متاح» و«مو فاضي» ثلاثة أعمدة لسببٍ واحد) ⇒ سؤال «ليش برفض؟»
  صار له جوابٌ يُقرأ في `/admin` لأوّل مرّة.
• **وما يصل اللاعب مكتوبٌ قبل الضغط**: «بيوصل اللاعب سببك، والتطبيق بيعرض عليه
  أقرب الأوقات الفاضية عندك». ⚠️ **ولا يُسمّى وقتٌ بعينه**: ما يُكتب الآن يتجمّد
  وقد يُحجَز قبل أن يُقرأ — فالبدائل تُحسَب حيّةً عند اللاعب (البند ② أدناه).
• **تراجع بشريطٍ يفرغ** (`undoToast`) بعد قبول/رفض/إلغاء.
  🔴 **والمسار المؤجَّل (نرسل بعد ٥ ثوانٍ) جُرّب ورُفض**: فتحُ واتساب يستلزم إيماءة
  مستخدم، وفتحُه **يُخلِّف التطبيق فورًا** فيقع «الإرسال عند الخروج» ويموت الشريط
  قبل أن يُرى. فالإرسال فوريّ والتراجع كتابةٌ ثانية إلى `pending`.
  🔴 **و`.toast` عليها `pointer-events:none`** — بلا `.has-undo{pointer-events:auto}`
  يُرسَم الزرّ ولا يُنقَر. كشفه `elementFromPoint` لا الهندسة.
• **«راحت بلا ردّ» بقيمتها** في تبويب اليوم: «٤ طلبات ≈ ١٠٠ د.أ». الرقم في
  التقارير منذ الدفعة ٣٣، وموضعه الصحيح حيث يُصحَّح الإهمال — اليوم لا آخر الشهر.
  ⚠️ **والمنقضي ليس المرفوض** (`isExpiredBooking`): الرفض قرارُك والانقضاء إهمالُك.
• **ورقة خليّة المخطّط** (`openSlotSheet`): فاضية ⇒ حجزٌ خارجي أو إغلاق **بتعبئةٍ
  مسبقة للملعب والساعة** · معلّقة ⇒ ردٌّ من هنا · مؤكّدة ⇒ البطاقة والاتصال ·
  مغلقة ⇒ إعادة فتح. ومعها **سياقٌ مقيس**: «هذي الساعة انحجزت ن من آخر ٧ ليالي».
  ⚠️ **والمغلق صار قابلًا للنقر** بعد أن كان `disabled` — إعادةُ الفتح فعلٌ يقع عليه
  وحده، ومنعُ نقره كان يترك المالك بلا طريقٍ إليه من حيث يراه. والماضي يبقى ميّتًا.
  🔴 و`withLoading(btn)` **تخرج فورًا بلا زرّ** ⇒ نداءٌ بلا عنصر حقيقي يصمت تمامًا
  (لا فعل ولا خطأ)، فالزرّ يُمرَّر إلى مُعالِجه.

**② التطبيق — اللاعب: خمسة، وواحدٌ منها بترحيل:**
• 🟡 **«ملاحظة لصاحب الملعب»** ([`33_booking_note.sql`](migration/33_booking_note.sql))
  — ما كان يُقال في المكالمة التي جاء المنتج ليلغيها: «نحن عشرة ولازم صدريّات».
  سقط في الطريق بلا بديل، فصار المالك يقبل ثمّ يتّصل ليسأل. سقف ٢٤٠ في **القاعدة**
  (`maxlength` يقترح ولا يمنع)، ولا تُعدَّل بعد الإرسال (مُشغِّل)، ولا يكتبها إلّا
  صاحب الحجز. **والعرض `bookings_full` يُعاد بناؤه في نفس الترحيل** — القاعدة
  المكتوبة هنا بعد أن كلّف `select b.*` ثلاث دفعات.
  ⚠️ **وعلَمٌ ثالث مستقلّ** (`SB_BK_NOTE`) لا عمودٌ في `SB_BK_EXTRA`: ضمُّه يجعل
  غيابَه يُسقط أعمدةَ ترحيلٍ آخر شُغِّل فعلًا. والحقل **يُخفى كلُّه** قبل الترحيل.
  🔴 **ويُفحَص قبل حارس المباراة المفتوحة**: كلاهما يردّ `PGRST204` فتُقرأ «ترحيل ٢٢
  معلَّق» وهي بريئة. وعند غيابه **يُعاد الإرسال بلا ملاحظة** لا يُردّ خطأ: الحجز هو
  المقصود، وإسقاطُه لأجل سطرٍ اختياري خسارةٌ في غير محلّها.
• **مسار الانتظار ثلاث خطوات** في شاشة النجاح: أُرسل ✓ ⇐ يراجع (نبضة) ⇐ **يصلك
  الردّ في الحالتين**. الأخيرة هي الجواب عن أكثر ما يقلق: «وإذا رفضوا، بعرف؟».
  ولا وعدَ مدّةٍ فيها — «عادةً يردّ خلال ن» تبقى حيث تُقاس وحدها (ترحيل ٢٨).
• **«وبينما تنتظر»** — بديلان حيّان من **نفس** محرّك لوح البدائل (عُمِّم إلى
  `findAlternatives(limit, skip)` ولم يُنسَخ). ⚠️ و`skip` تستثني الخانة التي طُلبت
  للتوّ: `State.bookedSlots` لا تحملها بعد، فبلاها يقترح التطبيق ما حُجز قبل ثانية.
• **شريط انتظار على البطاقة المعلّقة**: «بانتظار ردّ الملعب · قبل ١٢ دقيقة» + شريط
  يمتلئ نحو المهلة + «المهلة تنتهي خلال ن». ⚠️ والدالّتان `bookingAge`/`replyDeadlineMs`
  **مشتركتان مع لوحة المالك** ولا تُنسَخان: من يقول للمالك «بقيت ساعتان» يجب أن
  يقول للّاعب الشيء نفسه.
• **رفضٌ بمخرج**: زرّ «شوف أوقات بديلة» على المرفوض والملغى والمنقضي، يفتح صفحة
  الملعب على **نفس اليوم** إن كان ما زال في الشريط — قائمةٌ حيّة لا نصٌّ مجمَّد.

**③ الموقع — أربعة، وواحدٌ ثبت أنّه منفَّذ:**
• **«وهذه الصفحة بابه»** في الهيرو — أصدق وصفٍ لقرار ①، وكان مكتوبًا في
  `/download` وفي الأسئلة ولا يُقال حيث يقف القارئ.
• 🔴 **«ودقيقة واحدة» سقطت** (قرار المالك 2026-08-21): وعدُ زمنٍ غير مقيس، وهو
  **نفس الصنف** الذي رُفض في المراجع («٤٨ ثانية» · «٢٢ ثانية») — ورفضُ القاعدة
  عند الغير وقبولُها عندنا يُفقدها معناها. والعنوان صار «ثلاث خطوات بينك وبين
  المباراة»، **والقوس ينتهي عند الردّ لا عند الإرسال**: «تصفّح واختر وقتك» ⇐ «أرسل
  الطلب — بلا دفع» ⇐ «يصلك ردّ الملعب». وكان يقف عند «أرسل» فيبقى السؤال الذي
  يخاف منه اللاعب بلا جواب في القسم الذي وُضع له.
  ⚠️ ومعها **حُذفت أربعة مفاتيح ميّتة** (`howEyebrow` و`howStep*Desc`) تصف الخطوات
  القديمة ولا يقرؤها أحد — نفس درس الدفعة ٣٢ ⑤ (النصّ الاحتياطي يبلى بلا أن يصرخ شيء).
• **سؤال «هل أحجز من الموقع؟»** أُضيف ثانيًا في الرئيسة — السؤال الذي يسأله القارئ
  وهو واقفٌ هنا. والأوّل يبقى قرار ٧ (خمس رياضات · المفتوح كرة القدم).
• **صفوف الملاعب صارت مرقّمة ومعها المنطقة** (`Render-VenueRows`، مكوّن واحد
  للدليل وللرئيسة). قرار الدفعة ٢٦ يبقى قائمًا — لا سعر ولا نجوم ولا أرضية — لكن
  **المنطقة هي الحقيقة الوحيدة التي تساعد فعلًا على «إيجاد ملعبك»**، وهي نصف مهمّة
  الصفحة المكتوبة في تلك الدفعة نفسها.
  🔴 **والصور المسجَّلة كلُّها ستوك من Pexels** (١٠ من ١٥) — طبعُ واحدةٍ تحت اسم
  ملعبٍ يقول «هذه صورته» وهي ليست كذلك (م5، وقرار المالك). فالنائب مخطَّط،
  **والبوّابة هي المضيف** (`$script:StockHosts`) ⇒ يوم يرفع المالك صورةً حقيقية من
  أي مصدرٍ آخر تظهر في البناء التالي بلا تعديل سطر.
  🔴 **وفاصلة «·» حرفيةً في `.ps1` خرجت مشوَّهة** — القاعدة المكتوبة هنا («صفر محرف
  عربي في أي `.ps1`») مطبَّقةً على **المخرَجات** لا التعليقات. والعلاج
  `&middot;` بعد ترميز كل جزء (وهو ما يفعله الملفّ نفسه في موضعين). كشفه القياس.
• ✅ **والبند الخامس ثبت أنّه منفَّذ**: «شريط ثلاث حقائق بدل شريط أرقام» — عندنا
  `trust1..3` في الهيرو منذ دفعات، و`statsBand` **أرقامٌ حقيقية من القاعدة** لا
  مخترَعة. لا عمل.

**④ وحارس مرآةٍ سادس** — قائمة رموز الأسباب في موضعين بالضرورة (التطبيق يترجمها
من `I18N` و`/admin` من خريطتها، لأنّ اللوحة تقرأ `cancel_reason` خامًا). والحارس
**عضويّة مجموعة لا تساوي نصّ**: اللوحة يجوز أن تعرف رموزًا **أكثر** (قديمةً في
صفوفٍ قديمة) ولا يجوز أن تعرف أقلّ. ✅ **ومُتحقَّق بالتجربة**: أُضيف رمزٌ إلى
التطبيق وحده فأخرج `DRIFT` باسمه وخرج بـ1، وعلى الشجرة النظيفة **٦ رموز يعرفها
الطرفان**.

⚠️ **وما رُفض من المراجع، وسببُه لا وقتُه:** كلُّ الأرقام المخترَعة والشهادات ·
«توفّر لحظي» و«احجز بنقرة» (تنقضان أنّ الحجز طلب) · الدفع بالبطاقة والتحصيل
اليومي (لا بوّابة) · شارة «ملعب موثوق» بلا آليّة توثيق · مستوى اللاعبين (مستبعَد
في الدفعة ١٥) · دونات الإشغال (أُسقطت بقياس في ٣٣) · الخريطة والمؤشّرات (لا
إحداثيات — نفس سبب رفض `geo` في ٣١) · `backdrop-filter` على عناصر متكرّرة
(أُزيل في §9 و§13 و§28) · والحركات اللانهائية (§28).

**مقيس** (`owner-harness` على 375px، والانتقالات معطَّلة قبل كل قياس):
`npm run check` يمرّ · `npm test` **73/73** · **صفر تجاوز أفقي** (`docSW === vw`)
في التطبيق وفي الموقع بالعربية والإنجليزية · **صفر خطأ console من كود التطبيق**
(والـ400 الباقيتان `refresh_token` لجلسةٍ قديمة في المتصفّح) · أهداف اللمس:
شرائح الأسباب **٤٤** · أزرار ورقة الخليّة **٥٢–٥٦** · زرّ التراجع **٥٨×٤٤**
و`elementFromPoint` يردّه هو لا الغلاف · والتباين بتركيب الشفّافية على ٢٠ نقطة:
**أدنى نهاري 4.82 وأدنى ليلي 6.73** · و`+12,556 native` كما هي.
✅ **وترحيل ٣٣ شُغِّل 2026-08-21** — و`check-view-parity` صار يعدّ **٢٨ عمودًا** بلا تجاهل، وحقلُ الملاحظة ظاهرٌ في نافذة المراجعة.

---

**الدفعة الأربعون — الطبقة البصرية للمرجعين: خمسةُ حاملين يصيرون واحدًا (2026-08-21)**

قراءةٌ ثانية لملفَّي المرجع نفسيهما (تطبيق اللاعب وتطبيق المالك) — الأولى كانت
للميزات والنصوص، وهذه **للشكل**: بنيةُ البطاقة وسلالمُ التصميم. وقِستُ سلالمهما
مقابل سلالمنا قبل أخذ شيء، فخرج نصفُ النتيجة عكسَ المتوقَّع.

**① ما هو أفضل عندنا — فلم يُؤخَذ (مقيس):**

| | المرجع | عندنا |
|---|---|---|
| نصف القطر | **١٢ قيمة** متفرّقة (8·9·10·11·12·13·14·16·18·20·22) | **٤ رتب** (10·14·24·999) — الدفعة ٣٧ |
| الفجوات | **١٠ قيم** (3·5·6·7·8·9·10·11·12·14) | **٥ رتب** مضاعفات ٤ |
| المتمرِّر | `overflow-y` داخل الشاشة | المستند — **قرار مقيس**: السحب للتحديث مبنيّ على `scrollY<=0` |
| الشريط السفلي | شريط 22×3px فوق التسمية | أيقونة + نصّ + **حبّة مقيسة بالبكسل** (الدفعتان ١٠ و١٩) |
| حجم الخطّ | مركَّز على **11–13px** | 13.5–16.5 — الدفعة ٣٢ رفعته عمدًا |

نقلُ أيٍّ منها يهدم عملًا مقيسًا. **والمرجع ليس مرجعًا في كلّ شيء** — وهو نفس
درس الدفعات السابقة: يُفحَص بندًا بندًا لا يُستورَد جملةً.

**② 🔴 وما هو أفضل عندهم — وزنُ الخطّ عندنا كان مقلوبًا:**
```
المرجع:  700 → 92 مرّة  ·  800 → 29  ·  600 → 33     (٧٠٠ افتراضيّ)
عندنا:   800 → 188 مرّة ·  700 → 82  ·  900 → 17     (٨٠٠ افتراضيّ)
```
الدفعة ٢٨ كتبت أنّ «الأسود العريض إشارة *تطبيق شعبي*، والفخامة وزنٌ متوسّط»
ثمّ **طبّقت ذلك على الأزرار وحدها** — وبقي ٨٠٠ وزنَ كلّ تسمية وحاشية ورقمٍ
صغير. الآن: **٨٠٠ للأرقام والعناوين وشارات الحالة · ٧٠٠ لما عداها** —
١٠١ قاعدة نزلت، و**٨٧ بقيت** بقائمة إبقاءٍ صريحة مكتوبة في الشيفرة لا بكنسٍ أعمى.
✅ **والدليل أنّها لم تُزح شيئًا: ارتفاع المستند ٣٨٤٩ قبل وبعد**، وكلُّ صندوقٍ
مقيسٍ متطابق عدا زرًّا واحدًا بعرضٍ تلقائيّ ضاق **بكسلًا واحدًا** (99⇐98).
⚠️ **وأُعيدت اثنتان إلى ٨٠٠ بعد المراجعة**: `.d-state.is-closed` (حالةٌ لا تسمية)
و`.ot-next-when` (قيمةٌ لا عنوان) — سقطتا في الكنس لأنّ قائمة الإبقاء تطابق
**نهاية** المحدِّد، و`.d-state.is-closed` لا ينتهي بـ`.d-state`.

**③ ⭐ بنية البطاقة: خمسةُ حاملين لحالةٍ واحدة يصيرون بندًا واحدًا.**
كانت الحالةُ الواحدة موزّعةً على **خطٍّ جانبيّ ٣px · شارةٍ أعلى اليمين · شريحةِ
عمر · شريحةِ مهلة · وصندوقِ `.pw-strip`** الذي أضافته الدفعة ٣٩ — أربعةٌ منها
مكدَّسةٌ في الزاوية العليا تزاحم اسمَ الملعب على أضيق موضعٍ في البطاقة.
والبديل `bookingStripHead` — بندٌ **بعرض البطاقة** يُقرأ قبل المحتوى: نقطة ·
اسمُ الحالة · وفي الطرف الآخر عمرُها — وتحته شريطُ المهلة **٣px ملتصقًا** لا
صندوقًا يأكل ٨٩ بكسلًا.
• **مكوّنٌ واحد للوجهين** بوسيطٍ واحد (`forOwner`): «بانتظار ردّ الملعب» للّاعب
  و«بانتظار ردّك» للمالك — نفس الحالة وجملتان لأنّ القارئ اثنان. ونسختان تنحرفان.
• ⭐ **والخانة اليمنى تحمل الأعجل من الاثنين لا الاثنين معًا**: العمر ما دام
  الوقت مريحًا، وتنقلب إلى المهلة حين تدخل نافذة التحذير أو تفوت — ومعها يتصاعد
  لون الشريط كلِّه (`is-urgent`). فالبطاقة تحمل **ما يهمّ في هذه اللحظة** وحده
  بدل أن تحمل كلَّ ما قد يهمّ.
• ⚠️ **والحالة تبقى محمولةً على أكثر من اللون**: النصّ يقولها صراحةً والنقطة
  شكلٌ ثانٍ — والخطُّ الجانبيّ المنقّط لـ«لم يحضر» سقط لأنّ شارتَه الحمراء
  **بنصّها** أقوى منه.
• ⚠️ **وصنفٌ صريح `has-hd` لا `:has()`**: بطاقة المباراة المنضمّ إليها
  (`.joined-strip`) تشترك في `.booking-strip` ولا شريط لها، فتبقى بحشوتها وخطّها.

**④ بلاطة المقياس: أربعة أسطر ثابتة.** تسمية ⇐ رقم ⇐ **مقامه** ⇐ اتجاه. والسطر
الثالث هو الإضافة: رقمٌ بلا مقام لا يقول شيئًا («٧» مقابل ماذا؟) — وهي القاعدة
المطبَّقة على البطل منذ الدفعة ٣٣ ومتروكةٌ في الخانات الثلاث تحته.
⚠️ **ولكلٍّ مقامٌ يخصّه ولا يتكرّر اثنان**: «من ٣٢ خانة معروضة» تحت الحجوزات ·
«على ٤ ملاعب» تحت الفارغة · «أقدمها أول أمس» تحت المعلّق. ولو حمل الأوّلان نفس
المقام لقال اللوحُ الشيءَ مرّتين بصيغتين — ضجيجٌ لا معلومة.
⚠️ **وخطّ الاتجاه على البطل وحده**: المال يحمل اتجاهًا، وطابورٌ وعددُ خاناتٍ
فارغة لا يحملانه — ورسمُ خطٍّ عليهما زخرفة.
🔴 **و`createSparkline` أُعيد استعماله ولم يُنسَخ، فكشف عطلًا**: الصنف القائم
**زخرفةٌ خلفية** (`position:absolute` · `z-index:-1` · `opacity:.5`) صُمِّم ليقع
خلف بطاقة إحصاء. وبلا نقضِ الثلاث خرج الخطُّ من البطاقة إلى **عرض الشاشة كلِّه**
وغلافُه بارتفاع صفر — مقيس: `l:0 r:375` داخل بطاقةٍ عرضُها ٣٣٥. والنقض صريحٌ
في محدِّرٍ مقصور، و**التعريف الأصلي لم يُمَسّ** (له مستهلكوه، ومُتحقَّق أنّهم
ما زالوا `absolute` و`.5`).

**⑤ 🔴 وعطلٌ كامن ضخّمته الدفعة ٣٩ نفسها، كشفه القياس لا القراءة.**
`sbBookingsQuery` تتراجع عن الأعمدة الجديدة كلّما `!r.ok` — **و401 و5xx تدخلان
في ذلك**. فتوكنٌ منتهٍ يُخرج خمس طلباتٍ متتالية لا تُصلح شيئًا، **والأسوأ أنّ
الأعلام الثلاثة تُصفَّر نهائيًّا** فتُطفأ «لم يحضر» والمباريات المفتوحة والملاحظة
**بقيّة الجلسة** لأجل انقطاعٍ عابر في المصادقة. والعطل قديم، وزيادةُ العلَم
الثالث رفعت الجولات من ثلاث إلى خمس فأظهرته.
والعلاج أنّ PostgREST **يسمّي العلّة**: عمودٌ غير موجود ⇒ 400 ومعه `42703`.
فصار التراجع مشروطًا به وحده (`isMissingCol`). ✅ **مقيس**: تحميلٌ نظيف يُخرج
**٧ طلبات وصفر نداء لـ`bookings_full`** وصفر خطأ console.
⚠️ **والدرس أعمّ**: مسارُ تراجعٍ يُشغَّل على «أيّ فشل» يُحوّل عطلًا عابرًا إلى
تعطيلٍ دائم لميزةٍ سليمة — والشرط يكون على **اسم العلّة** لا على فشلٍ مجرَّد.

**⑥ وتصحيحٌ لسطرٍ كُتب أمس**: ترحيل ٣٣ **شُغِّل 2026-08-21** — مقيس بالمفتاح
العام: `bookings?select=player_note` و`bookings_full?select=player_note` يردّان
**200** كلاهما، والثاني هو الفحص الذي يهمّ. و`check-view-parity` صار يعدّ
**٢٨ عمودًا** بلا تجاهل.

**مقيس** (`owner-harness` على 375px، والانتقالات معطَّلة قبل كل قياس):
`npm run check` يمرّ · `npm test` **73/73** · **ارتفاع المستند ٣٨٤٩ قبل الوزن
وبعده** · الشريط **٤٢px** بعرض البطاقة كاملًا (332 من 335) و**صفر شريحةٍ قديمة
باقية** · البلاطات الثلاث **١١٢px** بثلاثة أسطر لكلٍّ · الخطّ داخل البطاقة
(41→334 في بطاقةٍ 20→355) · صفر تجاوز أفقي عند 375 · وصفر خطأ console في تبويبٍ
نظيف · والتباين بتركيب الشفّافية **من العنصر نفسه لا من أبيه**: أدنى نهاري
**5.15** وأدنى ليلي **5.47**، والزرّ المملوء **8.07** في الثيمين.
🔴 **وزلّة قياسٍ وقعت وصُحّحت**: قرأتُ «1.05» على زرّ الفعل — لأنّي قِستُ خلفية
**الأب** وتجاهلتُ خلفية الزرّ نفسه. وهي المزلق المسجَّل حرفيًّا في الدفعة ٣٣،
ووقعتُ فيه ثانيةً. الرقم الصحيح **8.07**، ولا انحدار.

---

**الدفعة الحادية والأربعون — ثلاثة عشر بلاغًا من المالك (2026-08-22)**

بلاغاتٌ من استعمالٍ حقيقي، تسعةٌ منها على جهة اللاعب وأربعةٌ على لوحة المالك.
**وكلُّ تشخيصٍ هنا مقيس في المعاينة** — واثنان منها قلبا ما كنتُ أظنّه.

**① 🔴 «تأكيدان لزرّ الحجز» — والسؤال أُعيد على المالك قبل أن يُلمَس شيء.**
تتبّعتُ المسار كلَّه (بطاقة ⇐ صفحة الملعب ⇐ «متابعة الحجز» ⇐ «مراجعة الطلب»
⇐ «أرسل الطلب») فوجدتُ **شاشةَ تأكيدٍ واحدة**، فسألتُ. وجوابه: «لمّا تكبس
أرسل الطلب بعطيني تمام مع إشارة صح — هاي الإشارة ما بدي إيّاها». وهي
`btnDone`: الزرّ يقول «تمّ» بعلامةٍ داخله **400ms** ثمّ تُغلَق الورقة وتُفتح
شاشة النجاح ⇒ تأكيدان متتاليان لفعلٍ واحد، والثاني يبتلع الأوّل فلا يُقرأ
الأوّل إلّا وميضًا. حُذف النداء **ومعه الدالّة وقاعدتاها في الورقة**
(‏`.sbtn.is-done` و`.btn-check`): كتلةٌ تُقرأ في المصدر وهي بلا مستهلك
تُضلّل من يقرأ بعدنا — وهو المزلق المسجَّل حرفيًّا.
⚠️ **والدرس في السؤال لا في الإصلاح**: ثلاثةُ قراءاتٍ محتملة للبلاغ، وكلٌّ
منها يقود إلى عملٍ مختلف تمامًا — وسؤالٌ واحد أرخص من دفعةٍ في الاتّجاه الخطأ.

**② 🔴 «تعليق أثناء الانتقال» — والتمرير كان يقع في الفريم الأوّل من الحركة.**
`pageScrollSet(y)` كان مؤجَّلًا إلى `requestAnimationFrame` في ذيل `showPage`،
أي إلى **أوّل فريمٍ من الانتقال**: `scrollTo` على مستندٍ طوله آلاف البكسلات
بينما طبقتان تنزلقان فوقه ⇒ إعادةُ رسمٍ كاملة في أثقل فريم، وقفزةٌ مرئية عند
العودة إلى صفحةٍ محفوظ موضعها. والتأجيل لم يكن لازمًا أصلًا: `void
page.offsetWidth` قبله بأسطر **يُجبر التخطيط**.
**مقيس قبله**: زمنُ `showPage` المتزامن **92.9 · 60.2 · 15.4 · 18.2 ms**،
ومهامٌ طويلة **95 · 181 · 60 ms**، وسقوطُ إطارٍ إلى ثلاثة في كل تنقّل
(ذروة 33.6ms). **وبعده**: **22.5 · 20.5 · 15.9 · 11.7 ms**، و**صفر إطارٍ
ساقط في ثلاثٍ من أربع** بذروة **16.9ms**، والمهمّة الطويلة الوحيدة الباقية
**127ms** هي أوّل تنقّلٍ في الجلسة.
⚠️ **والباقي ليس JS**: قِستُ `renderPlaces` وحدها فخرجت **4.5…7.6ms** دافئة
(٧ بطاقات · 1773 عقدة) ⇒ الـ127 هي أوّل تنسيقٍ ورسمٍ وفكّ صور، لا بناءَ DOM.
⚠️ ويبقى تصحيحٌ في الفريم التالي **للحالة الوحيدة التي تحتاجه**: موضعٌ محفوظ
أبعدُ من ارتفاعٍ لم يكتمل بعد — مقيس أنّ طلب 600 يهبط على **504** (أقصى ما
يسمح به المستند) ثمّ يصحّحه فريمٌ واحد بلا كلفةٍ في المسار العادي.

**③ الرياضات إلى الشريط، والمناطق إلى الفلتر.** كان العكس، ومعه **بابان
للرياضة**: قائمةٌ منسدلة على طرف الشريط، ومجموعةٌ ثانية داخل ورقة الفلاتر.
والرياضة هي التي **تقصّ البيانات كلَّها** (‏`applySportScope`: تبديلها يغيّر
الأماكن والمناطق والمقاسات معًا) بينما المنطقة تصفّي داخل ما بقي ⇒ الأعلى
صوتًا يجب أن يكون الأوسع أثرًا. حُذفت القائمة المنسدلة ودوالُّها الثلاث
(‏`renderSportDropdown` · `toggleSportDD` · `closeSportDD`) و`renderSportTabs`
هي الراسم الوحيد، وحُذف `renderRegionTabs` وبقي منه **الحارس وحده** باسمٍ
يقوله: `pruneRegionFilter` (منطقةٌ مختارة لم تعد موجودة في الرياضة الحالية
تُصفَّر بدل قائمةٍ فارغة بلا سبب).
🔴 **وشريط الرياضات لا يُخفى أبدًا**: `updateSportSections` كانت تُخفي
`#regionTabs` والعنوان عند رياضةٍ «قريبًا»، ومُبدِّلُ الرياضة كان في الورقة
فبقي مخرجًا. وقد صار الشريط **هو المخرج الوحيد** ⇒ إخفاؤه معه مصيدةٌ كاملة
(نفس مصيدة الدفعة ٢٣ بالحرف). مقيس: عند اختيار البادل يبقى الشريط بخمس حبّات
ويُخفى العنوان وحده، ولوحُ «قريبًا» يظهر بمخطّطه.
⚠️ ومعه: الحبّة المختارة **تُجلَب إلى الرؤية** (`scrollIntoView` أفقيًّا) —
خمسُ رياضاتٍ في مسارٍ عرضُه ٧٦٦px داخل ٣١١px، فآخرُها كان يُختار ولا يُرى.
⚠️ و**ارتفاعٌ حقيقي 44** لا شبح: الحبّة كانت `min-height:42px` (مقيسة) — وهو
مقبولٌ في ورقةٍ فسيحة وخرقٌ في شريطٍ لاصق يُلمَس دائمًا.

**④ ورقة الفلاتر أُعيد هيكلتها على السؤال لا على تاريخ الإضافة.**
كانت **إحدى عشرة مجموعةً مفتوحةً كلُّها** بارتفاع **1428px** (مقيس في الدفعة
٣٧) بترتيبٍ يضع أكثرَ ما يُصفَّى به (المنطقة · السعر) رابعًا وخامسًا،
ويتصدّرها أداتان لا تُخفيان نتيجةً واحدة (الترتيب · شكل البطاقات).
الآن: **أين ⇐ بكم ⇐ متى ⇐ الترتيب ⇐ [مواصفات أكثر] ⇐ شكل العرض**.
**مقيس بعده: 945px.** والمنطقة صارت **شرائح** لا قائمةً منسدلة (ستُّ مناطق
في القاعدة كلِّها، ومنسدلةٌ بستّة خيارات تخفيها كلَّها خلف نقرة)، وبنفس
`fxChip` المستعملة في بقيّة الورقة.
⚠️ **والطيّة تنفتح وحدها إن كان بداخلها مرشِّحٌ مفعَّل، ومعها عدده** — طيٌّ
يُخفي فلترًا يعمل هو بالضبط ما يجعل القائمة تُقرأ معطوبة. **ولا تُغلَق بيدنا
أبدًا**: كلُّ نقرة شريحةٍ تُعيد بناء الورقة، فإغلاقٌ تلقائي كان سيطويها تحت
إصبع من يختار داخلها.
⚠️ **وللمنطقة مسودّةٌ كبقيّة المرشِّحات** (`State.filterDraft`): كانت تُقرأ من
`<select>` عند «تطبيق»، والشرائح تُطبَّق فورًا لو لم تُعزَل ⇒ القائمة تتغيّر
تحت ورقةٍ مفتوحة. **ودخلت العدّاد**: كانت خارجه لأنها شريطٌ ظاهر يقول حالته
بنفسه، وصارت خلف زرٍّ فغيابُها منه يجعله يقول «صفر» وقائمةٌ مقصوصةٌ تحته.
✅ **مقيس من طرفٍ إلى طرف**: اختيار «المدينة الرياضية» ⇒ **٧ ⇐ ٢ بطاقة**،
وشريحةٌ في «الفلاتر المفعّلة»، وعدّادُ الزرّ **١**، وإزالةُ الشريحة تُرجع السبع.
⚠️ وحُذفت ثلاثةُ مفاتيح ماتت معها (`sportLbl` · `allRegions` · `sportsHint`) —
والأخيرة كانت تشرح قرار ٧، وشرحُها قائمٌ حيث يُقرأ فعلًا: لوحُ «قريبًا» نفسُه.

**⑤ صور الملعب: أوّل طريقٍ إليها من التطبيق.** `image_url` عمودٌ في `fields`
منذ `01_schema` يقرؤه `fieldImages` ويرسمه على بطاقة الملعب وفي مصغّرة تبويب
الملاعب — **ولا يكتبه إلّا `/admin`**. فمن يفتح ملعبًا يبقى بلا صورة إلى أن
يطلب ذلك من الأدمن. الآن قسمٌ في نافذة الملعب: صفٌّ لكل صورة (معاينة ⇐ الرابط
⇐ فعل)، وإضافةٌ وحذفٌ و«خلّيها الأولى».
⚠️ **بروابط لا برفع ملفّات، ويُقال ذلك في الحاشية**: لا مخزَن ملفّات في
المشروع، وزرُّ «اختر صورة» وعدٌ بما لا يقع (م5).
⚠️ **والحارس `isHttpUrl` نفسه** الذي يحرس `fieldImages` و`map_link` — قائمةٌ
بيضاء بالمخطَّط — **وعلى الطرفين**: الواجهة تقول سبب الرفض، والخادم يُنظّف
بـ`fieldImages({image_url})` أي **بنفس المحلّل** لا بنسخةٍ ثانية منه.
⚠️ و**الأولى هي التي تُعرَض** (‏`fieldImages(f)[0]` في `placeCard` و
`field-thumb`) فالترتيب معلومةٌ لا زينة، ولذلك زرُّ الترقية موجود.
⚠️ و`onerror` يقول «الرابط ما فتح صورة» **قبل الحفظ**: رابطٌ صحيح المخطَّط قد
يكون مكسورًا، والمالك لن يعرف إلّا حين يفتح التطبيق كلاعب.
✅ **مقيس**: `javascript:` مرفوض بسببه · المكرّر مرفوض · الترتيب والحذف يعملان
· وجسم الـPATCH يخرج `a.jpg|b.jpg` بالترتيب · وأهداف اللمس ٤٤.
🔴 **وزلّةُ أداة سُجِّلت**: كتبتُ تعبيرًا نمطيًّا للفصل عبر heredoc فوصلت
الشرطةُ المائلة **واحدةً** إلى الملفّ ⇒ سطرٌ حقيقي داخل التعبير ⇒
`Invalid regular expression` وشاشةٌ بيضاء. **والبناء نجح**، ولم يفضحه إلّا
الكونسول. والعلاج الأصحّ لم يكن تصحيح الهروب بل **إسقاط النسخة الثانية**
واستدعاء `fieldImages` القائمة.

**⑥ رأس الشبكة كان بلا حشوة أصلًا.** بلاغ «أيقونة التنبيهات ملزقة بطرف
الشاشة» — ومقيس: الجرس يبدأ عند **`left:0` بالضبط**. والسبب أنّ `.app-header`
المجرَّدة **لا تحمل حشوةً**، وكلُّ مستعمليها يحملون مُعدِّلًا فيه حشوته
(`home`/`compact`/`auth`)، والشبكة وحدها كانت بلا مُعدِّل — فلا حشوة **ولا
حيّز آمن أعلى** (نتوء الكاميرا). صار `.app-header.hub`. **مقيس بعده: الجرس
عند 20 = حاشية الصفحة نفسها.**

**⑦ لوح «حجزك القادم» كان أضيق من جاره بأربعين بكسلًا.** `.trk-wrap` تحمل
`padding:14px var(--s-5) 0` وهي **ابنةُ `.pad`** (‏`padding:28px var(--s-5)`)
⇒ حشوةٌ مضاعفة. **مقيس: 295.2px مقابل 335.2 لبطاقة «ملاعب»** المجاورة لها في
الشبكة. صارت `padding:0 0 12px`. **مقيس بعده: 335.2 = 335.2 بالبكسل.**

**⑧ مبدّل شكل البطاقات يُخفى في «مباريات».** `State.view` تحكم `#placesList`
وحدها، و`renderGames` تبني قائمتها بشكلٍ واحد **ولا تقرأ العلَم إطلاقًا** ⇒
زرّان يبدّلان شكلًا لا يتبدّل. مقيس: `hidden` و`display:none` في وضع
المباريات، ويعودان `flex` مع الملاعب.

**⑨ زرّ رجوعٍ إلى الشبكة في رؤوس التبويبات الثلاثة.** الشبكة مهبط الجلسة منذ
الدفعة ٣٤، والمخرج الوحيد منها إليها كان **زرّ «الرئيسية» في الشريط السفلي**
— يُطلَب من أسفل الشاشة والعين في أعلاها. والزرّ يذهب إلى `hub` **مباشرةً**
لا إلى ما قبله في المكدّس: «رجوع للرئيسية» وجهةٌ لا خطوة (‏`navigateBack`
كانت ستُهبط من «حسابي» على «حجوزاتي» والزرُّ يعِد بالرئيسية).
⚠️ و`{back:true}` كي يكون الانتقال رجوعًا في شكله كما هو في معناه.
✅ **مقيس في الاتجاهين**: الزرّ **44×44** على الحافّة الابتدائية
(‏RTL: 311.2 · LTR: 20)، والعنوانان يبدآن من 20 بلا تجاوز.

**⑩ لوحة المالك: ما ينتظر ردَّك أوّلًا.** كانت القائمة مرتَّبة بالتاريخ وحده
⇒ مقيس على بيانات الشبكة: `مؤكّد · مؤكّد · مؤكّد · معلّق · مؤكّد · معلّق` —
الطلب الذي ينتظر جوابًا مدفونٌ بين ما لا يحتاج جوابًا، وهو **المكان الوحيد
الذي يخسر فيه المالك خانةً حقيقية** (المعلّق يحجزها ثمّ تنقضي مهلته).
✅ **مقيس بعده**: ثلاثة أقسام — «بانتظار ردّك ٧» ⇐ «الحالية والقادمة ١٠» ⇐
«المنتهية ١٠٩»، والسبعة كلُّها في الرأس.
⚠️ **وقسمٌ ثالث لا فرزٌ داخل القسم**: تجميعُ اليوم هو ما يجعل القائمة تُقرأ،
وخلطُ معلّقٍ بعيدٍ في رأس يومٍ قريب كان سيكسره. **ولا يُعرَض القسم فارغًا.**

**⑪ الموعد صار عنوانًا على بطاقة المالك لا حاشيتين.** كان سطرين صغيرين بلون
`--muted`: أحدهما **التاريخ خامًا كما يخرج من القاعدة** (`2026-08-22`) والآخر
مدى الوقت — أي أنّ المالك يقرأ رقمًا آليًّا ليعرف أيّ يومٍ هذا. صار «اليوم
22/8 · 8:00 م - 10:00 م» بـ**16px/800** فوق اسم الملعب (14px)، ومعه
`tabular-nums` (البطاقات تُقرأ عمودًا، وأرقامٌ متفاوتة العرض تُعرّج الأعمدة)
و`<bdi dir="ltr">` على المدى وحده. **مقيس: 15.59 نهاري · 15.06 ليلي.**
🔴 **و`.bk-when` كانت محجوزةً** لحبّة الموعد في ورقة المراجعة (‏§35 ③: عمودٌ
موسَّط بصبغةٍ وحدّ) ⇒ اسمي كان سيرثها فيخرج صفُّ التاريخ عمودًا موسَّطًا داخل
بطاقةٍ محاذاتها من البداية. صار `owner-bk-when` — نفس عائلة `pl-card` و
`place-meta`: **فضاء الأصناف مشترك، والاسم العامّ يسرق قاعدةً لم تكتبها.**

**⑫ زرّ إضافة الملعب العائم حُذف.** كان بابين لفعلٍ واحد في الشاشة نفسها:
رابطٌ في عنوان «إدارة الملاعب» و`.owner-fab` فوق آخر بطاقة — والعائم هو
الزائد، يحجب محتوى القائمة التي جاء يضيف إليها. حُذف الوسم والقاعدتان
(الأساسية ونسخة `body.native`) والسطر الذي يُظهره في `showOwnerTab`.

**⑬ 🔴 ثلاثة عناصر بثلاث محاذيات في عمودٍ واحد.** بلاغ «تأكّد من محاذاة
الأزرار» — ومقيس عند 375px في بطاقة الملعب: «تعديل» **66.3px** و«التسعير»
**98.7px** يبدآن من 36.8 وينتهيان عند **103.1** و**135.5** ⇒ حافّةٌ مسنَّنة
على الطرفين؛ والمبدّل يحمل `align-self:center` (‏`.field-card .field-switch`
أعلى الورقة) فيغلب `align-items:flex-end` للأب ⇒ يبدأ عند **63.2** لا 36.8.
صار العمود صنفًا (`field-acts`) بـ`align-items:stretch` والمبدّل
`align-self:flex-end`. **مقيس بعده: الثلاثة تبدأ من 36.8، والزرّان بعرضٍ
واحد 98.7.**

**مقيس** (المعاينة و`owner-harness` على 375 و320، والانتقالات معطَّلة قبل كل
قياس): `npm run check` يمرّ · `npm test` **73/73** · **صفر تجاوز أفقي**
(`docSW === vw`) عند **320 و375** في العربية والإنجليزية وفي أربعة تبويبات
للمالك · **صفر خطأ console من كود التطبيق** · أهداف اللمس: حبّات الرياضات
**44** · زرّ الرجوع **44×44** · أزرار الصور **44** · والتباين بتركيب الشفّافية
**من خلفية العنصر نفسه**: أدنى نهاري **5.69** وأدنى ليلي **5.85** على ثلاث
عشرة نقطة · و`+12,556 native` كما هي.
⚠️ **وبصمة `public/` تغيّرت في اثنين وعشرين ملفًّا، وكلُّها ختمُ تاريخٍ آلي**
(‏«آخر تحديث: 2026-08-22» في الخصوصية والشروط · `lastmod` في `sitemap.xml`)
— لا سطر تصميمٍ واحد. الدفعة على التطبيق وحده.
🔴 **وزلّةُ قياسٍ وقعت وصُحّحت**: قرأتُ «1.21» على كل نصٍّ فحسبتُها كارثة —
والسبب أنّ `color(srgb …)` **بلا رقمٍ في «srgb»** فانزاحت فهرسةُ المكوّنات
واحدًا (‏`m[1],m[2],m[3]` بدل `m[0],m[1],m[2]`)، ومعها بقيت قيمُها 0..1.
وهو المزلق المسجَّل حرفيًّا في هذا الملفّ، ووقعتُ فيه. **وأيّ أداة قياس تباين
تُعايَر أوّلًا على قيمةٍ معروفة** — أبيض على أسود = 21 — قبل أن يُصدَّق رقمٌ
واحد منها.

---

**ترتيب الجذر — 2026-08-22 (بلا أي تغيير في المنتَج)**

الجذر كان **١٣ مجلّدًا** فيه خمسةٌ لا يقرؤها كودٌ ولا بناء. صارت كلُّها في `_refs/`
(‏`brand` · `design` من `docs/مراجع` · `ai-templates` · `legacy-backend` من `backend`
· `replit-exports` من `new`)، ومعها [`_refs/README.md`](_refs/README.md) يقول لماذا
بقي كلُّ مجلّد. **والنقل بـ`git mv`**: ١٦٨ سطرًا سجّلها git `R` لا «حذفًا وإضافة»
⇒ `git log --follow` يبقى يعمل على كلّ ملفّ.

**والمعيار القاطع: بناءٌ كامل ثمّ `git status --porcelain public/` فارغ** — الثلاثون
صفحةً والـAPK خرجت مطابقةً بايتًا ببايت، و`+12,556 native` كما هي. ومعه
`npm run check` يمرّ و`npm test` ٧٣/٧٣.

**وحُذف ثلاثةٌ لا غير، كلٌّ بسببه:** `.local` ×٢ من مشروعَي Replit (‏**٨٦٦MB ·
٨٧٬٩٦٧ ملفًّا** — مهاراتُ الوكيل ومخزنُ pnpm؛ والمصدر `artifacts`/`lib`/`.git` لم
يُمَسّ) · `docs/legal/venue-agreement-ar.txt` (مطابقٌ بالبصمة للـ`.tex`، ومتتبَّعٌ
فيُستَرجَع) · و`desktop.ini`.
⚠️ **و٢١٣ مجموعةً مكرَّرةً تُركت عمدًا لأنّ تكرارها بالتصميم**: الخطوط في ثلاثة
مواضع (‏`fetch-fonts.mjs` له ثلاثة أهداف)، و`app/src/assets/` كلُّها — مخرَجُ بناءٍ
مُتجاهَل في `.gitignore` يعيد `build.ps1` توليده. **فحصُ التكرار بالبصمة يجد
المقصودَ كما يجد الزائد، والفرق يُقرأ من البناء لا من التطابق.**

⚠️ **وأربعة ملفّات كانت ستشير إلى مسارات لم تعد موجودة**، أُصلحت قبل النقل لا بعده:
`.gitignore` · `tools/bundle-project.mjs` (قائمة الاستثناء) · خريطة `README.md` ·
وذكرُ `Code.gs` هنا. **وصفرُ مرجعٍ في الكود** — مقيسٌ بـ`grep` على `*.js|mjs|ps1|json|html`.

---

**الدفعة الثانية والأربعون — الحياد يصير عائلةً واحدة: سلّم درجات مقيس (2026-08-24)**

طلب المالك مراجعة «تدرّجات الألوان»، وحُسم المعنى معه أوّلًا: **سلالم الدرجات
(tints/shades) لا `linear-gradient`** — والثاني ينقض ثلاث دفعات مقيسة (مكتوبٌ حرفيًّا
على `--g-btn` أنه «لايم مسطّح صلب بلا تدرّجات»، و§28 حذفت الحوافّ اللامعة، و§31 أخرجت
حالة «مختار» من التعبئة).

**🔴 ① والقياس كشف ثلاث عائلات لونية في ثيمٍ واحد.** قِستُ كل توكن حيادي بـOKLCH
**بعد** حلّ الكاسكيد (لا بقراءة المصدر — الكتلة العليا يعيد `NATIVE APP REDESIGN`
تعريفها، وهو المزلق المسجَّل):

| | الأسطح | النصّ | الهوية |
|---|---|---|---|
| **نهاري (قبل)** | okH ‏248–265° (أزرق‑بنفسجي) | 216–220° | **208°** |
| **ليلي (قبل)** | okH ‏237–241° (أزرق) | **174–193° (أخضر)** | **208°** |

أي أنّ الليليّ كان **أسطحُه زرقاء ونصُّه أخضر على طرفَي التيل** — وهو ما يُقرأ
«رماديٌّ مخلوط باليد» لا عائلةً واحدة. **وأسوأ فردٍ فيها `--ink-2` النهاري**
(‏`#2C3A30` عند okH **154°** — أخضر صريح في باليتة تيل) **وله ٥٧ مستهلكًا**.

**② والعلاج قيدٌ يجعل الانحدار مستحيلًا لا مضبوطًا باليد: اللمعان النسبيّ ثابت.**
نسبةُ التباين دالّةٌ في `relL` وحدها ⇒ تدوير الصبغة وضبطُ الإشباع **مع تثبيت
`relL`** يُبقي كل رقم تباينٍ في هذا الملفّ صحيحًا بالتعريف. كل توكن حُلَّ ببحثٍ
ثنائي عن `okL` يطابق لمعانه، ثمّ صقلٍ صحيحٍ في جوار ±٦ لكل قناة يختار **أقرب نقطة
في مستوى OKLab a/b** إلى الهدف (‏H=208.3° = زاوية `#0F4B53` بالضبط)، والإشباع على
قوسٍ `sin` يبلغ ذروته في منتصف السلّم ويتلاشى عند الطرفين.
✅ **والنتيجة مقيسة:** الصبغة **نهارًا 111° ⇐ 17°** و**ليلًا 66° ⇐ 9°**، وأقصى
انزياحٍ في نسبة تباينٍ محروسة **0.07** ولا زوج ينزل عن عتبته.

**③ والإشباع على الأسطح وحدها لا على النصّ.** أوّل تمريرة (ذروة إشباع 0.028) خرجت
**بمتوسّط ΔE ‏0.83** — أي **دون عتبة الإدراك** لكل لونٍ على حدة: إصلاح نظامٍ لا
تغيير مظهر. ولمّا كان المطلوب «أجمل»، رُفعت الذروة إلى **0.048 على الأسطح**
فصار الليليُّ **«ليل تيل» فعلًا** — وهو ما **يصفه رأس الملفّ نفسه** («Deep Teal
Night مشتق من أغمق درجات الشعار») بينما كان الحيّ `#0A0E11` أسودَ مزرقًّا يناقضه.
⚠️ **والنصّ بقي عند 0.028 عمدًا**: الإشباع على مساحةٍ كبيرة يُقرأ **خامةَ سطح**،
وعلى حروفٍ يُقرأ **نصًّا ملوّنًا** — وهما شيئان مختلفان.

**④ وثلاثة توكنات كانت ستفلت لولا فحص المخرَج لا المصدر:** `--input-bg` (لم يكن في
كتلة الأسطح أصلًا) · و`--surface-soft` و`--glass` (ظهرا في الكتلة الحيّة وحدها).
كشفها `grep` على **الناتج المبنيّ** بعد التعديل، لا قراءة الباليتة.

**⑤ وتوكناتٌ ميتة تُركت حيث هي وأُعيد ضبطها معها**: `--light-surface-muted` ·
`--light-bg-secondary` · `--dark-bg-secondary` · `--dark-surface-muted` — **صفر
مستهلك** لكلٍّ (مقيس بـ`var()`)، لكنّ إبقاءها على العائلة القديمة كان سيجعل الكتلة
العليا **تكذب على قارئها** كما كذبت من قبل، ويعطي أوّلَ من يعيد وصلها لونًا من
باليتةٍ منقرضة.

**مقيس:** `npm run check` يمرّ (‏**check-contrast: ١٢ زوجًا في الثيمين فوق عتبتها**)
· `npm test` **73/73** · التوكنات كلُّها مقروءة حيّةً بـ`getComputedStyle` في
الثيمين بعد `build.ps1` **وبرابطٍ مبصوم** · `+12,556 native` كما هي · وبصمة
`site.0d1ec77a.css` **لم تتغيّر** ⇒ الموقع لم يُمَسّ، والتغيّر في `public/` **ختمُ
تاريخٍ آلي وحده** (مؤكَّد بـ`git diff` بعد استبعاد أسطر التاريخ: صفر سطر محتوى).

⚠️ **وشريط لقطات `/download` لم يُنفَّذ، وسببُه قياسٌ لا وقت:** الآليّة جاهزة منذ
الدفعة ١٩ وتنتظر ملفّات `shot-<n>-<lang>.png`. والتقاطها من هنا **متعذّر لسببين
مستقلّين**: ① **الحدّ الأدنى لعرض نافذة Chrome بلا واجهة على ويندوز** — أُعيد
إنتاجه في هذه الدفعة: صورةٌ 375×812 وتخطيطُها **504×660** بداخلها ⇒ لقطةٌ مشوَّهة
لا تصلح للنشر · ② **المعاينة تقرأ قاعدة الإنتاج**، ولقطةٌ من لوحة المالك تحمل
**اسم عميل ورقم هاتفه** حرفيًّا (رُئي في هذه الجلسة) ⇒ نشرُها على موقعٍ عامّ
تسريبُ بيانات. **والمصدر الصحيح جهازُ المالك نفسه**: لقطاتٌ حقيقية من الهاتف
تُوضَع في المجلّد فيلتقطها البناءُ التالي بلا سطرٍ واحد.

---

**الدفعة الثالثة والأربعون — تجانس أزرار الرؤوس، و«الرئيسية» تعود إلى الملاعب (2026-08-24)**

بلاغان من المالك مع **أربع لقطات من الجهاز** (مرجعٌ لا نشر: كلُّها تحمل اسمه
⇒ لا تُوضَع على موقعٍ عامّ).

**① صنفٌ واحد من الأزرار بثلاثة مقاسات وشكلين — مقيس لا مقدَّر:**

| الزرّ | كان | صار |
|---|---|---|
| `.hdr-back` (العودة) | 44×44 · **10px** | كما هو ✅ |
| الجرس `.notif-btn` | **38×40** (عرضُه من `.notif-btn` وارتفاعُه من `.ghost-btn`) | 44×44 · 10px |
| `.filter-btn` (الفلتر) | 44×44 · **999px دائرة** | 44×44 · 10px |
| `.hub-ava` (الأفاتار) | **38×38** دائرة | 44×44 دائرة |

**القاعدة المطبَّقة: مقاسٌ واحد لكل أزرار الرأس (‏44 = `--tap-min`)، والشكل يتبع
المعنى لا الموضع** — **فعلٌ ⇒ 10px** (رتبة «تحكّم» في سلّم §37: 10 · 14 · 24 · 999)
و**هويّةٌ ⇒ دائرة**. فبقاءُ الأفاتار دائريًّا **فرقٌ دلاليّ مقصود** لا انحراف:
الدائرة عُرفٌ راسخ للهويّة في iOS وMaterial معًا، والحرفُ يقول «حسابك أنت».
🔴 **وأوّل إصلاحٍ للفلتر لم يعمل**: حرّرتُ `.filter-btn` فبقي `999px` مقيسًا —
لأنّ `.browse-sticky>.filter-btn` أخصُّ منه. **والنوعية قبل الترتيب** — مزلقٌ
مسجَّل، ولم يكشفه إلّا سؤالُ الـCSSOM عن **كل قاعدةٍ تطابق العنصر**.
⚠️ و`.search-btn` يبقى **12px** عمدًا: هو زرُّ فعلٍ مملوء ومجموعٌ مع `.sbtn`
و`.onb-btn` في قاعدةٍ واحدة — رتبةٌ أخرى لا انحرافٌ عن هذه.

**② «الرئيسية» في الشريط كانت تخرج من التبويب كلّه.** `data-nav="hub"` ⇒ الضغطة
تصعد إلى الشبكة لا إلى قائمة الملاعب، والمالك يقرؤها خروجًا من مكانه. صارت
`data-nav="home"`، و`navKey` انقلب: `home` يُبقي مفتاحه، و**`hub` يُعيره**
(`navKey='home'`) فتبقى الحبّة مكانها على الشبكة أيضًا ⇒ لا زرَّ بلا إضاءة.
⚠️ **والشبكة لم تُيتَّم**: يصلها سهمُ `.hdr-back` في الرأس، وتبقى مهبطَ الجلسة
وجذرَ `navigateBack` (الدفعة ٣٥) — **بابان، وكلٌّ يقول وجهته**.

**مقيس** (المعاينة على 375، والانتقالات معطَّلة قبل كل قياس): أزرار الرؤوس
الأربعة **44×44** في الشبكة وصفحة التصفّح معًا · `npm run check` يمرّ ·
`npm test` **73/73** · **صفر تجاوز أفقي** (`docSW === vw`) · و`+12,556 native`
كما هي.

---

**الدفعة الرابعة والأربعون — الهاتف يصير هاتفًا، وشريط اللقطات يُملأ (2026-08-24)**

**🔴 ① وأخطر ما كشفته الدفعة: صورتا الهيرو كانتا تُعلنان واجهةً لم تعد موجودة.**
`app-mockup-{ar,en}.png` مولَّدتان 2026-07-29 وفيهما — بالنظر إليهما — **تعبئةُ
Lime على حالة «مختار»** (أسقطتها §31) و**«10:00 - 12:00 PM»** وهي التسمية
الملتبسة التي أصلحها الترحيل ٢٦ في مصادره الأربعة. أي أن الموقع كان يسوّق
شاشةً فيها عطلٌ مُصلَح. **وأصلٌ تسويقيّ لا يُعاد توليده يبلى كما تبلى الوثيقة**
— وهو نفس درسِ «النصّ الاحتياطي يبلى بلا أن يصرخ شيء» (الدفعة ٣٢ ⑤).

**② والهاتف لم يكن هاتفًا: نسبته 0.762.** مقيس — اللقطة 800×1050، و`.hp-scr`
كانت `height:auto` ⇒ **الجهاز نسخةٌ من نسبة الملفّ**، فخرج لوحًا قصيرًا عريضًا
لا هاتفًا (الحقيقي **0.46**). والإطار نفسه كان كاملًا أصلًا (حافّة معدنية ·
أزرار جانبية · جزيرة · مؤشّر · ميلة ٣D) — **الناقص كان النسبة وحدها**.
الآن `aspect-ratio:9/19.1` على **الإطار** و`object-fit:cover` على الصورة ⇒
يبقى هاتفًا مهما تبدّلت اللقطة، ولا تُعاد كتابة CSS مع كل تحديث صورة.
**مقيس بعده: 310.2×643.8 = 0.482.**

**③ وشريط لقطات `/download` امتلأ بعد أن كان مجلّده فارغًا منذ الدفعة ١٩.**
ثلاث لقطات حقيقية من جهاز المالك (قائمة الملاعب · الشبكة · المباريات المفتوحة)،
والبناء يقول `[shots] 3 per language`. **بإذن المالك الصريح 2026-08-24** رغم أن
سطر الترحيب فيها يحمل اسمه.
⚠️ **والإنجليزية تعرض واجهةً عربية اليوم** — وهو خرقٌ واعٍ لإصلاح الدفعة ٤:
البديل كان إبقاءَ صورةٍ تعلن واجهةً منقرضة، وهو أسوأ. **والحلّ الحقيقي لقطةٌ
إنجليزية من الجهاز** تُوضع باسم `shot-<n>-en.png` فيلتقطها البناء بلا سطر.
🔴 **وجُرِّب توليدُها آليًّا وفشل مرّتين، والسببان مسجَّلان**: Chrome بلا واجهة
على ويندوز **لا ينزل تحت 504px** (أُعيد إنتاجه: صورةٌ 375×812 وتخطيطُها 504×660)،
و**صور الملاعب لا تصل** داخل `--virtual-time-budget` فتخرج البطاقات فارغة —
فلقطةٌ لواجهةٍ نصفُ محمَّلة أسوأ من لا لقطة.

**④ والوزن رُوعي**: اللقطات مُصغَّرة ومكمَّمة (‏560×1244 · ٢٠٠ لون) ⇒ **٢١٨KB
للثلاث**، والهيرو **١٣٦KB** بدل ٦٣٢KB خامًا.

**مقيس:** `npm run check` يمرّ · `npm test` **73/73** · الصور الثلاث `complete`
وبمقاسٍ مصرَّح (‏228×507) · **صفر تجاوز أفقي** (`docSW === vw`) على `/download`
وعلى الرئيسة · و`+12,556 native` كما هي.

---

**الدفعة الخامسة والأربعون — المظهر يتبع الجهاز، والهاتف مقيسٌ على أربعين حالة (2026-08-25)**

**① «حسب الجهاز» صارت قيمةً مخزَّنة لا غيابًا.** كان `Session.theme()` يتبع الجهاز
**عند الغياب وحده** ⇒ ما إن يلمس المستخدم المبدّل حتى **يستحيل عليه العودة**: لا
قيمة في التخزين تعني «اتبع جهازي». الآن ثلاث حالات صريحة، ودالّتان لا واحدة:
`themePref()` تُرجع **ما اختاره** و`theme()` تُرجع **ما يُرسَم** — وخلطُهما كان
سيحفظ «حسب الجهاز» ثمّ يقرأها `dark` فتضيع عند أوّل إعادة فتح.
والدورة `نهاري ⇐ ليلي ⇐ حسب الجهاز`، ومعها **مستمعٌ حيّ** على
`matchMedia('(prefers-color-scheme:dark)')` — بدونه يبقى الاسمُ وعدًا لا يقع حتى
إعادة تشغيل التطبيق.
⚠️ **والأيقونة تصف ما اختاره لا ما يُرسَم**: في «حسب الجهاز» تبقى أيقونةَ الشاشة
ولو كان المرسوم ليليًّا، وإلّا استحال أن يعرف أنّه في الوضع التلقائي أصلًا.
⚠️ **ورُسمت داخل نفس المسار الواحد** (`THEME_ICON.auto`) لا عنصرًا ثانيًا: حارس
«الشمسين» يُخفي كلَّ ابنٍ غير `.thm-ic` بـ`!important` ⇒ أيقونةٌ ثانية كانت
تُرسَم ولا تُرى. **مقيس بعده: `otherChildrenShown = 0` والمسار مرئيّ داخل الزرّ.**

**② وصفّ «الوضع الليلي» كان يكذب بعد التغيير.** ضابطٌ **ثلاثيّ** تحت عنوانٍ ثنائي
وبلا نصٍّ يقول حالته = تخمينٌ لا اختيار. صار **«المظهر»/Appearance** ومعه قيمتُه
بجانب مبدّله. وراسمٌ **واحد** (`paintThemeLabels`) يخدم الموضعين (بند قائمة المالك
· صفّ التفضيلات)، **ويُنادى من مسار تبديل اللغة أيضًا**: النصّ مركَّبٌ من معطًى لا
من `data-i18n` فلا يمسّه المبدّل — مزلقٌ مسجَّل. **مقيس: «نهاري» ⇐ «Light».**
⚠️ و`margin-inline-start:auto` على القيمة: الصفّ `space-between` وصار ثلاثةَ أبناء
⇒ الثالثُ يطفو في **المنتصف** لا بجانب مبدّله (مقيس بعده: الفجوة 20px).

**🔴 ③ وثلاث قيمِ لونٍ بائتة لِلونين اثنين.** `applyTheme` تكتب
`#081D22`/`#FAFAF7` في `theme-color`، وسكربتُ الرأس يكتب `#0A0F0B`/`#F6F8F4`،
**ولا واحدة منها لون الصفحة** بعد الدفعة ٤٢. وُحِّدت على `#021012`/`#F2F6F7`.
والدرس مكرَّر: **قيمةٌ منسوخة في موضعين تنحرف** — وهذه كانت في ثلاثة.

**④ وهيكل الهاتف على الموقع قِيس على أربعين حالة، صفر خرق:**
١٥ عرضًا (‏320…1920) × لغتين = **30/30**، ومعها ٥ أشكال أجهزة × ثيمين = **10/10**
(هاتف أفقي 844×390 · لوح أفقي وعمودي · أندرويد صغير · **Galaxy Fold مطويًّا 280px**).
النسبة **0.482…0.486** في كلّها، والصورة محمَّلة، و**صفر تجاوز أفقي** وصفر خروجٍ
عن الشاشة. والاتّجاه صحيح في الوجهين (`rtl`/`ltr`).

**مقيس:** `npm run check` يمرّ · `npm test` **73/73** · الدورة الثلاثية ستّ نقراتٍ
متتالية بلا نقرةٍ ضائعة · وقلبُ وضع الجهاز **حيًّا بلا إعادة تحميل ولا نقرة**
يُبدّل التطبيق (‏`osDark:true ⇒ appDark:true`) · واختيارٌ صريح **لا يتبع الجهاز**
(‏`pref=light` مع `osDark=true` ⇒ نهاري) · و`+12,556 native` كما هي.

---

**الدفعة السادسة والأربعون — صورةُ الهيرو تُبصَم، فيستحيل أن تُخدَم بائتة (2026-08-25)**

بلاغ المالك: «صورة التلفون غلط **لمّا أفتح الموقع من اللابتوب**» — وكل قياساتي
كانت خضراء (٤٠ حالة في الدفعة ٤٥). **والسبب أنّ الخلل لم يكن في المخرَج بل في
ما يملكه المتصفّح**، ولا يراه من يفحص بملفٍّ شخصيّ نظيف أبدًا.

**🔴 والعلّة: الدفعة ٤٤ غيّرت شكل الجهاز ولم تغيّر اسم الملفّ.** صار الإطار يثبّت
`aspect-ratio:9/19.1` والصورة `object-fit:cover`، والصورة الجديدة **660×1400**
(‏0.471). لكنّ المسار بقي `/assets/app-mockup-<lang>.png` — والاسمُ نفسُه.
فكلّ متصفّحٍ زار الموقع قبلها يخدم نسخته المخزَّنة **800×1050** (‏0.762) داخل
إطارٍ بُني لنسبةٍ أضيق ⇒ `cover` يقصّ **٣٨٪ من العرض**.
✅ **وأُعيد إنتاجه حرفيًّا** باستخراج الصورة القديمة من git وحقنها في الصفحة
الحيّة: أرقامُ الخطوات وزرُّ «متابعة الحجز» **مقصوصةٌ من الحافّة**، والواجهة
المعروضة هي التصميم الميت (تعبئةُ Lime التي أسقطتها §31). صورةٌ تُقرأ عطلًا.

**والعلاج بصمةٌ في الاسم لا `Cache-Control`:** الصورة انتقلت إلى
`/build/app-mockup-<lang>.<hash>.png` — **نفس ما يفعله البناء بالورقة والسكربت
منذ البداية**، وتعليقُه يقول السبب حرفيًّا: «الشعارات في `/assets` تحتفظ
بأسمائها **فيلزمها كاشٌ أقصر**». والصورة لم تكن شعارًا: **شكلُها جزءٌ من العقد
مع الـCSS**، فتغيّرُه بلا تغيّر الاسم يكسر كلَّ زائرٍ عائد.
⚠️ **والنسخة القديمة تبقى في `/assets/`** عمدًا: صفحةٌ مخزَّنة تشير إليها فتجد
الصورة الصحيحة بالشكل الصحيح — شبكةُ أمانٍ لا وزنٌ ضائع.

**⚠️ والدرس أعمّ من الصورة: أيُّ أصلٍ يتقاسم عقدًا مع الـCSS يُبصَم.** بصمةُ
المحتوى ليست تحسينَ أداء هنا بل **شرطَ صحّة**: ما دام الاسم ثابتًا فالمتصفّح
محقٌّ في ألّا يسأل، والخطأ خطؤنا. ومعها درسُ قياسٍ ثالث يُضاف إلى المسجَّل:
**«مقيسٌ على المخرَج» لا يساوي «مقيسٌ عند المستخدم»** — فحصُ ملفٍّ نظيف يعمى
عن كلّ عطلٍ مصدرُه الكاش، وسؤالُ «ماذا يملك المتصفّح من قبل؟» جزءٌ من الفحص.

**مقيس:** المسار المنشور `/build/app-mockup-ar.7feb25e0.png` (وen بجانبه) ·
الصورة محمَّلة و`naturalRatio = 0.471` مقابل إطارٍ 0.471 ⇒ **صفر اقتصاص** ·
`npm run check` يمرّ · `npm test` **73/73** · `[keys] all placeholders resolved`
· وصفر تجاوز أفقي.

---

## 📌 قرارات محسومة — لا تُعاد مناقشتها

1. **الحجز من التطبيق فقط.** الـSPA سُحبت (`/app/*` ⇒ 301 إلى `/download`) لأنها كانت على
   Apps Script/Sheets بينما التطبيق على Postgres ⇒ **قاعدتان لا تريان بعضهما ⇒ حجز مزدوج**.
   ⚠️ **الأثر المقبول: مستخدمو الآيفون لا يستطيعون الحجز** — `/download` تقول ذلك صراحةً.
   ⇒ **المرحلة (د) من الخطة ② أُلغيت** (مصادر الـSPA حُذفت في 2026-07-29، مؤرشفة في git).
2. **لا PWA.** وجهان فقط. والتذييل **يُلغي أي service worker مسجَّل ويمسح كاشه**.
3. **الاستضافة Vercel** (`mustadeera.vercel.app`). ⚠️ **محجوب بفلترة SNI** على شبكتك وعلى
   بيئتي ⇒ **لا يُتحقَّق منه حيًّا من هنا إطلاقًا** — عبر لوحة `vercel.com` أو معاينة محلّية.
4. **`/admin` بالرابط فقط** — بلا رابط وارد + `Disallow` في robots. **الإخفاء ليس أمانًا**؛
   الأمان في RLS و`is_admin()`. الوصول اليومي: مفضّلة المتصفّح.
5. **إعادة تعيين كلمة السرّ لن تُبنى في `/admin`** — تحتاج `service_role` في المتصفّح = تسليم
   القاعدة. الوصفة في [`migration/RECIPES.sql`](migration/RECIPES.sql) §8. وعرضها **مستحيل** (bcrypt).
6. **قاعدة الصدق (م5):** لا «حجز فوري»، لا رقم مخترَع، لا نشاط وهمي. حقل فارغ ⇒ حالة صادقة.
8. **الرئيسية شبكة Bento، والأقسام الثلاثة القادمة تُعرَض بشارة «قريبًا»** (قرار المالك
   2026-08-18، بعد أن عُرض عليه أنّ لا جدول لأيٍّ منها وأنّ الشبكة تضيف شاشةً قبل
   التصفّح). ⚠️ **والشارة تلتزم بم5**: البطاقة **لا تفتح على فراغ** ونقرُها يقول ما هي
   ولا يَعِد بموعد. وحين يصير قسمٌ حقيقيًّا تُبنى شاشتُه في دفعته وتسقط شارتُه معها —
   ولا يُفتَح بعلَمٍ في القاعدة على شاشةٍ غير موجودة.

7. **الوعد عريض والمتاح مذكور.** المنتج للملاعب الرياضية عمومًا، فلا يُوصَف بـ«كرة القدم»
   في أي عنوان أو شعار. لكنّ م5 تمنع الإيحاء بأن رياضةً أخرى قابلة للحجز اليوم ⇒ كل موضع
   يوسّع الوعد **يذكر في جملته نفسها** أن المفتوح كرة القدم. **ومصدر الحقيقة صار القاعدة لا الكود** (2026-08-06): رياضةٌ لها ملعب
   نشط واحد هي المفتوحة — يشتقّها التطبيق بـ`sportHasVenues()` ويشتقّها بناء الموقع من
   نفس الصفوف (`$SportLive` في `build-site.ps1`). ⇒ **استحال أن يعد الموقعُ بما لا يعطيه
   التطبيق**: لا قيمة `ready:true/false` تُكتب بيد في أيّ من الوجهين.

---

## ⚠️ مزالق مقيسة — تكلفتها دفعات كاملة

**التحقّق**
- **🔴 «موجود في الفهرس» لا يساوي «مسموحٌ باستدعائه».** `ListModels` بمفتاح Gemini
  كان يُرجع `gemini-2.5-flash`، و`generateContent` عليه يردّ **404 «no longer
  available to new users»**. الفهرس يصف الكتالوج، والإذن يُقاس **بالنداء الحقيقي**
  وحده. ونفس البنية في فحص دوالّ PostgREST أدناه: **ردٌّ واحد لمعنيين.**
- **🔴 اسمُ نموذجٍ خارجيّ أصلٌ مؤرَّخ لا ثابت.** ماتَ افتراضيّان في يومٍ واحد
  بطريقتين (إيقافٌ تامّ · حجبٌ عن الحسابات الجديدة). فليكن الافتراضي **لاحقة**
  (‏`gemini-flash-latest`) لا لقطةً مثبَّتة، **ومتغيّرُ بيئةٍ يتجاوزه بلا إعادة نشر**
  — إلّا حيث يكون انحرافُ المخرَج نفسه خطرًا، فيُثبَّت ويُراقَب.
- **🔴 سقفُ التوكنات يفشل كخطأ صياغة لا كخطأ طول.** `maxOutputTokens` صغير ⇒ يُقطع
  الردّ في منتصف كائن ⇒ يجد المحلّلُ قوسَ إغلاقٍ داخليًّا ويرمي `JSON.parse` ⇒
  «غير قابل للتحليل». **لا شيء في السلسلة يقول «طويل».** والعربية أثقل بالتوكنات
  من الإنجليزية التي يُقدَّر عليها السقف عادةً ⇒ ما يمرّ بالإنجليزية يُقطع بالعربية
  (مقيس: التقييمات تنجح والمستشار يفشل عند نفس السقف). ⚠️ ومعه: النماذج **المفكِّرة**
  تخصم تفكيرها من **نفس** السقف، وقد تُعيد جزءَ تفكيرٍ **قبل** جزء الجواب ⇒
  **اجمع `parts` كلَّها** ولا تقرأ `parts[0]`.
- **🔴 نداءُ دالّةٍ بجسمٍ فارغ يقول «غير موجودة» عن دالّةٍ موجودة.** PostgREST
  يحسم الدالّة **باسمها وأسماء وسائطها معًا**، فـ`POST /rpc/f` بـ`{}` على دالّةٍ
  لها وسائط إلزامية يردّ `PGRST202` — وهو **نفس ردّ الغياب حرفًا بحرف**. أعلنت
  `/admin` «١ ترحيل ناقص» و«18 غير مُشغَّل» وكلاهما **مُشغَّل**: النداء بالوسائط
  الحقيقية يردّ `42501 permission denied` أو `200`، **ورفضُ الصلاحية دليلُ وجود**.
  فحصُ دالّةٍ يحمل وسائطها دائمًا، بقيمٍ لا تطابق شيئًا (uuid أصفار) فلا أثرَ يقع.
  ⚠️ و`OPTIONS /rpc/<any>` يردّ **200 لأي اسم** فلا يصلح فحصًا، والـOpenAPI الجذري
  لا يُظهر مسارات `rpc` لـanon (صفر مسار).
- **🔴 `select b.*` في تعريف عرضٍ **تُفكّ عند الإنشاء لا عند القراءة**.** فكلّ عمودٍ
  يضيفه ترحيلٌ لاحق إلى الجدول يبقى **خارج العرض** بلا خطأ ولا تحذير. `bookings_full`
  فقد `cancel_kind` و`no_show` و`visibility` و`players_needed` و`players_brought`
  ثلاثَ دفعات (‏15/16/22) — والتطبيق يقرأ العرض بينما `/admin` تقرأ الجدول خامًا،
  فبدت الميزة «غير مُفعّلة» في التطبيق و«تعمل» في اللوحة. ولا يُصلحه
  `create or replace view` (‏ترتيب الأعمدة يتغيّر) بل الحذف ثمّ الإنشاء.
  **وكلُّ ترحيلٍ يضيف عمودًا إلى `bookings` يجب أن يعيد بناء العرض معه.**
- **🔴 كتلةٌ تُقرأ في المصدر قد تكون مطفأةً بقاعدةٍ بينك وبينها ألف سطر.** ثمانِ
  طبقات تدرّج بـ`blur(36px)` وحركةِ ٢٦ ثانية كانت مكتوبةً في `#app::before`،
  ومقتولةً بـ`#app::before,#app::after{display:none!important}` في تمريرةٍ لاحقة
  ⇒ **صفر بكسل مرسوم**. ومراجعةٌ خارجية بنَت عليها أقوى بنودها («إعادة تركيب GPU
  في كل فريم») وهي لا تُرسَم أصلًا. و`grep` على **تعريف** القاعدة لا يرى قاتلَها.
  أيّ تشخيص أداءٍ أو مظهرٍ مبنيّ على قراءة CSS يُصدَّق بعد `getComputedStyle` وحده —
  ونفس الشيء ينطبق على كل `!important` لاحق (‏٤ من ٥ «لمعات» حذفتُها كانت ميتة كذلك).
- **قبول العملية ≠ نجاحها.** Realtime يردّ `phx_reply:"ok"` على جدول **غير موجود**؛ الإشارة
  الصادقة رسالة `system` بعده. وRLS «يبدو» صحيحًا حتى تستعلم بالمفتاح العام فعلًا.
- **عدّ عقد الـDOM أعمى عن `::before`/`::after`.** عطل «الشمسين» نجا من خمس دفعات لأن الفحص
  كان يعدّ الأبناء. اقرأ `getComputedStyle(el,'::before').content`، **وانظر إلى الصورة**.
- **عطل لا يُعاد إنتاجه بعد محاولتين؟** لا تُصلح الآلية — أزل **البنية** التي تسمح به.
- **لوحة خلف تسجيل دخول تُفحَص بشبكة مموَّهة لا بالنظر إلى الكود.** نسخةٌ من المخرَج
  المبنيّ يُحقَن فيها سكربتٌ يستبدل `fetch`/`WebSocket` ويزرع جلسة وهمية ⇒ كل لوح يُرى
  فعلًا وكل مسار خطأ يُجرَّب (نجاح · رفض RLS · تراجع). هكذا انكشفت **ثلاثة** أعطال ما كانت
  القراءة لتكشفها: «٠ نشِطين» · حلقة `setTimeout` · و«+15%» مقلوبة. تُبنى في
  `public/_admin_test.html` **وتُحذف بعدها** (‏`public/` مخرَج متتبَّع).

- **🔴 تعبيرٌ نمطيّ فيه علامة اقتباس يُعمي `check-globals` عن كلّ ما بعده.**
  `/[",\n]/` بريئةٌ في القراءة: الحارس يتخطّى النصوص ولا يعرف التعابير النمطية، فيقرأ
  `"` بدايةَ نصٍّ ويبتلع ما بعده ⇒ **ثمانية عشر بلاغًا كاذبًا** عن أسماء معرَّفة فعلًا
  (كلُّها معرَّفةٌ **بعد** موضع التعبير — وهي الأمارة التي تفضحه). **وبلاغٌ كاذب واحد
  يكفي ليُعطَّل الحارس كلُّه**، فالأصحّ تجنّب الصيغة لا إضعاف الحارس.
- **🔴 التطبيق مغلَّف في IIFE — فلا يُفحَص بحقن `State`.** «صفر متغيرات عامة في window»
  مكتوبةٌ في رأس `app.js`، فسكربتٌ لاحق يعطي `State is not defined` **بلا خطأٍ في
  الـconsole** والصفحة تبدو سليمة. المدخل الوحيد **الشبكة**: استبدلْ `fetch` بردودٍ
  بشكل **صفوف القاعدة الخام** فيجري المسار الحقيقي كلُّه (`tools/owner-harness.mjs`).
- **🔴 قياس التباين يحتاج شيئين معًا: تركيبَ الشفّافية، وخلفيةَ العنصر نفسه.**
  قرأتُ «1.15:1» على شريطٍ خلفيتُه `rgba(…,.12)` (بلا تركيب)، و«1.04» على خليّةٍ قِستُ
  خلفية **أبيها** وتجاهلتُ خلفيتها هي. وكلاهما رقمٌ كاذب يُرسل إلى إصلاحٍ لا لزوم له —
  والأخطر أنه يُخفي الخرق الحقيقي بين البلاغات الكاذبة.
- **🔴 `month:'short'` بالعربية ليست مختصرة** — نفس مزلق `weekday:'short'` بالضبط:
  Intl يردّ «أغسطس» و«سبتمبر» كاملتين بينما الإنجليزية تردّ «Aug». والمصفوفة تُكتب ولا
  تُقصّ من مخرَج Intl (قصُّ ثلاثة محارف يكسر عند أوّل لهجة تختلف: «آب» مقابل «أغسطس»).
- **توكنٌ غير معرَّف يفشل صامتًا لا صاخبًا.** `var(--line-1)` مستعمَلة خمس مرّات
  و**غير معرَّفة قطّ** ⇒ الحدّ يعود `currentColor` فيبدو «تصميمًا». `grep` على
  **التعريف** لا على الاستعمال قبل الاعتماد على أي توكن.

**PowerShell**
- **صفر محرف عربي في أي `.ps1`** — PS 5.1 يُسيء فكّ UTF-8 بلا BOM، والـBOM يضيع عند التحرير.
  كل النصوص في `site/strings/{ar,en}.txt`.
- **`$rel` و`$Rel` متغيّر واحد** (لا تفرقة بحالة الأحرف) · **`$pid` محجوز**.
- **🔴 `curl` في PowerShell ليست curl** — بل اسمٌ مستعار لـ`Invoke-WebRequest` لا
  يعرف `-s` ولا `-d`، فيتوقّف سائلًا عن `Uri` ويعلّق الطرفية. استعمل
  `Invoke-RestMethod` بصيغتها، أو `curl.exe` صراحةً.
- **🔴 `npx` يشغّل `npx.ps1` فيصطدم بسياسة التنفيذ** (`UnauthorizedAccess`) على
  جهازٍ لم يُرخَّص فيه تشغيل السكربتات. **`npx.cmd` يتجاوزها بلا تغيير أي إعداد
  نظام** — وهو الحلّ المفضَّل على `Set-ExecutionPolicy`.
- **كلُّ أمرٍ يُعطى للمالك يُكتب سطرًا واحدًا بفواصل `;`** — الأوامر متعدّدة
  الأسطر تُلصَق في الطرفية فتُنفَّذ سطرًا سطرًا وتنكسر (مبلَّغٌ من المالك).
- **البصمة تُثبت السرّ بلا كشفه:** `supabase secrets list` يُرجع **sha256** لكل
  قيمة ⇒ احسب بصمة القيمة المتوقّعة محلّيًّا وقارنها. بها ثبت أنّ مفتاح Gemini
  والنموذج مخزَّنان نظيفَين، **فانتقل البحث إلى المفتاح نفسه بدل تخمين التخزين**.
  ⚠️ وزلّةُ مسافةٍ واحدة تبتلع علَمًا في قيمة السرّ: `KEY=…--project-ref`.

**الويب**
- **🔴 `overflow-x:auto` تُبطل كلَّ `position:sticky` رأسيّ بداخلها — صامتةً.** حين لا
  يكون المحور الآخر `visible` تُحسَب `overflow-y` **`auto`** كذلك ⇒ يصير **الصندوق** هو
  مرسى الالتصاق لا الصفحة، وارتفاعه من محتواه (`clientH === scrollH`) فلا يتمرّر رأسيًّا
  أبدًا ⇒ `top:0` لا تُفعَّل قطّ. `thead th{position:sticky;top:0}` في `/admin` كانت مكتوبةً
  **ومعها تعليقٌ يشرح فائدتها** منذ الدفعة ١٩ وهي لا تفعل شيئًا: مقيس بعد تمرير 400px
  `thead.top = **−204**`. والعلاج سقفُ ارتفاعٍ على الصندوق (‏`max-block-size`) فيصير
  قابلًا للتمرير رأسيًّا — والجداول الأقصر منه لا تتأثّر بحرف. **ولا يُكشَف بالقراءة:
  القاعدة سليمة، والمُبطِل خاصّيةٌ على الأب.**
- **🔴 عنصرٌ `fixed` أسفل الشاشة يبتلع آخر ما تحته، ولا يقوله شيء.** شريط التنزيل
  (‏٦٩px عند `bottom:0`) كان يغطّي «Switch to English» في التذييل **بالكامل** (مقيس:
  الرابط 764…790 وقمّة الشريط 743) و`.ft` حشوتها السفلية `0`. والفحص الصحيح **إصابةٌ لا
  هندسة**: `elementFromPoint` على مركز الرابط يقول من يستقبل النقرة فعلًا. والحجز يكون
  **من نفس التوكن** الذي يفرض ارتفاع الشريط (`min-block-size`)، وإلّا صار رقمين يتفارقان.
  ⚠️ ومشروطًا بوجود الشريط (`body:has(…)`) وإلّا تركتَ فراغًا تحت كل صفحةٍ لا شريط فيها.
- **🔴 صفٌّ شبكتُه لنفسه لا يحاذي جاره.** `.fn-row{display:grid;grid-template-columns:
  minmax(84px,auto) …}` تجعل **كلَّ صفٍّ شبكةً مستقلّة** ⇒ عمودُ التسمية يتّسع
  بمقدار تسميته هو، فتخرج مسارات الأشرطة بأطوالٍ مختلفة في الرسم الواحد
  (مقيس: 365.4 · 361.1 · **345.6**). والأثر ليس شكليًّا: شريطٌ ممتلئ ١٠٠٪ في مسارٍ
  أقصر يبدو **أقلَّ** من شريطٍ أقلّ في مسارٍ أطول ⇒ **المقارنة البصرية تكذب**.
  الشبكة على **الحاوية** و`display:contents` على الصفّ.
- **حاويةٌ ثلاثية بـ`space-between` لا تُوسّط وسطَها.** التوسّط يتحقّق **فقط** حين
  يتساوى عرضُ الطرفين — وأيّ قاعدة تغيّر أحدهما (زرٌّ يصير ممتلئًا في الشريحة
  الأخيرة) تكسره صامتةً، **ولا يظهر إلّا في تلك الحالة**. `grid-template-columns:
  1fr auto 1fr` تجعل الطرفين متساويين **بالتعريف** فلا يعتمد التوسّط على المحتوى.
- **`@media` لا تضيف نوعيّة.** `.steps-lean .step` (0,2,0) تغلب `.step` (0,1,0)
  المكتوبة داخل `@media (min-width:820px)` مهما كان الترتيب ⇒ حشوة 66px تُسحق إلى
  18 فيقع النصّ **داخل** دائرة الرقم. الاستثناء يُكتب بنوعيّةٍ مكافئة **داخل**
  الاستعلام نفسه. (نفس عائلة «لاحقة أحدث تُبطل مختصرًا» المسجّلة.)
- **`weekday:'short'` بالعربية ليست مختصرة** — Intl يردّ «الخميس» و«الأربعاء» كما
  هما، والإنجليزية تردّ «Thu». أيّ تخطيطٍ يفترض ثلاثة محارف ينكسر بالعربية وحدها.
  ⚠️ **وفي SVG يتمدّد إلى عرض حاويته: تصغيرُ `font-size` يُكبّر النصَّ المرسوم**
  (‏viewBox أضيق ⇒ مقياسٌ أعلى) — الرقم في الورقة ليس الرقم على الشاشة.
- **صنفٌ يُكتب عليه `textContent` من JS لا يحتمل بنيةً داخله.**
  `$$('[data-action="toggleLang"]').forEach(b=>b.textContent=…)` محا أيقونةَ بندٍ
  في قائمةٍ وتسميتَه. وأيّ حارسٍ يقول `> :not(.x){display:none!important}` يفعل
  الشيء نفسه. **والعلاج إخراجُ العنصر من نطاق القاعدة (اسمُ فعلٍ آخر يستدعي
  الدالّة نفسها) لا إضعافُ القاعدة باستثناء.**
- **CSP يطابق المخطّط حرفيًّا:** `https://*.supabase.co` **لا يُجيز** `wss://` لنفس المضيف.
- **`inset-inline-start:50%` + `translateX(-50%)` لا يتمركز في RTL** (`transform` غير مدرك
  للاتجاه). البديل: `inset-inline:0; margin-inline:auto` بعرض محدَّد.
- **قبل تنعيم حركة:** هل الخاصية مركَّبة على الـGPU؟ (`transform`/`opacity` نعم ·
  `clip-path`/`box-shadow`/`filter` لا). وهل قاعدة الإيقاف تُوقف فعلًا؟ (`animation:none`
  **لا تمسّ** `transition`).
- **المسارات العربية:** مرمَّزة في الروابط/canonical/sitemap، **خام على القرص**. اختبرها عبر HTTP.
- **200 من المتصفّح قد تكون من كاشه** — افحص بـ`cache:'no-store'`.
- **`transform` فيزيائي ⇒ كل إزاحة أفقية تُضرَب في `--dirx`** (`1` في LTR · `-1` في RTL).
  بإشارة معكوسة كان درج الجوّال يعبر الشاشة عرضًا بدل أن ينزلق من حافّته — والخلل **لا يظهر
  إلّا في اتجاه واحد**، فاختبر اللغتين معًا دائمًا.
- **لا تُخفِ محتوى خلف سكربت قد لا يصل.** `.has-js` يضعها سكربت **مضمَّن** في `<head>`،
  ومعها مؤقّت 2ث يزيلها إن لم يصل الملفّ الخارجي ⇒ لا نصّ يبقى مخفيًّا لأن ملفًّا فشل.
- **قياس التباين نفسه قد يكذب:** `color-mix` يُحسَب إلى `color(srgb 0.98 …)` بقيم **0..1**
  لا 0..255، وقارئ ساذج يقرأها سوادًا ⇒ «1.06:1» كاذبة. وطبقةٌ شفّافة فوق خلفية داكنة
  تحتاج **تركيبًا** لا قراءة أقرب خلفية غير شفّافة.
- **ألوان الهوية لا تصلح نصًّا على سطح فاتح.** مقيسة على `#FFF`: Lime ‎`#8CC63E` ⇒
  **2.05:1** وMint ‎`#12B76A` ⇒ **2.62:1** — وكلاهما راسب لنصّ 11px (المطلوب 4.5:1)، بينما
  يقيسان 8.5:1 و10:1 على البطاقة الداكنة. **فالخطأ لا يُرى في الثيم الليلي إطلاقًا**، ومن
  يفحص واحدًا يمرّره. العلاج توكنان نصّيان بدرجتين أغمق في النهاري يعودان لون الهوية في
  الليلي (`--auth-lime-ink` · `--auth-mint-ink` في `app.css`). و`--soft` ‎(2.87:1) للزخرفة
  لا لتلميحٍ يحمل معلومة — استعمل `--muted` ‎(6.2:1).
- **نصّ فوق صورة: قِس بالبكسل لا بالتقدير.** ارسم الصورة على canvas بنفس
  `background-size/position`، وخذ **أسطع ٢٪** من بكسلات ما تحت النصّ (لا المتوسّط —
  التباين ينكسر عند ألمع بقعة)، ثمّ ركّبها بـopacity وقناع التدرّج.
- **المعدود العربي يتغيّر مع العدد** (١ · ٢ · ٣-١٠ جمع · ١١+ مفرد منصوب). كل رقم **حيّ**
  يسبق اسمًا يحتاج `countNoun`، وإلّا خرجت «مجموع 3 مكانًا». في مسمّيات ثابتة استعمل
  اسمًا معرَّفًا بأل يصحّ مع أي عدد. و`Intl.RelativeTimeFormat('ar')` يتكفّل بهذا مجّانًا.
- **كل عنصر يلاحق المؤشّر يضع نفسه تحته.** اختبار الإصابة يُحسَب بعد الـ`transform`،
  فالزرّ المنجذب يتناوب `pointerenter`/`pointerleave` مع **جاره** ويتعلّق. والعطل لا
  يظهر على عنصر منفرد ⇒ ينجو من الفحص. البديل: بقعة ضوء داخل العنصر بلا تحريكه.
- **`animation-timeline:view()` يقيس داخل أقرب حاوية ممرِّرة.** و`overflow:hidden`
  يصنع واحدة (‏`overflow-x:clip` لا) ⇒ حركة على صورة داخل إطار مقصوص تبقى
  `running` وتقدّمها صفر أبدًا. سمِّ المخطّط على الإطار: `view-timeline-name`.
  ⚠️ وعند الفحص: `scroll-behavior:smooth` يبتلع قفزات `scrollTop` فتقيس y≈50 وأنت
  تحسبك عند 600 — عطّله مؤقّتًا.
- **`--force-device-scale-factor` في Chrome بلا واجهة يضاعف اللوحة لا التخطيط**
  ⇒ الإطار يُقصّ في RTL. للمضاعفة: `zoom:2` على `body` مع `html{width:2×}`.
- **`--window-size` في Chrome بلا واجهة على ويندوز محكومٌ بحدّ أدنى ≈504px.** طلبتُ 390
  فخرجت اللقطة 390 بكسلًا والتخطيط داخلها **504** ⇒ محتوى مقصوص على الحافّتين يُقرأ
  «تجاوزًا أفقيًّا» وهو سليم تمامًا. **افحص `innerWidth` من داخل الصفحة قبل تصديق أي لقطة
  ضيّقة** (صفحة تطبع `innerWidth` بخطّ كبير تكفي)، أو صوّر عند 504 فأعلى وقِس ما دونها
  بـ`getBoundingClientRect` في متصفّح حقيقي.
- **`overflow-x:clip` يُبطل `scrollWidth` كاختبار تجاوز.** الـclip يقصّ بلا تمرير، فيبقى
  `scrollWidth === innerWidth` ولو كان المحتوى مبتورًا فعلًا. الاختبار الصحيح: مرّ على
  العناصر وقارن `getBoundingClientRect().right` بـ`innerWidth`.
- **قياس تباين نصّ **بالنصّ ظاهرًا** يكذب.** بكسلات تنعيم حواف الحرف نفسها تُعدّ خلفيةً
  ساطعة، فتسحب «أسطع ٢٪» إلى أعلى: أعطى السطر المحيطي 2.18:1 (راسب) بينما قيمته الحقيقية
  **6.2:1**. القياس الصحيح: أخفِ النصّ (`visibility:hidden`) ثمّ قِس الخلفية وحدها،
  وركّب لون النصّ فوقها حسابيًّا.
- **bidi يقلب المدى العددي في السطر العربي.** «40–60» (الشرطة محايدة بين رقمين أوروبيّين)
  تُعرَض **«60–40»** — أي أعلى سعر ثمّ أدناه. العلاج `<bdi dir="ltr">` حول المدى وحده،
  والعملة خارجه. أُصلح في موضعيه: بلاطة التسعير في الرئيسة و`Render-PlaceBody`
  (صفّ «سعر الساعة» في `/places/<اسم>/`). ⚠️ **ولا يُفحَص بقراءة الـDOM**: النصّ فيه
  «40–60» في الحالتين — الفحص بقياس موضع الرقمين (`Range.getBoundingClientRect`)
  أو بالنظر إلى اللقطة.
- **النوعية قبل الترتيب:** `.final h2` (صنف+وسم) تغلب `.final-big` (صنف)، فالحجم الجديد
  يُكتَب ولا يُطبَّق ولو جاء بعده في الملفّ. اكتب `h2.final-big` أو قِس الناتج — لا تفترض
  أن آخر قاعدة تفوز.
- **`:where()` نوعيّتها صفر — فلا تضع فيها ما ينازعه صنف.** حلقة التركيز كُتبت
  `:where(button,a,…):focus-visible{outline;box-shadow}` فظهر الحدّ **ولم تظهر الهالة**: لا
  ينازع أحدٌ `outline`، بينما `box-shadow` ينازعه ظلّ كل زرّ (`.sbtn` · `.cbtn`). بالوسم
  الصريح تصير (0,1,1) فتعلو. ⚠️ ولا يُفحَص بـ`el.focus()`: البرمجي لا يفعّل `:focus-visible`
  غالبًا — اضغط Tab فعلًا.
- **`0` قيمة زائفة تُرسَم.** `cond && h(...)` تصلح مع `false` و`''` لأنهما لا يظهران، أمّا
  الصفر فيمرّ إلى الأبناء ويُطبَع رقمًا عاريًا. ظهر «0» فوق صورة كل بطاقة، ولم يكشفه إلّا
  النظر إلى اللقطة. استعمل الثلاثية.
- **bidi يقلب أي مقطع لغته معاكسة للجملة**، لا الأرقام وحدها: اسم ملعب عربي («ملعب 2») داخل
  جملة إنجليزية يصير «2 ملعب»، ومدى وقت («10:00 - 12:00») داخل جملة عربية ينقلب فيسبق
  الانتهاءُ الابتداء. اعزل **القيمة** بـ`<bdi>` (‏`dir="ltr"` للمدى العددي) وأبقِ الجملة
  كاملة في ملفّ اللغة بعلامة نائبة ثمّ قسّمها.
- **الفحص البصري في جزء المعاينة يحتاج إدخالًا حقيقيًّا.** `scrollIntoView`/`scrollTo` من JS
  تُحرّك التخطيط فعلًا (‏`getBoundingClientRect` يثبت ذلك) لكنّ **اللقطة تبقى قديمة**؛ استعمل
  `computer scroll` كي يُعاد التركيب. و`scroll-behavior:smooth` يبتلع القفزات فعطّلها أوّلًا.
- **قبل تصديق أي رقم أداء: قِس خطّ أساس ساكن ونقرةً خاملة.** بدونهما لا تعرف إن كان السقوط
  من تغييرك أم من البيئة. هنا: ساكن صفر، نقرة خاملة صفر ⇒ سقوط تبديل الثيم حقيقي.
- **حقلٌ ثابت داخل لوحة يُعاد بناؤها = إدخالٌ يُمحى صامتًا.** حقول السعر و«متاح اليوم»
  تعيش في HTML ولا يُعاد إنشاؤها، لكن كل نقرة شريحة تُعيد بناء ورقة الفلاتر وتُعيد كتابة
  قيمها **من المسودّة** — والمسودّة لم تكن تُكتب إلا عند «تطبيق». العلاج: يكتب المستخدمُ
  المسودّةَ فور إدخاله (`oninput`). ولا تُزامن DOM⇒مسودّة قبل الرسم: عند أوّل فتح تحمل
  الحقول نصّ الجلسة السابقة فتُحييه في مسودّة جديدة.
  ⚠️ وعلى عنصر ثابت استعمل **إسناد الخاصّية** (`el.oninput=`) لا `addEventListener`:
  الإسناد متكافئ، والإضافة تكدّس مستمعًا في كل إعادة بناء.
- **العنصر المطلق يُوضَع من صندوق الحشوة لا صندوق الحدّ.** حبّة الشريط السفلي تُقاس بفارق
  مستطيلين (`item.rect − nav.rect`) وهو فارقُ **صندوقَي الحدّ**، و`.bnav` لها `border-top:1px`
  ⇒ الحبّة تجلس بكسلًا أخفض من زرّها أبدًا. اطرح `borderTopWidth`/`borderLeftWidth`،
  **والفيزيائي منهما لا المنطقي** — `getBoundingClientRect` فيزيائي فلا يُطرح منه
  `borderInlineStartWidth`.
- **`requestAnimationFrame` لا يُطلَق في تبويب مخفيّ.** أيّ قياس مؤجَّل إليه يتعلّق خطوةً
  كاملةً وراء الحالة (مقيس: الحبّة تحت التبويب **السابق** في كل لقطة بلا واجهة). ولا حاجة
  إليه أصلًا لتغيّر `display`: **قراءة `getBoundingClientRect` تُجبر إعادة التخطيط قبل أن
  تردّ** ⇒ قِس فورًا، واجعل الـrAF تصحيحًا لاحقًا لا مصدرًا وحيدًا.
- **`document.fonts.ready` مرّة واحدة لا تكفي في تطبيق ثنائي اللغة.** خطّ الإنجليزية لا
  يُحمَّل إلّا عند أوّل استعمال للإنجليزية، فقياسٌ عند قلب اللغة يقع على مقاييس الخطّ
  الاحتياطي **ويبقى خاطئًا أبدًا** (مقيس: 85.93px مقابل 85.40px). الحدث الصحيح `loadingdone`
  — يُطلَق مع كل دفعة خطوط تصل.
- **لقطة `--virtual-time-budget` تصوّر الانتقالات في منتصفها.** المؤقّتات تتقدّم مع الزمن
  الافتراضي، وساعةُ الحركات لا ⇒ قرأتُ ألوان الشريط السفلي **من الثيم السابق** وحسبتها عطلًا
  في الكود، والمتصفّح الحقيقي قال إنها صحيحة. أضف `*{transition:none!important}` إلى صفحة
  اللقطة، أو قِس الألوان في متصفّح حيّ.
- **`datalist` يقترح ولا يمنع.** كل قيمة يبني منها التطبيق شريحة تصفية (‏`distinctSizes` ·
  `distinctTypes` · المنطقة) تحتاج **قائمة مغلقة**، وإلّا صنع حرفٌ واحد شريحتين — `8x8`
  و`8×8` مثالٌ **قائم في القاعدة الآن**. والقيمة الجديدة تُدخَل عبر «أخرى…» فتصير قرارًا.
- **سياسة RLS تحكم الصفوف لا الأعمدة.** `bookings_update` تسمح للاعب بالتحديث إلى
  `cancelled` وحدها؛ وتوسيعها لتمرير `pending` (كي يعدّل موعده) كان سيفتح **كل** الأعمدة
  أمامه — السعر والملعب واسم صاحب الحجز. البديل: **دالّة `security definer`** تعدّل ما
  تُعدّله فقط وتفحص الملكية بنفسها. وأي دالّة جديدة تعني **migration معلَّقة على المالك**،
  فالواجهة تكشف `404/PGRST202` وتقولها صراحةً بدل «حدث خطأ».
- **`current_date` في القاعدة بتوقيت UTC** — يسبق يوم الأردن ثلاث ساعات، فيقبل موعدًا
  ماضيًا بين منتصف الليل و٣ فجرًا. الصواب `(now() at time zone 'Asia/Amman')::date`.
- **كل مسارات إغلاق النافذة أربعة لا واحد** (زرّ · Escape · نقر خارجها · سحب لأسفل)، وثلاثة
  منها كانت تزيل `.show` مباشرةً ⇒ أي حارس يوضع في `Modal.close` وحده **يُتخطّى صامتًا**.
  وحارس «تعديلات لم تُحفظ» لا يكفيه حدث `input`: الاختيارات (يوم · وقت · نجمة) ليست حقولًا.
- **`Modal.open` ينادي `closeAll`** ⇒ فتح نافذة تأكيد **يُخفي** النافذة التي تسأل عنها.
  إعادة إظهارها بعد «أكمل التعديل» تحتاج `Modal.open(id, keepDirty)` وإلّا نُزِّل العلَم.
- **المتمرِّر هو المستند لا `#app`** (لا `overflow-y` عليه) ⇒ شرط السحب للتحديث `scrollY<=0`.
  وعنصران لاصقان بـ`top:0` يتراكبان؛ الحلّ غلاف لاصق واحد لا قياس ارتفاع في JS.
- **`opacity<1` يجعل العنصر الكتلة الحاوية لأبنائه `fixed`.** «لنُخفِ `#app` أثناء تبديل
  اللغة» تبدو بريئة، لكنّ `.bnav` و`.detail-sticky` و`.ptr` أبناء مباشرون له وطولُه طولُ
  المستند ⇒ `bottom:0` يقفز من أسفل **الشاشة** إلى أسفل **الصفحة**: شريط التنقّل يطير
  خارج الرؤية طوال التلاشي. أي حجاب يُوضع على `body` مستقلًّا لا على حاوية المحتوى.
- **View Transition لا تُسرّع شيئًا** — `setLanguage` متزامنة فتبقى كذلك. ما تفعله أنها
  تستبدل بالقطع الحادّ بين تخطيطين متعاكسين ذوبانًا. ولا يُترك التلاشي الافتراضي: كلتا
  اللقطتين تصيران نصف شفّافة معًا ⇒ **شبح مزدوج** (سطر عربي وإنجليزي متراكبان). الصواب
  إبقاء القديمة معتمة تحت والجديدة تتلاشى فوقها — نفس معمار الثيم.
- **انتقالا جذر لا يعملان معًا:** بدء الثاني يُلغي الأوّل فيُقرأ الإلغاء وميضًا ⇒ قفل
  `vtBusy` **واحد** للثيم واللغة، والضغطة أثناء انتقال جارٍ تُبدَّل فورًا بلا حركة لا تُبتلع.
- **سُلَّم ظهور بلا سقف ولا كتم = عطل مضاعف.** `animationDelay = i*0.05` بلا `Math.min`
  يجعل البطاقة العشرين تنتظر ثانية؛ وبلا علم `quiet` يُعاد توزيع السُلَّم كلّه **مع كل حرف**
  يُكتب في البحث (‏`renderPlaces` مربوطة بـ`input` مؤجَّلة). ⚠️ وكتم الحركة وحده يترك
  البطاقة **غير مرئية** — `.place-card` تبدأ `opacity:0` وتعتمد على `fadeUp … forwards`.
- **«نشِط» تُقاس بتاريخ الطلب لا بتاريخ اللعب.** نافذة `last >= -30 && last <= today` على
  تاريخ اللعب أعطت **«٠ نشِطين»** وفي القاعدة مئات الحجوزات: من حجز أمس لمباراة الشهر
  القادم تاريخُ لعبه في المستقبل فيسقط من أي نافذة تنتهي عند اليوم — وهو أنشط الناس.
  وكل نافذة على `booking_date` تخلط ما مضى بما هو مقدَّم؛ اسأل أوّلًا: هذا الرقم عن
  **البيع** أم عن **اللعب**؟
- **`setTimeout` يخزّن المهلة في ٣٢ بت.** ما تجاوز `2³¹−1` يلتفّ ويُطلَق **فورًا**. جدولةُ
  تجديد جلسة من `exp` بعيد صارت حلقةً لا تتوقّف (**٦٣٧ ألف طلب في ثوانٍ** — مقيس). توكن
  Supabase ساعةٌ فلا يبلغها في الإنتاج، والحدّ `Math.min(…, 2147483000)` يمنعها على أي حال.
- **`+` في أوّل المقطع محرف محايد ولا رقمَ قبله** ⇒ يُحسَم باتّجاه السطر (RTL) فيقفز إلى
  الطرف الآخر: **«+15%» تُعرَض «15%+»**. و`<bdi>` وحده **لا يكفي** — الأرقام الأوروبية
  ليست محارف قوية فلا تحسم اتجاه العزل؛ اللازم `<bdi dir="ltr">`. ⚠️ ولا يُفحَص بلقطة
  منخفضة الدقّة (قرأتُها خطأً مرّتين): قِس موضع كل محرف بـ`Range.getBoundingClientRect`.
- **الصفة بعد معدود متغيّر تنكسر مع العدد.** `countNoun` يصلح الاسم ولا يصلح ما بعده:
  «٥ حجوزات مؤكّدًا» · «طلب واحد قادمة» · «١٣ ملعبًا نشِطة». العلاج: انزع الصفة —
  **عدد + جمع معرَّف بأل** («٥ من الحجوزات المؤكّدة») يصحّ مع أي عدد، أو أخرِج العدد من
  العنوان إلى جملة («عددها …»).
- **اللوحة تربط `site.css` ⇒ فضاء الأصناف مشترك معه.** سمّيتُ بطاقة الأماكن `pl-card`،
  وهو **موجود أصلًا** لشبكة `/places` بـ`flex-direction:column` ⇒ صارت البطاقة عمودًا
  ولم أكتب ذلك في سطر واحد، والأنماط التي كتبتها كلّها تعمل فيبدو أنها «تصميم مقصود».
  كل صنف جديد في `admin.html` يبدأ بـ`ad-`، أو يُفحَص غيابه من `site.css` أوّلًا.
  ⚠️ ولا يُكشَف بقراءة الـCSS: `getComputedStyle` هو الذي قال `column`.
- **لا تشتقّ عمودًا لا يثبته المخطّط.** «المعلّقة» تصحّ في `admin_daily` لأن فيه `lost`
  ومجال `status` أربع قيم؛ وفي `admin_places` لا `lost` ⇒ أقصى ما يصحّ «غير مؤكّدة».
  التسمية تقول ما يُثبَت لا ما يُشتهى.

- **قيمةٌ مشتركة بين سياقين تحمل تفاصيل أحدهما.** `spec_court_single` كُتبت «فردي (20×6)»
  فطُبعت أبعادُ **البادل** على ملعب **تنس** — والمفتاح واحد لأن المعنى واحد، أمّا المقاس فلا.
  ما يختلف باختلاف السياق يخرج من التسمية إلى حقله (‏`size`). ولم يظهر إلّا بالنظر إلى الشرائح.
- **صفرٌ لا يدخل جدول العدّ لا يُرشَّح أبدًا.** «اليوم الأضعف» كان يُبنى بـ`byDay[w]=(byDay[w]||0)+1`
  من الحجوزات وحدها ⇒ يومٌ **بلا حجز واحد** لا يصير مفتاحًا فيستحيل أن يُختار أضعفَ الأيام،
  فتُعلن اللوحة «السبت أضعف أيامك بثلاثة» وفي البيانات ثلاثاءُ صفرٌ تامّ. أيّ «أقلّ/أكثر» على
  فئات معدودة يُبذَر بكامل مجاله أوّلًا. **والغياب هو الخبر عادةً، لا القلّة.**
- **نصٌّ يُركَّب من معطيات لا يمسّه `data-i18n`.** مبدّل اللغة يترجم الوسوم الحاملة للسمة، أمّا
  ما بُني في JS من قيم (إشعار · لوح متابعة) فيبقى **بلغة لحظة بنائه** حتى الجلبة التالية.
  كل مُركِّب نصّ من معطيات يحتاج سطرًا صريحًا في مسار تبديل اللغة.
- **الأعمدة المذكورة صراحةً تُفشل الاستعلام كلّه إن غاب واحد.** `select=…,sport` قبل ترحيل 13
  يردّ 400 فتخلو `D.fields` في `/admin` — ومعها جدول الملاعب والطاقة والإشغال وحصّة كل مكان،
  وكلّها تبدو «فارغة» لا «معطّلة». اسأل بالعمود ثمّ ارجع بلا العمود (`fetchFields`)، **وقُل
  للمستخدم أيّ ترحيل ينقصه قبل أن يملأ نموذجًا سيُرفَض** لا بعد رفضه.
- **`PGRST204` (عمود مفقود) ليس دائمًا جديرًا بالتراجع الصامت.** في التطبيق تُعاد إضافة الملعب
  بلا `sport` لأن كل الملاعب كرة قدم حينها فلا شيء يُفقَد؛ وفي `/admin` **لا**: المالك اختار
  «بادل» صراحةً، وكتابتُه كرةَ قدم صامتًا تصنع بيانات كاذبة يصعب تتبّعها. نفس الخطأ، وقراران.
- **قاعدةٌ زمنية تُفرَض بـ`with check` تُردّ «صفًّا فارغًا مع 200».** وسّعتُ التفكير في حشر
  «قبل ٦ ساعات» داخل `bookings_update` فوجدتُ الردّ نفسه هو العطل: PostgREST يردّ 200 وقائمةً
  فارغة، فالعميل لا يملك رمزًا يقرؤه ولا سببًا يقوله — «العملية نجحت ولم يحدث شيء». **المُشغِّل
  يرفع خطأً له اسم** (`cancel_window_closed:6`) فتُترجمه الواجهة إلى جملة صادقة. والقاعدةُ
  عامّة: كل قيدٍ يحتاج المستخدمُ أن يعرف **لماذا** رُدّ يخرج من السياسة إلى مُشغِّل.
- **وأعِد رقم القيد من الخادم لا من ثابت الواجهة.** الرقم مكتوب في موضعين بالضرورة (‏`CONFIG`
  في التطبيق · `app_settings` في القاعدة)، وإن اختلفا يومًا فالجملة التي يقرؤها المستخدم يجب أن
  تحمل الرقم الذي رُفض به **فعلًا**. ولهذا يحمل رمز الخطأ قيمته: `cancel_window_closed:<n>`.
- **راية واحدة لسببين مختلفين تُخفي الاثنين.** `canCancel` كانت تجمع «الحالة لا تسمح» و«الوقت
  لا يسمح»، فيختفي الزرّ في الحالتين بلا كلمة. الأولى لا تستحقّ شرحًا (الحجز انتهى)، والثانية
  تستحقّ سببًا ومخرجًا (رقم الملعب). **افصل الشرطين متى اختلف ما تقوله لكلٍّ منهما.**
- **`select=` صريح على `bookings` كذلك، لا على `fields` وحدها.** `cancel_kind`/`no_show` قبل
  ترحيلهما يردّان 400 فتخلو قائمة الحجوزات كاملةً — في التطبيق **و**في `/admin` — ويبدو
  الاثنان مكسورين لا «ميزةً غير مفعّلة». الحلّ نفسه: اسأل بالأعمدة، ثمّ بلا أيٍّ منها، واحفظ
  ما ثبت للجلسة (‏`SB_BK_EXTRA` · `fetchBookings`). **ومقيس بشبكة مموَّهة**: الجولات ثلاث
  (‏`+kind+noshow` ⇒ `+kind` ⇒ عارية) واللوحة تحمّل كاملةً في الجولة الثالثة.
- **قبل الترحيل: أخفِ البلاطة، لا تعرض صفرًا.** «لم يحضروا: ٠» بلا عمود يقيسها يُقرأ «لا يحدث
  هذا عندنا» — وهو ادّعاء لا قياس. والفرق أن **الغياب يُقال** (اللوحة تسمّي الترحيل الناقص)
  لا يُملأ بصفر. نفس روح م5 في «حقل فارغ ⇒ حالة صادقة».
- **الفحص بشبكة مموَّهة يحتاج بياناتٍ محسوبةً من `Date.now()` بلا قصّ.** قصصتُ ساعةَ حجزٍ
  اختباري بـ`if(h<8) h=8` فوقع الاختبار عند منتصف الليل على حجزٍ بعد **ثماني** ساعات لا ثلاث،
  فمرّ «الزرّ ظاهر» على أنه صحيح وهو خطأ في المُعطى لا في الكود. **احسب اللحظة إزاحةً من الآن
  (`now + 3h`) واقرأ منها التاريخَ والساعةَ معًا** — ولا تقصّ قيمةً يعتمد عليها الشرط المفحوص.
- **الجزء المخفيّ لا يُصوَّر ولا يُنقَر.** `computer` (لقطة · نقر · تمرير) يفشل بمهلة ما دامت
  لوحة المتصفّح غير معروضة، ويعمل فور إظهارها وتصديرِ التبويب (`tabs_select`). والقياس
  بـ`getBoundingClientRect`/`getComputedStyle` يعمل في الحالتين — فابدأ به، ثمّ صوّر.
- **`[hidden]` قاعدةُ متصفّحٍ بنوعية (0,0,1) — وأيّ صنف يضع `display` يعلو عليها.**
  كتبتُ `.join-f{display:flex}` على عنصرٍ يُخفى بالسمة، فظهر حقلا «أخرى…» **معًا دائمًا**.
  و`el.hidden` كان يقول `true` بأمانة — الـDOM صحيح والعرض خاطئ، فلا يكشفه إلّا
  `getComputedStyle` أو النظر إلى اللقطة. كل صنف يضع `display` يحتاج `&[hidden]{display:none}`
  معه، وإلّا فالسمة تُكتب ولا تُطبَّق.
- **قِس التباين في الثيمين قبل تثبيت أي لون جديد — والفاتح أوّلًا.** شارة السعر المختلف
  بـ`--auth-lime-ink` قاست **4.31:1** على السطح الفاتح عند 10px (راسب) و**6.79** في الداكن.
  الرقم الفاشل لا يظهر إلّا في نصف واحد من الحالات، ومن يفحص الداكن وحده يمرّره. والعلاج
  هنا كان دلاليًّا أيضًا: `--warning-ink` (‏5.83 / 8.26) — وسعرٌ يخالف الأساسي **تنبيهٌ**
  لا بشرى، فلا يُصبَغ بلون النجاح أصلًا.
- **🔴 اسمُ جدولٍ جديد يُفحَص في الترحيلات المعلَّقة، لا في القاعدة الحيّة.** سمّيتُ جدول
  إعدادات `15` باسم `app_settings` بعد أن تأكّدتُ أنه غير موجود — **وكان موجودًا في
  `11` الذي لم يُشغَّل بعد**. و`create table if not exists` تُبقي الأسبق ويفشل الثاني
  عند الإدراج (‏`42703: column "value" does not exist`). **والأخطر ليس التصادم**: `11`
  يترك جدوله **بلا سياسة واحدة** عمدًا لأنه يحمل مزوّد الرسائل، و`15` أضاف على الاسم
  نفسه `using (true)` و`grant … to anon` ⇒ سياسةُ قراءةٍ عامّة تعلو على جدولٍ صُمِّم
  محجوبًا. القيم فارغة اليوم فلم يتسرّب شيء، وأوّل يوم يملأ فيه المالك المزوّد كان
  سينشره. القاعدة: **`grep` على `migration/*.sql` كلّها قبل حجز أيّ اسم** — والقاعدة
  الحيّة لا تعرف ما لم يُشغَّل. الإصلاح في [`23_settings_split.sql`](migration/23_settings_split.sql).
- **علَمُ «هل الميزة موجودة؟» يبدأ `null` لا `true`.** بدأتُ `GAMES_OK = true`، فظهر مبدّل
  «مباريات» وخيارُ «مباراة مفتوحة» قبل الترحيل، ولا يختفيان إلّا **بعد** أن يفتح المستخدم
  التبويب ويفشل الطلب — أي يُعرَض عليه بابٌ ثمّ يُسحَب. مقيس. ثلاث حالات لا اثنتان:
  `null` لا نعرف (⇒ لا يُعرَض) · `true` موجود · `false` غائب. والسؤال مرّةٌ واحدة عند أوّل
  تحميل بجلسة (`?select=id&limit=0`) — أرخص سؤال يفرّق بين «موجود» و«غير موجود».
- **`--soft` يرسب (2.87:1) — وكلمةٌ تحمل معنًى ليست زخرفة.** وسم «تقديري» تحت سعر الحصّة هو
  ما يمنع قراءةَ الرقم سعرًا مقطوعًا، فكُتب بـ`--soft` فقاس **2.87** ⇒ صار `--muted`
  (‏6.21 نهاري · 8.50 ليلي). القاعدة مسجَّلة من قبل، والزلّة أنّي حكمتُ على الكلمة بحجمها
  الصغير لا بوظيفتها.
- **`withLoading` يُعيد `innerHTML` في `finally` ⇒ أيّ نصّ تكتبه بداخله يُمحى.**
  كتبتُ «رح ننبّهك» على زرّ التنبيه داخل الغلاف، فعاد بعد ثوانٍ يقول «نبّهني» وقد سُجّلت
  الرغبة فعلًا — فيضغط صاحبه ثانيةً. مقيس. **الحالة النهائية تُكتب بعد `await withLoading`،
  والقيم تُنقَل بأعلامٍ من الداخل** لا بلمس الزرّ.
- **`disabled` يبتلع الحدث حتى عن الأب.** زرٌّ محجوزٌ نريده قابلًا للنقر لا يجوز أن يحمله؛
  البديل `aria-disabled="true"` — القارئ يقول «محجوز» والنقر يصل. وما لا يُنقَر (المغلق)
  يبقى `disabled` حقيقيًّا.
- **قياسٌ مربوط بـ`input` يسجّل صفًّا لكل حرف.** «صويلح» تمرّ على ستّ قوائم فارغة ⇒ ستّة
  صفوف لسؤال واحد، فيبدو أعلى بحثٍ فارغ ستّةَ أبحاثٍ صغيرة ويضيع الخبر. العلاج مهلةٌ **أطول
  من مهلة العرض** + **ابتلاعُ البادئة**: «صو» ثمّ «صويلح» سؤالٌ واحد. مقيس: ستّ ضربات ⇒ صفّ واحد.
- **انتقال الصفحة يبقى عالقًا في المعاينة فتُقرأ الصفحة كلّها مزاحة.** `pgFwd` بقيت
  `running` (‏WAAPI مجمّدة — مزلق مسجَّل) فقرأتُ الرأس عند `left:-83` وثمانيةَ عناصر
  «متجاوزة» وهي سليمة تمامًا. **أطفِئ الحركة (`*{animation:none}`) قبل أيّ قياس أفقي**،
  وانزع أصناف الانتقال. والقياس **الرأسي** لا يتأثّر بها فيبقى صحيحًا في الحالتين.
- **بعد إعادة البناء، المتصفّح يخدم صفحتك القديمة.** قرأتُ `display:flex` على عنصرٍ
  أصلحتُه للتوّ لأن الصفحة والورقة كلتاهما من كاش المتصفّح. الاسم المبصوم (`site.<hash>.css`)
  لا يُنجيك: الـ**HTML** نفسه هو المُخزَّن، وهو الذي يشير إلى الورقة القديمة. أضِف
  `?v=n` إلى الرابط أو افحص بـ`cache:'no-store'` بعد كل بناء.

- **🔴 اختبارٌ يمرّ دائمًا أخطر من غياب اختبار.** `check-i18n-parity` كان يقول ٩٣٩/٩٣٩
  وهو صادق، بينما **٢٢ رسالة** تصل مستخدم الإنجليزية بالعربية — لأنها لا تمرّ بـ`I18N`
  أصلًا. العمى في **النطاق** لا في التأكيد. قبل الاطمئنان إلى حارس، اسأل: ما الصنف الذي
  لا يراه أصلًا؟ (‏`tools/check-api-messages.js` أُضيف لهذا الصنف بالذات.)
- **الشرط المفحوص أوّلًا يبتلع ما بعده إن كان أعمّ منه.** `slotsToKeyword` تفحص
  `s.includes('22=10:00')` ⇒ **`full`**، والمسائي فيه الساعة 22 كذلك ⇒ كل ملعب مسائي
  يُقرأ «كامل اليوم». **والعطل يكتب لا يعرض فقط**: زرّ الإظهار/الإخفاء يعيد إرسال
  الكلمة المشتقّة، فضغطةٌ واحدة تمنح الملعبَ خانات لم يعرضها صاحبه. الحسم بالمجموعة
  (الساعات) لا بمطابقة نصّية. **ولم تكشفه القراءة بل اختبار وحدة.**
- **`url()` داخل ورقة أنماط تُحسَب من موضع الورقة لا من المستند.** كتبتُ
  `url('assets/fonts/x.woff2')` داخل `assets/fonts/fonts.css` فصار الطلب
  `assets/fonts/assets/fonts/x.woff2` ⇒ **ثلاثة عشر 404 والصفحة تُرسَم** بخطّ احتياطي
  بلا خطأ واحد في الـconsole يشير إلى السبب. لا يكشفه إلّا فتح المعاينة وقراءة الشبكة.
- **سياسة RLS تحكم الصفوف لا الأعمدة — و`profiles` أخطر جدول تنطبق عليه.**
  `profiles_self_update` بلا قيد عمود = `PATCH {"role":"admin"}` على صفّك ⇒ القاعدة
  كلّها، **وكلّ حارسٍ كُتب قبلها**، لأن كلًّا منها يبدأ بـ`if is_admin() then return new`.
  القاعدة عامّة: **حارسٌ يستثني الأدمن يفترض أن الدور نفسه محروس.**
- **قائمة الأعمدة المسموحة تُبنى من الصفّ لا تُكتب بيد.** حارس الإلغاء يأخذ
  `to_jsonb(old)` ويستثني المسموحَين (`jsonb_populate_record`)، فيعمل على قاعدةٍ
  شُغِّلت عليها `16`/`22` وعلى قاعدةٍ لم تُشغَّلا عليها بلا فرعٍ واحد — والأهمّ أنه
  **لا ينسى أوّل عمودٍ يضيفه ترحيلٌ قادم**. قائمةٌ مكتوبة بيد تنساه صامتةً.
- **مُشغِّلات `before` تُطلَق بترتيب أسمائها.** `t_booking_cancel_fields` قبل
  `t_booking_cancel_window` (‏`f` قبل `w`) ⇒ الموعد يعود إلى قيمته الحقيقية **قبل**
  أن تُحسَب عليه مهلة الإلغاء. بالترتيب المعاكس يزحزح عميلٌ معدَّل النافذةَ بإرسال
  `booking_date` بعيدٍ في نفس طلب الإلغاء. **سمِّ المُشغِّل بعد أن تقرّر ترتيبه.**
- **قيدٌ على «حالة» قد يكون اتّجاهيًّا لا مطلقًا.** تجميد `profiles.active` كان سيكسر
  «حذف الحساب» (وهو `PATCH {active:false}` حرفيًّا). المسموح `true ⇒ false`، والممنوع
  العكس. اسأل عن **الاتّجاهين** قبل تجميد أي عمود.
- **`type="url"` يقترح ولا يمنع** — كـ`datalist` تمامًا: المتصفّح يقبل `javascript:`
  عنوانًا صالحًا. وكل قيمة تصل `href` أو `src` من القاعدة تحتاج **قائمة بيضاء بالمخطَّط**
  (`^https?://`) لا قائمة سوداء: `JaVaScRiPt:` و`\tjava\nscript:` تمرّان من أي قائمة سوداء.
- **`textContent` ثمّ `innerHTML` يهرّب `& < >` ولا يهرّب `"`.** تهريبٌ صحيح للنصّ
  وخاطئ تمامًا للسمة. أي دالّة `esc()` تُستعمل داخل `attr="…"` تحتاج `&quot;` صراحةً،
  و`&` أوّلًا وإلّا ضاعفَت ما بعدها.
- **`AbortController` بلا `signal` يصل الطلبَ = مؤقّتٌ يطلق في الفراغ.** والوعد
  المُنشَأ قبل دخول الغلاف لا يمكن إلغاؤه أصلًا: مرّر **مصنعًا** لا وعدًا.
  ⚠️ **وتعليقٌ يصف نيّةً لا سلوكًا أخطر من غياب التعليق** — القارئ التالي يدقّق النثر.
- **ملفّان يصفان العقد نفسه يختلفان يومًا، والذي لا يُنفَّذ هو الذي يكذب.**
  `_headers`/`_redirects` (صيغة Cloudflare) على استضافة Vercel حملا HSTS وتحويلَ
  `/ar/*` وليسا في `vercel.json` ⇒ حمايةٌ موعودةٌ غير موجودة. مصدرٌ واحد أو توليدٌ منه.
- **الخطوط المتغيّرة تُعيد نفس البايتات لكل وزن.** التسمية بالوزن نزّلت Montserrat
  أربع مرّات وInter خمسًا (٧٨٤ ⇐ ٢٦٨KB). **جزّئ المحتوى، ولا تسمِّ بالوصف.**
- **`allowBackup="true"` مع جلسة في `localStorage` = جلسة قابلة للاستعادة على جهاز آخر.**
  توكن التحديث يدخل نسخة Drive ويُستخرَج بـ`adb backup`. القيمة الافتراضية في قالب
  Capacitor ليست قرارًا اتّخذتَه.
- **التحقّق والتوحيد وجهان لقاعدة واحدة.** `validPhone` المتساهلة لا تصنع أخطاءً فحسب،
  بل **هويّةً ثانية**: صيغةٌ يقبلها الفحص ولا يعرفها التوحيد تُكتب كما هي فيُشتقّ لها
  حسابٌ مستقلّ. افحص **الناتج الموحَّد** لا المدخَل الخام. ⚠️ ولا تعمّم القاعدة على
  سياقٍ آخر: رقم الملعب قد يكون أرضيًّا، والسؤال هناك «هل يُطلَب؟» لا «هل هو حساب؟».

- **🔴 `opts.key` في `sbFetch` ليس مفتاح API بل **رمز إلغاء** — وتمريره مرّتين في
  `Promise.all` واحدة يجعل الدفعة تُلغي نفسها.** `fetchWithTimeout` يُجهض الطلبَ
  السابق المسجَّل تحت نفس الرمز. أضفتُ `sbGetReplySpeed(key)` إلى دفعة
  `sbGetInitialData` فأجهض `/places`، فتفشل الجلبة كلّها وتُقرأ الرئيسية
  **«لا ملاعب متاحة حالياً»** — رسالةُ فراغٍ سليمةِ الشكل لا رسالةَ خطأ، فلا شيء
  يصرخ. ولهذا كانت `sbGetClosures()` تُنادى **بلا وسيط** رغم أن توقيعها يقبله،
  والاسمُ وحده هو ما ضلَّل.
  ⚠️ **والأخطر أنّ العطل كان مقنَّعًا**: ما دام العرض غير موجود كان الردّ 404
  فيُنزَّل العلَم إلى `false`، ومن الجلبة الثانية فصاعدًا **لا طلب ⇒ لا إجهاض**
  ⇒ الشاشة سليمة. فمرّ الفحص كلّه أخضر، **ولم ينكشف إلّا يوم شُغِّل الترحيل**.
  الدرس: **ميزةٌ خلف علَمٍ مُطفأ ليست ميزةً مفحوصة** — لا يُختبَر المسار الجديد
  إلّا والعلَم مُشعَل (بترحيلٍ حقيقي أو بردٍّ مموَّه).

- **🔴 مُشغِّلٌ يعيد بناء الصفّ من `old` يمحو ما كتبه مُشغِّلٌ قبله — صامتًا.**
  `t_booking_cancel_fields` (‏24) تأخذ `to_jsonb(old)` وتستثني عمودين، وهي **الآليّة
  التي جعلتها لا تنسى أعمدةً قادمة**. لكنّ لها وجهًا ثانيًا: أيّ عمودٍ جديد يكتبه
  مُشغِّلٌ أسبقُ اسمًا (‏`a_…` قبل `t_…`) يعود قديمًا في هذا المسار بالذات. صادفتُه في
  `replied_at` — والنتيجة كانت **صحيحة بالمصادفة** (إلغاء اللاعب ليس ردًّا) لا بالقصد.
  القاعدة: **لا تجعل صدقَ عمودٍ جديد معتمدًا على حارسٍ كُتب لغرضٍ آخر** — اكتب الشرط
  في مُشغِّلك نفسه، وابحث في كل `jsonb_populate_record` قبل إضافة أي عمود.

- **🔴 مقياسُ إهمالٍ يُكافئ المُهمِل إن دخله الانقضاء التلقائي.** `expire_stale_bookings()`
  تكتب `rejected` + `cancel_kind='expired'` على ما **لم يُردَّ عليه أصلًا**. لو دخلت
  «متوسّط زمن الردّ» لامتلأ عمودُ المُهمِل بصفوفٍ زمنها «٢٤ ساعة بالضبط» فبدا **منضبطًا**،
  والأسوأ أنّ من يردّ على النصف ويترك النصف يُقاس بنصفٍ واحد وينجو من الآخر. أيّ مقياسٍ
  للاستجابة **يُستثنى منه ما يولّده النظام نيابةً عن الإنسان** — وفي موضعين: المُشغِّل
  (فلا يُكتب) والعرض (فلا يدخل صفٌّ قديم كُتب قبل الترحيل).

- **`Number(null)` صفرٌ صالح — والغياب ليس صفرًا.** `replySpeedText(null)` أعطت
  «يردّ خلال دقيقة» لأن `Number.isFinite(0)` صحيحة: قيمةٌ **غائبة** خرجت جملةً واثقة.
  الحارس صريحٌ قبل التحويل (`null`/`undefined`/`''`)، **وكشفه الاختبار لا القراءة** —
  وهو بالضبط نوع الخطأ الصامت الذي بُنيت له `test-pure.mjs`.

- **`role="img"` مع `aria-hidden="true"` على العنصر نفسه = لا شيء.** الثانية تحذفه من
  شجرة الوصول تمامًا فلا يبقى للأولى أثر: الرسم يقول «أنا صورة» ثمّ يختفي. وكانتا معًا
  في `/admin` وفي رسمَي لوحة المالك. **ولا يُصلَح بحذف الثانية وحدها**: `role="img"` بلا
  اسم يُعلن عنصرًا بلا محتوى، و«رسم بياني» اسمٌ عامّ **أسوأ من لا اسم** لأنه يَعِد بمحتوى
  ولا يعطيه. الاسم يُولَّد من نفس المصفوفة المرسومة.

- **`border-collapse:collapse` لا يرسم حدود الخليّة اللاصقة.** الحدود تُوحَّد على مستوى
  الجدول لا الخليّة، فالخليّة المرفوعة فوق ما ينزلق تحتها تفقد فاصلها. البديل `box-shadow`
  (وهو فيزيائي فلا مقابل منطقيّ له ⇒ اكتب الاتجاه صراحةً وعلّق أنه مقصود). ⚠️ **ومعه
  خلفيةٌ معتمة لازمة** — والتظليل المتناوب وصفُّ المجاميع كلٌّ منهما يحتاج إعادة إعلانها.

- **`<summary>` قابل للتركيز ولم يكن في قائمة `:focus-visible`.** القائمة كانت
  `button,a,input,select,textarea,[tabindex]` — و`summary` ليس منها ولا يحمل `tabindex`،
  فيصله Tab بلا حلقةٍ واحدة. ⚠️ **ولا يُكشَف بـ`el.focus()`**: البرمجي لا يفعّل
  `:focus-visible` غالبًا (مقيس: أعطى حلقة المتصفّح الافتراضية فبدا سليمًا) — اضغط Tab فعلًا.

- **تخطيطُ شريطٍ يتّسع لثلاثة أزرار ليس تخطيطًا يتّسع لأربعة.** `.nitem` كان صفًّا
  (أيقونة ثمّ نصّ): أربعُ حشواتٍ جانبية مضاعفة + فجوات + أربعة نصوص في سطرٍ واحد تخرج
  عن 375px. بالتكديس يصير عرض الزرّ **أعرضَ عنصرَيه** لا مجموعَهما. ⚠️ والحبّة نجت من
  التغيير كلّه لأنها **تُقاس ولا تُحسَب** — وهذا هو الفرق العملي بين التصميمين، لا
  نعومة الحركة وحدها.

- **قبل حجز صنفٍ للتطبيق بسابقة `ad-`: تلك سابقةُ `/admin` لا التطبيق.** بدأتُ بطاقة
  «التقارير» بـ`ad-entry` بحكم العادة، وهي القاعدة المكتوبة لـ`admin.html` وحده (يربط
  `site.css` ⇒ فضاء أصنافٍ مشترك). في `app/src/app.css` الفضاء منفصل، والسابقة المضلِّلة
  تُقرأ لاحقًا «هذا من اللوحة» فيُبحَث عنه في الملفّ الخطأ.

- **🔴 التطبيق له ورقتا أنماط لا واحدة، والثانية تفوز.** `app/src/native.css` يحقنها
  `build.ps1` **بعد** `app.css` ⇒ عند تساوي التخصيص تغلبها، وكل قواعدها `body.native` أي
  أنها تنافس بالضبط ما تكتبه في ذيل `app.css`. كتبتُ حركة دخول البطاقة فبدت سليمة، وفيها
  `transition` على `.place-card` **لا تذكر `translate`** ⇒ كانت ستُلغيها صامتةً، ومعها
  `:active{transform:scale(.975)}` تتضاعف مع `.985`. **و`grep` على ملفٍّ واحد لا يكفي.**
  ⚠️ **ومزلق في أداة الفحص نفسها:** في كروم الحديث لكلّ `CSSStyleRule` خاصّية `cssRules`
  (فارغة، لدعم التداخل) ⇒ `if(r.cssRules){recurse; continue;}` **يتخطّى كل قاعدة أنماط**
  فتخرج «صفر تطابق» وهي كاذبة. افحص `selectorText` أوّلًا ثمّ انزل إن كانت `length>0`.
- **`animation … forwards` على `transform` تبتلع `:active` بعد انتهائها لا أثناءها.**
  الانتقال يعلو على الأنيميشن ما دام جاريًا فقط. لا يُصلَح بالتخصيص ولا بـ`!important` —
  **الخاصّية المشتركة هي العطل**. افصل المحاور: `translate` للدخول و`scale` للضغطة
  (ولا تنسَ `transform:none` صراحةً وإلّا **تضاعف** الأثران). واستبدل `opacity:0 + forwards`
  بـ`@starting-style` فتصير الحالة الافتراضية **مرئية** ⇒ أسوأ فشلٍ «بلا حركة» لا «عنصر
  غير مرئي». و«بلا حركة» يُنفَّذ بتصفير **المدّة** لا التأخير: بتأخيرٍ صفر تظلّ البطاقات
  تتلاشى معًا مع كل حرف يُكتب.
- **`display:none ⇒ flex` لا يُنشئ حالة قبل‑التغيير ⇒ لا انتقال إطلاقًا.** والعلاج
  `transition-behavior:allow-discrete` + `@starting-style` **على الصنف نفسه** لا في JS:
  مسارات إغلاق النافذة أربعة وثلاثة منها تزيل `.show` مباشرةً، فأي حلٍّ في `Modal.open`
  يُتخطّى في ثلاثة أرباع الحالات.
- **قِس ارتفاع البطاقة بعد كل تغيير في محتواها.** `contain-intrinsic-size` رقمٌ خاطئ =
  قفزة شريط تمرير عند دخول ما هو خارج الشاشة. وبعد إعادة الترتيب خرج **346** وسيطًا
  والمكتوب **392**. والوسيط لا المتوسّط: بطاقةٌ طويلة واحدة تسحب المتوسّط ولا تصف البقيّة.
- **ضغطةٌ محمولة 600ms غير قابلة للإنتاج في أداة المتصفّح** (‏`left_click_drag` يعلّق،
  والنقرة ضغطُها فريم). البديل الأمين: بدّل `:active` بصنفٍ **مكافئ التخصيص** عبر CSSOM
  (`selectorText.split(':active').join('.is-pressed')`) — يحفظ التصريحات وترتيب المصدر
  تمامًا — ثمّ اقرأ القيمة عند 300ms و**1200ms**.
- **`android:windowBackground` تُرسم خلف الـWebView، والـWebView يرسم أبيضه فوقها.**
  فإصلاح وميض الإقلاع يحتاج الاثنين: خلفية النافذة **و**`android.backgroundColor` في
  `capacitor.config.json`. ⚠️ **وافحص `splash.png` بالنظر إليه**: كان لوح Capacitor
  القياسي (شعارٌ أزرق على أبيض) يُعرَض في كل إقلاع بارد — القيمة الافتراضية في قالب
  Capacitor ليست قرارًا اتّخذتَه.
- **ما يجب أن يسبق أوّل فريم مرسوم يُكتب في `<head>` لا في `app.js`.** الأخير مُحمَّل عند
  `</body>` ⇒ لوح الافتتاح يُرسَم قبله، فرايةُ سرعةٍ تُضاف من `app.js` تبدأ الحركة
  بالقيمة القديمة ثمّ **تقفز**. السكربت المضمَّن الذي يمنع وميض الثيم هو المكان، وللسبب
  نفسه حرفيًّا.

- **🔴 `overflow:hidden` يصنع حاوية تمرير، وRTL يرسو على حافّتها.** مسارُ شرائح
  يُزاح بـ`transform` **موجبًا** في RTL (‏`--nav-dir = -1`) يفيض إلى اليمين، فيزحف
  التمرير إلى الحافّة اليمنى ويُزيح **اللوح كلّه** بعرض شاشة: زرّ التخطّي وشريط
  التقدّم والزرّ الأساسي تخرج جميعًا من الرؤية. `overflow:clip` تقصّ **بلا تمرير**
  فلا يوجد ما يرسو. ⚠️ **ولا يظهر إلّا بالعربية ومن الشريحة الثانية فصاعدًا** —
  من فحص الأولى أو فحص الإنجليزية مرّره. والقياس الصحيح بعد `*{transition:none}`:
  القراءة أثناء الانتقال تعطي **منتصفه** فتبدو الإزاحة خاطئةً وهي سليمة.
- **رقم يُعرَض بلا طريقٍ إلى صفوفه يترك القارئ يبحث عمّا أُخبر به للتوّ.** شريحة
  «٣ فات وقتها» في `/admin` كانت `<span>`؛ صارت زرًّا يفتح القسم **ويطبّق المرشِّح**.
  والشريحة التي لا تقود إلى مكان تبقى نصًّا: زرٌّ لا يفعل شيئًا أسوأ من نصّ.
- **مرشِّحٌ يُطبَّق على الأساس الخطأ يخرج صفرًا دائمًا بثقة.** «فات وقتها» في قائمة
  العمل كانت ستُرشَّح من **القادمة**، والطلب الذي فات موعده ليس فيها أصلًا ⇒ صفرٌ
  بينما الشريط فوقها يقول «٣». **الأساس يتبدّل بالمرشِّح لا يُقصّ بعده.**
- **تقسيم نموذج إلى خطوات ≠ تقسيم حفظه.** «احفظ بعد الخطوة الأولى» في «إضافة مكان»
  يعني مكانًا في القاعدة بلا ملعب — والتطبيق **يُسقط كل مكان بلا ملعب**
  (`filter(p => p.fields.length > 0)`) ⇒ صفٌّ لا يراه أحد ولا يعرف صاحبه أنه موجود،
  وهو بالضبط ما بُني له التراجعُ القائم. الخطوات للقراءة، والالتزام يبقى واحدًا.

- **🔴 `[hidden]{display:none!important}` عامّة في `app.css`** ⇒ أيّ محاولة لإبقاء
  عنصرٍ يحمل السمة في التدفّق (‏`visibility:hidden` مثلًا كي يحجز مكانه) **تخسر**،
  ولو كانت أعلى تخصيصًا: `!important` يغلب. استعمل صنفًا لا السمة حين يكون
  المطلوب «مخفيّ ويشغل مكانه». مقيس: زرّ رجوعٍ في شاشات الترحيب فقد حصّته من
  الصفّ فانزاحت النقاط عن المنتصف **في الشريحة الأولى وحدها** (329 مقابل 188).
- **🔴 `--g-btn` تساوي Lime في الثيم الليلي.** فأي زرّ يُصبَغ بها **ويُكتب نصّه
  أبيض** يقيس 2.05:1 ليلًا وهو يبدو سليمًا نهارًا (تيل). لا تصبغ زرًّا بيدك:
  أعطِه صنف `.sbtn` — هو الذي يعرف أن يقلب النصّ إلى حبرٍ داكن على الـLime وإلى
  أبيض على التيل (مقيس بعده: 9.76 · 8.07).
- **قيمةٌ افتراضية على عمودٍ جديد قد تكون اختراعَ بيان.** `fields.gender` تُترك
  `null` لا `'mixed'`: الصفوف القائمة لم يقل عنها أحدٌ شيئًا، و«مشترك» المفترضة
  ترسل لاعبةً إلى ملعبٍ للرجال. اسأل دائمًا: هل الافتراضي **يصف** الصفوف القائمة،
  أم يضع في فمها قولًا؟ وحين لا يصفها، **الغياب هو القيمة الصحيحة**.
- **«راح/مضى» تُقاس بالزمن المعروض لا بالزمن الحالي.** مخطّط اليوم كان يقارن كل
  ساعة بـ`new Date().getHours()`، فلمّا صار يعرض أيامًا أخرى رُسم نصف **الغد**
  رماديًّا لأن الساعة عندنا الآن الثامنة. أي حكم «انتهى» على شبكةٍ قابلة للتنقّل
  يحتاج تاريخ الشبكة في الشرط لا الساعة وحدها.

**البناء**
- **`build-site.ps1` يمسح `public/` أوّلًا** — بدونها يبقى ملفّ حُذف من المولّد يُخدَم للأبد.
- **حرّر `site/` لا `public/`** · **حرّر `app/src/` لا `app/www/`**.
- **تعليقات HTML تُحذف من المخرَج** (`Remove-HtmlComments`، الدفعة السابعة): كانت 7.5KB في
  الرئيسة وحدها — ربع ميزانيتها — وتُشحن في الـ28 صفحة كلّها. المصدر يحتفظ بكل كلمة.
  ⚠️ **الحاذف يرفع أجسام `<script>`/`<style>` قبل أن يمرّ ثمّ يعيدها**: `<!--` و`-->` محرفان
  عاديّان داخل نصّ JavaScript، وكنسٌ ساذج يبتلع ما بينهما فيُفسد السكربت صامتًا. وصيغة
  `MatchEvaluator` مقصودة كذلك — تُعيد قيمتها حرفيًّا فلا يُقرأ `$1` داخل JS مرجعَ التقاط.
  ⚠️ وميزانية الرئيسة (28,000 محرف) **تحذير بلون أحمر لا فشل بناء** — انظر إلى السطر.
- **حذف التعليقات يخصّ الموقع وحده — والتطبيق يشحنها كلّها.** `Remove-HtmlComments` تمرّ على
  صفحات `site/` فقط؛ أمّا `app/src/app.html` فيُنسخ إلى `app/www/index.html` كما هو ويُحزَم
  في الـAPK. فكل تعليق تكتبه في التطبيق **يُوزَّع** على كل من ينزّله ويُفكّ بأمر واحد.
  مقيس: كتبتُ تعليقًا يشرح أنّي حذفتُ شارة النسبة **وفيه النسبة نفسها**، فحلّ محلّ ما حذفتُه.
  أيّ رقمٍ أو مفتاحٍ أو سببٍ تجاري لا يُكتب في تعليقات `app/src/` أصلًا.
- **🔴 والحارس كان يفحص ما يُحقَن ولا يفحص ما يُحقَن فيه.** `String.Replace` في .NET يستبدل
  **كل** التطابقات لا الأوّل، فوجودُ `</body>` ثانٍ في `app.html` — **ولو داخل تعليق** —
  يحقن طبقة الهاتف **مرّتين**. ونسخة الـCSS المكرّرة غير ضارّة، أمّا `native.js` فيعمل
  مرّتين: يسجّل مستمعَ زرّ الرجوع ومراقبَ شريط الحالة ومستمعَ الإشعارات ثانيةً ⇒ **ضغطة
  رجوع واحدة تُنقّل صفحتين**. مقيس 2026-08-11: تعليقٌ اقتبس الوسم حرفيًّا فعل ذلك،
  **والبناء أعلن نجاحه** (‏`+19,398 native` بدل `+9,699` — وهو الرقم الوحيد الذي فضحه).
  الحارس الجديد في `build.ps1` يشترط **تطابقًا واحدًا بالضبط** ويقول العدد الذي وجده.
  ⚠️ والدرس أعمّ: حارسٌ يفحص المُدخَلات وحدها أعمى عن المُضيف — اسأل دائمًا «وماذا عن
  الطرف الآخر من العملية؟» (وهو نفس صنف العمى في [`check-i18n-parity`](tools/check-i18n-parity.js)).
- **الدمج سَبْكُ نصّ ⇒ `</script` أو `</style` أو `</body>` في المصدر يُغلق الوسم مبكّرًا**،
  فيصير باقي السكربت وسمًا وتقع الطبقةُ الأصلية في منتصف دالّة — **والبناء ينجح** والـAPK
  يخرج معطوبًا. الحارس في `build.ps1` يرفع `throw` باسم الملفّ. وصفرُ ظهورٍ اليوم هو
  **بالضبط** وقت إضافته: ما يُدخل أوّل ظهور عاديٌّ جدًّا (أيقونة SVG · تعليق يشرح وسمًا ·
  نصّ I18N يقتبس HTML).
- **الخطوط لا تُدمَج، وتُنسَخ.** `app/assets/` ⇒ `app/www/assets/` (وإلى `app/src/assets/`
  للمعاينة). ٢٦٨KB من base64 داخل ملفٍّ من ١٫١ مليون محرف تُدفع عند كل تحليل بلا مقابل،
  وقراءة `file://` بجوار `index.html` مجّانية. والبناء **يتوقّف** إن غاب `fonts.css` بدل
  أن يُخرج تطبيقًا بخطّ النظام. توليدها: `node tools/fetch-fonts.mjs` (مُلتزَمة، فلا
  يحتاج بناءٌ عاديّ إلى الشبكة).

---

## 🗄️ قاعدة البيانات

Supabase (Postgres مُدار) — المشروع `nxqddfuwtrsabprxcfez`. المفتاح `anon` عامّ بالتصميم
(الأمان في RLS). المصادقة ببريد **مشتقّ من رقم الهاتف** داخليًّا (OTP ليس مجانيًّا).

- **الملفّات `01…33` تُشغَّل بالترتيب، مرّة واحدة لكلٍّ.** ومقيسٌ حيًّا 2026-08-18 أنّ **`08…32` كلَّها مُشغَّلة**، و**`33` شُغِّل 2026-08-21** ومُتحقَّقٌ حيًّا في العرض لا في الجدول وحده. وأحدثُها `30` أصلح خمسة أعمدة كانت في الجدول وخارج العرض `bookings_full` (‏«لم يحضر» · المباريات المفتوحة · «انقضت المهلة»)، وكلّها آمنة لإعادة التشغيل.
  🔴 **ومن شغّل `15` بالنسخة الأولى: `23_settings_split.sql` قبل `11`** — الأولى حجزت اسم
  `app_settings` وهو اسم جدول `11`، فيفشل `11` بـ`42703`، **وتبقى سياسة قراءة عامّة فوق
  جدولٍ صُمِّم بلا سياسة** فتنشر مزوّد الرسائل. و`23` ينقل ويُصلح، وآمن للتكرار.
  ⚠️ **الترتيب يهمّ في ثلاثة مواضع:** `14` قبل `15` و`20` و`22` — كلّها توسّع قائمةَ أنواع الإشعارات، ولو سبقها `14` كتب قيدَه فوقها (أعِد المتأخّر بعده، وكلّها آمنة للتكرار).
  و`14` قبل `20` **شرطٌ لا راحة**: `20` يتوقّف برسالة صريحة إن غاب، لأن جدول رغباتٍ بلا وسيلة إشعارٍ جمعُ بياناتٍ بلا فائدة.
- **[`RECIPES.sql`](migration/RECIPES.sql) مرجع لا سكربت** — انسخ الوصفة وحدها، لا تشغّل الملفّ.
- **أعمدة `reviews` العامّة بعد `07`:** `id, legacy_id, created_at, place_id, field_id,`
  `author_name, rating, comment`. ⚠️ **أي قراءة جديدة تذكر الأعمدة صراحةً** — `select=*` يفشل عمدًا.
- **`.gitignore` يستثني بيانات العملاء** (`CSV/` · `02` · `03`) — **المستودع عامّ**.

---

## 🔧 استئناف العمل

```bash
powershell -ExecutionPolicy Bypass -File build.ps1   # التطبيق + الموقع
npx cap sync android                                  # نقل البناء إلى أندرويد
cd android && .\gradlew.bat assembleDebug             # APK
```

**قبل كل التزام — الحرّاس والاختبارات:**
```bash
npm run check && npm test
```
`check` = **اسمٌ استُعمل ولم يُعرَّف** (`check-globals` — أوّل حلقة، وهو الحارس الذي
كان غيابه يعني تطبيقًا لا يرسم شيئًا) · **قواعد CSS** (منطقية · تتبّع · ارتفاعٌ
قاطع على عنصر تفاعلي · **توكن مُعرَّف مرّتين تحت نفس المحدِّد**) · **التباين** (١٢ زوجًا في الثيمين، محسوبةً من التوكنات
بحلّ سلاسل `var()` وتركيب الشفّافية) · تكافؤ `I18N` · مرآة `FIELD_SPECS` ·
**ترجمة كل رسالة `message:`** ·
**الثوابت المكرّرة** (العمولة في ثلاثة مواضع منها عرضٌ في القاعدة · `versionCode` ·
المهلتان · تسمية العاشرة) · **وتطابقُ العرض** (`check-view-parity` — كلُّ عمودٍ في
`bookings` موجودٌ في `bookings_full`؛ **يحتاج شبكة، ويتخطّى بصوتٍ عالٍ بلا إنترنت**).
و`npm test` = **٧٣** تأكيدًا على الدوالّ النقيّة، **تُقرأ من `app.js` نفسه** لا تُنسَخ.

**وبعد أي ترحيل — الحرّاس في القاعدة نفسها:**
```bash
node tools/security-matrix.mjs --url https://<ref>.supabase.co --anon <key> --phone 07… --pass …
```
بتوكن **لاعب عادي**؛ لا يقبل `service_role` عمدًا. ⚠️ ويُنشئ حجزًا ويُلغيه، **ويترك
حساب الاختبار `active=false`** (فحصُ «الموقوف لا يعيد تفعيل نفسه» ينجح بذلك) — أعِده
من `/admin`.

**والخطوط:** `node tools/fetch-fonts.mjs` — عند تغيير وزنٍ في التصميم فقط. المخرَج
مُلتزَم، والبناء يتوقّف إن غاب.

**والـAPK على الموقع:** يُجلَب في البناء من `site/release.txt` إلى
`site/static/downloads/` (مُتجاهَل في git) فيصير الموقع يخدمه بنفسه، ومعه
`/app.apk` نسخةً ثابتةً هي الرابط الذي يُنشَر ولا يتغيّر. وسطر `[apk]` في نهاية
البناء يقول أيّ الحالات وقعت.

**فحص `/admin` بلا قاعدة ولا كلمة سرّ:**
```bash
node tools/admin-harness.mjs
```
تُولّد `public/_admin_test.html` — نسخةً من المخرَج المبنيّ يُحقَن فيها سكربتٌ
يستبدل `fetch`/`WebSocket` ويزرع جلسةً وهمية ⇒ كل لوح يُرى فعلًا. **وتُحذَف بعد
الفحص دائمًا** بـ`node tools/admin-harness.mjs --rm` (‏`public/` مخرَجٌ متتبَّع).

**معاينة:** `koora-app` على 8012 (`/_preview_app.html`) · `koora-site` على 8020.
⚠️ عند تثبيت APK جديد: **احذف النسخة القديمة أولًا** (توقيعا debug/release متعارضان).

**الخطط** في [`docs/plans/`](docs/plans/) — سجلّ قرارات لا تعليمات جارية، وفيها **جدول تحويل
المسارات** بعد إعادة ترتيب 2026-07-29.
