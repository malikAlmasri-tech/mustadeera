# الخطة ② — الموقع الرسمي للمستديرة

> **🟢 الحالة (2026-07-28، بعد المراجعة): جاهزة للبدء.** الخطة ① أُغلقت من طرف المساعد، ولم يبقَ فيها إلّا ثلاثة بنود على المالك (اختبار جهاز · أرقام المُلّاك · مفتاح التوقيع) **لا تحجب البدء هنا**.
> الهدف: موقع **رسمي محترف** للمنصّة، فيه **تنزيل التطبيق**، وصفحات حقيقية لها عناوين حقيقية.

---

## ⏱️ ما تغيّر منذ كتابة الخطة — اقرأ هذا أوّلًا

| البند في الخطة | ما استجدّ (2026-07-28) |
|---|---|
| **القرار 4** — إسقاط `rating_seed` | ✅ **نُفِّذ في التطبيق.** مكان بلا تقييم حقيقي لا تُرسم له شارة نجمة. ⚠️ **الموقع لم يُعالَج** — ثلاثيّه (`app/app.js`) ما زال على Apps Script. **يجب تكرار نفس المعالجة (`hasRating`) عند المرحلة (د).** هذا أوضح مثال على ثمن الفصل |
| **§7 بند 2** — APK بتوقيع release | 🟡 **البنية جاهزة**: `signingConfigs` في `android/app/build.gradle` + `.gitignore` + دليل [`android/SIGNING.md`](android/SIGNING.md). يبقى أن يُنشئ المالك المفتاح بكلمة سرّه |
| **§8 `/admin`** | ➕ **يُضاف عنصر**: زرّ **«إعادة تعيين كلمة سرّ مستخدم»**. ⚠️ **العرض مستحيل** — bcrypt أحادي الاتجاه؛ لا أحد يرى كلمة سرّ أحد. المنطق جاهز كوصفة في [`migration/RECIPES.sql`](migration/RECIPES.sql) §8، ويشمل إخراج الجلسات المفتوحة. (وفي التطبيق: المستخدم يغيّرها بنفسه من «حسابي») |
| **§0** — «677,863 محرفًا» | ✏️ **الرقم بايتات لا محارف.** الصحيح **638,160 محرفًا** (العربي متعدّد البايتات). `build.ps1` يطبع المحارف — لا تخلط بينهما عند قياس الميزانية في §5 |
| **§9** — البنية التحتية | ➕ **صارت جاهزة:** نسخة احتياطية يومية مشفّرة + نبضة تمنع إيقاف المشروع (`.github/workflows/`). لا حاجة لبنائها ضمن هذه الخطة |
| **القرار 2 + §7 بند 1** | ⚠️ **تناقض ظاهري يجب حسمه:** القرار 2 يقول «بلا Google Play»، و§7 يوصي بالتسجيل في Play Console **لتوثيق الهوية فقط** (مجاني، بلا نشر). **الاثنان متوافقان** — التوثيق يُبقي قناة التنزيل المباشر سالكة دون نشر أي شيء في المتجر. **اعتبره جزءًا من القرار 2، لا نقضًا له** |

**✅ السؤال المفتوح أُغلق (2026-07-28) — الحجب حقيقي ومحصور بـ`vercel.app` وحده.** قِيس من جهازك على شبكتك:

| المضيف | النتيجة | الدلالة |
|--------|---------|---------|
| `mustadeera.vercel.app` | **فشل TLS** (`schannel: failed to receive handshake`) | DNS يحلّ سليمًا (216.198.79.67) وTCP يتّصل، **ثم تُقطع المصافحة** |
| `vercel.app` (الجذر) · `nextjs-blog-starter.vercel.app` | **فشل TLS كذلك** | ليس خاصًّا بمشروعك — **كل النطاق مفلتَر** |
| `vercel.com` | **200** | ليست الشركة محجوبة |
| `cloudflare-pages-template.pages.dev` | **200** | ✅ **`*.pages.dev` سالك** |
| `malikalmasri-tech.github.io` · `*.netlify.app` · `example.com` | 404/301/200 (الخادم يردّ) | البدائل كلّها سالكة |

**التشخيص:** فلترة **SNI** — الجدار يقرأ اسم المضيف في مصافحة TLS ويقطع الاتصال عند `vercel.app`. لهذا يعمل `vercel.com` ولا يعمل أيّ `*.vercel.app`. وهو نفس العرَض الذي اضطرّنا لإلغاء `server.url` سابقًا، **والسبب كان مشخَّصًا صحيحًا في حينه**.

⇒ **القرار 1 حُسم: ننتقل إلى Cloudflare Pages (`*.pages.dev`) مجانًا.** لا حاجة لاختبار الهاتف — الحجب مُثبَت على شبكتك الأساسية، وهي التي تُطوّر وتُشارك منها.

---

## 0) ما الموقع اليوم — بالأرقام لا بالانطباع

| الحقيقة | الرقم / الدليل |
|---------|----------------|
| جذر `index.html` = **ملفّ واحد** فيه الموقع التسويقي **والتطبيق كاملًا** | **638,160 محرفًا** (= 677,863 بايتًا — العربي متعدّد البايتات)، 8 صفحات SPA، «الموقع» ليس إلّا القسم `#page-welcome` بداخله |
| **عنوان واحد لا غير** | لا `/download` · لا `/owners` · لا صفحة خصوصية. لا يستطيع أحد مشاركة رابط صفحة بعينها |
| المحتوى يُبنى بالـJS بعد التحميل | محرّك البحث يرى هيكلًا شبه فارغ؛ عنوان ووصف واحدان لكل شيء |
| الباكند | الموقع ما زال على **Apps Script (~4 ثوانٍ للردّ)** بينما التطبيق انتقل إلى Postgres |
| **مكان تنزيل التطبيق** | **لا يوجد.** الـAPK على سطح مكتبك فقط، وبتوقيع debug |

الخلاصة: ما عندنا اليوم **تطبيق ويب له صفحة ترحيب**، لا **موقع**. والفرق ليس تجميليًّا: الموقع هو ما يُشارَك ويُؤرشَف ويُبحَث عنه ويُنزَّل منه.

---

## 1) القرار المعماري — فصل الموقع عن التطبيق

| الطبقة | المسار | ما هي | التقنية |
|--------|--------|-------|---------|
| **الموقع** | `/` وما تحته | صفحات ثابتة متعدّدة، خفيفة، محتواها في الـHTML نفسه | HTML/CSS مولَّد وقت البناء · **صفر JS للمحتوى** |
| **التطبيق (ويب)** | `/app` | الـSPA الحالية **كما هي بلا إعادة كتابة** | نفس `app/index-new.html` + تبديل الباكند إلى Postgres (P3ب) |
| **الأدمن** | `/admin` | لوحة مالك المنصّة | ملفّ مستقلّ + Supabase + RLS |
| **التطبيق (أندرويد)** | APK | كما هو | يُنزَّل من `/download` |

**البديل المرفوض: توسيع `#page-welcome` داخل الـSPA.** لأنه (١) يُبقي عنوانًا واحدًا فلا يحلّ المشكلة الأصلية، (٢) يزيد وزن أثقل ملفّ في المشروع وهو أول ما يُحمَّل، (٣) يجعل كل تعديل تسويقي صغير يخاطر بكسر التطبيق الحيّ الذي يستعمله الناس.

**الترقية بلا كسر:** من يفتح الجذر اليوم يصل إلى التطبيق؛ بعد النقلة يصل إلى الموقع وزرّه الأول «احجز الآن ← `/app`». لا رابط يُكسَر، والمسار أقصر لا أطول لأن الموقع سيُحمَّل في جزء من وزن اليوم.

---

## 2) خريطة الصفحات

| المسار | الغرض | الإجراء المطلوب من الزائر |
|--------|-------|---------------------------|
| `/` | ما هي المستديرة، لمن، وكيف تعمل | **نزّل التطبيق** / احجز من الويب |
| `/download` | **الصفحة الأهمّ** — تنزيل وتثبيت | التنزيل والتثبيت بنجاح |
| `/owners` | صفحة بيع لأصحاب الملاعب | «سجّل ملعبك» (نموذج) |
| `/places` + `/places/<اسم-المكان>` | دليل الملاعب — **محتوى حقيقي مولَّد من القاعدة** | فتح المكان في التطبيق |
| `/about` · `/contact` | مَن نحن · تواصل ودعم | ثقة |
| `/privacy` · `/terms` | سياسة الخصوصية وشروط الاستخدام | **إلزاميان** لأي متجر تطبيقات لاحقًا، وشرط مصداقية اليوم |
| `/en/…` | النسخة الإنجليزية الكاملة | — |

> **`/places` هو خندق الظهور في البحث.** «ملاعب كرة قدم في <المنطقة>» بحث يجري فعلًا، وصفحة لكل مكان بمحتواها الحقيقي (اسم · منطقة · أسعار · مرافق · تقييم) هي الشيء الوحيد في هذه الخطة الذي يجلب زوّارًا **بلا إعلان**. تُولَّد وقت البناء من Postgres ⇒ صفحات ثابتة سريعة، لا استعلام وقت الزيارة.

---

## 3) المراجع التسعة — ماذا نأخذ من كلٍّ بالضبط

| المرجع | ما هو | ما نأخذه | القيد |
|--------|-------|----------|-------|
| **Land-book** | معرض مواقع منتجات حقيقية | **بنية الأقسام وترتيبها وإيقاع الصفحة** — أقوى ما فيه أنه واقعي لا معرض فنّ | مرجع بنيوي لا بصري |
| **Awwwards** | جوائز مواقع | فكرة واحدة: **الطباعة الكبيرة الجريئة كبطل الصفحة** | معظم ما فيه عروض لا تُشحن — راجع «ما نرفضه» |
| **Dribbble** | لقطات تصميم | أفكار تفاصيل صغيرة (شكل بطاقة، حالة فارغة) | لقطات لا تعمل غالبًا؛ لا نلتزم بشيء منها قبل تجربته حيًّا |
| **Uiverse.io** | +3500 مكوّن CSS/Tailwind، **رخصة MIT** | 3-4 عناصر جاهزة (زرّ تنزيل، بطاقة، مبدّل) **معادة الصبغ بتوكناتنا** | MIT ⇒ الاستعمال حرّ. لا يدخل شيء بألوانه الأصلية |
| **CodePen** | مقتطفات حيّة | تقنيات بعينها عند الحاجة (هيكل هاتف CSS، شبكة bento مرنة) | نسخ مقتطف = نسخ صيانته؛ نأخذ الفكرة ونكتبها بأسلوب الملفّ |
| **Animista** | مولّد `@keyframes` بلا JS | **المنحنيات والـkeyframes** لحركات الدخول | لا نستورد ملفّه كاملًا — سطران أو ثلاثة لكل حركة |
| **LottieFiles** | أنيميشن JSON | ⚠️ **مستبعَد من الرئيسية** | محرّك dotLottie ≈ **500KB مضغوطًا**. يُسمح بأنيميشن واحد **مؤجَّل التحميل** في `/download` إن أثبت قيمته. البديل الافتراضي: SVG + CSS |
| **GSAP** | مكتبة الحركة | **صار مجانيًّا 100% بكل إضافاته** (3.13، بعد استحواذ Webflow) ⇒ `ScrollTrigger` و`SplitText` متاحان بلا اشتراك | النواة ~70KB مضغوطة ⇒ **تُحمَّل مؤجَّلًا وعلى الصفحات التسويقية فقط**. كل ما يمكن بـCSS يبقى CSS |
| **Figma** | التصميم | توليد صور المشاركة (OG) وضبط الشبكة عند اللزوم | لا نصمّم الموقع كاملًا فيه — التصميم يجري في المتصفّح مباشرة كعادة المشروع |
| **Coolors** | الألوان | **توليد سلّم درجات وفحص التباين** لـTeal/Lime | **الهوية لا تتغيّر**: Teal `#0F4B53` + Lime `#8CC63E` |

### ما نرفضه صراحةً — وكلٌّ لسبب
- **الطباعة الحركية (kinetic typography):** تملأ Awwwards وDribbble وتكاد لا تُشحن في الإنتاج — تعادي قارئ الشاشة، وتُربك الزحف، وتُنتج انزياحًا يهدم CLS. (وهذا الحكم من مراجعة نصف 2026 لما «صمد» من اتجاهات، لا رأيًا.)
- **Lottie في الهيرو** · فيديو ثقيل يعمل تلقائيًّا · scroll-jacking يسرق التمرير من المستخدم.
- **أي رقم غير حقيقي** («10,000 حجز») — قاعدة الصدق (م5) سارية على الموقع كما على التطبيق.

---

## 4) الاتجاه البصري

الهوية كما هي (Teal + Lime مسطّحة، Montserrat + خطّ عربي)، والجديد في **البنية والإيقاع**:

1. **هيرو بطباعة كبيرة + إثبات فوري:** جملة واحدة تقول ما نفعله، وتحتها **زرّان لا أكثر**: «نزّل التطبيق» (أساسي) و«احجز من المتصفّح» (ثانوي).
2. **عرض المنتج بدل وصفه:** هيكل هاتف مبني بـCSS داخله **لقطات حقيقية من التطبيق** تتبدّل مع التمرير. أصدق من أي رسم، وأرخص من فيديو.
3. **شبكة Bento للمميزات:** ما زالت أقوى نمط تخطيط صامد في 2026، وتناسب منتجًا بميزات متقاربة الوزن (بلا مكالمات · أوقات واضحة · تتبّع الحجز · تقييمات حقيقية).
4. **«كيف يعمل» في ثلاث خطوات** بخطّ زمني — موجود في الترحيب الحالي ويُعاد بناؤه أوسع.
5. **قسم أصحاب الملاعب** بلغة مختلفة (دخل · إشغال · لوحة تحكّم) وزرّه إلى `/owners`.
6. **تذييل حقيقي:** روابط · خصوصية · شروط · تواصل · لغة · وسائل التواصل.

**الحركة:** كشف عند التمرير بـ`IntersectionObserver` + CSS (كما في الموقع اليوم)، وGSAP `ScrollTrigger` **فقط** حيث يعجز CSS (تتابع مرتبط بموضع التمرير في قسم الهاتف). واحترام `prefers-reduced-motion` في كل ما سبق.

---

## 5) ميزانية الأداء — مكتوبة كي تُقاس

| المقياس | الحدّ (مئين 75) | تنبيه داخلي عند |
|---------|----------------|-----------------|
| LCP | < 2.5s | 2.0s |
| INP | < 200ms | 160ms |
| CLS | < 0.1 | 0.08 |

قيود مشتقّة: **HTML لكل صفحة < 40KB** · CSS الحرج مضمّن < 14KB والباقي مؤجَّل · **صفر JS لعرض المحتوى** · صور WebP/AVIF **بأبعاد صريحة** (لا انزياح) · خطّ واحد مُسبَق التحميل مع `font-display:swap` · **لا صور base64 في CSS على الموقع** (النمط المستعمل في التطبيق مبرَّر هناك لأنه مدمج في APK، ومكلف هنا لأنه يمنع التخزين المؤقّت المستقلّ).

---

## 6) العربية والإنجليزية

- **الحجم:** النصّ العربي أكبر 10-15% من اللاتيني عند نفس المستوى (16px لاتيني ⇒ 18px عربي)، و`line-height` لا يقلّ عن **1.8** للمتن (التشكيل والنقاط تحتاج فسحة)، و**أوزان متوسطة لا رفيعة** (الرفيع يتفتّت في العربية).
- **الاتجاه:** `dir` على `<html>`، وخصائص منطقية حصرًا (`margin-inline-start`، `text-align:start`) — وهذا منضبط في المشروع أصلًا. تُعكس القوائم والأشرطة، **ولا تُعكس** الرموز الكونية (تشغيل/إيقاف، وسائط).
- **الفهرسة:** `hreflang` متبادل بين `/x` و`/en/x` + `x-default`، ومبدّل لغة ظاهر وثابت في كل صفحة.
- الخطّان (عربي/لاتيني) متوافقان في الحجم البصري مع `unicode-range` للمحتوى المختلط.

---

## 7) `/download` — أدقّ صفحة في الموقع

هذه ليست زرًّا؛ هي **مسار تثبيت** ينجح أو يفشل عند حوار تحذير.

1. **⚠️ واقع 2026 يجب أن تعرفه:** جوجل بدأت تفرض **توثيق هوية المطوّر حتى للتطبيقات الموزَّعة خارج المتجر** (بدأ التطبيق بعدّة دول في سبتمبر 2026 ويتوسّع عالميًّا بعدها). المطوّر غير المُوثَّق ⇒ **تحذيرات Play Protect أعلى** على الأجهزة المعتمدة. **التوصية: سجّل في Play Console وأكمل توثيق الهوية (مجاني) حتى لو لم تنشر في المتجر** — يبقي قناة التنزيل المباشر سالكة.
2. **APK بتوقيع release** (الحالي debug). سببان: التحذير أشدّ مع debug، **والأهم** أن مفتاح التوقيع لا يمكن تغييره لاحقًا — كل تحديث لاحق يجب أن يُوقَّع بنفس المفتاح، وإلا فشل التثبيت فوق النسخة القديمة. ⇒ يُولَّد مفتاح release **قبل أن يثبّت أوّل مستخدم**، ويُحفَظ نسخة احتياطية منه.
3. **تعليم مسبق قبل الاصطدام:** نقطة التسرّب الأولى هي حوار Play Protect، ومعالجتها بالشرح المسبق ترفع إكمال التثبيت **15-25%**. ⇒ ثلاث خطوات مصوّرة + جملة صريحة: «سيقول أندرويد ‹تعذّر فحص التطبيق›، هذا طبيعي للتطبيقات خارج المتجر — اضغط ‹تثبيت على أي حال›».
4. **الشفافية تبني الثقة:** رقم الإصدار · تاريخه · حجمه · **بصمة SHA-256 منشورة** · HTTPS.
5. **ضبط الخادم:** ترويسة `Content-Type: application/vnd.android.package-archive` و`Content-Disposition: attachment` في `vercel.json`.
6. **بديل لمن لا يريد التثبيت (وللآيفون): PWA** — `manifest.json` + service worker بسيط ⇒ «ثبّت من المتصفّح». مجاني، ويغطّي iOS عبر «إضافة إلى الشاشة الرئيسية».
7. **الملفّ لا يدخل git** (7MB لكل إصدار يضخّم المستودع بلا رجعة) — يُرفَع كـ**GitHub Release** ويشير إليه الزرّ.

---

## 8) `/admin` — لوحة مالك المنصّة (كانت P4)

> ✅ **مُثبَّت بقرار المستخدم (2026-07-26): على الموقع فقط، ولا شيء منها في التطبيق.**

- **ملفّ مستقلّ** لا يُشحن في الـAPK ولا في الـSPA، يقرأ من نفس Supabase بجلسة الأدمن.
- **الأمان في القاعدة لا في الإخفاء:** كل الجداول محكومة بـRLS و`is_admin()`؛ من يفتح `/admin` بلا الدور لا يحصل على صفّ واحد مهما فعل.
- **المحتوى:** حجوزات اليوم/الأسبوع · الإيراد · **عمولتك 10%** · نسبة التأكيد · الإلغاءات · لاعبون جدد — من العرضين الجاهزين `admin_daily` و`admin_places` (الحساب يجري في القاعدة فتبقى الصفحة سريعة مهما كبرت البيانات) · ترتيب الأماكن بالإيراد · أكثر الساعات إشغالًا · **تدفّق `audit_log` الحيّ** عبر Realtime (مجاني) بدل الاستطلاع.
- **إدارة المستخدمين (أُضيف 2026-07-28):** بحث بالرقم · تفعيل/تعطيل حساب · **إعادة تعيين كلمة سرّ** + إخراج الجلسات المفتوحة (المنطق جاهز في [`RECIPES.sql`](migration/RECIPES.sql) §8).
  > ⛔ **عرض كلمات السرّ مستحيل ولن يُبنى.** المخزَّن بصمة bcrypt أحادية الاتجاه. وهذا حماية لا نقص: لو أمكن للوحة عرضها، لأمكن ذلك لكل من يخترق حساب الأدمن أو يسرّب نسخة من القاعدة — والناس يعيدون استعمال كلمات سرّهم خارج التطبيق. **إعادة التعيين هي البديل الصحيح، وهي ما يفعله كل تطبيق.**

---

## 9) البناء والنشر — بلا أدوات جديدة

- **توسيع `app/build.ps1`** (موجود ويعمل): مجلّد `site/` فيه رأس/تذييل مشتركان + محتوى كل صفحة + قاموس النصوص ⇒ يولّد HTML ثابتًا لكل مسار + النسخة الإنجليزية + `sitemap.xml` + `robots.txt` + صفحات `/places/*` من Postgres.
- **لماذا لا Astro/Next؟** المشروع كلّه فانيلا بلا bundler، وهذا قرار معماري قائم. إدخال نظام بناء = تبعيات وتحديثات وسطح عطل جديد، مقابل ما يفعله سكربت موجود أصلًا. (إن كبر الموقع لاحقًا يُعاد النظر — القرار قابل للعكس لأن المخرَج HTML ثابت.)
- **الاستضافة: Cloudflare Pages** (القرار 1، أُسقط بالقياس). ⚠️ **أثر مباشر على المخرَجات:** Cloudflare **لا يقرأ `vercel.json`** — المكافئ ملفّان نصّيان في جذر المخرَج:
  - **`_headers`** — ترويسات الأمان (`X-Content-Type-Options`, `Referrer-Policy`, CSP معقولة) · كاش طويل للأصول ذات البصمة · **وترويستا الـAPK** (§7 بند 5): `Content-Type: application/vnd.android.package-archive` + `Content-Disposition: attachment`.
  - **`_redirects`** — التحويلات (الجذر ⇐ الموقع، `/app` ⇐ الـSPA، مسارات `/en/…`).
  - كلاهما يولّده `build.ps1` مع بقية الصفحات ⇒ لا أدوات جديدة.
- **النشر:** Cloudflare Pages يربط بنفس مستودع GitHub (`malikAlmasri-tech/mustadeera`) ⇒ `git push` ينشر تلقائيًّا كما هو الحال اليوم. **يمكن إبقاء Vercel عاملًا بالتوازي** أثناء الانتقال (الزوّار خارج شبكتك يصلونه) ثمّ إسقاطه.
- **النطاق المدفوع مؤجَّل** لا ملغى: `*.pages.dev` يحلّ مشكلة الوصول اليوم مجانًا؛ ويبقى `mustadeera.com` ترقية مصداقية متى أراد المالك، وهي **قابلة للتنفيذ لاحقًا بلا إعادة بناء** (تغيير نطاق في لوحة Cloudflare + `hreflang`/`sitemap` مولَّدان من متغيّر واحد في `build.ps1` — **اجعله متغيّرًا من اليوم**).

---

## 10) المراحل

| المرحلة | المخرَج | التحقّق |
|---------|---------|---------|
| **أ · الأساس** ✅ | `site/` + `site/build-site.ps1` مربوط بـ`app/build.ps1` + توكنات + رأس/تذييل + **`_headers` و`_redirects`** + `$SiteOrigin` واحد + `sitemap.xml`/`robots.txt` + `make-og.ps1` | ✅ **14 صفحة تُبنى** (7 مسارات × لغتين) · صفر مفتاح غير محلول · **بناء الموقع القديم 638,160 محرفًا ثابت = صفر أثر** |
| **ب · الرئيسية** ✅ | الهيرو · شريط الحقائق · هاتف CSS · Bento · كيف يعمل · المُلّاك · الختام | ✅ صفر overflow على 320/390/768/1280 × لغتين · تباين مقيس (أدنى 5.42:1) · صفر JS للمحتوى · الرئيسية 27,829 محرفًا (الميزانية 40,000) |
| **ج · التنزيل** 🟡 | ✅ `/download` كاملة (تعليم مسبق بنصّ التحذيرين حرفيًّا · مواصفات مقروءة من المشروع لا مخمَّنة) + ترويستا الـAPK + ✅ **PWA** (مانيفست · أيقونتان مربّعتان · service worker بكاش موسوم بتاريخ البناء) · ⏳ الرابط والبصمة | ✅ الحالتان (منشور / غير منشور) مبنيّتان ومُختبَرتان · بصمة 64 محرفًا تلتفّ بلا تجاوز · ✅ **الـSW يُسجَّل فعلًا والمانيفست وأيقوناته 200** ⇒ التثبيت من المتصفّح متاح (بديل الآيفون) · ⏳ تثبيت APK فعلي — **يحتاج إصدارًا موقَّعًا أوّلًا** |
| ~~**د · نقل التطبيق**~~ ⚪ **أُلغيت 2026-07-29** | سقطت أرضيتها بقرار «الحجز من التطبيق فقط»: الـSPA **لم تعد تُنشَر** (`/app/*` ⇒ 301، ولا مجلّد `public/app` أصلًا) ⇒ تبديل باكندها = ترحيل صفحة لا يفتحها أحد. و`hasRating` **مُنجَزة** في مولّد الموقع ([`build-site.ps1:202`](site/build-site.ps1:202)) | ✅ مُتحقَّق: لا `public/app` · التحويل مثبَّت في `_redirects` و`vercel.json` · `/places` بلا نجوم لمن لا تقييم له. **بقي منها بند واحد مستقلّ: P5** — إيقاف Apps Script، محجوب بلوحات الـAI الثلاث في التطبيق ([`app.js:2538`](app/native/app.js:2538)) ⇒ قرار المالك |
| **هـ · الصفحات الباقية** ✅ | ✅ `/owners` · `/about` · `/contact` · `/privacy` · `/terms` · ✅ **`/places` + 6 صفحات أماكن مولَّدة من Postgres** (بلغتين ⇒ **28 صفحة**) | ✅ لكل صفحة عنوان ووصف و`hreflang` متبادل وsitemap صحيح (مسارات عربية مرمَّزة في الروابط وخام على القرص — مُختبَرة عبر HTTP) · ⏳ صور مشاركة خاصّة بكل صفحة (الآن صورة واحدة مشتركة) |
| **و · الأدمن** ✅ | ✅ `/admin` مبنيّة (6 بلاطات · `admin_daily` 14 يومًا · `admin_places` بالإيراد · سجلّ تدقيق · بحث مستخدم + إيقاف/تفعيل) · ✅ **التدفّق الحيّ عبر Realtime** (عميل Phoenix مكتوب باليد — بلا مكتبات) + مؤشّر حالة + استطلاع احتياطي + **تجديد الجلسة** · ⛔ إعادة تعيين كلمة السرّ **لن تُبنى هنا** (تحتاج `service_role` في المتصفّح = تسليم القاعدة) ⇒ وصفة SQL | ✅ **مقيس بالمفتاح العام:** الجداول تُعيد `[]` · ✅ دخول أدمن حقيقي — اختبره المالك 2026-07-29 · ✅ `07` شُغِّل ومُتحقَّق (`phone` محجوب حتى من `order`/`filter`) · ⏳ **يبقى تشغيل [`08_realtime_audit.sql`](migration/08_realtime_audit.sql)** — بدونه اللوحة تعمل بالاستطلاع وتقول ذلك صراحةً |
| **ز · الإطلاق** | نطاق · **APK release** · فهرسة · قياس | Lighthouse ≥ 95 أداء/وصولية/SEO على الرئيسية والتنزيل |

> **ترتيب متعمّد:** المرحلة (د) بعد (ج) لا قبلها — لأن نقل التطبيق يمسّ شيئًا حيًّا يستعمله الناس، والصفحات الجديدة لا تمسّ أحدًا. نبني الجديد أولًا ثم ننقل القائم.

---

## 11) المخاطر

| الخطر | العلاج |
|-------|--------|
| **نقل الـSPA يكسر التطبيق الحيّ** | المسار القديم يبقى عاملًا حتى تثبت النسخة الجديدة؛ التحويل خطوة منفصلة قابلة للعكس |
| **تحذير Play Protect يقتل التنزيل** | توثيق المطوّر + توقيع release + التعليم المسبق + بديل PWA |
| **الموقع يثقل ككل مشروع تسويقي** | ميزانية مكتوبة في القسم 5 تُقاس في كل مرحلة، لا في النهاية |
| **صفحات `/places` تصير قديمة** | تُولَّد في كل بناء من القاعدة؛ والبناء يجري مع كل نشر |
| **ازدواج النصوص بين الموقع والتطبيق** | قاموس نصوص الموقع مستقلّ ومقصود — الموقع يتكلّم لغة تسويق لا لغة واجهة |

---

## 12) القرارات — ✅ حُسمت كلّها (2026-07-28)

| # | القرار | الجواب |
|---|--------|--------|
| 1 | النطاق | ✅ **أُسقط 2026-07-28 بالقياس.** ثبت أن الحجب **SNI على `vercel.app` وحده** (الجذر وكل نطاق فرعي)، وأن `*.pages.dev` سالك ⇒ **الاستضافة: Cloudflare Pages، مجانًا، بلا نطاق مدفوع.** الجدول التفصيلي في «ما تغيّر» أعلاه |
| 2 | التوزيع | **تنزيل مباشر فقط** — بلا Google Play (المقايضة: تحذير Play Protect أشدّ ⇒ التعليم المسبق في `/download` يصير أهمّ) |
| 3 | `/places` | **يُفتح للفهرسة** |
| 4 | التقييمات | **الحقيقية فقط** — يُسقَط `rating_seed`/`reviews_seed` · ✅ **نُفِّذ في التطبيق** · ⏳ يُكرَّر في الموقع بالمرحلة (د) |
| 5 | «سجّل ملعبك» | **صفّ في القاعدة** يظهر في `/admin` |

### (المرجع الأصلي للأسئلة)

1. **النطاق:** نشتري `mustadeera.com` (أو ما يشبهه) أم نبقى على `*.vercel.app` مؤقّتًا؟ *(توصيتي: نطاق — للمصداقية ولأن شبكتك تحجب vercel)*
2. **التوزيع:** أكتفي بالتنزيل المباشر، أم نسجّل في **Google Play Console** أيضًا (25$ مرّة واحدة للنشر، والتوثيق مجاني)؟ *(توصيتي: التوثيق على الأقل)*
3. **`/places`:** نفتح دليل الملاعب العام للفهرسة؟ *(توصيتي: نعم — هو المصدر الوحيد لزيارات مجانية)*
4. **`rating_seed`:** نُبقي التقييمات المكتوبة يدويًّا (4.8 من 120) أم نعرض الحقيقية فقط؟ *(توصيتي: الحقيقية — وقاعدة الصدق تصير أثقل حين يراها جمهور أوسع)*
5. **نموذج «سجّل ملعبك»:** يصلك بالبريد أم كصفّ في القاعدة تراه في `/admin`؟ *(توصيتي: صفّ في القاعدة)*

---

## المراجع (بحث 2026-07-26)

- GSAP صار مجانيًّا بالكامل بكل إضافاته — [Webflow](https://webflow.com/updates/gsap-becomes-free) · [GSAP 3.13](https://gsap.com/blog/3-13/) · [CSS-Tricks](https://css-tricks.com/gsap-is-now-completely-free-even-for-commercial-use/)
- ما صمد من اتجاهات 2026 (Bento باقٍ · الطباعة الحركية لا تُشحن) — [studiomeyer](https://dev.to/studiomeyer_io/web-design-trends-2026-what-actually-held-up-after-six-months-23p8) · [line25](https://line25.com/articles/web-design-trends-2026/)
- اتجاهات صفحات المنتجات 2026 — [SaaSFrame](https://www.saasframe.io/blog/10-saas-landing-page-trends-for-2026-with-real-examples)
- توزيع APK خارج المتجر وتوثيق المطوّر 2026 — [Forasoft](https://www.forasoft.com/blog/article/distribute-android-app-without-google-play) · [Foresight Mobile](https://foresightmobile.com/blog/complete-guide-to-android-app-publishing-in-2026) · [Sideloading بعد 2026](https://androidexperto.com/how-to-sideload-apps-on-android-after-april-2026-googles-new-rules/)
- Uiverse برخصة MIT — [uiverse.io](https://uiverse.io/) · [المستودع](https://github.com/uiverse-io/galaxy)
- كلفة محرّك Lottie — [dotlottie-web](https://github.com/LottieFiles/dotlottie-web) · [مقارنة](https://npmtrends.com/@dotlottie/player-component-vs-@lottiefiles/lottie-player-vs-lottie-web)
- حدود Core Web Vitals 2026 — [corewebvitals.io](https://www.corewebvitals.io/core-web-vitals) · [Digital Applied](https://www.digitalapplied.com/blog/core-web-vitals-2026-inp-lcp-cls-optimization-guide)
- الطباعة العربية وRTL — [Voxire 2026](https://voxire.com/blog/arabic-rtl-typography-web-design-2026/) · [GTech RTL SEO](https://www.gtechme.com/insights/right-to-left-seo-and-ux-optimization-guide/)
