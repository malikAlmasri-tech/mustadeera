# الخطة ① — إكمال التطبيق (نشطة)

> **🔀 تحديث 2026-07-28 (قرار المستخدم): نُنهي التطبيق أولًا، ثم ننتقل إلى الموقع.**
> هذه الخطة **نشطة الآن**. خطة الموقع [`PLAN-2-website.md`](PLAN-2-website.md) جاهزة وقراراتها محسومة، وتبدأ **بعد** إغلاق هذه.

---

## 🔴 الحاجز الحالي — سطر واحد يمنع كل شيء

**كل الحسابات المستورَدة لا تستطيع تسجيل الدخول.** مُشخَّص حيًّا 2026-07-28، والسبب ليس كلمات السرّ:

| الاختبار | النتيجة |
|---------|---------|
| حساب أنشأه الـAPI | ✅ يدخل |
| مالك مستورَد `222` بكلمة السرّ **الصحيحة** | ❌ HTTP 500 «Database error querying schema» |
| نفس الحساب بكلمة سرّ **خاطئة** | ❌ **الخطأ نفسه** ⇒ العطل **قبل** فحص كلمة السرّ |

السبب: ثمانية أعمدة توكن في `auth.users` تُركت `NULL` أثناء الاستيراد، وخدمة المصادقة تقرأها في حقول نصّية لا تقبل NULL.

**⇒ الإجراء:** تشغيل [`migration/05_fix_auth_users.sql`](migration/05_fix_auth_users.sql) — **عليك، دقيقة واحدة**. آمن التكرار، وينتهي بتحقّق ذاتي.
**⇒ أُصلح المولّد أيضًا** ([`build_import.mjs`](migration/build_import.mjs)) وأُعيد توليد `02`/`03` — فلا يعود العطل في أي استيراد قادم.

**ملاحظة تُريحك:** بعد هذا الإصلاح تعمل كلمات السرّ من 3-4 أرقام كما هي. الحدّ الأدنى (6) يُفحص عند **إنشاء حساب جديد فقط**، ولا يُفحص عند الدخول إطلاقًا.

---

## ✅ ما صار جاهزًا (2026-07-28)

- **«Confirm email» مطفأ** — مُتحقَّق حيًّا (`mailer_autoconfirm=true`).
- **[`04_policy_fixes.sql`](migration/04_policy_fixes.sql) شُغِّل بنجاح** — مُتحقَّق حيًّا بإدراج ملفّ جديد عبر RLS. (خطأ `42710` الذي ظهر كان من تشغيل ثانٍ؛ لا تُعده — نسخته الآمنة داخل `05`.)
- **إنشاء حساب جديد يعمل** من الطرف إلى الطرف — مُتحقَّق بتسجيل حقيقي.
- **APK محدَّث ومبنيّ** (7.07MB) ومُتحقَّق من أصوله بايتًا ببايت.
- **النسخ الاحتياطي والنبضة مكتوبان ومُتحقَّقان:** [`db-backup.yml`](.github/workflows/db-backup.yml) + [`keepalive.yml`](.github/workflows/keepalive.yml) (النبضة مُختبَرة حيًّا: HTTP 200).
- **`.github/` مدفوع إلى GitHub** (`fa594e0`) والسرّان مُضافان. ⚠️ **درس:** الدفع رُفض أوّل مرّة لأن التوكن بلا صلاحية `workflow` — GitHub يمنع أي PAT من إنشاء ملفّ داخل `.github/workflows/` بدونها، ولو كانت صلاحية `repo` كاملة. الحلّ: تفعيل المربّع في إعدادات التوكن (النصّ نفسه لا يتغيّر).
- **التقييمات صارت حقيقية فقط** (البند 6) — `rating_seed`/`reviews_seed` ما عادا يُقرآن، ومكان بلا تقييم لا تُرسم له شارة نجمة. مُتحقَّق حيًّا: 6 بطاقات، 2 فقط بتقييم (`★ 5 (5)` و`★ 5 (1)` = التقييمات الستّة الحقيقية).

---

## 📋 ما تبقّى — بالترتيب

| # | البند | مَن | الحالة |
|---|------|-----|--------|
| **1** | تشغيل [`05_fix_auth_users.sql`](migration/05_fix_auth_users.sql) | **أنت** | ✅ **تمّ 2026-07-28 — الحاجز زال** |
| **2** | تحقّق حيّ بعده | أنا | ✅ **تمّ** — الثلاثة `111`/`222`/`333` + لاعب مستورَد صاروا `400 invalid_credentials` بدل `500 Database error querying schema`. الاختبار بكلمة سرّ **خاطئة عمدًا** فلم تُستعمل أي كلمة سرّ حقيقية |
| **10** | **تغيير كلمة السرّ للمستخدم** (طلب 2026-07-28) | أنا | ✅ **تمّ** — بطاقة في «حسابي»، تشترط كلمة السرّ الحالية |
| **11** | **إعادة تعيين كلمة سرّ من الأدمن** | — | ⚠️ **العرض مستحيل** (bcrypt أحادي الاتجاه). الإعادة جاهزة كوصفة في [`RECIPES.sql`](migration/RECIPES.sql) §8، وواجهتها تُبنى في `/admin` بالخطة ② |
| **3** | **اختبار جهاز حقيقي:** تصفّح · تفاصيل · دخول · حجز كامل · إيماءة الرجوع · الاهتزاز · شريط الحالة | أنت | ⏳ |
| **4** | **دفع ملفّي `.github/`** + إضافة السرّين (`SUPABASE_DB_URL` · `BACKUP_PASSPHRASE`) ⇒ النسخ الاحتياطي يعمل | كلانا | 🟡 **الدفع تمّ** (`fa594e0`) والسرّان مُضافان — يبقى تشغيل **Run workflow** مرّة للتحقّق |
| **5** | **استبدال حسابات المُلّاك التجريبية** `111`/`222`/`333` (كلمة سرّ `1234` للجميع) بأرقامهم الحقيقية | أنا — أحتاج الأرقام | ⏳ |
| **6** | **إسقاط `rating_seed`/`reviews_seed`** وعرض التقييمات الحقيقية فقط (قرارك رقم 4) | أنا | ✅ **تمّ في التطبيق ومُتحقَّق حيًّا** — تنظيف القاعدة اختياري في [`06_drop_rating_seed.sql`](migration/06_drop_rating_seed.sql) |
| **7** | **APK بتوقيع release** بدل debug + حفظ نسخة من المفتاح | كلانا | 🟡 **البنية جاهزة** — `signingConfigs` في [`build.gradle`](android/app/build.gradle) + [`.gitignore`](.gitignore) + دليل [`SIGNING.md`](android/SIGNING.md). يبقى أن تُنشئ المفتاح بكلمة سرّك |
| **8** | صقل: skeletons للوحة المالك · `@capacitor/haptics` · bottom sheet للفلاتر · تبويب Browse منفصل | أنا | ✅ **أُغلق** — التفصيل تحت |
| **9** | **تنعيم تبديل الثيم** (طلب المستخدم 2026-07-28) | أنا | ✅ **تمّ** — سببان جذريّان أُزيلا |

### 🎚️ البند 9 — تنعيم تبديل الثيم (2026-07-28)

**سببان جذريّان، لا ضبط أرقام:**

1. **🐛 إيقاف الانتقالات لم يكن يعمل أصلًا.** [`app.css:193`](app/native/app.css:193) يضع `transition:.45s` على ستّ خصائص لون (منها `box-shadow`) لكل وسم شائع. والقاعدة التي يُفترض أن توقفه لحظة التبديل كانت تكتب **`animation:none`** — وهي خاصية أخرى تمامًا. الأثر: لقطة «بعد» تُلتقط والألوان ما زالت **القديمة** ⇒ الدائرة تكشف صورة شبه مطابقة، ثم تُكمل الصفحة كلّها تحوّلها اللوني 450ms **تحتها** = طبقتان متزامنتان + إعادة رسم كل عقدة في الشجرة على الخيط الرئيسي. صارت `transition:none!important`. **مقيس: `.45s × 6` ⟵ `none | 0s`.**
2. **`clip-path` ليست مركَّبة على الـGPU** في Chromium. تكبير نصف قطر الدائرة يُلزم الخيط الرئيسي بإعادة رسم لقطة بحجم الشاشة **في كل فريم** — وحافّتها حادّة فتُقرأ كحلقة تجتاح الشاشة. استُبدلت بـ`opacity`+`transform` فقط (مركَّبتان بالكامل) و`transform-origin` عند الإصبع ⇒ اللقطة الجديدة **تتفتّح** من لمستك بلا رسم في أي فريم. 560ms بمنحنى ease-out خالص.

**مُتحقَّق:** أصل التفتّح `--vt-ox:88.80%` = مركز الزرّ بالضبط · رشقة 6 ضغطات ⇒ صفر أصناف عالقة وصفر قرص عالق والثيم يستقرّ صحيحًا · حارس «الشمسين» صامد (1 svg · 1 path · `::before/::after = none` على الأزرار الأربعة).

### ✨ البند 8 — ما نُفّذ وما كان موجودًا أصلًا

| العنصر | النتيجة |
|---|---|
| **`@capacitor/haptics`** | ✅ ثُبّت (8.0.2) ومُسجَّل في الـAPK (`HapticsPlugin`). جسر `window.__haptic` في [`native.js`](app/native/native.js) يشتقّ الشدّة من نفس أرقام الـms ⇒ **صفر تغيير في نقاط النداء**، و`buzz()` يرجع إلى `navigator.vibrate` في المتصفّح (مُتحقَّق: الجسر `undefined` هناك) |
| **skeletons للوحة المالك** | ✅ بلاطات الإحصاء كانت تعرض **«0»** أثناء الجلب — يُقرأ كخبر («لا حجوزات اليوم») لا كانتظار، وقد يتصرّف المالك بناءً عليه. صارت مُقنَّعة بـshimmer + 3 بطاقات حجز هيكلية في قائمتَي اليوم والحجوزات |
| **bottom sheet للفلاتر** | ⚠️ **كان موجودًا أصلًا** — [`app.css:763`](app/native/app.css:763): مثبّت أسفل، ينزلق من `translate3d(0,100%,0)`، مقبض سحب، إغلاق بالسحب، زوايا علوية، منطقة أمان. و`.fx-actions` **لاصقة** أصلًا. البند كُتب قبل إعادة الهيكلة |
| **تبويب Browse منفصل** | ⚠️ **لاغٍ** — صفحة `home` **هي** التصفّح (شريط بحث + شريط فلاتر لاصق + قائمة الأماكن). تبويب رابع سيكرّرها حرفيًّا. المرجع كان 4 تبويبات لأن صفحته الرئيسية لوحة تحكّم لا قائمة |

**خارج نطاق هذه الخطة عمدًا:**
- **P3ب** (تبديل باكند الموقع إلى Postgres) و**P5** (إيقاف Apps Script) — لأن الموقع ما زال على Apps Script، فلا يمكن إيقافه قبل نقل الموقع. P5 تُغلَق في نهاية الخطة ②.
- صفحة الأدمن — على الموقع فقط (`/admin`)، بقرارك.

---

## 🧭 قاعدة الاستئناف

الترتيب أعلاه ملزم: **1 ← 2 ← 3** ثمّ الباقي بالتوازي. حين تُغلق البنود 1-7 ننتقل إلى [`PLAN-2-website.md`](PLAN-2-website.md) من مرحلتها (أ).
