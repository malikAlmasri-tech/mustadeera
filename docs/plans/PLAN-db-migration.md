# خطة الترحيل: Google Sheets ← Postgres (Supabase)

> الحالة: **مخطّط جاهز، لم يُنفَّذ بعد.** الملفّ الأول جاهز للتشغيل: [`migration/01_schema.sql`](migration/01_schema.sql)

---

## 0) لماذا الآن — أسباب مقيسة من الكود الحالي، لا نظرية

| # | المشكلة | الدليل في الكود |
|---|---------|-----------------|
| 1 | **كلمات السر نصّ صريح** في شيت جوجل — من يفتح الشيت يقرأ كلمة سرّ كل مستخدم | [`Code.gs:453`](Code.gs:453) و[`:558`](Code.gs:558): `String(p.password).trim() === password` — بلا تجزئة |
| 2 | **التوكن بلا توقيع ولا انتهاء** — `base64(id\|phone\|place_id)`، ولا سبيل لإبطاله إن تسرّب | [`Code.gs:534`](Code.gs:534) و[`:373`](Code.gs:373) |
| 3 | **الحجز المزدوج يمنعه منطق تطبيقي فقط** (قراءة كل الحجوزات ثمّ `some()`) داخل قفل عام يُسلسِل كل الكتابات | [`Code.gs:712`](Code.gs:712) + `LockService` |
| 4 | **البطء** — الرد ~4 ثوانٍ، ولذلك بُني الكاش والتحديث الخلفي كتعويض | قياسات الدفعة الخامسة في `CLAUDE.md` |
| 5 | **كل قراءة تمسح الشيت كاملًا** — لا فهارس ولا استعلامات | `getRowsAsObjects` ثمّ `filter` في الذاكرة |

بعد الترحيل: (1) و(2) يحلّهما Supabase Auth، و(3) يصير **قيدًا فريدًا في القاعدة** (مستحيل بنيويًّا)، و(4) و(5) يصيران 50–200ms بفهارس حقيقية.

---

## 1) ما الذي تعمله أنت — 6 خطوات، ~45 دقيقة إجمالًا

> لا أحتاج منك أي **مفتاح سرّي**. كل ما أحتاجه عام وآمن نشره.

1. **أنشئ مشروع Supabase** على <https://supabase.com> (تسجيل بحساب GitHub أسرع).
   - الاسم: `mustadeera` · **المنطقة: `eu-central-1` (فرانكفورت)** — أقرب منطقة مجانية للأردن.
   - احفظ كلمة سرّ قاعدة البيانات التي يعرضها لك (تظهر مرّة واحدة).
2. **شغّل المخطّط:** لوحة Supabase ← `SQL Editor` ← `New query` ← الصق محتوى [`migration/01_schema.sql`](migration/01_schema.sql) ← `Run`. (يجب أن ينتهي بـ`Success`.)
3. **صدّر الشيت إلى CSV:** في جدول البيانات، لكل تبويب: `File → Download → Comma-separated values`. الستّة المطلوبة: `Places` · `Fields` · `Bookings` · `Reviews` · `Owners` · `Players`. ضعها في مجلّد `migration/CSV/` بنفس الأسماء. ✅ **تمّ**
4. **أرسل لي سطرين من إعدادات المشروع** (`Settings → API`): `Project URL` و`anon public key`. **هذان عامّان بالتصميم** (يُضمَّنان في كل تطبيق Supabase) — الأمان يقوم على RLS لا على إخفائهما. **ولا ترسل `service_role`** أبدًا.
5. **قرارٌ واحد منك:** هل نُبقي شيت جوجل **مرآةً للقراءة** (تصدير يومي تلقائي) كي تبقى عادتك في تصفّح البيانات؟ (أنصح: نعم، شهرًا واحدًا بعد الترحيل.)
6. **أرسل رقم هاتفك** الذي ستدخل به كـ**مالك للمشروع (admin)** — سأربطه بالدور `admin` في الاستيراد.

بعدها الباقي عليّ: الاستيراد، تبديل وحدة `API`، صفحة الأدمن، والتحويل.

---

## 2) هل هو مجاني بالكامل؟ — نعم بمقاسك، مع مصيدتين اسمهما بالصريح

**الخطة المجانية في Supabase (2026):** 500MB قاعدة بيانات · 5GB نقل بيانات شهريًا · **50,000 مستخدم نشط شهريًا** · 1GB تخزين ملفّات · طلبات API بلا حدّ · مشروعان نشطان.

**بمقاس المستديرة:** بياناتك اليوم ستّة أماكن وبضع مئات من الصفوف = **أقلّ من 5 ميغابايت**. حتى بعشرة آلاف حجز شهريًا تبقى تحت 50MB. النقل: استجابة JSON نموذجية ~40KB ⇒ 5GB تكفي **~125,000 فتحة للتطبيق شهريًا**. أي: **مجاني فعليًّا لسنوات على هذا المقياس**، وVercel مجاني أصلًا.

**المصيدتان:**

1. **المشروع يتوقّف مؤقتًا بعد أسبوع بلا أي نشاط** (نقرة واحدة لإعادته). لا تُقلق تطبيقًا حيًّا فيه مستخدمون يوميًّا، لكنها تضربك **قبل الإطلاق** إن تركته شهرًا. الحلّ: نبضة يومية مجانية عبر GitHub Action.
2. **لا نسخ احتياطية في الخطة المجانية.** الحلّ: `pg_dump` يومي عبر GitHub Action (مجاني) + مرآة الشيت من الخطوة 1.5. **لا تنتقل بلا هذا.**

**فخّ ثالث يجب تفاديه من البداية:** تسجيل الدخول بـ**رقم الهاتف عبر OTP في Supabase يحتاج مزوّد رسائل SMS مدفوعًا** (Twilio وأمثاله). ولأن مستخدميك يسجّلون برقم + كلمة سرّ (لا بريد)، الحلّ المجاني المعتمد: نستعمل مصادقة البريد داخليًّا بعنوان مشتقّ من الرقم (`962xxxxxxxxx@mustadeera.app`) مع تعطيل التحقّق بالبريد. **تجربة المستخدم لا تتغيّر إطلاقًا** (يكتب رقمه وكلمة سرّه)، ونكسب التجزئة والجلسات القابلة للإبطال مجانًا.

متى تخرج عن المجاني؟ حين تتجاوز 5GB نقل شهريًا (~أكثر من 100 ألف جلسة) أو تريد نسخًا احتياطية مُدارة ⇒ خطة Pro بـ25$/شهر. عندها يكون المشروع يدرّ دخلًا.

---

## 3) صفحة «مالك المشروع» — ثلاث طبقات، أوّلها جاهزة يوم الترحيل

### الطبقة 0 — لوحة Supabase نفسها (بلا أي كود، من اليوم الأول)
`Table Editor` يعطيك كل الجداول للقراءة والتعديل، و`SQL Editor` يجيب عن أي سؤال. هذا وحده يفوق ما يعطيه الشيت اليوم، بلا سطر واحد.

### الطبقة 1 — صفحة `#page-admin` داخل التطبيق
تُبنى من مكوّنات لوحة المالك الموجودة أصلًا (`createBarChart`/`createDonut`/`renderOwnerCharts`) لكن **على مستوى المنصّة كلّها**:
- **الأرقام:** حجوزات اليوم/الأسبوع · الإيراد · **عمولتك 10%** (`CONFIG.COMMISSION`) · نسبة التأكيد · الإلغاءات · لاعبون جدد.
- **الجداول:** ترتيب الأماكن بالإيراد · أكثر الساعات إشغالًا · آخر التقييمات.
- **الحساب يجري في القاعدة** عبر العرضين `admin_daily` و`admin_places` الجاهزين في `01_schema.sql` ⇒ الصفحة تقرأ أرقامًا جاهزة، فتبقى سريعة مهما كبرت البيانات.

### الطبقة 2 — «المدخلات والمخرجات» حرفيًّا: سجلّ `audit_log`
كل كتابة في النظام (تسجيل لاعب · حجز جديد · تغيير حالة · تقييم · تعديل ملعب أو سعر) تُسجَّل تلقائيًّا بـ**trigger داخل القاعدة**: مَن فعل، وماذا، ومتى، و**القيمة قبل وبعد**. الجدول موجود في `01_schema.sql`.
- **لا يمكن تجاوزه:** لأنه في القاعدة لا في التطبيق — حتى لو كُتب في الجدول من أي مسار آخر يُسجَّل.
- في صفحة الأدمن: تدفّق حيّ «آخر ما جرى» يمكن تصفيته بالنوع أو الكيان.
- **ومع Realtime** (مجاني) يصلك الحجز الجديد لحظيًّا بلا الاستطلاع كل 90 ثانية.
- الوصول محصور بـ`role='admin'` عبر RLS ⇒ لا أحد غيرك يقرأه ولو حاول.

---

## 4) المراحل والترتيب

**المشروع الحيّ:** `https://nxqddfuwtrsabprxcfez.supabase.co` · مفتاح `anon` مسجَّل في `migration/` (عام بالتصميم — الأمان على RLS).
**الأدمن:** `962795097771` · **مرآة الشيت:** مُبقاة (قرار المستخدم).

| المرحلة | المحتوى | مَن | الحالة |
|---------|---------|-----|--------|
| **P0** | المخطّط + RLS + العروض + سجلّ التدقيق | أنا | ✅ `migration/01_schema.sql` |
| **P1** | إنشاء المشروع · تشغيل المخطّط · تصدير CSV | أنت | ✅ **مُتحقَّق حيًّا: الجداول السبعة موجودة وRLS يعمل** |
| **P2** | مولّد الاستيراد + استيراد البيانات | أنا/أنت | ✅ **مُستورَد ومُتحقَّق حيًّا** (18 مكانًا · 25 ملعبًا · 65 حجزًا · 6 تقييمات)؛ يبقى تشغيل `03_owners.sql` |
| **P2.5** | دليل التعديل اليومي | أنا | ✅ [`migration/RECIPES.sql`](migration/RECIPES.sql) |
| **P3** | تبديل وحدة `API` — **التطبيق** (`app/native/app.js`) | أنا | ✅ **مُتحقَّق حيًّا ومبنيّ في APK** (صفر طلبات إلى Apps Script عدا AI) |
| **P3ب** | نفس التبديل لـ**الموقع** (`app/app.js`) | أنا | ⏳ (الموقع مُجمّد وحيّ — يُنفَّذ بعد استقرار التطبيق) |
| **P4** | صفحة الأدمن + التدفّق الحيّ | أنا | ⏳ |
| **P3.5** | إصلاحا سياسات ظهرا عند الربط | أنت | ⏳ [`migration/04_policy_fixes.sql`](migration/04_policy_fixes.sql) + تعطيل «Confirm email» |
| **P5** | تشغيل مزدوج أسبوعًا (Apps Script يبقى منشورًا كشبكة أمان) ثمّ التحويل | كلانا | ⏳ |

**عن المستخدمين الحاليين:** كلمات السرّ نصّ صريح اليوم ⇒ يمكن **تجزئتها أثناء الاستيراد** ⇒ **لا أحد يحتاج إعادة تعيين كلمة سرّه**. لكن الجلسات المحفوظة (`player_token`/`owner_token` في localStorage) ستُصبح غير صالحة ⇒ **تسجيل خروج مرّة واحدة** لكل مستخدم. (يمكن تفاديه بنقطة توافق تبدّل التوكن القديم بجلسة جديدة — أخبرني إن أردتها.)

**تنبيه على المعلّق:** تعديلات `Code.gs` غير المنشورة (`reviews_dist` + إلغاء اللاعب + الملاعب الموقوفة) — إن كان الترحيل خلال أسابيع فلا تصرف وقتًا على نشرها؛ ستذوب في P2/P3.

---

## 5) المخاطر والتراجع

- **الخطر الأكبر: خطأ في RLS يكشف بيانات.** العلاج: بعد P2 أختبر كل سياسة بثلاثة حسابات (لاعب/مالك/أدمن) وأثبت أن كلًّا يرى ما له فقط، **قبل** التحويل.
- **فرق شكل البيانات** بين الشيت والجداول (تواريخ، `slots` نصًّا، هواتف غير موحّدة): يُعالَج في سكربت الاستيراد مع **تقرير تعارضات** يُطبع قبل الكتابة لا بعدها.
- **التراجع:** حتى P5 يبقى Apps Script منشورًا وسليمًا؛ العودة = تبديل `API_URL` وإعادة بناء. لذلك التحويل **قرار قابل للعكس** حتى آخر لحظة.
