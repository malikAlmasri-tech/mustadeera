# مجلّد الترحيل — ما هو كل ملفّ، وأيّها يُشغَّل

> الخطة الكاملة والمراحل في [`../PLAN-db-migration.md`](../PLAN-db-migration.md).
> **المشروع الحيّ:** `https://nxqddfuwtrsabprxcfez.supabase.co` · المفتاح `anon` مضمَّن في `app/src/app.js` (عام بالتصميم — الأمان قائم على RLS لا على إخفائه).

---

## الملفّات

| الملفّ | ما هو | الحالة |
|-------|-------|--------|
| `01_schema.sql` | الجداول + القيود + RLS + سجلّ التدقيق + العروض | ✅ **شُغِّل** — لا يُعاد |
| `02_import.sql` | استيراد الأماكن/الملاعب/الحجوزات/التقييمات/حسابك | ✅ **شُغِّل** — لا يُعاد |
| `03_owners.sql` | حسابات أصحاب الملاعب الثلاثة وربطهم بأماكنهم | ✅ **شُغِّل** — لا يُعاد |
| `04_policy_fixes.sql` | إصلاحا سياسات (إدراج `profiles` + تقييم الزائر) | ✅ **شُغِّل** (مُتحقَّق حيًّا: إدراج ملفّ جديد يمرّ عبر RLS). إعادة تشغيله تعطي `42710` — لا تُعده، هو داخل `05` |
| `05_fix_auth_users.sql` | 🔴 **إصلاح حرج: المستخدمون المستورَدون لا يستطيعون الدخول** — أعمدة توكن NULL في `auth.users` + حسابات يتيمة بلا ملفّ | ⏳ **لم يُشغَّل — يحجب دخول كل الحسابات القديمة**؛ آمن التكرار |
| `06_drop_rating_seed.sql` | إسقاط `rating_seed`/`reviews_seed` — التقييمات الحقيقية وحدها | ✅ **شُغِّل** |
| `07_hide_review_phone.sql` | إغلاق تسريب `reviews.phone` (مُتحقَّق حيًّا بالمفتاح العام) | ✅ **شُغِّل** |
| `08_realtime_audit.sql` | بثّ حيّ لسجلّ التدقيق في `/admin` | ⏳ بدونه اللوحة تستطلع كل 20ث — **تعمل**، وتقول ذلك |
| `09_player_reschedule.sql` | دالّة تعديل موعد الحجز للاعب | ⏳ بدونه زرّ «تعديل» يردّ `PGRST202` والتطبيق يقولها صراحةً |
| `10_admin_place_owners.sql` | سياسة كتابة على `place_owners` | ⏳ بدونه ربط مالك بمكان يفشل بـ403؛ إنشاء المكان يعمل |
| `11_phone_verification.sql` | تأكيد رقم الهاتف: كود مُجزَّأ + حدّا إرسال + طابور رسائل | ⏳ بدونه شاشة التأكيد تعرض لوحًا صادقًا؛ **التسجيل والحجز يعملان**. ويلزم بعده **مزوّد رسائل** (§6 في الملفّ) |
| `12_payment_method.sql` | `payment_method/status/ref` + مُشغِّل يمنع كتابتها من غير الأدمن | ⏳ **اختياري — التطبيق لا يعتمد عليه**؛ الافتراضي `'cash'` |
| `13_field_sports.sql` | `fields.sport` + `fields.attrs` + العرض `sport_availability` | ⏳ بدونه **لا يُسجَّل ملعب بادل/تنس/سلة/طائرة**؛ اللوحة تقول ذلك **قبل** ملء النموذج. وكل ملعب يُقرأ كرة قدم كما كان |
| `14_notifications.sql` | جدول الإشعارات + مُشغِّل على `bookings` + حارس أعمدة | ⏳ بدونه **لا إشعارات**؛ المركز يقول سببه صراحةً. الحجز والتأكيد يعملان |
| `15_booking_expiry.sql` | **ساعة الخانة**: `booking_rules` + دوالّ توقيت عمّان + مُشغِّل مهلة إلغاء اللاعب + `cancel_kind` + `expire_stale_bookings()` | ⏳ بدونه ① **اللاعب يستطيع الإلغاء قبل دقيقة من الموعد** (الواجهة تُخفي الزرّ، لكن REST مباشرةً يمرّ) · ② **لا انقضاء تلقائي**: الطلب غير المُجاب عليه يبقى حاجزًا خانته أبدًا، ولوحة المالك **تقول ذلك صراحةً** بدل الوعد بانقضاء لا يقع · ③ `/admin` لا يفصل «انقضت المهلة» عن «رفض المالك» ويقول أيّ ترحيل ينقصه. **وكل ما عدا ذلك يعمل** |
| `16_no_show.sql` | `bookings.no_show` (+ من علّم ومتى) + حارس: المالك/الأدمن فقط، وبعد انتهاء الخانة فقط، وقابل للرجوع | ⏳ بدونه **لا يُسجَّل تخلّف عن الحضور**: زرّ «لم يحضر» في لوحة المالك يردّ `PGRST204` **ويقول اسم الترحيل الناقص**، وبلاطة «لم يحضروا» تُخفى (صفرٌ لا مصدر له يُقرأ «لا يحدث هذا عندنا»). مستقلّ تمامًا عن 15 |
| `17_closures.sql` | `field_closures` + حارس يمنع إغلاقًا يبتلع حجزًا مؤكّدًا + `owner_close_field()` تُرجع التعارض مفصّلًا | ⏳ بدونه **لا وسيلة لإغلاق يوم** إلّا تزوير حجوزات وهمية في كل خانة — وهي تدخل إيرادَ المالك وإشغالَه فيقرأ يومَ صيانةٍ يومًا ناجحًا. الزرّ موجود في تبويب «التقويم» ويقول اسم الترحيل الناقص، ولوحُ اليوم يقول «هذا اليوم مفتوح». **وكل ما عداه يعمل** |
| `18_pricing_rules.sql` | `field_price_rules` + `resolve_field_price()` + `place_price_grid()` + **مُشغِّل يفرض السعر على إدراج اللاعب** | ⏳ بدونه **لا تسعير بالساعة**: كل خانة بسعر `fields.price` كما اليوم، ونافذة «التسعير» تقول اسم الترحيل وتُخفي نموذجها. ⚠️ **وبعده يصير السعر بيد الخادم لا العميل**: لاعبٌ يُرسل `price:0` يُكتب له السعر المحسوب — قبل الترحيل كان يُكتب كما أرسله |
| `19_place_applications.sql` | `place_applications` (إدراج للجميع · **قراءة للأدمن وحده**) + حدّ معدّل في مُشغِّل | ⏳ بدونه **نموذج «سجّل ملعبك» في `/owners` لا يجد جدولًا** فيردّ 404 ويقول ذلك ويحيل إلى `/contact`، وصندوق الوارد في `/admin` يقول اسم الترحيل بدل قائمة فارغة. ⚠️ **الصفوف تحمل أرقام هواتف** — راجع فحص التسريب في ذيل الملفّ قبل الاعتماد عليه |
| `20_slot_watch.sql` | `slot_watch` + مُشغِّل يُشعر عند تحرّر الخانة | 🔴 **يتوقّف إن لم يُشغَّل 14 قبله** — ويقول ذلك برسالة صريحة بدل إنشاء جدولٍ يجمع رغباتٍ لا وسيلةَ للردّ عليها. وبدونه **زرّ «نبّهني إذا فضيت» لا يظهر أصلًا** في لوح البدائل (لا نقبل نيّةً لا نستطيع الوفاء بها)، ولوحُ «الطلب الذي لم يُلبَّ» في `/admin` يقول ذلك |
| `21_events.sql` | `events` (إدراج للجميع · **قراءة للأدمن وحده**) + عروض القمع والبحث الفارغ والطلب | ⏳ بدونه **لا قياس لما لم يحدث**: التطبيق يحاول مرّةً ثمّ يكفّ صامتًا (لا رسالة للاعب — غياب القياس ليس عطلًا يخصّه)، ولوح `/admin` يقول اسم الترحيل. ⚠️ **والقياس لا يعمل بأثر رجعي**: ما مضى قبل تشغيله لا يظهر أبدًا، فكلّ يوم تأخير يومٌ ضائع من البيانات |
| `22_open_games.sql` | المباريات المفتوحة: أعمدة على `bookings` + `booking_players` + العرضان `open_games`/`open_game_players` + أربع دوالّ | ⏳ بدونه **لا مبدّل «مباريات» ولا خيار «مباراة مفتوحة»**: التطبيق يسأل القاعدة مرّة عند الإقلاع (‏`limit=0`) ويُخفيهما إن لم يجد — لا يعرض بابًا ثمّ يسحبه. وكلّ حجز خاصّ كما اليوم. ⚠️ **لا مقعد يُنشَر قبل تأكيد الملعب** (شرطٌ في العرض نفسه)، و**لا رقم هاتف في أيٍّ من العرضين** — نفّذ الفحوص الأربعة في ذيل الملفّ |
| `23_settings_split.sql` | 🔴 **إصلاح تصادم**: `15` كان يسمّي جدوله `app_settings` وهو اسم جدول `11` بمخطّطٍ آخر وسياسةٍ معاكسة. ينقل إعدادات `15` إلى `booking_rules` ويُعيد `app_settings` كما صمّمه `11` | 🔴 **لازم لمن شغّل `15` قبل `11`**: بدونه يفشل `11` بـ`42703: column "value" does not exist`، **والأخطر** أن سياسة القراءة العامّة التي أضافها `15` تبقى فوق جدول `11` ⇒ `sms_provider` مقروء بالمفتاح العام. ومن لم يشغّل `15` بعد: النسخة المصحَّحة منه تُنشئ `booking_rules` مباشرةً، و`23` يمرّ بلا أثر |
| `24_integrity_guards.sql` | 🚨 **حرّاس السلامة**: مُشغِّل على `profiles` (الدور والراية) · مُشغِّل إدراج على `bookings` (الحالة · الاتّساق المرجعي · التاريخ الماضي · الساعة خارج `slots` · الخانة المغلقة) · مُشغِّل يجمّد كلّ عمودٍ لا يخصّ الإلغاء | 🚨 **شغّله أوّلًا وقبل كل شيء.** بدونه: `PATCH /profiles {"role":"admin"}` على صفّك يفتح **القاعدة كلّها** — وكلّ حارسٍ سبقه، لأن كلًّا منها يبدأ بـ`if is_admin() then return new`. ومعه: الموقوف يعيد تفعيل نفسه · اللاعب يؤكّد حجزه (فينكسر القيد ① في `22`) · ثلاث فحوصات كانت في `Code.gs` وسقطت بلا بديل · حجزٌ على يوم مغلق · وتزوير «انقضت المهلة». **مستقلّ عن الترتيب** ويعيد تعريف دوالّ التوقيت بأجسامها نفسها |
| `25_review_limits.sql` | سقف طول على `reviews` + مُشغِّل حدّ معدّل وتطهير + مفتاح `reviews_require_booking` | ⏳ **بعد 15 و24.** بدونه `reviews` أضعف الجداول المفتوحة لـ`anon`: لا سقف طول ولا حدّ معدّل ولا تطهير — بينما `place_applications` محروس من ثلاث جهات. وهو **المصدر الوحيد** للنجوم منذ `06`. ⚠️ ومفتاح «لا يقيّم إلّا من لعب» **مطفأ افتراضًا**: قرار منتَج لا أمن، وتشغيله اليوم قد يترك أماكن بلا نجمة — وهي حالة صادقة، لكنّ اختيارها قرار صاحب المنصّة |
| `26_slot_label_fix.sql` | تصحيح تسمية العاشرة صباحًا داخل `fields.slots` وفي حجوزات المستقبل | ⏳ مستقلّ. `hour:10` و`hour:22` كانتا تحملان **نفس السلسلة حرفًا بحرف** فلا شيء في «حجوزاتي» ولا في الإشعار ولا في رسالة الواتساب يفرّق صباحًا من ليل. ⚠️ **والحجوزات الماضية لا تُمَسّ**: `time_label` لقطةٌ كـ`price` |
| `27_min_app_version.sql` | `min_app_version` + `block_app_version` في `booking_rules` | ⏳ **بعد 15.** بدونه لا شيء يخبر صاحبَ نسخةٍ قديمة أنها تخلّفت — والتوزيع بـAPK مباشر بلا متجر يدفع تحديثًا. والشريط **ناصح لا حاجب**، و`block_app_version` تبقى صفرًا حتى يوجد سبب يُكتب |
| `28_reply_speed.sql` | `bookings.replied_at` + مُشغِّل يكتبه عند **أوّل** ردّ + العرض العامّ `place_reply_speed` (وسيط 90 يومًا، وعتبة `n>=7` **داخل العرض**) | ⏳ **بعد 15** (‏`cancel_kind`). بدونه لا سطر «عادةً يردّ خلال ن» — والتطبيق يعمل كما هو بلا رسالة ولا فراغ (علَمٌ ثلاثيّ الحالات، والغياب تحسينٌ فائت لا ميزة مكسورة). 🔴 **والانقضاء التلقائي مستثنًى** في المُشغِّل وفي العرض معًا: عدُّه ردًّا يجعل المُهمِل يبدو أسرعَ كلّما أهمل. ولا يخرج من العرض إلّا `place_id` و`median_minutes` و`n` |
| `29_field_gender.sql` | `fields.gender` (رجال · سيدات · مشترك) + قيد على القيم | ✅ **شُغِّل** — مقيس حيًّا (`fields?select=gender` ⇒ 200). ⚠️ والافتراضي `null` لا `'mixed'`: «مشترك» على صفوفٍ لم يقل عنها أحدٌ شيئًا اختراعُ بيان، وضررُه حقيقي. والملاعب كلُّها `null` اليوم ⇒ لا شارة ولا مرشِّح حتى يملأها المالك من `/admin` |
| `30_bookings_full_refresh.sql` | 🔴 إعادة بناء العرض `bookings_full` — `select b.*` **تُفكّ عند الإنشاء** فتجمّدت أعمدتُه على 01 | ✅ **شُغِّل 2026-08-13** — مقيس: `bookings_full?select=cancel_kind,no_show,visibility,players_needed,players_brought` ⇒ 200 بعد `42703`. وبدونه «لم يحضر» والمباريات المفتوحة وتمييز «انقضت المهلة» **معطَّلة في التطبيق بلا أن يصرخ شيء** |
| `31_push_tokens.sql` | `profiles.fcm_token`/`fcm_at`/`lang` + مُشغِّل يستدعي دالّة الدفع | ✅ **شُغِّل** — ومُتحقَّق حيًّا على الإنتاج (`{sent:false, reason:"no_token"}` وهي الإجابة الصحيحة قبل أن يسجّل جهازٌ رمزه) |
| `32_owner_demand.sql` | **الطلب المكبوت**: العرض `place_slot_demand` — تجميعٌ لـ`slot_watch` يراه مالك المكان وحده، بلا هويّة أحد | ✅ **شُغِّل 2026-08-18** ومُتحقَّقٌ حيًّا بفحوصه الثلاثة (‏`200 []` بلا توكن · و`profile_id`/`id`/`order` تردّ **400 `42703`**). وقبله كان بدونه لا يُعرَض شيء إطلاقًا: لوح «طلبٌ وصلك وما لقى مكان» **مخفيّ** والعلَم ثلاثيّ الحالات (لا رسالة ولا فراغ). وبه يرى المالك أثمنَ إشارةٍ في القاعدة — ساعاتٌ عليها طلبٌ يفوق ما يعرضه، تقود التسعير (18) والإغلاق (17) مباشرةً. 🔒 **ولا يخرج منه `profile_id` ولا اسمٌ ولا رقم**: عددٌ مجمَّع لا صفّ، والحصرُ بـ`owns_place` **داخل** العرض |
| `RECIPES.sql` | **مرجع للتعديل اليومي** | ⛔ **لا يُشغَّل كملفّ أبدًا** |
| `build_import.mjs` | مولّد `02` و`03` من ملفّات CSV | يُعاد تشغيله عند تصدير جديد |
| `CSV/` | تصدير Google Sheets (المصدر) | — |

### 🔴 إن كنت قد شغّلت `15` بالفعل
شغّل **`23_settings_split.sql` قبل `11`**. النسخة الأولى من `15` حجزت اسم `app_settings` وهو اسم
جدول `11`، فيفشل `11` عند الإدراج — **والأخطر** أن سياسة القراءة العامّة تبقى فوق جدول `11`
فتُنشر إعدادات مزوّد الرسائل. و`23` ينقل ويُصلح الاثنين، وآمن للتكرار وللترتيب أيًّا كان.

### ⚠️ ترتيب واحد يهمّ فعلًا: **14 قبل 15**
كلّ ملفّ هنا مستقلّ بنفسه ويعمل وحده — إلّا وصلةً واحدة: `15` يستبدل مُشغِّل الإشعارات
بنسخة تميّز «انقضت المهلة» عن «رفض المالك». فإن شغّلتَ `15` قبل `14`، يتخطّى `15` هذا
القسم بلا خطأ (ويطبع `notice` يقول ذلك) ثمّ يكتب `14` المُشغِّلَ بنسخته القديمة فوقَه.
**العلاج: أعِد تشغيل `15` بعد `14`** — كلاهما آمن للتكرار. وما عدا هذا، الترتيب راحةٌ لا شرط.

### 🚨 وترتيب 2026-08-09 بعد المراجعة الأمنية
```
24  ⇐  أعِد 22  ⇐  25  ⇐  26  ⇐  27
```
`24` **أوّلًا** لأنه وحده يغلق تصعيد الصلاحيات، وكلّ حارسٍ آخر في القاعدة يفترض أن
`profiles.role` محروس (كلٌّ منها يبدأ بـ`if public.is_admin() then return new`).
و`22` يُعاد لأنه حُرِّر: عرضُ `open_games` كان يعرض مباريات مكانٍ أخفاه الأدمن
(‏`security definer` فلا يمرّ بـ`places_read`)، وفحصُ `clash` كان أعمى عن **المضيف**
نفسه. و`25` بعد `15` (يقرأ `booking_rules`) و`24` (يقرأ `amman_date()`)، و`27` بعد `15`.
وكلّها آمنة لإعادة التشغيل.

**ثمّ تحقّق بأمر واحد** بدل تسعة فحوص يدوية:
```bash
node tools/security-matrix.mjs --url https://<ref>.supabase.co --anon <key> --phone 07… --pass …
```
بتوكن **لاعب عادي** — لا يقبل `service_role` عمدًا: اختبارٌ يحتاج المفتاح الرئيسي لا
يختبر ما يستطيعه المهاجم. ويؤكّد على **النتيجة لا على رمز الحالة**، لأن PostgREST يردّ
خرقَ السياسة بـ«200 وصفٍّ فارغ». ⚠️ ويترك حساب الاختبار `active=false` — أعِده من `/admin`.

### ⛔ تحذير على `RECIPES.sql`
هو **دليل مرجعي لا سكربت**. فيه معرّفات وهمية (`'…'`) وتعديلات فعلية (إنشاء «ملعب جديد» ومالك تجريبي، إخفاء خدمة). تشغيله كاملًا إمّا يفشل أو — أسوأ — ينفّذ تغييرات لا تريدها.
**الاستعمال الصحيح:** افتحه ← انسخ **الوصفة الواحدة** المطلوبة ← عدّل قيمها ← الصقها وحدها في نافذة جديدة.

### قاعدة عامّة عند اللصق في SQL Editor
كل ملفّ مغلّف بـ`begin;`…`commit;` وينتهي بتحقّق ذاتي. **امسح النافذة قبل كل لصق.** إلحاق ملفّ فوق سابقه يُعيد تشغيل الأول ⇒ خطأ مفتاح مكرّر (`23505`) — غير مؤذٍ (تتراجع المعاملة كلّها) لكنه يمنع تنفيذ ما بعده.

---

## إعادة توليد الاستيراد من تصدير جديد

```bash
node migration/build_import.mjs
```

يقرأ `CSV/`، ويطبع **تقرير تعارضات قبل الكتابة** (خانات مزدوجة تصطدم بالقيد الفريد · معرّفات يتيمة · طوابع زمنية لا تُفسَّر · ملفّات ناقصة)، ثم يكتب `02_import.sql` و`03_owners.sql`.

**ثلاثة دروس مثبّتة في المولّد — لا تُزلها:**
1. **`qNN` لأعمدة `not null`.** الدالّة العادية تحوّل الفراغ إلى `null`، وهذا يفجّر أي عمود `not null` حين يكون المصدر فارغًا (وقع فعلًا: `Owners.csv` بلا عمود اسم ⇒ `profiles.name`).
2. **بادئة المجال في `legacy_id`** (`player:` / `owner:`). المعرّفات تأتي من شيتين بترقيم مستقلّ فيتصادمان عند 1 و2 و3، والعمود فريد.
3. **`set local search_path = public, extensions`** كي تُحلّ `crypt()`/`gen_salt()` أيًّا كان مخطّط تثبيت pgcrypto.

---

## حقائق تخصّ البيانات المستورَدة

- **الحسابات:** أدمن واحد (`962795097771`) + 3 مُلّاك أرقامهم `111`/`222`/`333` وكلمة سرّهم `1234` جميعًا واسمهم «محمد».
  ⚠️ **تجريبية بحتة — تُستبدل قبل أن يستخدمها أحد فعليًّا** (الوصفة 4 في `RECIPES.sql`).
- **كلمات السرّ لم تعد قابلة للقراءة من أحد ولا منك** (bcrypt داخل `auth.users`). يمكن **تعيين** واحدة جديدة فقط.
- **البريد مشتقّ من الرقم** (`<phone>@mustadeera.app`) ولا يستقبل شيئًا — لأن مصادقة الهاتف بـOTP تتطلّب مزوّد SMS مدفوعًا. تجربة المستخدم لم تتغيّر: يكتب رقمه وكلمة سرّه.
- **`rating_seed`/`reviews_seed`:** تقييمات مكتوبة يدويًّا في الشيت (مثلًا 4.8 من 120) بينما التقييمات الحقيقية **ستّة**. نُقلت كما هي حفاظًا على السلوك القائم، وتُستعمل فقط حين لا توجد تقييمات حقيقية.
  **قرار معلّق:** إبقاؤها أم التحوّل إلى الأرقام الحقيقية (قاعدة الصدق م5 تميل إلى الثاني).

---

## نسخ احتياطية — غير منفَّذة بعد ⚠️
الخطة المجانية **بلا نسخ احتياطية**. قبل الاعتماد الكامل: `pg_dump` يومي عبر GitHub Action + مرآة الشيت (قرار المستخدم: تُبقى). ونبضة يومية تمنع توقّف المشروع بعد أسبوع خمول.
