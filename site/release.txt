# =========================================================
#  بيانات إصدار التطبيق — المصدر الوحيد لصفحة /download
#
#  ما دام `local` و`url` فارغَين: الصفحة تعرض حالة «التنزيل غير متاح بعد»
#  مع البديل. لا رقم ولا بصمة تُخترَع.
#
#  طريقان إلى الملفّ، و`local` يفوز لأنه الصادق اليوم:
#    local = مسار الـAPK الموقَّع كما يخرجه gradle على هذا الجهاز.
#            البناء ينسخه إلى site/static/downloads/ في كل مرّة — **نسخٌ لا
#            تخزين**: نسخةٌ بائتة تحت اسم إصدار اليوم أسوأ أنواع الخطأ، لأن
#            الصفحة والبصمة كلتاهما تبدوان صحيحتين.
#    url   = يبقى لحالة أن يكون الملفّ منشورًا في مكان آخر (GitHub Release).
#
#  🔴 والـAPK **لا يُلتزَم في git أبدًا** — المستودع عامّ، و5MB لكل إصدار
#     تضخّمه بلا رجعة. `*.apk` في `.gitignore`، والبناء ينسخه عند كل تشغيل.
#
#  البصمة تُحسَب من الملفّ نفسه لا تُكتب بيد:
#    certutil -hashfile <الملفّ>.apk SHA256
#
#  ⚠️ وعند كل إصدار جديد: ارفع `versionCode` و`versionName` في
#     android/app/build.gradle **قبل** البناء، وإلّا شُحن إصدارٌ يحمل رقم
#     سابقه فلا يعرف أحدٌ أيّهما على جهازه. و`check-mirrors` يحرس الرقم.
# =========================================================

version = 1.0
date = 2026-08-14
size = 4.9 MB
sha256 = 43af0af9d4adf70bfd15216e2665614aea1eddcfb96bb8c4b33775e8f31bd4dc
local = android/app/build/outputs/apk/release/app-release.apk
url =
